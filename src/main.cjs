/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD
if you want to view the source, please visit the github repository of this plugin
*/
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __esm = (fn, res) => function __init() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
};
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
// node_modules/runtypes/lib/reflect.js
var require_reflect = __commonJS({
  "node_modules/runtypes/lib/reflect.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
  }
});
// node_modules/runtypes/lib/result.js
var require_result = __commonJS({
  "node_modules/runtypes/lib/result.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Failcode = void 0;
    exports.Failcode = {
      /** The type of the received primitive value is incompatible with expected one. */
      TYPE_INCORRECT: "TYPE_INCORRECT",
      /** The received primitive value is incorrect. */
      VALUE_INCORRECT: "VALUE_INCORRECT",
      /** The key of the property is incorrect. */
      KEY_INCORRECT: "KEY_INCORRECT",
      /** One or more elements or properties of the received object are incorrect. */
      CONTENT_INCORRECT: "CONTENT_INCORRECT",
      /** One or more arguments passed to the function is incorrect. */
      ARGUMENT_INCORRECT: "ARGUMENT_INCORRECT",
      /** The value returned by the function is incorrect. */
      RETURN_INCORRECT: "RETURN_INCORRECT",
      /** The received value does not fulfill the constraint. */
      CONSTRAINT_FAILED: "CONSTRAINT_FAILED",
      /** The property must be present but missing. */
      PROPERTY_MISSING: "PROPERTY_MISSING",
      /** The property must not be present but present. */
      PROPERTY_PRESENT: "PROPERTY_PRESENT",
      /** The value must not be present but present. */
      NOTHING_EXPECTED: "NOTHING_EXPECTED"
    };
  }
});
// node_modules/runtypes/lib/errors.js
var require_errors = __commonJS({
  "node_modules/runtypes/lib/errors.js"(exports) {
    "use strict";
    var __extends2 = exports && exports.__extends || function() {
      var extendStatics2 = function(d, b) {
        extendStatics2 = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
          d2.__proto__ = b2;
        } || function(d2, b2) {
          for (var p in b2)
            if (Object.prototype.hasOwnProperty.call(b2, p))
              d2[p] = b2[p];
        };
        return extendStatics2(d, b);
      };
      return function(d, b) {
        if (typeof b !== "function" && b !== null)
          throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics2(d, b);
        function __() {
          this.constructor = d;
        }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
      };
    }();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ValidationError = void 0;
    var ValidationError = (
      /** @class */
      function(_super) {
        __extends2(ValidationError2, _super);
        function ValidationError2(failure) {
          var _this = _super.call(this, failure.message) || this;
          _this.name = "ValidationError";
          _this.code = failure.code;
          if (failure.details !== void 0)
            _this.details = failure.details;
          Object.setPrototypeOf(_this, ValidationError2.prototype);
          return _this;
        }
        return ValidationError2;
      }(Error)
    );
    exports.ValidationError = ValidationError;
  }
});
// node_modules/runtypes/lib/show.js
var require_show = __commonJS({
  "node_modules/runtypes/lib/show.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var showStringified = function(circular) {
      return function(refl) {
        switch (refl.tag) {
          case "literal":
            return '"'.concat(String(refl.value), '"');
          case "string":
            return "string";
          case "brand":
            return refl.brand;
          case "constraint":
            return refl.name || showStringified(circular)(refl.underlying);
          case "union":
            return refl.alternatives.map(showStringified(circular)).join(" | ");
          case "intersect":
            return refl.intersectees.map(showStringified(circular)).join(" & ");
          default:
            break;
        }
        return "`${".concat(show(false, circular)(refl), "}`");
      };
    };
    var showEmbedded = function(circular) {
      return function(refl) {
        switch (refl.tag) {
          case "literal":
            return String(refl.value);
          case "brand":
            return "${".concat(refl.brand, "}");
          case "constraint":
            return refl.name ? "${".concat(refl.name, "}") : showEmbedded(circular)(refl.underlying);
          case "union":
            if (refl.alternatives.length === 1) {
              var inner = refl.alternatives[0];
              return showEmbedded(circular)(inner.reflect);
            }
            break;
          case "intersect":
            if (refl.intersectees.length === 1) {
              var inner = refl.intersectees[0];
              return showEmbedded(circular)(inner.reflect);
            }
            break;
          default:
            break;
        }
        return "${".concat(show(false, circular)(refl), "}");
      };
    };
    var show = function(needsParens, circular) {
      return function(refl) {
        var parenthesize = function(s) {
          return needsParens ? "(".concat(s, ")") : s;
        };
        if (circular.has(refl))
          return parenthesize("CIRCULAR ".concat(refl.tag));
        else
          circular.add(refl);
        try {
          switch (refl.tag) {
            case "unknown":
            case "never":
            case "void":
            case "boolean":
            case "number":
            case "bigint":
            case "string":
            case "symbol":
            case "function":
              return refl.tag;
            case "literal": {
              var value = refl.value;
              return typeof value === "string" ? '"'.concat(value, '"') : String(value);
            }
            case "template": {
              if (refl.strings.length === 0)
                return '""';
              else if (refl.strings.length === 1)
                return '"'.concat(refl.strings[0], '"');
              else if (refl.strings.length === 2) {
                if (refl.strings.every(function(string) {
                  return string === "";
                })) {
                  var runtype = refl.runtypes[0];
                  return showStringified(circular)(runtype.reflect);
                }
              }
              var backtick_1 = false;
              var inner = refl.strings.reduce(function(inner2, string, i) {
                var prefix = inner2 + string;
                var runtype2 = refl.runtypes[i];
                if (runtype2) {
                  var suffix = showEmbedded(circular)(runtype2.reflect);
                  if (!backtick_1 && suffix.startsWith("$"))
                    backtick_1 = true;
                  return prefix + suffix;
                } else
                  return prefix;
              }, "");
              return backtick_1 ? "`".concat(inner, "`") : '"'.concat(inner, '"');
            }
            case "array":
              return "".concat(readonlyTag(refl)).concat(show(true, circular)(refl.element), "[]");
            case "dictionary":
              return "{ [_: ".concat(refl.key, "]: ").concat(show(false, circular)(refl.value), " }");
            case "record": {
              var keys = Object.keys(refl.fields);
              return keys.length ? "{ ".concat(keys.map(function(k) {
                return "".concat(readonlyTag(refl)).concat(k).concat(partialTag(refl, k), ": ").concat(refl.fields[k].tag === "optional" ? show(false, circular)(refl.fields[k].underlying) : show(false, circular)(refl.fields[k]), ";");
              }).join(" "), " }") : "{}";
            }
            case "tuple":
              return "[".concat(refl.components.map(show(false, circular)).join(", "), "]");
            case "union":
              return parenthesize("".concat(refl.alternatives.map(show(true, circular)).join(" | ")));
            case "intersect":
              return parenthesize("".concat(refl.intersectees.map(show(true, circular)).join(" & ")));
            case "optional":
              return show(needsParens, circular)(refl.underlying) + " | undefined";
            case "constraint":
              return refl.name || show(needsParens, circular)(refl.underlying);
            case "instanceof":
              return refl.ctor.name;
            case "brand":
              return show(needsParens, circular)(refl.entity);
          }
        } finally {
          circular.delete(refl);
        }
        throw Error("impossible");
      };
    };
    exports.default = show(false, /* @__PURE__ */ new Set());
    function partialTag(_a, key) {
      var isPartial = _a.isPartial, fields = _a.fields;
      return isPartial || key !== void 0 && fields[key].tag === "optional" ? "?" : "";
    }
    function readonlyTag(_a) {
      var isReadonly = _a.isReadonly;
      return isReadonly ? "readonly " : "";
    }
  }
});
// node_modules/runtypes/lib/util.js
var require_util = __commonJS({
  "node_modules/runtypes/lib/util.js"(exports) {
    "use strict";
    var __assign2 = exports && exports.__assign || function() {
      __assign2 = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
          s = arguments[i];
          for (var p in s)
            if (Object.prototype.hasOwnProperty.call(s, p))
              t[p] = s[p];
        }
        return t;
      };
      return __assign2.apply(this, arguments);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.FAILURE = exports.SUCCESS = exports.enumerableKeysOf = exports.typeOf = exports.hasKey = void 0;
    var result_1 = require_result();
    var show_1 = require_show();
    function hasKey(key, object) {
      return typeof object === "object" && object !== null && key in object;
    }
    exports.hasKey = hasKey;
    var typeOf = function(value) {
      var _a, _b, _c;
      return typeof value === "object" ? value === null ? "null" : Array.isArray(value) ? "array" : ((_a = value.constructor) === null || _a === void 0 ? void 0 : _a.name) === "Object" ? "object" : (_c = (_b = value.constructor) === null || _b === void 0 ? void 0 : _b.name) !== null && _c !== void 0 ? _c : typeof value : typeof value;
    };
    exports.typeOf = typeOf;
    var enumerableKeysOf = function(object) {
      return typeof object === "object" && object !== null ? (
        // Objects with a null prototype may not have `propertyIsEnumerable`
        Reflect.ownKeys(object).filter(function(key) {
          var _a, _b;
          return (_b = (_a = object.propertyIsEnumerable) === null || _a === void 0 ? void 0 : _a.call(object, key)) !== null && _b !== void 0 ? _b : true;
        })
      ) : [];
    };
    exports.enumerableKeysOf = enumerableKeysOf;
    function SUCCESS(value) {
      return { success: true, value };
    }
    exports.SUCCESS = SUCCESS;
    exports.FAILURE = Object.assign(function(code, message, details) {
      return __assign2({ success: false, code, message }, details ? { details } : {});
    }, {
      TYPE_INCORRECT: function(self, value) {
        var message = "Expected ".concat(self.tag === "template" ? "string ".concat((0, show_1.default)(self)) : (0, show_1.default)(self), ", but was ").concat((0, exports.typeOf)(value));
        return (0, exports.FAILURE)(result_1.Failcode.TYPE_INCORRECT, message);
      },
      VALUE_INCORRECT: function(name, expected, received) {
        return (0, exports.FAILURE)(result_1.Failcode.VALUE_INCORRECT, "Expected ".concat(name, " ").concat(String(expected), ", but was ").concat(String(received)));
      },
      KEY_INCORRECT: function(self, expected, value) {
        return (0, exports.FAILURE)(result_1.Failcode.KEY_INCORRECT, "Expected ".concat((0, show_1.default)(self), " key to be ").concat((0, show_1.default)(expected), ", but was ").concat((0, exports.typeOf)(value)));
      },
      CONTENT_INCORRECT: function(self, details) {
        var formattedDetails = JSON.stringify(details, null, 2).replace(/^ *null,\n/gm, "");
        var message = "Validation failed:\n".concat(formattedDetails, ".\nObject should match ").concat((0, show_1.default)(self));
        return (0, exports.FAILURE)(result_1.Failcode.CONTENT_INCORRECT, message, details);
      },
      ARGUMENT_INCORRECT: function(message) {
        return (0, exports.FAILURE)(result_1.Failcode.ARGUMENT_INCORRECT, message);
      },
      RETURN_INCORRECT: function(message) {
        return (0, exports.FAILURE)(result_1.Failcode.RETURN_INCORRECT, message);
      },
      CONSTRAINT_FAILED: function(self, message) {
        var info = message ? ": ".concat(message) : "";
        return (0, exports.FAILURE)(result_1.Failcode.CONSTRAINT_FAILED, "Failed constraint check for ".concat((0, show_1.default)(self)).concat(info));
      },
      PROPERTY_MISSING: function(self) {
        var message = "Expected ".concat((0, show_1.default)(self), ", but was missing");
        return (0, exports.FAILURE)(result_1.Failcode.PROPERTY_MISSING, message);
      },
      PROPERTY_PRESENT: function(value) {
        var message = "Expected nothing, but was ".concat((0, exports.typeOf)(value));
        return (0, exports.FAILURE)(result_1.Failcode.PROPERTY_PRESENT, message);
      },
      NOTHING_EXPECTED: function(value) {
        var message = "Expected nothing, but was ".concat((0, exports.typeOf)(value));
        return (0, exports.FAILURE)(result_1.Failcode.NOTHING_EXPECTED, message);
      }
    });
  }
});
// node_modules/runtypes/lib/contract.js
var require_contract = __commonJS({
  "node_modules/runtypes/lib/contract.js"(exports) {
    "use strict";
    var __read2 = exports && exports.__read || function(o, n) {
      var m = typeof Symbol === "function" && o[Symbol.iterator];
      if (!m)
        return o;
      var i = m.call(o), r, ar = [], e;
      try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done)
          ar.push(r.value);
      } catch (error) {
        e = { error };
      } finally {
        try {
          if (r && !r.done && (m = i["return"]))
            m.call(i);
        } finally {
          if (e)
            throw e.error;
        }
      }
      return ar;
    };
    var __spreadArray2 = exports && exports.__spreadArray || function(to, from, pack) {
      if (pack || arguments.length === 2)
        for (var i = 0, l = from.length, ar; i < l; i++) {
          if (ar || !(i in from)) {
            if (!ar)
              ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
          }
        }
      return to.concat(ar || Array.prototype.slice.call(from));
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Contract = void 0;
    var errors_1 = require_errors();
    var util_1 = require_util();
    function Contract() {
      var runtypes = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        runtypes[_i] = arguments[_i];
      }
      var lastIndex = runtypes.length - 1;
      var argRuntypes = runtypes.slice(0, lastIndex);
      var returnRuntype = runtypes[lastIndex];
      return {
        enforce: function(f) {
          return function() {
            var args = [];
            for (var _i2 = 0; _i2 < arguments.length; _i2++) {
              args[_i2] = arguments[_i2];
            }
            if (args.length < argRuntypes.length) {
              var message = "Expected ".concat(argRuntypes.length, " arguments but only received ").concat(args.length);
              var failure = util_1.FAILURE.ARGUMENT_INCORRECT(message);
              throw new errors_1.ValidationError(failure);
            }
            for (var i = 0; i < argRuntypes.length; i++)
              argRuntypes[i].check(args[i]);
            return returnRuntype.check(f.apply(void 0, __spreadArray2([], __read2(args), false)));
          };
        }
      };
    }
    exports.Contract = Contract;
  }
});
// node_modules/runtypes/lib/asynccontract.js
var require_asynccontract = __commonJS({
  "node_modules/runtypes/lib/asynccontract.js"(exports) {
    "use strict";
    var __read2 = exports && exports.__read || function(o, n) {
      var m = typeof Symbol === "function" && o[Symbol.iterator];
      if (!m)
        return o;
      var i = m.call(o), r, ar = [], e;
      try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done)
          ar.push(r.value);
      } catch (error) {
        e = { error };
      } finally {
        try {
          if (r && !r.done && (m = i["return"]))
            m.call(i);
        } finally {
          if (e)
            throw e.error;
        }
      }
      return ar;
    };
    var __spreadArray2 = exports && exports.__spreadArray || function(to, from, pack) {
      if (pack || arguments.length === 2)
        for (var i = 0, l = from.length, ar; i < l; i++) {
          if (ar || !(i in from)) {
            if (!ar)
              ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
          }
        }
      return to.concat(ar || Array.prototype.slice.call(from));
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AsyncContract = void 0;
    var errors_1 = require_errors();
    var util_1 = require_util();
    function AsyncContract() {
      var runtypes = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        runtypes[_i] = arguments[_i];
      }
      var lastIndex = runtypes.length - 1;
      var argRuntypes = runtypes.slice(0, lastIndex);
      var returnRuntype = runtypes[lastIndex];
      return {
        enforce: function(f) {
          return function() {
            var args = [];
            for (var _i2 = 0; _i2 < arguments.length; _i2++) {
              args[_i2] = arguments[_i2];
            }
            if (args.length < argRuntypes.length) {
              var message = "Expected ".concat(argRuntypes.length, " arguments but only received ").concat(args.length);
              var failure = util_1.FAILURE.ARGUMENT_INCORRECT(message);
              throw new errors_1.ValidationError(failure);
            }
            for (var i = 0; i < argRuntypes.length; i++)
              argRuntypes[i].check(args[i]);
            var returnedPromise = f.apply(void 0, __spreadArray2([], __read2(args), false));
            if (!(returnedPromise instanceof Promise)) {
              var message = "Expected function to return a promise, but instead got ".concat(returnedPromise);
              var failure = util_1.FAILURE.RETURN_INCORRECT(message);
              throw new errors_1.ValidationError(failure);
            }
            return returnedPromise.then(returnRuntype.check);
          };
        }
      };
    }
    exports.AsyncContract = AsyncContract;
  }
});
// node_modules/runtypes/lib/match.js
var require_match = __commonJS({
  "node_modules/runtypes/lib/match.js"(exports) {
    "use strict";
    var __values2 = exports && exports.__values || function(o) {
      var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
      if (m)
        return m.call(o);
      if (o && typeof o.length === "number")
        return {
          next: function() {
            if (o && i >= o.length)
              o = void 0;
            return { value: o && o[i++], done: !o };
          }
        };
      throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
    };
    var __read2 = exports && exports.__read || function(o, n) {
      var m = typeof Symbol === "function" && o[Symbol.iterator];
      if (!m)
        return o;
      var i = m.call(o), r, ar = [], e;
      try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done)
          ar.push(r.value);
      } catch (error) {
        e = { error };
      } finally {
        try {
          if (r && !r.done && (m = i["return"]))
            m.call(i);
        } finally {
          if (e)
            throw e.error;
        }
      }
      return ar;
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.when = exports.match = void 0;
    function match() {
      var cases = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        cases[_i] = arguments[_i];
      }
      return function(x) {
        var e_1, _a;
        try {
          for (var cases_1 = __values2(cases), cases_1_1 = cases_1.next(); !cases_1_1.done; cases_1_1 = cases_1.next()) {
            var _b = __read2(cases_1_1.value, 2), T = _b[0], f = _b[1];
            if (T.guard(x))
              return f(x);
          }
        } catch (e_1_1) {
          e_1 = { error: e_1_1 };
        } finally {
          try {
            if (cases_1_1 && !cases_1_1.done && (_a = cases_1.return))
              _a.call(cases_1);
          } finally {
            if (e_1)
              throw e_1.error;
          }
        }
        throw new Error("No alternatives were matched");
      };
    }
    exports.match = match;
    function when(runtype, transformer) {
      return [runtype, transformer];
    }
    exports.when = when;
  }
});
// node_modules/runtypes/lib/runtype.js
var require_runtype = __commonJS({
  "node_modules/runtypes/lib/runtype.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.innerValidate = exports.create = exports.isRuntype = void 0;
    var index_1 = require_lib();
    var show_1 = require_show();
    var errors_1 = require_errors();
    var util_1 = require_util();
    var RuntypeSymbol = Symbol();
    var isRuntype = function(x) {
      return (0, util_1.hasKey)(RuntypeSymbol, x);
    };
    exports.isRuntype = isRuntype;
    function create(validate, A) {
      A[RuntypeSymbol] = true;
      A.check = check;
      A.assert = check;
      A._innerValidate = function(value, visited) {
        if (visited.has(value, A))
          return (0, util_1.SUCCESS)(value);
        return validate(value, visited);
      };
      A.validate = function(value) {
        return A._innerValidate(value, VisitedState());
      };
      A.guard = guard;
      A.Or = Or;
      A.And = And;
      A.optional = optional;
      A.nullable = nullable;
      A.withConstraint = withConstraint;
      A.withGuard = withGuard;
      A.withBrand = withBrand;
      A.reflect = A;
      A.toString = function() {
        return "Runtype<".concat((0, show_1.default)(A), ">");
      };
      return A;
      function check(x) {
        var result = A.validate(x);
        if (result.success)
          return result.value;
        else
          throw new errors_1.ValidationError(result);
      }
      function guard(x) {
        return A.validate(x).success;
      }
      function Or(B) {
        return (0, index_1.Union)(A, B);
      }
      function And(B) {
        return (0, index_1.Intersect)(A, B);
      }
      function optional() {
        return (0, index_1.Optional)(A);
      }
      function nullable() {
        return (0, index_1.Union)(A, index_1.Null);
      }
      function withConstraint(constraint, options) {
        return (0, index_1.Constraint)(A, constraint, options);
      }
      function withGuard(guard2, options) {
        return (0, index_1.Constraint)(A, guard2, options);
      }
      function withBrand(B) {
        return (0, index_1.Brand)(B, A);
      }
    }
    exports.create = create;
    function innerValidate(targetType, value, visited) {
      return targetType._innerValidate(value, visited);
    }
    exports.innerValidate = innerValidate;
    function VisitedState() {
      var members = /* @__PURE__ */ new WeakMap();
      var add = function(candidate, type) {
        if (candidate === null || !(typeof candidate === "object"))
          return;
        var typeSet = members.get(candidate);
        members.set(candidate, typeSet ? typeSet.set(type, true) : (/* @__PURE__ */ new WeakMap()).set(type, true));
      };
      var has = function(candidate, type) {
        var typeSet = members.get(candidate);
        var value = typeSet && typeSet.get(type) || false;
        add(candidate, type);
        return value;
      };
      return { has };
    }
  }
});
// node_modules/runtypes/lib/types/unknown.js
var require_unknown = __commonJS({
  "node_modules/runtypes/lib/types/unknown.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Unknown = void 0;
    var runtype_1 = require_runtype();
    var util_1 = require_util();
    var self = { tag: "unknown" };
    exports.Unknown = (0, runtype_1.create)(function(value) {
      return (0, util_1.SUCCESS)(value);
    }, self);
  }
});
// node_modules/runtypes/lib/types/never.js
var require_never = __commonJS({
  "node_modules/runtypes/lib/types/never.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Never = void 0;
    var runtype_1 = require_runtype();
    var util_1 = require_util();
    var self = { tag: "never" };
    exports.Never = (0, runtype_1.create)(util_1.FAILURE.NOTHING_EXPECTED, self);
  }
});
// node_modules/runtypes/lib/types/void.js
var require_void = __commonJS({
  "node_modules/runtypes/lib/types/void.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Void = void 0;
    var unknown_1 = require_unknown();
    exports.Void = unknown_1.Unknown;
  }
});
// node_modules/runtypes/lib/types/union.js
var require_union = __commonJS({
  "node_modules/runtypes/lib/types/union.js"(exports) {
    "use strict";
    var __values2 = exports && exports.__values || function(o) {
      var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
      if (m)
        return m.call(o);
      if (o && typeof o.length === "number")
        return {
          next: function() {
            if (o && i >= o.length)
              o = void 0;
            return { value: o && o[i++], done: !o };
          }
        };
      throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Union = void 0;
    var runtype_1 = require_runtype();
    var util_1 = require_util();
    function Union() {
      var alternatives = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        alternatives[_i] = arguments[_i];
      }
      var match = function() {
        var cases = [];
        for (var _i2 = 0; _i2 < arguments.length; _i2++) {
          cases[_i2] = arguments[_i2];
        }
        return function(x) {
          for (var i = 0; i < alternatives.length; i++) {
            if (alternatives[i].guard(x)) {
              return cases[i](x);
            }
          }
        };
      };
      var self = { tag: "union", alternatives, match };
      return (0, runtype_1.create)(function(value, visited) {
        var e_1, _a, e_2, _b, e_3, _c, e_4, _d;
        if (typeof value !== "object" || value === null) {
          try {
            for (var alternatives_1 = __values2(alternatives), alternatives_1_1 = alternatives_1.next(); !alternatives_1_1.done; alternatives_1_1 = alternatives_1.next()) {
              var alternative = alternatives_1_1.value;
              if ((0, runtype_1.innerValidate)(alternative, value, visited).success)
                return (0, util_1.SUCCESS)(value);
            }
          } catch (e_1_1) {
            e_1 = { error: e_1_1 };
          } finally {
            try {
              if (alternatives_1_1 && !alternatives_1_1.done && (_a = alternatives_1.return))
                _a.call(alternatives_1);
            } finally {
              if (e_1)
                throw e_1.error;
            }
          }
          return util_1.FAILURE.TYPE_INCORRECT(self, value);
        }
        var commonLiteralFields = {};
        try {
          for (var alternatives_2 = __values2(alternatives), alternatives_2_1 = alternatives_2.next(); !alternatives_2_1.done; alternatives_2_1 = alternatives_2.next()) {
            var alternative = alternatives_2_1.value;
            if (alternative.reflect.tag === "record") {
              var _loop_1 = function(fieldName2) {
                var field2 = alternative.reflect.fields[fieldName2];
                if (field2.tag === "literal") {
                  if (commonLiteralFields[fieldName2]) {
                    if (commonLiteralFields[fieldName2].every(function(value2) {
                      return value2 !== field2.value;
                    })) {
                      commonLiteralFields[fieldName2].push(field2.value);
                    }
                  } else {
                    commonLiteralFields[fieldName2] = [field2.value];
                  }
                }
              };
              for (var fieldName in alternative.reflect.fields) {
                _loop_1(fieldName);
              }
            }
          }
        } catch (e_2_1) {
          e_2 = { error: e_2_1 };
        } finally {
          try {
            if (alternatives_2_1 && !alternatives_2_1.done && (_b = alternatives_2.return))
              _b.call(alternatives_2);
          } finally {
            if (e_2)
              throw e_2.error;
          }
        }
        for (var fieldName in commonLiteralFields) {
          if (commonLiteralFields[fieldName].length === alternatives.length) {
            try {
              for (var alternatives_3 = (e_3 = void 0, __values2(alternatives)), alternatives_3_1 = alternatives_3.next(); !alternatives_3_1.done; alternatives_3_1 = alternatives_3.next()) {
                var alternative = alternatives_3_1.value;
                if (alternative.reflect.tag === "record") {
                  var field = alternative.reflect.fields[fieldName];
                  if (field.tag === "literal" && (0, util_1.hasKey)(fieldName, value) && value[fieldName] === field.value) {
                    return (0, runtype_1.innerValidate)(alternative, value, visited);
                  }
                }
              }
            } catch (e_3_1) {
              e_3 = { error: e_3_1 };
            } finally {
              try {
                if (alternatives_3_1 && !alternatives_3_1.done && (_c = alternatives_3.return))
                  _c.call(alternatives_3);
              } finally {
                if (e_3)
                  throw e_3.error;
              }
            }
          }
        }
        try {
          for (var alternatives_4 = __values2(alternatives), alternatives_4_1 = alternatives_4.next(); !alternatives_4_1.done; alternatives_4_1 = alternatives_4.next()) {
            var targetType = alternatives_4_1.value;
            if ((0, runtype_1.innerValidate)(targetType, value, visited).success)
              return (0, util_1.SUCCESS)(value);
          }
        } catch (e_4_1) {
          e_4 = { error: e_4_1 };
        } finally {
          try {
            if (alternatives_4_1 && !alternatives_4_1.done && (_d = alternatives_4.return))
              _d.call(alternatives_4);
          } finally {
            if (e_4)
              throw e_4.error;
          }
        }
        return util_1.FAILURE.TYPE_INCORRECT(self, value);
      }, self);
    }
    exports.Union = Union;
  }
});
// node_modules/runtypes/lib/types/literal.js
var require_literal = __commonJS({
  "node_modules/runtypes/lib/types/literal.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Nullish = exports.Null = exports.Undefined = exports.Literal = exports.literal = void 0;
    var runtype_1 = require_runtype();
    var util_1 = require_util();
    var union_1 = require_union();
    function literal(value) {
      return Array.isArray(value) ? String(value.map(String)) : typeof value === "bigint" ? String(value) + "n" : String(value);
    }
    exports.literal = literal;
    function Literal(valueBase) {
      var self = { tag: "literal", value: valueBase };
      return (0, runtype_1.create)(function(value) {
        return value === valueBase ? (0, util_1.SUCCESS)(value) : util_1.FAILURE.VALUE_INCORRECT("literal", "`".concat(literal(valueBase), "`"), "`".concat(literal(value), "`"));
      }, self);
    }
    exports.Literal = Literal;
    exports.Undefined = Literal(void 0);
    exports.Null = Literal(null);
    exports.Nullish = (0, union_1.Union)(exports.Null, exports.Undefined);
  }
});
// node_modules/runtypes/lib/types/template.js
var require_template = __commonJS({
  "node_modules/runtypes/lib/types/template.js"(exports) {
    "use strict";
    var __read2 = exports && exports.__read || function(o, n) {
      var m = typeof Symbol === "function" && o[Symbol.iterator];
      if (!m)
        return o;
      var i = m.call(o), r, ar = [], e;
      try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done)
          ar.push(r.value);
      } catch (error) {
        e = { error };
      } finally {
        try {
          if (r && !r.done && (m = i["return"]))
            m.call(i);
        } finally {
          if (e)
            throw e.error;
        }
      }
      return ar;
    };
    var __spreadArray2 = exports && exports.__spreadArray || function(to, from, pack) {
      if (pack || arguments.length === 2)
        for (var i = 0, l = from.length, ar; i < l; i++) {
          if (ar || !(i in from)) {
            if (!ar)
              ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
          }
        }
      return to.concat(ar || Array.prototype.slice.call(from));
    };
    var __values2 = exports && exports.__values || function(o) {
      var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
      if (m)
        return m.call(o);
      if (o && typeof o.length === "number")
        return {
          next: function() {
            if (o && i >= o.length)
              o = void 0;
            return { value: o && o[i++], done: !o };
          }
        };
      throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Template = void 0;
    var runtype_1 = require_runtype();
    var show_1 = require_show();
    var util_1 = require_util();
    var literal_1 = require_literal();
    var escapeRegExp = function(string) {
      return string.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    };
    var parseArgs = function(args) {
      if (0 < args.length && Array.isArray(args[0])) {
        var _a = __read2(args), strings = _a[0], runtypes = _a.slice(1);
        return [Array.from(strings), runtypes];
      } else {
        var convenient = args;
        var strings = convenient.reduce(function(strings2, arg) {
          if (!(0, runtype_1.isRuntype)(arg))
            strings2.push(strings2.pop() + String(arg));
          else
            strings2.push("");
          return strings2;
        }, [""]);
        var runtypes = convenient.filter(runtype_1.isRuntype);
        return [strings, runtypes];
      }
    };
    var flattenInnerRuntypes = function(strings, runtypes) {
      for (var i = 0; i < runtypes.length; ) {
        switch (runtypes[i].reflect.tag) {
          case "literal": {
            var literal_2 = runtypes[i];
            runtypes.splice(i, 1);
            var string = String(literal_2.value);
            strings.splice(i, 2, strings[i] + string + strings[i + 1]);
            break;
          }
          case "template": {
            var template = runtypes[i];
            runtypes.splice.apply(runtypes, __spreadArray2([i, 1], __read2(template.runtypes), false));
            var innerStrings = template.strings;
            if (innerStrings.length === 1) {
              strings.splice(i, 2, strings[i] + innerStrings[0] + strings[i + 1]);
            } else {
              var first = innerStrings[0];
              var rest = innerStrings.slice(1, -1);
              var last = innerStrings[innerStrings.length - 1];
              strings.splice.apply(strings, __spreadArray2(__spreadArray2([i, 2, strings[i] + first], __read2(rest), false), [last + strings[i + 1]], false));
            }
            break;
          }
          case "union": {
            var union = runtypes[i];
            if (union.alternatives.length === 1) {
              try {
                var literal_3 = getInnerLiteral(union);
                runtypes.splice(i, 1);
                var string = String(literal_3.value);
                strings.splice(i, 2, strings[i] + string + strings[i + 1]);
                break;
              } catch (_) {
                i++;
                break;
              }
            } else {
              i++;
              break;
            }
          }
          case "intersect": {
            var intersect = runtypes[i];
            if (intersect.intersectees.length === 1) {
              try {
                var literal_4 = getInnerLiteral(intersect);
                runtypes.splice(i, 1);
                var string = String(literal_4.value);
                strings.splice(i, 2, strings[i] + string + strings[i + 1]);
                break;
              } catch (_) {
                i++;
                break;
              }
            } else {
              i++;
              break;
            }
          }
          default:
            i++;
            break;
        }
      }
    };
    var normalizeArgs = function(args) {
      var _a = __read2(parseArgs(args), 2), strings = _a[0], runtypes = _a[1];
      flattenInnerRuntypes(strings, runtypes);
      return [strings, runtypes];
    };
    var getInnerLiteral = function(runtype) {
      switch (runtype.reflect.tag) {
        case "literal":
          return runtype;
        case "brand":
          return getInnerLiteral(runtype.reflect.entity);
        case "union":
          if (runtype.reflect.alternatives.length === 1)
            return getInnerLiteral(runtype.reflect.alternatives[0]);
          break;
        case "intersect":
          if (runtype.reflect.intersectees.length === 1)
            return getInnerLiteral(runtype.reflect.intersectees[0]);
          break;
        default:
          break;
      }
      throw void 0;
    };
    var identity = function(s) {
      return s;
    };
    var revivers = {
      string: [function(s) {
        return globalThis.String(s);
      }, ".*"],
      number: [
        function(s) {
          return globalThis.Number(s);
        },
        "[+-]?(?:\\d*\\.\\d+|\\d+\\.\\d*|\\d+)(?:[Ee][+-]?\\d+)?",
        "0[Bb][01]+",
        "0[Oo][0-7]+",
        "0[Xx][0-9A-Fa-f]+"
        // Note: `"NaN"` isn't here, as TS doesn't allow `"NaN"` to be a `` `${number}` ``
      ],
      bigint: [function(s) {
        return globalThis.BigInt(s);
      }, "-?[1-9]d*"],
      boolean: [function(s) {
        return s === "false" ? false : true;
      }, "true", "false"],
      null: [function() {
        return null;
      }, "null"],
      undefined: [function() {
        return void 0;
      }, "undefined"]
    };
    var getReviversFor = function(reflect) {
      switch (reflect.tag) {
        case "literal": {
          var _a = __read2(revivers[(0, util_1.typeOf)(reflect.value)] || [identity], 1), reviver_1 = _a[0];
          return reviver_1;
        }
        case "brand":
          return getReviversFor(reflect.entity);
        case "constraint":
          return getReviversFor(reflect.underlying);
        case "union":
          return reflect.alternatives.map(getReviversFor);
        case "intersect":
          return reflect.intersectees.map(getReviversFor);
        default:
          var _b = __read2(revivers[reflect.tag] || [identity], 1), reviver = _b[0];
          return reviver;
      }
    };
    var reviveValidate = function(reflect, visited) {
      return function(value) {
        var e_1, _a, e_2, _b;
        var revivers2 = getReviversFor(reflect);
        if (Array.isArray(revivers2)) {
          switch (reflect.tag) {
            case "union":
              try {
                for (var _c = __values2(reflect.alternatives), _d = _c.next(); !_d.done; _d = _c.next()) {
                  var alternative = _d.value;
                  var validated = reviveValidate(alternative.reflect, visited)(value);
                  if (validated.success)
                    return validated;
                }
              } catch (e_1_1) {
                e_1 = { error: e_1_1 };
              } finally {
                try {
                  if (_d && !_d.done && (_a = _c.return))
                    _a.call(_c);
                } finally {
                  if (e_1)
                    throw e_1.error;
                }
              }
              return util_1.FAILURE.TYPE_INCORRECT(reflect, value);
            case "intersect":
              try {
                for (var _e = __values2(reflect.intersectees), _f = _e.next(); !_f.done; _f = _e.next()) {
                  var intersectee = _f.value;
                  var validated = reviveValidate(intersectee.reflect, visited)(value);
                  if (!validated.success)
                    return validated;
                }
              } catch (e_2_1) {
                e_2 = { error: e_2_1 };
              } finally {
                try {
                  if (_f && !_f.done && (_b = _e.return))
                    _b.call(_e);
                } finally {
                  if (e_2)
                    throw e_2.error;
                }
              }
              return (0, util_1.SUCCESS)(value);
            default:
              throw Error("impossible");
          }
        } else {
          var reviver = revivers2;
          var validated = (0, runtype_1.innerValidate)(reflect, reviver(value), visited);
          if (!validated.success && validated.code === "VALUE_INCORRECT" && reflect.tag === "literal")
            return util_1.FAILURE.VALUE_INCORRECT("literal", '"'.concat((0, literal_1.literal)(reflect.value), '"'), '"'.concat(value, '"'));
          return validated;
        }
      };
    };
    var getRegExpPatternFor = function(reflect) {
      switch (reflect.tag) {
        case "literal":
          return escapeRegExp(String(reflect.value));
        case "brand":
          return getRegExpPatternFor(reflect.entity);
        case "constraint":
          return getRegExpPatternFor(reflect.underlying);
        case "union":
          return reflect.alternatives.map(getRegExpPatternFor).join("|");
        case "template": {
          return reflect.strings.map(escapeRegExp).reduce(function(pattern, string, i) {
            var prefix = pattern + string;
            var runtype = reflect.runtypes[i];
            if (runtype)
              return prefix + "(?:".concat(getRegExpPatternFor(runtype.reflect), ")");
            else
              return prefix;
          }, "");
        }
        default:
          var _a = __read2(revivers[reflect.tag] || [void 0, ".*"]), patterns = _a.slice(1);
          return patterns.join("|");
      }
    };
    var createRegExpForTemplate = function(reflect) {
      var pattern = reflect.strings.map(escapeRegExp).reduce(function(pattern2, string, i) {
        var prefix = pattern2 + string;
        var runtype = reflect.runtypes[i];
        if (runtype)
          return prefix + "(".concat(getRegExpPatternFor(runtype.reflect), ")");
        else
          return prefix;
      }, "");
      return new RegExp("^".concat(pattern, "$"), "su");
    };
    function Template() {
      var args = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        args[_i] = arguments[_i];
      }
      var _a = __read2(normalizeArgs(args), 2), strings = _a[0], runtypes = _a[1];
      var self = { tag: "template", strings, runtypes };
      var regexp = createRegExpForTemplate(self);
      var test = function(value, visited) {
        var matches = value.match(regexp);
        if (matches) {
          var values = matches.slice(1);
          for (var i = 0; i < runtypes.length; i++) {
            var runtype = runtypes[i];
            var value_1 = values[i];
            var validated = reviveValidate(runtype.reflect, visited)(value_1);
            if (!validated.success)
              return validated;
          }
          return (0, util_1.SUCCESS)(value);
        } else {
          return util_1.FAILURE.VALUE_INCORRECT("string", "".concat((0, show_1.default)(self)), '"'.concat((0, literal_1.literal)(value), '"'));
        }
      };
      return (0, runtype_1.create)(function(value, visited) {
        if (typeof value !== "string")
          return util_1.FAILURE.TYPE_INCORRECT(self, value);
        else {
          var validated = test(value, visited);
          if (!validated.success) {
            var result = util_1.FAILURE.VALUE_INCORRECT("string", "".concat((0, show_1.default)(self)), '"'.concat(value, '"'));
            if (result.message !== validated.message)
              result.message += " (inner: ".concat(validated.message, ")");
            return result;
          } else
            return (0, util_1.SUCCESS)(value);
        }
      }, self);
    }
    exports.Template = Template;
  }
});
// node_modules/runtypes/lib/types/boolean.js
var require_boolean = __commonJS({
  "node_modules/runtypes/lib/types/boolean.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Boolean = void 0;
    var runtype_1 = require_runtype();
    var util_1 = require_util();
    var self = { tag: "boolean" };
    exports.Boolean = (0, runtype_1.create)(function(value) {
      return typeof value === "boolean" ? (0, util_1.SUCCESS)(value) : util_1.FAILURE.TYPE_INCORRECT(self, value);
    }, self);
  }
});
// node_modules/runtypes/lib/types/number.js
var require_number = __commonJS({
  "node_modules/runtypes/lib/types/number.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Number = void 0;
    var runtype_1 = require_runtype();
    var util_1 = require_util();
    var self = { tag: "number" };
    exports.Number = (0, runtype_1.create)(function(value) {
      return typeof value === "number" ? (0, util_1.SUCCESS)(value) : util_1.FAILURE.TYPE_INCORRECT(self, value);
    }, self);
  }
});
// node_modules/runtypes/lib/types/bigint.js
var require_bigint = __commonJS({
  "node_modules/runtypes/lib/types/bigint.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.BigInt = void 0;
    var runtype_1 = require_runtype();
    var util_1 = require_util();
    var self = { tag: "bigint" };
    exports.BigInt = (0, runtype_1.create)(function(value) {
      return typeof value === "bigint" ? (0, util_1.SUCCESS)(value) : util_1.FAILURE.TYPE_INCORRECT(self, value);
    }, self);
  }
});
// node_modules/runtypes/lib/types/string.js
var require_string = __commonJS({
  "node_modules/runtypes/lib/types/string.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.String = void 0;
    var runtype_1 = require_runtype();
    var util_1 = require_util();
    var self = { tag: "string" };
    exports.String = (0, runtype_1.create)(function(value) {
      return typeof value === "string" ? (0, util_1.SUCCESS)(value) : util_1.FAILURE.TYPE_INCORRECT(self, value);
    }, self);
  }
});
// node_modules/runtypes/lib/types/symbol.js
var require_symbol = __commonJS({
  "node_modules/runtypes/lib/types/symbol.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Symbol = void 0;
    var runtype_1 = require_runtype();
    var util_1 = require_util();
    var f = function(key) {
      var self2 = { tag: "symbol", key };
      return (0, runtype_1.create)(function(value) {
        if (typeof value !== "symbol")
          return util_1.FAILURE.TYPE_INCORRECT(self2, value);
        else {
          var keyForValue = globalThis.Symbol.keyFor(value);
          if (keyForValue !== key)
            return util_1.FAILURE.VALUE_INCORRECT("symbol key", quoteIfPresent(key), quoteIfPresent(keyForValue));
          else
            return (0, util_1.SUCCESS)(value);
        }
      }, self2);
    };
    var self = { tag: "symbol" };
    exports.Symbol = (0, runtype_1.create)(function(value) {
      return typeof value === "symbol" ? (0, util_1.SUCCESS)(value) : util_1.FAILURE.TYPE_INCORRECT(self, value);
    }, Object.assign(f, self));
    var quoteIfPresent = function(key) {
      return key === void 0 ? "undefined" : '"'.concat(key, '"');
    };
  }
});
// node_modules/runtypes/lib/types/array.js
var require_array = __commonJS({
  "node_modules/runtypes/lib/types/array.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Array = void 0;
    var runtype_1 = require_runtype();
    var util_1 = require_util();
    function InternalArr(element, isReadonly) {
      var self = { tag: "array", isReadonly, element };
      return withExtraModifierFuncs((0, runtype_1.create)(function(xs, visited) {
        if (!Array.isArray(xs))
          return util_1.FAILURE.TYPE_INCORRECT(self, xs);
        var keys = (0, util_1.enumerableKeysOf)(xs);
        var results = keys.map(function(key) {
          return (0, runtype_1.innerValidate)(element, xs[key], visited);
        });
        var details = keys.reduce(function(details2, key) {
          var result = results[key];
          if (!result.success)
            details2[key] = result.details || result.message;
          return details2;
        }, []);
        if ((0, util_1.enumerableKeysOf)(details).length !== 0)
          return util_1.FAILURE.CONTENT_INCORRECT(self, details);
        else
          return (0, util_1.SUCCESS)(xs);
      }, self));
    }
    function Arr(element) {
      return InternalArr(element, false);
    }
    exports.Array = Arr;
    function withExtraModifierFuncs(A) {
      A.asReadonly = asReadonly;
      return A;
      function asReadonly() {
        return InternalArr(A.element, true);
      }
    }
  }
});
// node_modules/runtypes/lib/types/tuple.js
var require_tuple = __commonJS({
  "node_modules/runtypes/lib/types/tuple.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Tuple = void 0;
    var runtype_1 = require_runtype();
    var util_1 = require_util();
    function Tuple() {
      var components = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        components[_i] = arguments[_i];
      }
      var self = { tag: "tuple", components };
      return (0, runtype_1.create)(function(xs, visited) {
        if (!Array.isArray(xs))
          return util_1.FAILURE.TYPE_INCORRECT(self, xs);
        if (xs.length !== components.length)
          return util_1.FAILURE.CONSTRAINT_FAILED(self, "Expected length ".concat(components.length, ", but was ").concat(xs.length));
        var keys = (0, util_1.enumerableKeysOf)(xs);
        var results = keys.map(function(key) {
          return (0, runtype_1.innerValidate)(components[key], xs[key], visited);
        });
        var details = keys.reduce(function(details2, key) {
          var result = results[key];
          if (!result.success)
            details2[key] = result.details || result.message;
          return details2;
        }, []);
        if ((0, util_1.enumerableKeysOf)(details).length !== 0)
          return util_1.FAILURE.CONTENT_INCORRECT(self, details);
        else
          return (0, util_1.SUCCESS)(xs);
      }, self);
    }
    exports.Tuple = Tuple;
  }
});
// node_modules/runtypes/lib/types/record.js
var require_record = __commonJS({
  "node_modules/runtypes/lib/types/record.js"(exports) {
    "use strict";
    var __read2 = exports && exports.__read || function(o, n) {
      var m = typeof Symbol === "function" && o[Symbol.iterator];
      if (!m)
        return o;
      var i = m.call(o), r, ar = [], e;
      try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done)
          ar.push(r.value);
      } catch (error) {
        e = { error };
      } finally {
        try {
          if (r && !r.done && (m = i["return"]))
            m.call(i);
        } finally {
          if (e)
            throw e.error;
        }
      }
      return ar;
    };
    var __spreadArray2 = exports && exports.__spreadArray || function(to, from, pack) {
      if (pack || arguments.length === 2)
        for (var i = 0, l = from.length, ar; i < l; i++) {
          if (ar || !(i in from)) {
            if (!ar)
              ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
          }
        }
      return to.concat(ar || Array.prototype.slice.call(from));
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Partial = exports.Record = exports.InternalRecord = void 0;
    var runtype_1 = require_runtype();
    var util_1 = require_util();
    function InternalRecord(fields, isPartial, isReadonly) {
      var self = { tag: "record", isPartial, isReadonly, fields };
      return withExtraModifierFuncs((0, runtype_1.create)(function(x, visited) {
        if (x === null || x === void 0) {
          return util_1.FAILURE.TYPE_INCORRECT(self, x);
        }
        var keysOfFields = (0, util_1.enumerableKeysOf)(fields);
        if (keysOfFields.length !== 0 && typeof x !== "object")
          return util_1.FAILURE.TYPE_INCORRECT(self, x);
        var keys = __spreadArray2([], __read2(new Set(__spreadArray2(__spreadArray2([], __read2(keysOfFields), false), __read2((0, util_1.enumerableKeysOf)(x)), false))), false);
        var results = keys.reduce(function(results2, key) {
          var fieldsHasKey = (0, util_1.hasKey)(key, fields);
          var xHasKey = (0, util_1.hasKey)(key, x);
          if (fieldsHasKey) {
            var runtype = fields[key];
            var isOptional = isPartial || runtype.reflect.tag === "optional";
            if (xHasKey) {
              var value = x[key];
              if (isOptional && value === void 0)
                results2[key] = (0, util_1.SUCCESS)(value);
              else
                results2[key] = (0, runtype_1.innerValidate)(runtype, value, visited);
            } else {
              if (!isOptional)
                results2[key] = util_1.FAILURE.PROPERTY_MISSING(runtype.reflect);
              else
                results2[key] = (0, util_1.SUCCESS)(void 0);
            }
          } else if (xHasKey) {
            var value = x[key];
            results2[key] = (0, util_1.SUCCESS)(value);
          } else {
            throw new Error("impossible");
          }
          return results2;
        }, {});
        var details = keys.reduce(function(details2, key) {
          var result = results[key];
          if (!result.success)
            details2[key] = result.details || result.message;
          return details2;
        }, {});
        if ((0, util_1.enumerableKeysOf)(details).length !== 0)
          return util_1.FAILURE.CONTENT_INCORRECT(self, details);
        else
          return (0, util_1.SUCCESS)(x);
      }, self));
    }
    exports.InternalRecord = InternalRecord;
    function Record(fields) {
      return InternalRecord(fields, false, false);
    }
    exports.Record = Record;
    function Partial(fields) {
      return InternalRecord(fields, true, false);
    }
    exports.Partial = Partial;
    function withExtraModifierFuncs(A) {
      A.asPartial = asPartial;
      A.asReadonly = asReadonly;
      A.pick = pick;
      A.omit = omit;
      A.extend = extend;
      return A;
      function asPartial() {
        return InternalRecord(A.fields, true, A.isReadonly);
      }
      function asReadonly() {
        return InternalRecord(A.fields, A.isPartial, true);
      }
      function pick() {
        var keys = [];
        for (var _i = 0; _i < arguments.length; _i++) {
          keys[_i] = arguments[_i];
        }
        var result = {};
        keys.forEach(function(key) {
          result[key] = A.fields[key];
        });
        return InternalRecord(result, A.isPartial, A.isReadonly);
      }
      function omit() {
        var keys = [];
        for (var _i = 0; _i < arguments.length; _i++) {
          keys[_i] = arguments[_i];
        }
        var result = {};
        var existingKeys = (0, util_1.enumerableKeysOf)(A.fields);
        existingKeys.forEach(function(key) {
          if (!keys.includes(key))
            result[key] = A.fields[key];
        });
        return InternalRecord(result, A.isPartial, A.isReadonly);
      }
      function extend(fields) {
        return InternalRecord(Object.assign({}, A.fields, fields), A.isPartial, A.isReadonly);
      }
    }
  }
});
// node_modules/runtypes/lib/types/constraint.js
var require_constraint = __commonJS({
  "node_modules/runtypes/lib/types/constraint.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Guard = exports.Constraint = void 0;
    var runtype_1 = require_runtype();
    var util_1 = require_util();
    var unknown_1 = require_unknown();
    function Constraint(underlying, constraint, options) {
      var name = options && options.name;
      var args = options && options.args;
      var self = {
        tag: "constraint",
        underlying,
        constraint,
        name,
        args
      };
      return (0, runtype_1.create)(function(value) {
        var result = underlying.validate(value);
        if (!result.success)
          return result;
        var message = constraint(result.value);
        if (typeof message === "string")
          return util_1.FAILURE.CONSTRAINT_FAILED(self, message);
        else if (!message)
          return util_1.FAILURE.CONSTRAINT_FAILED(self);
        return (0, util_1.SUCCESS)(result.value);
      }, self);
    }
    exports.Constraint = Constraint;
    var Guard = function(guard, options) {
      return unknown_1.Unknown.withGuard(guard, options);
    };
    exports.Guard = Guard;
  }
});
// node_modules/runtypes/lib/types/dictionary.js
var require_dictionary = __commonJS({
  "node_modules/runtypes/lib/types/dictionary.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Dictionary = void 0;
    var runtype_1 = require_runtype();
    var string_1 = require_string();
    var constraint_1 = require_constraint();
    var show_1 = require_show();
    var util_1 = require_util();
    var NumberKey = (0, constraint_1.Constraint)(string_1.String, function(s) {
      return !isNaN(+s);
    }, { name: "number" });
    function Dictionary(value, key) {
      var keyRuntype = key === void 0 ? string_1.String : key === "string" ? string_1.String : key === "number" ? NumberKey : key;
      var keyString = (0, show_1.default)(keyRuntype);
      var self = { tag: "dictionary", key: keyString, value };
      return (0, runtype_1.create)(function(x, visited) {
        if (x === null || x === void 0 || typeof x !== "object")
          return util_1.FAILURE.TYPE_INCORRECT(self, x);
        if (Object.getPrototypeOf(x) !== Object.prototype) {
          if (!Array.isArray(x) || keyString === "string")
            return util_1.FAILURE.TYPE_INCORRECT(self, x);
        }
        var numberString = /^(?:NaN|-?\d+(?:\.\d+)?)$/;
        var keys = (0, util_1.enumerableKeysOf)(x);
        var results = keys.reduce(function(results2, key2) {
          var isNumberLikeKey = typeof key2 === "string" && numberString.test(key2);
          var keyInterop = isNumberLikeKey ? globalThis.Number(key2) : key2;
          if (isNumberLikeKey ? !keyRuntype.guard(keyInterop) && !keyRuntype.guard(key2) : !keyRuntype.guard(keyInterop)) {
            results2[key2] = util_1.FAILURE.KEY_INCORRECT(self, keyRuntype.reflect, keyInterop);
          } else
            results2[key2] = (0, runtype_1.innerValidate)(value, x[key2], visited);
          return results2;
        }, {});
        var details = keys.reduce(function(details2, key2) {
          var result = results[key2];
          if (!result.success)
            details2[key2] = result.details || result.message;
          return details2;
        }, {});
        if ((0, util_1.enumerableKeysOf)(details).length !== 0)
          return util_1.FAILURE.CONTENT_INCORRECT(self, details);
        else
          return (0, util_1.SUCCESS)(x);
      }, self);
    }
    exports.Dictionary = Dictionary;
  }
});
// node_modules/runtypes/lib/types/intersect.js
var require_intersect = __commonJS({
  "node_modules/runtypes/lib/types/intersect.js"(exports) {
    "use strict";
    var __values2 = exports && exports.__values || function(o) {
      var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
      if (m)
        return m.call(o);
      if (o && typeof o.length === "number")
        return {
          next: function() {
            if (o && i >= o.length)
              o = void 0;
            return { value: o && o[i++], done: !o };
          }
        };
      throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Intersect = void 0;
    var runtype_1 = require_runtype();
    var util_1 = require_util();
    function Intersect() {
      var intersectees = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        intersectees[_i] = arguments[_i];
      }
      var self = { tag: "intersect", intersectees };
      return (0, runtype_1.create)(function(value, visited) {
        var e_1, _a;
        try {
          for (var intersectees_1 = __values2(intersectees), intersectees_1_1 = intersectees_1.next(); !intersectees_1_1.done; intersectees_1_1 = intersectees_1.next()) {
            var targetType = intersectees_1_1.value;
            var result = (0, runtype_1.innerValidate)(targetType, value, visited);
            if (!result.success)
              return result;
          }
        } catch (e_1_1) {
          e_1 = { error: e_1_1 };
        } finally {
          try {
            if (intersectees_1_1 && !intersectees_1_1.done && (_a = intersectees_1.return))
              _a.call(intersectees_1);
          } finally {
            if (e_1)
              throw e_1.error;
          }
        }
        return (0, util_1.SUCCESS)(value);
      }, self);
    }
    exports.Intersect = Intersect;
  }
});
// node_modules/runtypes/lib/types/optional.js
var require_optional = __commonJS({
  "node_modules/runtypes/lib/types/optional.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Optional = void 0;
    var runtype_1 = require_runtype();
    var util_1 = require_util();
    function Optional(runtype) {
      var self = { tag: "optional", underlying: runtype };
      return (0, runtype_1.create)(function(value) {
        return value === void 0 ? (0, util_1.SUCCESS)(value) : runtype.validate(value);
      }, self);
    }
    exports.Optional = Optional;
  }
});
// node_modules/runtypes/lib/types/function.js
var require_function = __commonJS({
  "node_modules/runtypes/lib/types/function.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Function = void 0;
    var runtype_1 = require_runtype();
    var util_1 = require_util();
    var self = { tag: "function" };
    exports.Function = (0, runtype_1.create)(function(value) {
      return typeof value === "function" ? (0, util_1.SUCCESS)(value) : util_1.FAILURE.TYPE_INCORRECT(self, value);
    }, self);
  }
});
// node_modules/runtypes/lib/types/instanceof.js
var require_instanceof = __commonJS({
  "node_modules/runtypes/lib/types/instanceof.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.InstanceOf = void 0;
    var runtype_1 = require_runtype();
    var util_1 = require_util();
    function InstanceOf(ctor) {
      var self = { tag: "instanceof", ctor };
      return (0, runtype_1.create)(function(value) {
        return value instanceof ctor ? (0, util_1.SUCCESS)(value) : util_1.FAILURE.TYPE_INCORRECT(self, value);
      }, self);
    }
    exports.InstanceOf = InstanceOf;
  }
});
// node_modules/runtypes/lib/types/lazy.js
var require_lazy = __commonJS({
  "node_modules/runtypes/lib/types/lazy.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Lazy = void 0;
    var runtype_1 = require_runtype();
    function Lazy(delayed) {
      var data = {
        get tag() {
          return getWrapped()["tag"];
        }
      };
      var cached;
      function getWrapped() {
        if (!cached) {
          cached = delayed();
          for (var k in cached)
            if (k !== "tag")
              data[k] = cached[k];
        }
        return cached;
      }
      return (0, runtype_1.create)(function(x) {
        return getWrapped().validate(x);
      }, data);
    }
    exports.Lazy = Lazy;
  }
});
// node_modules/runtypes/lib/types/brand.js
var require_brand = __commonJS({
  "node_modules/runtypes/lib/types/brand.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Brand = void 0;
    var runtype_1 = require_runtype();
    function Brand(brand, entity) {
      var self = { tag: "brand", brand, entity };
      return (0, runtype_1.create)(function(value) {
        return entity.validate(value);
      }, self);
    }
    exports.Brand = Brand;
  }
});
// node_modules/runtypes/lib/decorator.js
var require_decorator = __commonJS({
  "node_modules/runtypes/lib/decorator.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.checked = exports.check = void 0;
    var errors_1 = require_errors();
    var util_1 = require_util();
    var prototypes = /* @__PURE__ */ new WeakMap();
    function check(target, propertyKey, parameterIndex) {
      var prototype = prototypes.get(target) || /* @__PURE__ */ new Map();
      prototypes.set(target, prototype);
      var validParameterIndices = prototype.get(propertyKey) || [];
      prototype.set(propertyKey, validParameterIndices);
      validParameterIndices.push(parameterIndex);
    }
    exports.check = check;
    function getValidParameterIndices(target, propertyKey, runtypeCount) {
      var prototype = prototypes.get(target);
      var validParameterIndices = prototype && prototype.get(propertyKey);
      if (validParameterIndices) {
        return validParameterIndices;
      }
      var indices = [];
      for (var i = 0; i < runtypeCount; i++) {
        indices.push(i);
      }
      return indices;
    }
    function checked() {
      var runtypes = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        runtypes[_i] = arguments[_i];
      }
      if (runtypes.length === 0) {
        throw new Error("No runtype provided to `@checked`. Please remove the decorator.");
      }
      return function(target, propertyKey, descriptor) {
        var method = descriptor.value;
        var methodId = (target.name || target.constructor.name + ".prototype") + (typeof propertyKey === "string" ? '["'.concat(propertyKey, '"]') : "[".concat(String(propertyKey), "]"));
        var validParameterIndices = getValidParameterIndices(target, propertyKey, runtypes.length);
        if (validParameterIndices.length !== runtypes.length) {
          throw new Error("Number of `@checked` runtypes and @check parameters not matched.");
        }
        if (validParameterIndices.length > method.length) {
          throw new Error("Number of `@checked` runtypes exceeds actual parameter length.");
        }
        descriptor.value = function() {
          var args = [];
          for (var _i2 = 0; _i2 < arguments.length; _i2++) {
            args[_i2] = arguments[_i2];
          }
          runtypes.forEach(function(type, typeIndex) {
            var parameterIndex = validParameterIndices[typeIndex];
            var result = type.validate(args[parameterIndex]);
            if (!result.success) {
              var message = "".concat(methodId, ", argument #").concat(parameterIndex, ": ").concat(result.message);
              var failure = util_1.FAILURE.ARGUMENT_INCORRECT(message);
              throw new errors_1.ValidationError(failure);
            }
          });
          return method.apply(this, args);
        };
      };
    }
    exports.checked = checked;
  }
});
// node_modules/runtypes/lib/index.js
var require_lib = __commonJS({
  "node_modules/runtypes/lib/index.js"(exports) {
    "use strict";
    var __createBinding2 = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0)
        k2 = k;
      Object.defineProperty(o, k2, { enumerable: true, get: function() {
        return m[k];
      } });
    } : function(o, m, k, k2) {
      if (k2 === void 0)
        k2 = k;
      o[k2] = m[k];
    });
    var __exportStar2 = exports && exports.__exportStar || function(m, exports2) {
      for (var p in m)
        if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p))
          __createBinding2(exports2, m, p);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.InstanceOf = exports.Nullish = exports.Null = exports.Undefined = exports.Literal = void 0;
    __exportStar2(require_reflect(), exports);
    __exportStar2(require_result(), exports);
    __exportStar2(require_contract(), exports);
    __exportStar2(require_asynccontract(), exports);
    __exportStar2(require_match(), exports);
    __exportStar2(require_errors(), exports);
    __exportStar2(require_unknown(), exports);
    __exportStar2(require_never(), exports);
    __exportStar2(require_void(), exports);
    var literal_1 = require_literal();
    Object.defineProperty(exports, "Literal", { enumerable: true, get: function() {
      return literal_1.Literal;
    } });
    Object.defineProperty(exports, "Undefined", { enumerable: true, get: function() {
      return literal_1.Undefined;
    } });
    Object.defineProperty(exports, "Null", { enumerable: true, get: function() {
      return literal_1.Null;
    } });
    Object.defineProperty(exports, "Nullish", { enumerable: true, get: function() {
      return literal_1.Nullish;
    } });
    __exportStar2(require_template(), exports);
    __exportStar2(require_boolean(), exports);
    __exportStar2(require_number(), exports);
    __exportStar2(require_bigint(), exports);
    __exportStar2(require_string(), exports);
    __exportStar2(require_symbol(), exports);
    __exportStar2(require_array(), exports);
    __exportStar2(require_tuple(), exports);
    __exportStar2(require_record(), exports);
    __exportStar2(require_dictionary(), exports);
    __exportStar2(require_union(), exports);
    __exportStar2(require_intersect(), exports);
    __exportStar2(require_optional(), exports);
    __exportStar2(require_function(), exports);
    var instanceof_1 = require_instanceof();
    Object.defineProperty(exports, "InstanceOf", { enumerable: true, get: function() {
      return instanceof_1.InstanceOf;
    } });
    __exportStar2(require_lazy(), exports);
    __exportStar2(require_constraint(), exports);
    __exportStar2(require_brand(), exports);
    __exportStar2(require_decorator(), exports);
  }
});
// node_modules/axios/lib/helpers/bind.js
var require_bind = __commonJS({
  "node_modules/axios/lib/helpers/bind.js"(exports, module2) {
    "use strict";
    module2.exports = function bind(fn, thisArg) {
      return function wrap() {
        var args = new Array(arguments.length);
        for (var i = 0; i < args.length; i++) {
          args[i] = arguments[i];
        }
        return fn.apply(thisArg, args);
      };
    };
  }
});
// node_modules/axios/lib/utils.js
var require_utils = __commonJS({
  "node_modules/axios/lib/utils.js"(exports, module2) {
    "use strict";
    var bind = require_bind();
    var toString = Object.prototype.toString;
    var kindOf = function(cache) {
      return function(thing) {
        var str = toString.call(thing);
        return cache[str] || (cache[str] = str.slice(8, -1).toLowerCase());
      };
    }(/* @__PURE__ */ Object.create(null));
    function kindOfTest(type) {
      type = type.toLowerCase();
      return function isKindOf(thing) {
        return kindOf(thing) === type;
      };
    }
    function isArray(val) {
      return Array.isArray(val);
    }
    function isUndefined(val) {
      return typeof val === "undefined";
    }
    function isBuffer(val) {
      return val !== null && !isUndefined(val) && val.constructor !== null && !isUndefined(val.constructor) && typeof val.constructor.isBuffer === "function" && val.constructor.isBuffer(val);
    }
    var isArrayBuffer = kindOfTest("ArrayBuffer");
    function isArrayBufferView(val) {
      var result;
      if (typeof ArrayBuffer !== "undefined" && ArrayBuffer.isView) {
        result = ArrayBuffer.isView(val);
      } else {
        result = val && val.buffer && isArrayBuffer(val.buffer);
      }
      return result;
    }
    function isString(val) {
      return typeof val === "string";
    }
    function isNumber(val) {
      return typeof val === "number";
    }
    function isObject(val) {
      return val !== null && typeof val === "object";
    }
    function isPlainObject(val) {
      if (kindOf(val) !== "object") {
        return false;
      }
      var prototype = Object.getPrototypeOf(val);
      return prototype === null || prototype === Object.prototype;
    }
    var isDate = kindOfTest("Date");
    var isFile = kindOfTest("File");
    var isBlob = kindOfTest("Blob");
    var isFileList = kindOfTest("FileList");
    function isFunction(val) {
      return toString.call(val) === "[object Function]";
    }
    function isStream(val) {
      return isObject(val) && isFunction(val.pipe);
    }
    function isFormData(thing) {
      var pattern = "[object FormData]";
      return thing && (typeof FormData === "function" && thing instanceof FormData || toString.call(thing) === pattern || isFunction(thing.toString) && thing.toString() === pattern);
    }
    var isURLSearchParams = kindOfTest("URLSearchParams");
    function trim(str) {
      return str.trim ? str.trim() : str.replace(/^\s+|\s+$/g, "");
    }
    function isStandardBrowserEnv() {
      if (typeof navigator !== "undefined" && (navigator.product === "ReactNative" || navigator.product === "NativeScript" || navigator.product === "NS")) {
        return false;
      }
      return typeof window !== "undefined" && typeof document !== "undefined";
    }
    function forEach(obj, fn) {
      if (obj === null || typeof obj === "undefined") {
        return;
      }
      if (typeof obj !== "object") {
        obj = [obj];
      }
      if (isArray(obj)) {
        for (var i = 0, l = obj.length; i < l; i++) {
          fn.call(null, obj[i], i, obj);
        }
      } else {
        for (var key in obj) {
          if (Object.prototype.hasOwnProperty.call(obj, key)) {
            fn.call(null, obj[key], key, obj);
          }
        }
      }
    }
    function merge() {
      var result = {};
      function assignValue(val, key) {
        if (isPlainObject(result[key]) && isPlainObject(val)) {
          result[key] = merge(result[key], val);
        } else if (isPlainObject(val)) {
          result[key] = merge({}, val);
        } else if (isArray(val)) {
          result[key] = val.slice();
        } else {
          result[key] = val;
        }
      }
      for (var i = 0, l = arguments.length; i < l; i++) {
        forEach(arguments[i], assignValue);
      }
      return result;
    }
    function extend(a, b, thisArg) {
      forEach(b, function assignValue(val, key) {
        if (thisArg && typeof val === "function") {
          a[key] = bind(val, thisArg);
        } else {
          a[key] = val;
        }
      });
      return a;
    }
    function stripBOM(content) {
      if (content.charCodeAt(0) === 65279) {
        content = content.slice(1);
      }
      return content;
    }
    function inherits(constructor, superConstructor, props, descriptors) {
      constructor.prototype = Object.create(superConstructor.prototype, descriptors);
      constructor.prototype.constructor = constructor;
      props && Object.assign(constructor.prototype, props);
    }
    function toFlatObject(sourceObj, destObj, filter) {
      var props;
      var i;
      var prop;
      var merged = {};
      destObj = destObj || {};
      do {
        props = Object.getOwnPropertyNames(sourceObj);
        i = props.length;
        while (i-- > 0) {
          prop = props[i];
          if (!merged[prop]) {
            destObj[prop] = sourceObj[prop];
            merged[prop] = true;
          }
        }
        sourceObj = Object.getPrototypeOf(sourceObj);
      } while (sourceObj && (!filter || filter(sourceObj, destObj)) && sourceObj !== Object.prototype);
      return destObj;
    }
    function endsWith(str, searchString, position) {
      str = String(str);
      if (position === void 0 || position > str.length) {
        position = str.length;
      }
      position -= searchString.length;
      var lastIndex = str.indexOf(searchString, position);
      return lastIndex !== -1 && lastIndex === position;
    }
    function toArray(thing) {
      if (!thing)
        return null;
      var i = thing.length;
      if (isUndefined(i))
        return null;
      var arr = new Array(i);
      while (i-- > 0) {
        arr[i] = thing[i];
      }
      return arr;
    }
    var isTypedArray = function(TypedArray) {
      return function(thing) {
        return TypedArray && thing instanceof TypedArray;
      };
    }(typeof Uint8Array !== "undefined" && Object.getPrototypeOf(Uint8Array));
    module2.exports = {
      isArray,
      isArrayBuffer,
      isBuffer,
      isFormData,
      isArrayBufferView,
      isString,
      isNumber,
      isObject,
      isPlainObject,
      isUndefined,
      isDate,
      isFile,
      isBlob,
      isFunction,
      isStream,
      isURLSearchParams,
      isStandardBrowserEnv,
      forEach,
      merge,
      extend,
      trim,
      stripBOM,
      inherits,
      toFlatObject,
      kindOf,
      kindOfTest,
      endsWith,
      toArray,
      isTypedArray,
      isFileList
    };
  }
});
// node_modules/axios/lib/helpers/buildURL.js
var require_buildURL = __commonJS({
  "node_modules/axios/lib/helpers/buildURL.js"(exports, module2) {
    "use strict";
    var utils = require_utils();
    function encode(val) {
      return encodeURIComponent(val).replace(/%3A/gi, ":").replace(/%24/g, "$").replace(/%2C/gi, ",").replace(/%20/g, "+").replace(/%5B/gi, "[").replace(/%5D/gi, "]");
    }
    module2.exports = function buildURL(url, params, paramsSerializer) {
      if (!params) {
        return url;
      }
      var serializedParams;
      if (paramsSerializer) {
        serializedParams = paramsSerializer(params);
      } else if (utils.isURLSearchParams(params)) {
        serializedParams = params.toString();
      } else {
        var parts = [];
        utils.forEach(params, function serialize(val, key) {
          if (val === null || typeof val === "undefined") {
            return;
          }
          if (utils.isArray(val)) {
            key = key + "[]";
          } else {
            val = [val];
          }
          utils.forEach(val, function parseValue(v) {
            if (utils.isDate(v)) {
              v = v.toISOString();
            } else if (utils.isObject(v)) {
              v = JSON.stringify(v);
            }
            parts.push(encode(key) + "=" + encode(v));
          });
        });
        serializedParams = parts.join("&");
      }
      if (serializedParams) {
        var hashmarkIndex = url.indexOf("#");
        if (hashmarkIndex !== -1) {
          url = url.slice(0, hashmarkIndex);
        }
        url += (url.indexOf("?") === -1 ? "?" : "&") + serializedParams;
      }
      return url;
    };
  }
});
// node_modules/axios/lib/core/InterceptorManager.js
var require_InterceptorManager = __commonJS({
  "node_modules/axios/lib/core/InterceptorManager.js"(exports, module2) {
    "use strict";
    var utils = require_utils();
    function InterceptorManager() {
      this.handlers = [];
    }
    InterceptorManager.prototype.use = function use(fulfilled, rejected, options) {
      this.handlers.push({
        fulfilled,
        rejected,
        synchronous: options ? options.synchronous : false,
        runWhen: options ? options.runWhen : null
      });
      return this.handlers.length - 1;
    };
    InterceptorManager.prototype.eject = function eject(id) {
      if (this.handlers[id]) {
        this.handlers[id] = null;
      }
    };
    InterceptorManager.prototype.forEach = function forEach(fn) {
      utils.forEach(this.handlers, function forEachHandler(h) {
        if (h !== null) {
          fn(h);
        }
      });
    };
    module2.exports = InterceptorManager;
  }
});
// node_modules/axios/lib/helpers/normalizeHeaderName.js
var require_normalizeHeaderName = __commonJS({
  "node_modules/axios/lib/helpers/normalizeHeaderName.js"(exports, module2) {
    "use strict";
    var utils = require_utils();
    module2.exports = function normalizeHeaderName(headers, normalizedName) {
      utils.forEach(headers, function processHeader(value, name) {
        if (name !== normalizedName && name.toUpperCase() === normalizedName.toUpperCase()) {
          headers[normalizedName] = value;
          delete headers[name];
        }
      });
    };
  }
});
// node_modules/axios/lib/core/AxiosError.js
var require_AxiosError = __commonJS({
  "node_modules/axios/lib/core/AxiosError.js"(exports, module2) {
    "use strict";
    var utils = require_utils();
    function AxiosError(message, code, config, request, response) {
      Error.call(this);
      this.message = message;
      this.name = "AxiosError";
      code && (this.code = code);
      config && (this.config = config);
      request && (this.request = request);
      response && (this.response = response);
    }
    utils.inherits(AxiosError, Error, {
      toJSON: function toJSON() {
        return {
          // Standard
          message: this.message,
          name: this.name,
          // Microsoft
          description: this.description,
          number: this.number,
          // Mozilla
          fileName: this.fileName,
          lineNumber: this.lineNumber,
          columnNumber: this.columnNumber,
          stack: this.stack,
          // Axios
          config: this.config,
          code: this.code,
          status: this.response && this.response.status ? this.response.status : null
        };
      }
    });
    var prototype = AxiosError.prototype;
    var descriptors = {};
    [
      "ERR_BAD_OPTION_VALUE",
      "ERR_BAD_OPTION",
      "ECONNABORTED",
      "ETIMEDOUT",
      "ERR_NETWORK",
      "ERR_FR_TOO_MANY_REDIRECTS",
      "ERR_DEPRECATED",
      "ERR_BAD_RESPONSE",
      "ERR_BAD_REQUEST",
      "ERR_CANCELED"
      // eslint-disable-next-line func-names
    ].forEach(function(code) {
      descriptors[code] = { value: code };
    });
    Object.defineProperties(AxiosError, descriptors);
    Object.defineProperty(prototype, "isAxiosError", { value: true });
    AxiosError.from = function(error, code, config, request, response, customProps) {
      var axiosError = Object.create(prototype);
      utils.toFlatObject(error, axiosError, function filter(obj) {
        return obj !== Error.prototype;
      });
      AxiosError.call(axiosError, error.message, code, config, request, response);
      axiosError.name = error.name;
      customProps && Object.assign(axiosError, customProps);
      return axiosError;
    };
    module2.exports = AxiosError;
  }
});
// node_modules/axios/lib/defaults/transitional.js
var require_transitional = __commonJS({
  "node_modules/axios/lib/defaults/transitional.js"(exports, module2) {
    "use strict";
    module2.exports = {
      silentJSONParsing: true,
      forcedJSONParsing: true,
      clarifyTimeoutError: false
    };
  }
});
// node_modules/axios/lib/helpers/toFormData.js
var require_toFormData = __commonJS({
  "node_modules/axios/lib/helpers/toFormData.js"(exports, module2) {
    "use strict";
    var utils = require_utils();
    function toFormData(obj, formData) {
      formData = formData || new FormData();
      var stack = [];
      function convertValue(value) {
        if (value === null)
          return "";
        if (utils.isDate(value)) {
          return value.toISOString();
        }
        if (utils.isArrayBuffer(value) || utils.isTypedArray(value)) {
          return typeof Blob === "function" ? new Blob([value]) : Buffer.from(value);
        }
        return value;
      }
      function build(data, parentKey) {
        if (utils.isPlainObject(data) || utils.isArray(data)) {
          if (stack.indexOf(data) !== -1) {
            throw Error("Circular reference detected in " + parentKey);
          }
          stack.push(data);
          utils.forEach(data, function each(value, key) {
            if (utils.isUndefined(value))
              return;
            var fullKey = parentKey ? parentKey + "." + key : key;
            var arr;
            if (value && !parentKey && typeof value === "object") {
              if (utils.endsWith(key, "{}")) {
                value = JSON.stringify(value);
              } else if (utils.endsWith(key, "[]") && (arr = utils.toArray(value))) {
                arr.forEach(function(el) {
                  !utils.isUndefined(el) && formData.append(fullKey, convertValue(el));
                });
                return;
              }
            }
            build(value, fullKey);
          });
          stack.pop();
        } else {
          formData.append(parentKey, convertValue(data));
        }
      }
      build(obj);
      return formData;
    }
    module2.exports = toFormData;
  }
});
// node_modules/axios/lib/core/settle.js
var require_settle = __commonJS({
  "node_modules/axios/lib/core/settle.js"(exports, module2) {
    "use strict";
    var AxiosError = require_AxiosError();
    module2.exports = function settle(resolve, reject, response) {
      var validateStatus = response.config.validateStatus;
      if (!response.status || !validateStatus || validateStatus(response.status)) {
        resolve(response);
      } else {
        reject(new AxiosError(
          "Request failed with status code " + response.status,
          [AxiosError.ERR_BAD_REQUEST, AxiosError.ERR_BAD_RESPONSE][Math.floor(response.status / 100) - 4],
          response.config,
          response.request,
          response
        ));
      }
    };
  }
});
// node_modules/axios/lib/helpers/cookies.js
var require_cookies = __commonJS({
  "node_modules/axios/lib/helpers/cookies.js"(exports, module2) {
    "use strict";
    var utils = require_utils();
    module2.exports = utils.isStandardBrowserEnv() ? (
      // Standard browser envs support document.cookie
      function standardBrowserEnv() {
        return {
          write: function write(name, value, expires, path, domain, secure) {
            var cookie = [];
            cookie.push(name + "=" + encodeURIComponent(value));
            if (utils.isNumber(expires)) {
              cookie.push("expires=" + new Date(expires).toGMTString());
            }
            if (utils.isString(path)) {
              cookie.push("path=" + path);
            }
            if (utils.isString(domain)) {
              cookie.push("domain=" + domain);
            }
            if (secure === true) {
              cookie.push("secure");
            }
            document.cookie = cookie.join("; ");
          },
          read: function read(name) {
            var match = document.cookie.match(new RegExp("(^|;\\s*)(" + name + ")=([^;]*)"));
            return match ? decodeURIComponent(match[3]) : null;
          },
          remove: function remove(name) {
            this.write(name, "", Date.now() - 864e5);
          }
        };
      }()
    ) : (
      // Non standard browser env (web workers, react-native) lack needed support.
      function nonStandardBrowserEnv() {
        return {
          write: function write() {
          },
          read: function read() {
            return null;
          },
          remove: function remove() {
          }
        };
      }()
    );
  }
});
// node_modules/axios/lib/helpers/isAbsoluteURL.js
var require_isAbsoluteURL = __commonJS({
  "node_modules/axios/lib/helpers/isAbsoluteURL.js"(exports, module2) {
    "use strict";
    module2.exports = function isAbsoluteURL(url) {
      return /^([a-z][a-z\d+\-.]*:)?\/\//i.test(url);
    };
  }
});
// node_modules/axios/lib/helpers/combineURLs.js
var require_combineURLs = __commonJS({
  "node_modules/axios/lib/helpers/combineURLs.js"(exports, module2) {
    "use strict";
    module2.exports = function combineURLs(baseURL, relativeURL) {
      return relativeURL ? baseURL.replace(/\/+$/, "") + "/" + relativeURL.replace(/^\/+/, "") : baseURL;
    };
  }
});
// node_modules/axios/lib/core/buildFullPath.js
var require_buildFullPath = __commonJS({
  "node_modules/axios/lib/core/buildFullPath.js"(exports, module2) {
    "use strict";
    var isAbsoluteURL = require_isAbsoluteURL();
    var combineURLs = require_combineURLs();
    module2.exports = function buildFullPath(baseURL, requestedURL) {
      if (baseURL && !isAbsoluteURL(requestedURL)) {
        return combineURLs(baseURL, requestedURL);
      }
      return requestedURL;
    };
  }
});
// node_modules/axios/lib/helpers/parseHeaders.js
var require_parseHeaders = __commonJS({
  "node_modules/axios/lib/helpers/parseHeaders.js"(exports, module2) {
    "use strict";
    var utils = require_utils();
    var ignoreDuplicateOf = [
      "age",
      "authorization",
      "content-length",
      "content-type",
      "etag",
      "expires",
      "from",
      "host",
      "if-modified-since",
      "if-unmodified-since",
      "last-modified",
      "location",
      "max-forwards",
      "proxy-authorization",
      "referer",
      "retry-after",
      "user-agent"
    ];
    module2.exports = function parseHeaders(headers) {
      var parsed = {};
      var key;
      var val;
      var i;
      if (!headers) {
        return parsed;
      }
      utils.forEach(headers.split("\n"), function parser(line) {
        i = line.indexOf(":");
        key = utils.trim(line.substr(0, i)).toLowerCase();
        val = utils.trim(line.substr(i + 1));
        if (key) {
          if (parsed[key] && ignoreDuplicateOf.indexOf(key) >= 0) {
            return;
          }
          if (key === "set-cookie") {
            parsed[key] = (parsed[key] ? parsed[key] : []).concat([val]);
          } else {
            parsed[key] = parsed[key] ? parsed[key] + ", " + val : val;
          }
        }
      });
      return parsed;
    };
  }
});
// node_modules/axios/lib/helpers/isURLSameOrigin.js
var require_isURLSameOrigin = __commonJS({
  "node_modules/axios/lib/helpers/isURLSameOrigin.js"(exports, module2) {
    "use strict";
    var utils = require_utils();
    module2.exports = utils.isStandardBrowserEnv() ? (
      // Standard browser envs have full support of the APIs needed to test
      // whether the request URL is of the same origin as current location.
      function standardBrowserEnv() {
        var msie = /(msie|trident)/i.test(navigator.userAgent);
        var urlParsingNode = document.createElement("a");
        var originURL;
        function resolveURL(url) {
          var href = url;
          if (msie) {
            urlParsingNode.setAttribute("href", href);
            href = urlParsingNode.href;
          }
          urlParsingNode.setAttribute("href", href);
          return {
            href: urlParsingNode.href,
            protocol: urlParsingNode.protocol ? urlParsingNode.protocol.replace(/:$/, "") : "",
            host: urlParsingNode.host,
            search: urlParsingNode.search ? urlParsingNode.search.replace(/^\?/, "") : "",
            hash: urlParsingNode.hash ? urlParsingNode.hash.replace(/^#/, "") : "",
            hostname: urlParsingNode.hostname,
            port: urlParsingNode.port,
            pathname: urlParsingNode.pathname.charAt(0) === "/" ? urlParsingNode.pathname : "/" + urlParsingNode.pathname
          };
        }
        originURL = resolveURL(window.location.href);
        return function isURLSameOrigin(requestURL) {
          var parsed = utils.isString(requestURL) ? resolveURL(requestURL) : requestURL;
          return parsed.protocol === originURL.protocol && parsed.host === originURL.host;
        };
      }()
    ) : (
      // Non standard browser envs (web workers, react-native) lack needed support.
      function nonStandardBrowserEnv() {
        return function isURLSameOrigin() {
          return true;
        };
      }()
    );
  }
});
// node_modules/axios/lib/cancel/CanceledError.js
var require_CanceledError = __commonJS({
  "node_modules/axios/lib/cancel/CanceledError.js"(exports, module2) {
    "use strict";
    var AxiosError = require_AxiosError();
    var utils = require_utils();
    function CanceledError(message) {
      AxiosError.call(this, message == null ? "canceled" : message, AxiosError.ERR_CANCELED);
      this.name = "CanceledError";
    }
    utils.inherits(CanceledError, AxiosError, {
      __CANCEL__: true
    });
    module2.exports = CanceledError;
  }
});
// node_modules/axios/lib/helpers/parseProtocol.js
var require_parseProtocol = __commonJS({
  "node_modules/axios/lib/helpers/parseProtocol.js"(exports, module2) {
    "use strict";
    module2.exports = function parseProtocol(url) {
      var match = /^([-+\w]{1,25})(:?\/\/|:)/.exec(url);
      return match && match[1] || "";
    };
  }
});
// node_modules/axios/lib/adapters/xhr.js
var require_xhr = __commonJS({
  "node_modules/axios/lib/adapters/xhr.js"(exports, module2) {
    "use strict";
    var utils = require_utils();
    var settle = require_settle();
    var cookies = require_cookies();
    var buildURL = require_buildURL();
    var buildFullPath = require_buildFullPath();
    var parseHeaders = require_parseHeaders();
    var isURLSameOrigin = require_isURLSameOrigin();
    var transitionalDefaults = require_transitional();
    var AxiosError = require_AxiosError();
    var CanceledError = require_CanceledError();
    var parseProtocol = require_parseProtocol();
    module2.exports = function xhrAdapter(config) {
      return new Promise(function dispatchXhrRequest(resolve, reject) {
        var requestData = config.data;
        var requestHeaders = config.headers;
        var responseType = config.responseType;
        var onCanceled;
        function done() {
          if (config.cancelToken) {
            config.cancelToken.unsubscribe(onCanceled);
          }
          if (config.signal) {
            config.signal.removeEventListener("abort", onCanceled);
          }
        }
        if (utils.isFormData(requestData) && utils.isStandardBrowserEnv()) {
          delete requestHeaders["Content-Type"];
        }
        var request = new XMLHttpRequest();
        if (config.auth) {
          var username = config.auth.username || "";
          var password = config.auth.password ? unescape(encodeURIComponent(config.auth.password)) : "";
          requestHeaders.Authorization = "Basic " + btoa(username + ":" + password);
        }
        var fullPath = buildFullPath(config.baseURL, config.url);
        request.open(config.method.toUpperCase(), buildURL(fullPath, config.params, config.paramsSerializer), true);
        request.timeout = config.timeout;
        function onloadend() {
          if (!request) {
            return;
          }
          var responseHeaders = "getAllResponseHeaders" in request ? parseHeaders(request.getAllResponseHeaders()) : null;
          var responseData = !responseType || responseType === "text" || responseType === "json" ? request.responseText : request.response;
          var response = {
            data: responseData,
            status: request.status,
            statusText: request.statusText,
            headers: responseHeaders,
            config,
            request
          };
          settle(function _resolve(value) {
            resolve(value);
            done();
          }, function _reject(err) {
            reject(err);
            done();
          }, response);
          request = null;
        }
        if ("onloadend" in request) {
          request.onloadend = onloadend;
        } else {
          request.onreadystatechange = function handleLoad() {
            if (!request || request.readyState !== 4) {
              return;
            }
            if (request.status === 0 && !(request.responseURL && request.responseURL.indexOf("file:") === 0)) {
              return;
            }
            setTimeout(onloadend);
          };
        }
        request.onabort = function handleAbort() {
          if (!request) {
            return;
          }
          reject(new AxiosError("Request aborted", AxiosError.ECONNABORTED, config, request));
          request = null;
        };
        request.onerror = function handleError() {
          reject(new AxiosError("Network Error", AxiosError.ERR_NETWORK, config, request, request));
          request = null;
        };
        request.ontimeout = function handleTimeout() {
          var timeoutErrorMessage = config.timeout ? "timeout of " + config.timeout + "ms exceeded" : "timeout exceeded";
          var transitional = config.transitional || transitionalDefaults;
          if (config.timeoutErrorMessage) {
            timeoutErrorMessage = config.timeoutErrorMessage;
          }
          reject(new AxiosError(
            timeoutErrorMessage,
            transitional.clarifyTimeoutError ? AxiosError.ETIMEDOUT : AxiosError.ECONNABORTED,
            config,
            request
          ));
          request = null;
        };
        if (utils.isStandardBrowserEnv()) {
          var xsrfValue = (config.withCredentials || isURLSameOrigin(fullPath)) && config.xsrfCookieName ? cookies.read(config.xsrfCookieName) : void 0;
          if (xsrfValue) {
            requestHeaders[config.xsrfHeaderName] = xsrfValue;
          }
        }
        if ("setRequestHeader" in request) {
          utils.forEach(requestHeaders, function setRequestHeader(val, key) {
            if (typeof requestData === "undefined" && key.toLowerCase() === "content-type") {
              delete requestHeaders[key];
            } else {
              request.setRequestHeader(key, val);
            }
          });
        }
        if (!utils.isUndefined(config.withCredentials)) {
          request.withCredentials = !!config.withCredentials;
        }
        if (responseType && responseType !== "json") {
          request.responseType = config.responseType;
        }
        if (typeof config.onDownloadProgress === "function") {
          request.addEventListener("progress", config.onDownloadProgress);
        }
        if (typeof config.onUploadProgress === "function" && request.upload) {
          request.upload.addEventListener("progress", config.onUploadProgress);
        }
        if (config.cancelToken || config.signal) {
          onCanceled = function(cancel) {
            if (!request) {
              return;
            }
            reject(!cancel || cancel && cancel.type ? new CanceledError() : cancel);
            request.abort();
            request = null;
          };
          config.cancelToken && config.cancelToken.subscribe(onCanceled);
          if (config.signal) {
            config.signal.aborted ? onCanceled() : config.signal.addEventListener("abort", onCanceled);
          }
        }
        if (!requestData) {
          requestData = null;
        }
        var protocol = parseProtocol(fullPath);
        if (protocol && ["http", "https", "file"].indexOf(protocol) === -1) {
          reject(new AxiosError("Unsupported protocol " + protocol + ":", AxiosError.ERR_BAD_REQUEST, config));
          return;
        }
        request.send(requestData);
      });
    };
  }
});
// node_modules/axios/lib/helpers/null.js
var require_null = __commonJS({
  "node_modules/axios/lib/helpers/null.js"(exports, module2) {
    module2.exports = null;
  }
});
// node_modules/axios/lib/defaults/index.js
var require_defaults = __commonJS({
  "node_modules/axios/lib/defaults/index.js"(exports, module2) {
    "use strict";
    var utils = require_utils();
    var normalizeHeaderName = require_normalizeHeaderName();
    var AxiosError = require_AxiosError();
    var transitionalDefaults = require_transitional();
    var toFormData = require_toFormData();
    var DEFAULT_CONTENT_TYPE = {
      "Content-Type": "application/x-www-form-urlencoded"
    };
    function setContentTypeIfUnset(headers, value) {
      if (!utils.isUndefined(headers) && utils.isUndefined(headers["Content-Type"])) {
        headers["Content-Type"] = value;
      }
    }
    function getDefaultAdapter() {
      var adapter;
      if (typeof XMLHttpRequest !== "undefined") {
        adapter = require_xhr();
      } else if (typeof process !== "undefined" && Object.prototype.toString.call(process) === "[object process]") {
        adapter = require_xhr();
      }
      return adapter;
    }
    function stringifySafely(rawValue, parser, encoder) {
      if (utils.isString(rawValue)) {
        try {
          (parser || JSON.parse)(rawValue);
          return utils.trim(rawValue);
        } catch (e) {
          if (e.name !== "SyntaxError") {
            throw e;
          }
        }
      }
      return (encoder || JSON.stringify)(rawValue);
    }
    var defaults = {
      transitional: transitionalDefaults,
      adapter: getDefaultAdapter(),
      transformRequest: [function transformRequest(data, headers) {
        normalizeHeaderName(headers, "Accept");
        normalizeHeaderName(headers, "Content-Type");
        if (utils.isFormData(data) || utils.isArrayBuffer(data) || utils.isBuffer(data) || utils.isStream(data) || utils.isFile(data) || utils.isBlob(data)) {
          return data;
        }
        if (utils.isArrayBufferView(data)) {
          return data.buffer;
        }
        if (utils.isURLSearchParams(data)) {
          setContentTypeIfUnset(headers, "application/x-www-form-urlencoded;charset=utf-8");
          return data.toString();
        }
        var isObjectPayload = utils.isObject(data);
        var contentType = headers && headers["Content-Type"];
        var isFileList;
        if ((isFileList = utils.isFileList(data)) || isObjectPayload && contentType === "multipart/form-data") {
          var _FormData = this.env && this.env.FormData;
          return toFormData(isFileList ? { "files[]": data } : data, _FormData && new _FormData());
        } else if (isObjectPayload || contentType === "application/json") {
          setContentTypeIfUnset(headers, "application/json");
          return stringifySafely(data);
        }
        return data;
      }],
      transformResponse: [function transformResponse(data) {
        var transitional = this.transitional || defaults.transitional;
        var silentJSONParsing = transitional && transitional.silentJSONParsing;
        var forcedJSONParsing = transitional && transitional.forcedJSONParsing;
        var strictJSONParsing = !silentJSONParsing && this.responseType === "json";
        if (strictJSONParsing || forcedJSONParsing && utils.isString(data) && data.length) {
          try {
            return JSON.parse(data);
          } catch (e) {
            if (strictJSONParsing) {
              if (e.name === "SyntaxError") {
                throw AxiosError.from(e, AxiosError.ERR_BAD_RESPONSE, this, null, this.response);
              }
              throw e;
            }
          }
        }
        return data;
      }],
      /**
       * A timeout in milliseconds to abort a request. If set to 0 (default) a
       * timeout is not created.
       */
      timeout: 0,
      xsrfCookieName: "XSRF-TOKEN",
      xsrfHeaderName: "X-XSRF-TOKEN",
      maxContentLength: -1,
      maxBodyLength: -1,
      env: {
        FormData: require_null()
      },
      validateStatus: function validateStatus(status) {
        return status >= 200 && status < 300;
      },
      headers: {
        common: {
          "Accept": "application/json, text/plain, */*"
        }
      }
    };
    utils.forEach(["delete", "get", "head"], function forEachMethodNoData(method) {
      defaults.headers[method] = {};
    });
    utils.forEach(["post", "put", "patch"], function forEachMethodWithData(method) {
      defaults.headers[method] = utils.merge(DEFAULT_CONTENT_TYPE);
    });
    module2.exports = defaults;
  }
});
// node_modules/axios/lib/core/transformData.js
var require_transformData = __commonJS({
  "node_modules/axios/lib/core/transformData.js"(exports, module2) {
    "use strict";
    var utils = require_utils();
    var defaults = require_defaults();
    module2.exports = function transformData(data, headers, fns) {
      var context = this || defaults;
      utils.forEach(fns, function transform(fn) {
        data = fn.call(context, data, headers);
      });
      return data;
    };
  }
});
// node_modules/axios/lib/cancel/isCancel.js
var require_isCancel = __commonJS({
  "node_modules/axios/lib/cancel/isCancel.js"(exports, module2) {
    "use strict";
    module2.exports = function isCancel(value) {
      return !!(value && value.__CANCEL__);
    };
  }
});
// node_modules/axios/lib/core/dispatchRequest.js
var require_dispatchRequest = __commonJS({
  "node_modules/axios/lib/core/dispatchRequest.js"(exports, module2) {
    "use strict";
    var utils = require_utils();
    var transformData = require_transformData();
    var isCancel = require_isCancel();
    var defaults = require_defaults();
    var CanceledError = require_CanceledError();
    function throwIfCancellationRequested(config) {
      if (config.cancelToken) {
        config.cancelToken.throwIfRequested();
      }
      if (config.signal && config.signal.aborted) {
        throw new CanceledError();
      }
    }
    module2.exports = function dispatchRequest(config) {
      throwIfCancellationRequested(config);
      config.headers = config.headers || {};
      config.data = transformData.call(
        config,
        config.data,
        config.headers,
        config.transformRequest
      );
      config.headers = utils.merge(
        config.headers.common || {},
        config.headers[config.method] || {},
        config.headers
      );
      utils.forEach(
        ["delete", "get", "head", "post", "put", "patch", "common"],
        function cleanHeaderConfig(method) {
          delete config.headers[method];
        }
      );
      var adapter = config.adapter || defaults.adapter;
      return adapter(config).then(function onAdapterResolution(response) {
        throwIfCancellationRequested(config);
        response.data = transformData.call(
          config,
          response.data,
          response.headers,
          config.transformResponse
        );
        return response;
      }, function onAdapterRejection(reason) {
        if (!isCancel(reason)) {
          throwIfCancellationRequested(config);
          if (reason && reason.response) {
            reason.response.data = transformData.call(
              config,
              reason.response.data,
              reason.response.headers,
              config.transformResponse
            );
          }
        }
        return Promise.reject(reason);
      });
    };
  }
});
// node_modules/axios/lib/core/mergeConfig.js
var require_mergeConfig = __commonJS({
  "node_modules/axios/lib/core/mergeConfig.js"(exports, module2) {
    "use strict";
    var utils = require_utils();
    module2.exports = function mergeConfig(config1, config2) {
      config2 = config2 || {};
      var config = {};
      function getMergedValue(target, source) {
        if (utils.isPlainObject(target) && utils.isPlainObject(source)) {
          return utils.merge(target, source);
        } else if (utils.isPlainObject(source)) {
          return utils.merge({}, source);
        } else if (utils.isArray(source)) {
          return source.slice();
        }
        return source;
      }
      function mergeDeepProperties(prop) {
        if (!utils.isUndefined(config2[prop])) {
          return getMergedValue(config1[prop], config2[prop]);
        } else if (!utils.isUndefined(config1[prop])) {
          return getMergedValue(void 0, config1[prop]);
        }
      }
      function valueFromConfig2(prop) {
        if (!utils.isUndefined(config2[prop])) {
          return getMergedValue(void 0, config2[prop]);
        }
      }
      function defaultToConfig2(prop) {
        if (!utils.isUndefined(config2[prop])) {
          return getMergedValue(void 0, config2[prop]);
        } else if (!utils.isUndefined(config1[prop])) {
          return getMergedValue(void 0, config1[prop]);
        }
      }
      function mergeDirectKeys(prop) {
        if (prop in config2) {
          return getMergedValue(config1[prop], config2[prop]);
        } else if (prop in config1) {
          return getMergedValue(void 0, config1[prop]);
        }
      }
      var mergeMap = {
        "url": valueFromConfig2,
        "method": valueFromConfig2,
        "data": valueFromConfig2,
        "baseURL": defaultToConfig2,
        "transformRequest": defaultToConfig2,
        "transformResponse": defaultToConfig2,
        "paramsSerializer": defaultToConfig2,
        "timeout": defaultToConfig2,
        "timeoutMessage": defaultToConfig2,
        "withCredentials": defaultToConfig2,
        "adapter": defaultToConfig2,
        "responseType": defaultToConfig2,
        "xsrfCookieName": defaultToConfig2,
        "xsrfHeaderName": defaultToConfig2,
        "onUploadProgress": defaultToConfig2,
        "onDownloadProgress": defaultToConfig2,
        "decompress": defaultToConfig2,
        "maxContentLength": defaultToConfig2,
        "maxBodyLength": defaultToConfig2,
        "beforeRedirect": defaultToConfig2,
        "transport": defaultToConfig2,
        "httpAgent": defaultToConfig2,
        "httpsAgent": defaultToConfig2,
        "cancelToken": defaultToConfig2,
        "socketPath": defaultToConfig2,
        "responseEncoding": defaultToConfig2,
        "validateStatus": mergeDirectKeys
      };
      utils.forEach(Object.keys(config1).concat(Object.keys(config2)), function computeConfigValue(prop) {
        var merge = mergeMap[prop] || mergeDeepProperties;
        var configValue = merge(prop);
        utils.isUndefined(configValue) && merge !== mergeDirectKeys || (config[prop] = configValue);
      });
      return config;
    };
  }
});
// node_modules/axios/lib/env/data.js
var require_data = __commonJS({
  "node_modules/axios/lib/env/data.js"(exports, module2) {
    module2.exports = {
      "version": "0.27.2"
    };
  }
});
// node_modules/axios/lib/helpers/validator.js
var require_validator = __commonJS({
  "node_modules/axios/lib/helpers/validator.js"(exports, module2) {
    "use strict";
    var VERSION = require_data().version;
    var AxiosError = require_AxiosError();
    var validators = {};
    ["object", "boolean", "number", "function", "string", "symbol"].forEach(function(type, i) {
      validators[type] = function validator(thing) {
        return typeof thing === type || "a" + (i < 1 ? "n " : " ") + type;
      };
    });
    var deprecatedWarnings = {};
    validators.transitional = function transitional(validator, version, message) {
      function formatMessage(opt, desc) {
        return "[Axios v" + VERSION + "] Transitional option '" + opt + "'" + desc + (message ? ". " + message : "");
      }
      return function(value, opt, opts) {
        if (validator === false) {
          throw new AxiosError(
            formatMessage(opt, " has been removed" + (version ? " in " + version : "")),
            AxiosError.ERR_DEPRECATED
          );
        }
        if (version && !deprecatedWarnings[opt]) {
          deprecatedWarnings[opt] = true;
          console.warn(
            formatMessage(
              opt,
              " has been deprecated since v" + version + " and will be removed in the near future"
            )
          );
        }
        return validator ? validator(value, opt, opts) : true;
      };
    };
    function assertOptions(options, schema, allowUnknown) {
      if (typeof options !== "object") {
        throw new AxiosError("options must be an object", AxiosError.ERR_BAD_OPTION_VALUE);
      }
      var keys = Object.keys(options);
      var i = keys.length;
      while (i-- > 0) {
        var opt = keys[i];
        var validator = schema[opt];
        if (validator) {
          var value = options[opt];
          var result = value === void 0 || validator(value, opt, options);
          if (result !== true) {
            throw new AxiosError("option " + opt + " must be " + result, AxiosError.ERR_BAD_OPTION_VALUE);
          }
          continue;
        }
        if (allowUnknown !== true) {
          throw new AxiosError("Unknown option " + opt, AxiosError.ERR_BAD_OPTION);
        }
      }
    }
    module2.exports = {
      assertOptions,
      validators
    };
  }
});
// node_modules/axios/lib/core/Axios.js
var require_Axios = __commonJS({
  "node_modules/axios/lib/core/Axios.js"(exports, module2) {
    "use strict";
    var utils = require_utils();
    var buildURL = require_buildURL();
    var InterceptorManager = require_InterceptorManager();
    var dispatchRequest = require_dispatchRequest();
    var mergeConfig = require_mergeConfig();
    var buildFullPath = require_buildFullPath();
    var validator = require_validator();
    var validators = validator.validators;
    function Axios(instanceConfig) {
      this.defaults = instanceConfig;
      this.interceptors = {
        request: new InterceptorManager(),
        response: new InterceptorManager()
      };
    }
    Axios.prototype.request = function request(configOrUrl, config) {
      if (typeof configOrUrl === "string") {
        config = config || {};
        config.url = configOrUrl;
      } else {
        config = configOrUrl || {};
      }
      config = mergeConfig(this.defaults, config);
      if (config.method) {
        config.method = config.method.toLowerCase();
      } else if (this.defaults.method) {
        config.method = this.defaults.method.toLowerCase();
      } else {
        config.method = "get";
      }
      var transitional = config.transitional;
      if (transitional !== void 0) {
        validator.assertOptions(transitional, {
          silentJSONParsing: validators.transitional(validators.boolean),
          forcedJSONParsing: validators.transitional(validators.boolean),
          clarifyTimeoutError: validators.transitional(validators.boolean)
        }, false);
      }
      var requestInterceptorChain = [];
      var synchronousRequestInterceptors = true;
      this.interceptors.request.forEach(function unshiftRequestInterceptors(interceptor) {
        if (typeof interceptor.runWhen === "function" && interceptor.runWhen(config) === false) {
          return;
        }
        synchronousRequestInterceptors = synchronousRequestInterceptors && interceptor.synchronous;
        requestInterceptorChain.unshift(interceptor.fulfilled, interceptor.rejected);
      });
      var responseInterceptorChain = [];
      this.interceptors.response.forEach(function pushResponseInterceptors(interceptor) {
        responseInterceptorChain.push(interceptor.fulfilled, interceptor.rejected);
      });
      var promise;
      if (!synchronousRequestInterceptors) {
        var chain = [dispatchRequest, void 0];
        Array.prototype.unshift.apply(chain, requestInterceptorChain);
        chain = chain.concat(responseInterceptorChain);
        promise = Promise.resolve(config);
        while (chain.length) {
          promise = promise.then(chain.shift(), chain.shift());
        }
        return promise;
      }
      var newConfig = config;
      while (requestInterceptorChain.length) {
        var onFulfilled = requestInterceptorChain.shift();
        var onRejected = requestInterceptorChain.shift();
        try {
          newConfig = onFulfilled(newConfig);
        } catch (error) {
          onRejected(error);
          break;
        }
      }
      try {
        promise = dispatchRequest(newConfig);
      } catch (error) {
        return Promise.reject(error);
      }
      while (responseInterceptorChain.length) {
        promise = promise.then(responseInterceptorChain.shift(), responseInterceptorChain.shift());
      }
      return promise;
    };
    Axios.prototype.getUri = function getUri(config) {
      config = mergeConfig(this.defaults, config);
      var fullPath = buildFullPath(config.baseURL, config.url);
      return buildURL(fullPath, config.params, config.paramsSerializer);
    };
    utils.forEach(["delete", "get", "head", "options"], function forEachMethodNoData(method) {
      Axios.prototype[method] = function(url, config) {
        return this.request(mergeConfig(config || {}, {
          method,
          url,
          data: (config || {}).data
        }));
      };
    });
    utils.forEach(["post", "put", "patch"], function forEachMethodWithData(method) {
      function generateHTTPMethod(isForm) {
        return function httpMethod(url, data, config) {
          return this.request(mergeConfig(config || {}, {
            method,
            headers: isForm ? {
              "Content-Type": "multipart/form-data"
            } : {},
            url,
            data
          }));
        };
      }
      Axios.prototype[method] = generateHTTPMethod();
      Axios.prototype[method + "Form"] = generateHTTPMethod(true);
    });
    module2.exports = Axios;
  }
});
// node_modules/axios/lib/cancel/CancelToken.js
var require_CancelToken = __commonJS({
  "node_modules/axios/lib/cancel/CancelToken.js"(exports, module2) {
    "use strict";
    var CanceledError = require_CanceledError();
    function CancelToken(executor) {
      if (typeof executor !== "function") {
        throw new TypeError("executor must be a function.");
      }
      var resolvePromise;
      this.promise = new Promise(function promiseExecutor(resolve) {
        resolvePromise = resolve;
      });
      var token = this;
      this.promise.then(function(cancel) {
        if (!token._listeners)
          return;
        var i;
        var l = token._listeners.length;
        for (i = 0; i < l; i++) {
          token._listeners[i](cancel);
        }
        token._listeners = null;
      });
      this.promise.then = function(onfulfilled) {
        var _resolve;
        var promise = new Promise(function(resolve) {
          token.subscribe(resolve);
          _resolve = resolve;
        }).then(onfulfilled);
        promise.cancel = function reject() {
          token.unsubscribe(_resolve);
        };
        return promise;
      };
      executor(function cancel(message) {
        if (token.reason) {
          return;
        }
        token.reason = new CanceledError(message);
        resolvePromise(token.reason);
      });
    }
    CancelToken.prototype.throwIfRequested = function throwIfRequested() {
      if (this.reason) {
        throw this.reason;
      }
    };
    CancelToken.prototype.subscribe = function subscribe(listener) {
      if (this.reason) {
        listener(this.reason);
        return;
      }
      if (this._listeners) {
        this._listeners.push(listener);
      } else {
        this._listeners = [listener];
      }
    };
    CancelToken.prototype.unsubscribe = function unsubscribe(listener) {
      if (!this._listeners) {
        return;
      }
      var index = this._listeners.indexOf(listener);
      if (index !== -1) {
        this._listeners.splice(index, 1);
      }
    };
    CancelToken.source = function source() {
      var cancel;
      var token = new CancelToken(function executor(c) {
        cancel = c;
      });
      return {
        token,
        cancel
      };
    };
    module2.exports = CancelToken;
  }
});
// node_modules/axios/lib/helpers/spread.js
var require_spread = __commonJS({
  "node_modules/axios/lib/helpers/spread.js"(exports, module2) {
    "use strict";
    module2.exports = function spread(callback) {
      return function wrap(arr) {
        return callback.apply(null, arr);
      };
    };
  }
});
// node_modules/axios/lib/helpers/isAxiosError.js
var require_isAxiosError = __commonJS({
  "node_modules/axios/lib/helpers/isAxiosError.js"(exports, module2) {
    "use strict";
    var utils = require_utils();
    module2.exports = function isAxiosError(payload) {
      return utils.isObject(payload) && payload.isAxiosError === true;
    };
  }
});
// node_modules/axios/lib/axios.js
var require_axios = __commonJS({
  "node_modules/axios/lib/axios.js"(exports, module2) {
    "use strict";
    var utils = require_utils();
    var bind = require_bind();
    var Axios = require_Axios();
    var mergeConfig = require_mergeConfig();
    var defaults = require_defaults();
    function createInstance(defaultConfig) {
      var context = new Axios(defaultConfig);
      var instance = bind(Axios.prototype.request, context);
      utils.extend(instance, Axios.prototype, context);
      utils.extend(instance, context);
      instance.create = function create(instanceConfig) {
        return createInstance(mergeConfig(defaultConfig, instanceConfig));
      };
      return instance;
    }
    var axios = createInstance(defaults);
    axios.Axios = Axios;
    axios.CanceledError = require_CanceledError();
    axios.CancelToken = require_CancelToken();
    axios.isCancel = require_isCancel();
    axios.VERSION = require_data().version;
    axios.toFormData = require_toFormData();
    axios.AxiosError = require_AxiosError();
    axios.Cancel = axios.CanceledError;
    axios.all = function all(promises) {
      return Promise.all(promises);
    };
    axios.spread = require_spread();
    axios.isAxiosError = require_isAxiosError();
    module2.exports = axios;
    module2.exports.default = axios;
  }
});
// node_modules/axios/index.js
var require_axios2 = __commonJS({
  "node_modules/axios/index.js"(exports, module2) {
    module2.exports = require_axios();
  }
});
// node_modules/tslib/tslib.es6.js
var tslib_es6_exports = {};
__export(tslib_es6_exports, {
  __assign: () => __assign,
  __asyncDelegator: () => __asyncDelegator,
  __asyncGenerator: () => __asyncGenerator,
  __asyncValues: () => __asyncValues,
  __await: () => __await,
  __awaiter: () => __awaiter,
  __classPrivateFieldGet: () => __classPrivateFieldGet,
  __classPrivateFieldIn: () => __classPrivateFieldIn,
  __classPrivateFieldSet: () => __classPrivateFieldSet,
  __createBinding: () => __createBinding,
  __decorate: () => __decorate,
  __exportStar: () => __exportStar,
  __extends: () => __extends,
  __generator: () => __generator,
  __importDefault: () => __importDefault,
  __importStar: () => __importStar,
  __makeTemplateObject: () => __makeTemplateObject,
  __metadata: () => __metadata,
  __param: () => __param,
  __read: () => __read,
  __rest: () => __rest,
  __spread: () => __spread,
  __spreadArray: () => __spreadArray,
  __spreadArrays: () => __spreadArrays,
  __values: () => __values
});
function __extends(d, b) {
  if (typeof b !== "function" && b !== null)
    throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
  extendStatics(d, b);
  function __() {
    this.constructor = d;
  }
  d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
}
function __rest(s, e) {
  var t = {};
  for (var p in s)
    if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
      t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
}
function __decorate(decorators, target, key, desc) {
  var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
  if (typeof Reflect === "object" && typeof Reflect.decorate === "function")
    r = Reflect.decorate(decorators, target, key, desc);
  else
    for (var i = decorators.length - 1; i >= 0; i--)
      if (d = decorators[i])
        r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
  return c > 3 && r && Object.defineProperty(target, key, r), r;
}
function __param(paramIndex, decorator) {
  return function(target, key) {
    decorator(target, key, paramIndex);
  };
}
function __metadata(metadataKey, metadataValue) {
  if (typeof Reflect === "object" && typeof Reflect.metadata === "function")
    return Reflect.metadata(metadataKey, metadataValue);
}
function __awaiter(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
}
function __generator(thisArg, body) {
  var _ = { label: 0, sent: function() {
    if (t[0] & 1)
      throw t[1];
    return t[1];
  }, trys: [], ops: [] }, f, y, t, g;
  return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() {
    return this;
  }), g;
  function verb(n) {
    return function(v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f)
      throw new TypeError("Generator is already executing.");
    while (_)
      try {
        if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done)
          return t;
        if (y = 0, t)
          op = [op[0] & 2, t.value];
        switch (op[0]) {
          case 0:
          case 1:
            t = op;
            break;
          case 4:
            _.label++;
            return { value: op[1], done: false };
          case 5:
            _.label++;
            y = op[1];
            op = [0];
            continue;
          case 7:
            op = _.ops.pop();
            _.trys.pop();
            continue;
          default:
            if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
              _ = 0;
              continue;
            }
            if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
              _.label = op[1];
              break;
            }
            if (op[0] === 6 && _.label < t[1]) {
              _.label = t[1];
              t = op;
              break;
            }
            if (t && _.label < t[2]) {
              _.label = t[2];
              _.ops.push(op);
              break;
            }
            if (t[2])
              _.ops.pop();
            _.trys.pop();
            continue;
        }
        op = body.call(thisArg, _);
      } catch (e) {
        op = [6, e];
        y = 0;
      } finally {
        f = t = 0;
      }
    if (op[0] & 5)
      throw op[1];
    return { value: op[0] ? op[1] : void 0, done: true };
  }
}
function __exportStar(m, o) {
  for (var p in m)
    if (p !== "default" && !Object.prototype.hasOwnProperty.call(o, p))
      __createBinding(o, m, p);
}
function __values(o) {
  var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
  if (m)
    return m.call(o);
  if (o && typeof o.length === "number")
    return {
      next: function() {
        if (o && i >= o.length)
          o = void 0;
        return { value: o && o[i++], done: !o };
      }
    };
  throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
}
function __read(o, n) {
  var m = typeof Symbol === "function" && o[Symbol.iterator];
  if (!m)
    return o;
  var i = m.call(o), r, ar = [], e;
  try {
    while ((n === void 0 || n-- > 0) && !(r = i.next()).done)
      ar.push(r.value);
  } catch (error) {
    e = { error };
  } finally {
    try {
      if (r && !r.done && (m = i["return"]))
        m.call(i);
    } finally {
      if (e)
        throw e.error;
    }
  }
  return ar;
}
function __spread() {
  for (var ar = [], i = 0; i < arguments.length; i++)
    ar = ar.concat(__read(arguments[i]));
  return ar;
}
function __spreadArrays() {
  for (var s = 0, i = 0, il = arguments.length; i < il; i++)
    s += arguments[i].length;
  for (var r = Array(s), k = 0, i = 0; i < il; i++)
    for (var a = arguments[i], j = 0, jl = a.length; j < jl; j++, k++)
      r[k] = a[j];
  return r;
}
function __spreadArray(to, from, pack) {
  if (pack || arguments.length === 2)
    for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar)
          ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
  return to.concat(ar || Array.prototype.slice.call(from));
}
function __await(v) {
  return this instanceof __await ? (this.v = v, this) : new __await(v);
}
function __asyncGenerator(thisArg, _arguments, generator) {
  if (!Symbol.asyncIterator)
    throw new TypeError("Symbol.asyncIterator is not defined.");
  var g = generator.apply(thisArg, _arguments || []), i, q = [];
  return i = {}, verb("next"), verb("throw"), verb("return"), i[Symbol.asyncIterator] = function() {
    return this;
  }, i;
  function verb(n) {
    if (g[n])
      i[n] = function(v) {
        return new Promise(function(a, b) {
          q.push([n, v, a, b]) > 1 || resume(n, v);
        });
      };
  }
  function resume(n, v) {
    try {
      step(g[n](v));
    } catch (e) {
      settle(q[0][3], e);
    }
  }
  function step(r) {
    r.value instanceof __await ? Promise.resolve(r.value.v).then(fulfill, reject) : settle(q[0][2], r);
  }
  function fulfill(value) {
    resume("next", value);
  }
  function reject(value) {
    resume("throw", value);
  }
  function settle(f, v) {
    if (f(v), q.shift(), q.length)
      resume(q[0][0], q[0][1]);
  }
}
function __asyncDelegator(o) {
  var i, p;
  return i = {}, verb("next"), verb("throw", function(e) {
    throw e;
  }), verb("return"), i[Symbol.iterator] = function() {
    return this;
  }, i;
  function verb(n, f) {
    i[n] = o[n] ? function(v) {
      return (p = !p) ? { value: __await(o[n](v)), done: n === "return" } : f ? f(v) : v;
    } : f;
  }
}
function __asyncValues(o) {
  if (!Symbol.asyncIterator)
    throw new TypeError("Symbol.asyncIterator is not defined.");
  var m = o[Symbol.asyncIterator], i;
  return m ? m.call(o) : (o = typeof __values === "function" ? __values(o) : o[Symbol.iterator](), i = {}, verb("next"), verb("throw"), verb("return"), i[Symbol.asyncIterator] = function() {
    return this;
  }, i);
  function verb(n) {
    i[n] = o[n] && function(v) {
      return new Promise(function(resolve, reject) {
        v = o[n](v), settle(resolve, reject, v.done, v.value);
      });
    };
  }
  function settle(resolve, reject, d, v) {
    Promise.resolve(v).then(function(v2) {
      resolve({ value: v2, done: d });
    }, reject);
  }
}
function __makeTemplateObject(cooked, raw) {
  if (Object.defineProperty) {
    Object.defineProperty(cooked, "raw", { value: raw });
  } else {
    cooked.raw = raw;
  }
  return cooked;
}
function __importStar(mod) {
  if (mod && mod.__esModule)
    return mod;
  var result = {};
  if (mod != null) {
    for (var k in mod)
      if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k))
        __createBinding(result, mod, k);
  }
  __setModuleDefault(result, mod);
  return result;
}
function __importDefault(mod) {
  return mod && mod.__esModule ? mod : { default: mod };
}
function __classPrivateFieldGet(receiver, state, kind, f) {
  if (kind === "a" && !f)
    throw new TypeError("Private accessor was defined without a getter");
  if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver))
    throw new TypeError("Cannot read private member from an object whose class did not declare it");
  return kind === "m" ? f : kind === "a" ? f.call(receiver) : f ? f.value : state.get(receiver);
}
function __classPrivateFieldSet(receiver, state, value, kind, f) {
  if (kind === "m")
    throw new TypeError("Private method is not writable");
  if (kind === "a" && !f)
    throw new TypeError("Private accessor was defined without a setter");
  if (typeof state === "function" ? receiver !== state || !f : !state.has(receiver))
    throw new TypeError("Cannot write private member to an object whose class did not declare it");
  return kind === "a" ? f.call(receiver, value) : f ? f.value = value : state.set(receiver, value), value;
}
function __classPrivateFieldIn(state, receiver) {
  if (receiver === null || typeof receiver !== "object" && typeof receiver !== "function")
    throw new TypeError("Cannot use 'in' operator on non-object");
  return typeof state === "function" ? receiver === state : state.has(receiver);
}
var extendStatics, __assign, __createBinding, __setModuleDefault;
var init_tslib_es6 = __esm({
  "node_modules/tslib/tslib.es6.js"() {
    extendStatics = function(d, b) {
      extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
        d2.__proto__ = b2;
      } || function(d2, b2) {
        for (var p in b2)
          if (Object.prototype.hasOwnProperty.call(b2, p))
            d2[p] = b2[p];
      };
      return extendStatics(d, b);
    };
    __assign = function() {
      __assign = Object.assign || function __assign2(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
          s = arguments[i];
          for (var p in s)
            if (Object.prototype.hasOwnProperty.call(s, p))
              t[p] = s[p];
        }
        return t;
      };
      return __assign.apply(this, arguments);
    };
    __createBinding = Object.create ? function(o, m, k, k2) {
      if (k2 === void 0)
        k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m[k];
        } };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0)
        k2 = k;
      o[k2] = m[k];
    };
    __setModuleDefault = Object.create ? function(o, v) {
      Object.defineProperty(o, "default", { enumerable: true, value: v });
    } : function(o, v) {
      o["default"] = v;
    };
  }
});
// node_modules/lower-case/dist/index.js
var require_dist = __commonJS({
  "node_modules/lower-case/dist/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.lowerCase = exports.localeLowerCase = void 0;
    var SUPPORTED_LOCALE = {
      tr: {
        regexp: /\u0130|\u0049|\u0049\u0307/g,
        map: {
          \u0130: "i",
          I: "\u0131",
          I\u0307: "i"
        }
      },
      az: {
        regexp: /\u0130/g,
        map: {
          \u0130: "i",
          I: "\u0131",
          I\u0307: "i"
        }
      },
      lt: {
        regexp: /\u0049|\u004A|\u012E|\u00CC|\u00CD|\u0128/g,
        map: {
          I: "i\u0307",
          J: "j\u0307",
          \u012E: "\u012F\u0307",
          \u00CC: "i\u0307\u0300",
          \u00CD: "i\u0307\u0301",
          \u0128: "i\u0307\u0303"
        }
      }
    };
    function localeLowerCase(str, locale) {
      var lang = SUPPORTED_LOCALE[locale.toLowerCase()];
      if (lang)
        return lowerCase(str.replace(lang.regexp, function(m) {
          return lang.map[m];
        }));
      return lowerCase(str);
    }
    exports.localeLowerCase = localeLowerCase;
    function lowerCase(str) {
      return str.toLowerCase();
    }
    exports.lowerCase = lowerCase;
  }
});
// node_modules/no-case/dist/index.js
var require_dist2 = __commonJS({
  "node_modules/no-case/dist/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.noCase = void 0;
    var lower_case_1 = require_dist();
    var DEFAULT_SPLIT_REGEXP = [/([a-z0-9])([A-Z])/g, /([A-Z])([A-Z][a-z])/g];
    var DEFAULT_STRIP_REGEXP = /[^A-Z0-9]+/gi;
    function noCase(input, options) {
      if (options === void 0) {
        options = {};
      }
      var _a = options.splitRegexp, splitRegexp = _a === void 0 ? DEFAULT_SPLIT_REGEXP : _a, _b = options.stripRegexp, stripRegexp = _b === void 0 ? DEFAULT_STRIP_REGEXP : _b, _c = options.transform, transform = _c === void 0 ? lower_case_1.lowerCase : _c, _d = options.delimiter, delimiter = _d === void 0 ? " " : _d;
      var result = replace(replace(input, splitRegexp, "$1\0$2"), stripRegexp, "\0");
      var start = 0;
      var end = result.length;
      while (result.charAt(start) === "\0")
        start++;
      while (result.charAt(end - 1) === "\0")
        end--;
      return result.slice(start, end).split("\0").map(transform).join(delimiter);
    }
    exports.noCase = noCase;
    function replace(input, re, value) {
      if (re instanceof RegExp)
        return input.replace(re, value);
      return re.reduce(function(input2, re2) {
        return input2.replace(re2, value);
      }, input);
    }
  }
});
// node_modules/pascal-case/dist/index.js
var require_dist3 = __commonJS({
  "node_modules/pascal-case/dist/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.pascalCase = exports.pascalCaseTransformMerge = exports.pascalCaseTransform = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var no_case_1 = require_dist2();
    function pascalCaseTransform(input, index) {
      var firstChar = input.charAt(0);
      var lowerChars = input.substr(1).toLowerCase();
      if (index > 0 && firstChar >= "0" && firstChar <= "9") {
        return "_" + firstChar + lowerChars;
      }
      return "" + firstChar.toUpperCase() + lowerChars;
    }
    exports.pascalCaseTransform = pascalCaseTransform;
    function pascalCaseTransformMerge(input) {
      return input.charAt(0).toUpperCase() + input.slice(1).toLowerCase();
    }
    exports.pascalCaseTransformMerge = pascalCaseTransformMerge;
    function pascalCase(input, options) {
      if (options === void 0) {
        options = {};
      }
      return no_case_1.noCase(input, tslib_1.__assign({ delimiter: "", transform: pascalCaseTransform }, options));
    }
    exports.pascalCase = pascalCase;
  }
});
// node_modules/camel-case/dist/index.js
var require_dist4 = __commonJS({
  "node_modules/camel-case/dist/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.camelCase = exports.camelCaseTransformMerge = exports.camelCaseTransform = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var pascal_case_1 = require_dist3();
    function camelCaseTransform(input, index) {
      if (index === 0)
        return input.toLowerCase();
      return pascal_case_1.pascalCaseTransform(input, index);
    }
    exports.camelCaseTransform = camelCaseTransform;
    function camelCaseTransformMerge(input, index) {
      if (index === 0)
        return input.toLowerCase();
      return pascal_case_1.pascalCaseTransformMerge(input);
    }
    exports.camelCaseTransformMerge = camelCaseTransformMerge;
    function camelCase(input, options) {
      if (options === void 0) {
        options = {};
      }
      return pascal_case_1.pascalCase(input, tslib_1.__assign({ transform: camelCaseTransform }, options));
    }
    exports.camelCase = camelCase;
  }
});
// node_modules/dot-case/dist/index.js
var require_dist5 = __commonJS({
  "node_modules/dot-case/dist/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.dotCase = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var no_case_1 = require_dist2();
    function dotCase(input, options) {
      if (options === void 0) {
        options = {};
      }
      return no_case_1.noCase(input, tslib_1.__assign({ delimiter: "." }, options));
    }
    exports.dotCase = dotCase;
  }
});
// node_modules/snake-case/dist/index.js
var require_dist6 = __commonJS({
  "node_modules/snake-case/dist/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.snakeCase = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var dot_case_1 = require_dist5();
    function snakeCase(input, options) {
      if (options === void 0) {
        options = {};
      }
      return dot_case_1.dotCase(input, tslib_1.__assign({ delimiter: "_" }, options));
    }
    exports.snakeCase = snakeCase;
  }
});
// node_modules/upper-case-first/dist/index.js
var require_dist7 = __commonJS({
  "node_modules/upper-case-first/dist/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.upperCaseFirst = void 0;
    function upperCaseFirst(input) {
      return input.charAt(0).toUpperCase() + input.substr(1);
    }
    exports.upperCaseFirst = upperCaseFirst;
  }
});
// node_modules/capital-case/dist/index.js
var require_dist8 = __commonJS({
  "node_modules/capital-case/dist/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.capitalCase = exports.capitalCaseTransform = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var no_case_1 = require_dist2();
    var upper_case_first_1 = require_dist7();
    function capitalCaseTransform(input) {
      return upper_case_first_1.upperCaseFirst(input.toLowerCase());
    }
    exports.capitalCaseTransform = capitalCaseTransform;
    function capitalCase(input, options) {
      if (options === void 0) {
        options = {};
      }
      return no_case_1.noCase(input, tslib_1.__assign({ delimiter: " ", transform: capitalCaseTransform }, options));
    }
    exports.capitalCase = capitalCase;
  }
});
// node_modules/header-case/dist/index.js
var require_dist9 = __commonJS({
  "node_modules/header-case/dist/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.headerCase = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var capital_case_1 = require_dist8();
    function headerCase(input, options) {
      if (options === void 0) {
        options = {};
      }
      return capital_case_1.capitalCase(input, tslib_1.__assign({ delimiter: "-" }, options));
    }
    exports.headerCase = headerCase;
  }
});
// node_modules/axios-case-converter/lib/decorators.js
var require_decorators = __commonJS({
  "node_modules/axios-case-converter/lib/decorators.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.preserveSpecificKeys = exports.applyCaseOptions = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var applyCaseOptions = function(fn, defaultOptions) {
      return function(input, options) {
        return fn(input, tslib_1.__assign(tslib_1.__assign({}, defaultOptions), options));
      };
    };
    exports.applyCaseOptions = applyCaseOptions;
    var preserveSpecificKeys = function(fn, keys) {
      var condition = typeof keys === "function" ? keys : function(input) {
        return keys.includes(input);
      };
      return function(input, options) {
        return condition(input, options) ? input : fn(input, options);
      };
    };
    exports.preserveSpecificKeys = preserveSpecificKeys;
  }
});
// node_modules/axios-case-converter/lib/util.js
var require_util2 = __commonJS({
  "node_modules/axios-case-converter/lib/util.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isAxiosHeaders = exports.isTransformable = exports.isPlainObject = exports.isFormData = exports.isURLSearchParams = void 0;
    var isURLSearchParams = function(value) {
      return typeof URLSearchParams !== "undefined" && value instanceof URLSearchParams;
    };
    exports.isURLSearchParams = isURLSearchParams;
    var isFormData = function(value) {
      return typeof FormData !== "undefined" && value instanceof FormData;
    };
    exports.isFormData = isFormData;
    var isPlainObject = function(value) {
      if (value == null) {
        return false;
      }
      var proto = Object.getPrototypeOf(value);
      return proto === null || proto === Object.prototype;
    };
    exports.isPlainObject = isPlainObject;
    var isTransformable = function(value) {
      return Array.isArray(value) || (0, exports.isPlainObject)(value) || (0, exports.isFormData)(value) || (0, exports.isURLSearchParams)(value);
    };
    exports.isTransformable = isTransformable;
    var isAxiosHeaders = function(value) {
      var _a, _b;
      if (value == null) {
        return false;
      }
      return ((_b = (_a = Object.getPrototypeOf(value)) === null || _a === void 0 ? void 0 : _a.constructor) === null || _b === void 0 ? void 0 : _b.name) === "AxiosHeaders";
    };
    exports.isAxiosHeaders = isAxiosHeaders;
  }
});
// node_modules/axios-case-converter/lib/transformers.js
var require_transformers = __commonJS({
  "node_modules/axios-case-converter/lib/transformers.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createObjectTransformers = exports.createObjectTransformerOf = exports.createObjectTransformer = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var camel_case_1 = require_dist4();
    var snake_case_1 = require_dist6();
    var header_case_1 = require_dist9();
    var decorators_1 = require_decorators();
    var util_1 = require_util2();
    var caseFunctions = {
      snake: snake_case_1.snakeCase,
      camel: camel_case_1.camelCase,
      header: header_case_1.headerCase
    };
    var transformObjectUsingCallbackRecursive = function(data, fn, overwrite) {
      var e_1, _a, e_2, _b, e_3, _c;
      if (!(0, util_1.isTransformable)(data)) {
        return data;
      }
      if (((0, util_1.isFormData)(data) || (0, util_1.isURLSearchParams)(data)) && (!data.entries || overwrite && !data.delete)) {
        var type = (0, util_1.isFormData)(data) ? "FormData" : "URLSearchParams";
        var polyfill = (0, util_1.isFormData)(data) ? "https://github.com/jimmywarting/FormData" : "https://github.com/jerrybendy/url-search-params-polyfill";
        if (typeof navigator !== "undefined" && navigator.product === "ReactNative") {
          console.warn("Be careful that ".concat(type, " cannot be transformed on React Native. If you intentionally implemented, ignore this kind of warning: https://facebook.github.io/react-native/docs/debugging.html"));
        } else {
          if (!data.entries) {
            console.warn("You must use polyfill of ".concat(type, ".prototype.entries() on Internet Explorer or Safari: ").concat(polyfill));
          }
          if (overwrite && !data.delete) {
            console.warn("You must use polyfill of ".concat(type, ".prototype.delete() on Internet Explorer or Safari: ").concat(polyfill));
          }
        }
        return data;
      }
      var prototype = Object.getPrototypeOf(data);
      var store = overwrite ? data : !prototype ? /* @__PURE__ */ Object.create(null) : new prototype.constructor();
      var series;
      if ((0, util_1.isFormData)(data) || (0, util_1.isURLSearchParams)(data)) {
        series = data.entries();
        if (overwrite) {
          series = tslib_1.__spreadArray([], tslib_1.__read(series), false);
          try {
            for (var series_1 = tslib_1.__values(series), series_1_1 = series_1.next(); !series_1_1.done; series_1_1 = series_1.next()) {
              var _d = tslib_1.__read(series_1_1.value, 1), key = _d[0];
              data.delete(key);
            }
          } catch (e_1_1) {
            e_1 = { error: e_1_1 };
          } finally {
            try {
              if (series_1_1 && !series_1_1.done && (_a = series_1.return))
                _a.call(series_1);
            } finally {
              if (e_1)
                throw e_1.error;
            }
          }
        }
      } else {
        series = Object.entries(data);
        if (overwrite && !Array.isArray(data)) {
          try {
            for (var series_2 = tslib_1.__values(series), series_2_1 = series_2.next(); !series_2_1.done; series_2_1 = series_2.next()) {
              var _e = tslib_1.__read(series_2_1.value, 1), key = _e[0];
              delete data[key];
            }
          } catch (e_2_1) {
            e_2 = { error: e_2_1 };
          } finally {
            try {
              if (series_2_1 && !series_2_1.done && (_b = series_2.return))
                _b.call(series_2);
            } finally {
              if (e_2)
                throw e_2.error;
            }
          }
        }
      }
      try {
        for (var series_3 = tslib_1.__values(series), series_3_1 = series_3.next(); !series_3_1.done; series_3_1 = series_3.next()) {
          var _f = tslib_1.__read(series_3_1.value, 2), key = _f[0], value = _f[1];
          if ((0, util_1.isFormData)(store) || (0, util_1.isURLSearchParams)(store)) {
            store.append(fn(key), value);
          } else if (key !== "__proto__") {
            store[fn(typeof key === "string" ? key : "".concat(key))] = transformObjectUsingCallbackRecursive(value, fn, overwrite);
          }
        }
      } catch (e_3_1) {
        e_3 = { error: e_3_1 };
      } finally {
        try {
          if (series_3_1 && !series_3_1.done && (_c = series_3.return))
            _c.call(series_3);
        } finally {
          if (e_3)
            throw e_3.error;
        }
      }
      return store;
    };
    var transformObjectUsingCallback = function(data, fn, options) {
      fn = (0, decorators_1.applyCaseOptions)(fn, tslib_1.__assign({ stripRegexp: /[^A-Z0-9[\]]+/gi }, options === null || options === void 0 ? void 0 : options.caseOptions));
      if (options === null || options === void 0 ? void 0 : options.preservedKeys) {
        fn = (0, decorators_1.preserveSpecificKeys)(fn, options.preservedKeys);
      }
      return transformObjectUsingCallbackRecursive(data, fn, (options === null || options === void 0 ? void 0 : options.overwrite) || false);
    };
    var createObjectTransformer = function(fn) {
      return function(data, options) {
        return transformObjectUsingCallback(data, fn, options);
      };
    };
    exports.createObjectTransformer = createObjectTransformer;
    var createObjectTransformerOf = function(functionName, options) {
      return (0, exports.createObjectTransformer)((options === null || options === void 0 ? void 0 : options[functionName]) || caseFunctions[functionName]);
    };
    exports.createObjectTransformerOf = createObjectTransformerOf;
    var createObjectTransformers = function(options) {
      var e_4, _a;
      var functionNames = Object.keys(caseFunctions);
      var objectTransformers = {};
      try {
        for (var functionNames_1 = tslib_1.__values(functionNames), functionNames_1_1 = functionNames_1.next(); !functionNames_1_1.done; functionNames_1_1 = functionNames_1.next()) {
          var functionName = functionNames_1_1.value;
          objectTransformers[functionName] = (0, exports.createObjectTransformerOf)(functionName, options);
        }
      } catch (e_4_1) {
        e_4 = { error: e_4_1 };
      } finally {
        try {
          if (functionNames_1_1 && !functionNames_1_1.done && (_a = functionNames_1.return))
            _a.call(functionNames_1);
        } finally {
          if (e_4)
            throw e_4.error;
        }
      }
      return objectTransformers;
    };
    exports.createObjectTransformers = createObjectTransformers;
  }
});
// node_modules/axios-case-converter/lib/middleware.js
var require_middleware = __commonJS({
  "node_modules/axios-case-converter/lib/middleware.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.applyCaseMiddleware = exports.createCamelResponseTransformer = exports.createSnakeRequestTransformer = exports.createSnakeParamsInterceptor = void 0;
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var transformers_1 = require_transformers();
    var util_1 = require_util2();
    var createSnakeParamsInterceptor = function(options) {
      var snake = (0, transformers_1.createObjectTransformers)(options === null || options === void 0 ? void 0 : options.caseFunctions).snake;
      return function(config) {
        if (config.params) {
          config.params = snake(config.params, options);
        }
        return config;
      };
    };
    exports.createSnakeParamsInterceptor = createSnakeParamsInterceptor;
    var createSnakeRequestTransformer = function(options) {
      var _a = (0, transformers_1.createObjectTransformers)(options === null || options === void 0 ? void 0 : options.caseFunctions), snake = _a.snake, header = _a.header;
      return function(data, headers) {
        overwriteHeadersOrNoop(headers, header, options, [
          "common",
          "delete",
          "get",
          "head",
          "post",
          "put",
          "patch"
        ]);
        return snake(data, options);
      };
    };
    exports.createSnakeRequestTransformer = createSnakeRequestTransformer;
    var createCamelResponseTransformer = function(options) {
      var camel = (0, transformers_1.createObjectTransformers)(options === null || options === void 0 ? void 0 : options.caseFunctions).camel;
      return function(data, headers) {
        overwriteHeadersOrNoop(headers, camel, options);
        return camel(data, options);
      };
    };
    exports.createCamelResponseTransformer = createCamelResponseTransformer;
    var overwriteHeadersOrNoop = function(headers, fn, options, excludedKeys) {
      var e_1, _a, _b, _c;
      if ((options === null || options === void 0 ? void 0 : options.ignoreHeaders) || !(0, util_1.isPlainObject)(headers) && !(0, util_1.isAxiosHeaders)(headers)) {
        return;
      }
      try {
        for (var _d = tslib_1.__values(Object.entries(headers)), _e = _d.next(); !_e.done; _e = _d.next()) {
          var _f = tslib_1.__read(_e.value, 2), key = _f[0], value = _f[1];
          fn(value, tslib_1.__assign({ overwrite: true }, options));
          if ((excludedKeys || []).includes(key)) {
            continue;
          }
          if ((0, util_1.isAxiosHeaders)(headers)) {
            headers.delete(key);
            headers.set(Object.keys(fn((_b = {}, _b[key] = null, _b), options))[0], value, true);
          } else {
            delete headers[key];
            headers[Object.keys(fn((_c = {}, _c[key] = null, _c), options))[0]] = value;
          }
        }
      } catch (e_1_1) {
        e_1 = { error: e_1_1 };
      } finally {
        try {
          if (_e && !_e.done && (_a = _d.return))
            _a.call(_d);
        } finally {
          if (e_1)
            throw e_1.error;
        }
      }
    };
    var applyCaseMiddleware = function(axios, options) {
      var _a, _b, _c;
      axios.defaults.transformRequest = tslib_1.__spreadArray([
        ((_a = options === null || options === void 0 ? void 0 : options.caseMiddleware) === null || _a === void 0 ? void 0 : _a.requestTransformer) || (0, exports.createSnakeRequestTransformer)(options)
      ], tslib_1.__read(Array.isArray(axios.defaults.transformRequest) ? axios.defaults.transformRequest : axios.defaults.transformRequest !== void 0 ? [axios.defaults.transformRequest] : []), false);
      axios.defaults.transformResponse = tslib_1.__spreadArray(tslib_1.__spreadArray([], tslib_1.__read(Array.isArray(axios.defaults.transformResponse) ? axios.defaults.transformResponse : axios.defaults.transformResponse !== void 0 ? [axios.defaults.transformResponse] : []), false), [
        ((_b = options === null || options === void 0 ? void 0 : options.caseMiddleware) === null || _b === void 0 ? void 0 : _b.responseTransformer) || (0, exports.createCamelResponseTransformer)(options)
      ], false);
      axios.interceptors.request.use(((_c = options === null || options === void 0 ? void 0 : options.caseMiddleware) === null || _c === void 0 ? void 0 : _c.requestInterceptor) || (0, exports.createSnakeParamsInterceptor)(options));
      return axios;
    };
    exports.applyCaseMiddleware = applyCaseMiddleware;
  }
});
// node_modules/axios-case-converter/lib/types.js
var require_types = __commonJS({
  "node_modules/axios-case-converter/lib/types.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
  }
});
// node_modules/axios-case-converter/lib/index.js
var require_lib2 = __commonJS({
  "node_modules/axios-case-converter/lib/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var tslib_1 = (init_tslib_es6(), __toCommonJS(tslib_es6_exports));
    var middleware_1 = require_middleware();
    exports.default = middleware_1.applyCaseMiddleware;
    tslib_1.__exportStar(require_types(), exports);
  }
});
// node_modules/ts-custom-error/dist/custom-error.js
var require_custom_error = __commonJS({
  "node_modules/ts-custom-error/dist/custom-error.js"(exports) {
    function fixProto(target, prototype) {
      var setPrototypeOf = Object.setPrototypeOf;
      setPrototypeOf ? setPrototypeOf(target, prototype) : target.__proto__ = prototype;
    }
    function fixStack(target, fn) {
      if (fn === void 0) {
        fn = target.constructor;
      }
      var captureStackTrace = Error.captureStackTrace;
      captureStackTrace && captureStackTrace(target, fn);
    }
    var __extends2 = function() {
      var _extendStatics = function extendStatics2(d, b) {
        _extendStatics = Object.setPrototypeOf || {
          __proto__: []
        } instanceof Array && function(d2, b2) {
          d2.__proto__ = b2;
        } || function(d2, b2) {
          for (var p in b2) {
            if (Object.prototype.hasOwnProperty.call(b2, p))
              d2[p] = b2[p];
          }
        };
        return _extendStatics(d, b);
      };
      return function(d, b) {
        if (typeof b !== "function" && b !== null)
          throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        _extendStatics(d, b);
        function __() {
          this.constructor = d;
        }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
      };
    }();
    var CustomError = function(_super) {
      __extends2(CustomError2, _super);
      function CustomError2(message, options) {
        var _newTarget = this.constructor;
        var _this = _super.call(this, message, options) || this;
        Object.defineProperty(_this, "name", {
          value: _newTarget.name,
          enumerable: false,
          configurable: true
        });
        fixProto(_this, _newTarget.prototype);
        fixStack(_this);
        return _this;
      }
      return CustomError2;
    }(Error);
    var __spreadArray2 = function(to, from, pack) {
      if (pack || arguments.length === 2)
        for (var i = 0, l = from.length, ar; i < l; i++) {
          if (ar || !(i in from)) {
            if (!ar)
              ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
          }
        }
      return to.concat(ar || Array.prototype.slice.call(from));
    };
    function customErrorFactory(fn, parent) {
      if (parent === void 0) {
        parent = Error;
      }
      function CustomError2() {
        var args = [];
        for (var _i = 0; _i < arguments.length; _i++) {
          args[_i] = arguments[_i];
        }
        if (!(this instanceof CustomError2))
          return new (CustomError2.bind.apply(CustomError2, __spreadArray2([void 0], args, false)))();
        parent.apply(this, args);
        Object.defineProperty(this, "name", {
          value: fn.name || parent.name,
          enumerable: false,
          configurable: true
        });
        fn.apply(this, args);
        fixStack(this, CustomError2);
      }
      return Object.defineProperties(CustomError2, {
        prototype: {
          value: Object.create(parent.prototype, {
            constructor: {
              value: CustomError2,
              writable: true,
              configurable: true
            }
          })
        }
      });
    }
    exports.CustomError = CustomError;
    exports.customErrorFactory = customErrorFactory;
  }
});
// node_modules/@doist/todoist-api-typescript/dist/types/errors.js
var require_errors2 = __commonJS({
  "node_modules/@doist/todoist-api-typescript/dist/types/errors.js"(exports) {
    "use strict";
    var __extends2 = exports && exports.__extends || function() {
      var extendStatics2 = function(d, b) {
        extendStatics2 = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d2, b2) {
          d2.__proto__ = b2;
        } || function(d2, b2) {
          for (var p in b2)
            if (Object.prototype.hasOwnProperty.call(b2, p))
              d2[p] = b2[p];
        };
        return extendStatics2(d, b);
      };
      return function(d, b) {
        if (typeof b !== "function" && b !== null)
          throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics2(d, b);
        function __() {
          this.constructor = d;
        }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
      };
    }();
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.TodoistRequestError = void 0;
    var ts_custom_error_1 = require_custom_error();
    var authenticationErrorCodes = [401, 403];
    var TodoistRequestError = (
      /** @class */
      function(_super) {
        __extends2(TodoistRequestError2, _super);
        function TodoistRequestError2(message, httpStatusCode, responseData) {
          var _this = _super.call(this, message) || this;
          _this.message = message;
          _this.httpStatusCode = httpStatusCode;
          _this.responseData = responseData;
          _this.isAuthenticationError = function() {
            if (!_this.httpStatusCode) {
              return false;
            }
            return authenticationErrorCodes.includes(_this.httpStatusCode);
          };
          Object.defineProperty(_this, "name", { value: "TodoistRequestError" });
          return _this;
        }
        return TodoistRequestError2;
      }(ts_custom_error_1.CustomError)
    );
    exports.TodoistRequestError = TodoistRequestError;
  }
});
// node_modules/uuid/dist/commonjs-browser/rng.js
var require_rng = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/rng.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = rng;
    var getRandomValues;
    var rnds8 = new Uint8Array(16);
    function rng() {
      if (!getRandomValues) {
        getRandomValues = typeof crypto !== "undefined" && crypto.getRandomValues && crypto.getRandomValues.bind(crypto);
        if (!getRandomValues) {
          throw new Error("crypto.getRandomValues() not supported. See https://github.com/uuidjs/uuid#getrandomvalues-not-supported");
        }
      }
      return getRandomValues(rnds8);
    }
  }
});
// node_modules/uuid/dist/commonjs-browser/regex.js
var require_regex = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/regex.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _default = /^(?:[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}|00000000-0000-0000-0000-000000000000)$/i;
    exports.default = _default;
  }
});
// node_modules/uuid/dist/commonjs-browser/validate.js
var require_validate = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/validate.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _regex = _interopRequireDefault(require_regex());
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    function validate(uuid) {
      return typeof uuid === "string" && _regex.default.test(uuid);
    }
    var _default = validate;
    exports.default = _default;
  }
});
// node_modules/uuid/dist/commonjs-browser/stringify.js
var require_stringify = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/stringify.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    exports.unsafeStringify = unsafeStringify;
    var _validate = _interopRequireDefault(require_validate());
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    var byteToHex = [];
    for (let i = 0; i < 256; ++i) {
      byteToHex.push((i + 256).toString(16).slice(1));
    }
    function unsafeStringify(arr, offset = 0) {
      return (byteToHex[arr[offset + 0]] + byteToHex[arr[offset + 1]] + byteToHex[arr[offset + 2]] + byteToHex[arr[offset + 3]] + "-" + byteToHex[arr[offset + 4]] + byteToHex[arr[offset + 5]] + "-" + byteToHex[arr[offset + 6]] + byteToHex[arr[offset + 7]] + "-" + byteToHex[arr[offset + 8]] + byteToHex[arr[offset + 9]] + "-" + byteToHex[arr[offset + 10]] + byteToHex[arr[offset + 11]] + byteToHex[arr[offset + 12]] + byteToHex[arr[offset + 13]] + byteToHex[arr[offset + 14]] + byteToHex[arr[offset + 15]]).toLowerCase();
    }
    function stringify(arr, offset = 0) {
      const uuid = unsafeStringify(arr, offset);
      if (!(0, _validate.default)(uuid)) {
        throw TypeError("Stringified UUID is invalid");
      }
      return uuid;
    }
    var _default = stringify;
    exports.default = _default;
  }
});
// node_modules/uuid/dist/commonjs-browser/v1.js
var require_v1 = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/v1.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _rng = _interopRequireDefault(require_rng());
    var _stringify = require_stringify();
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    var _nodeId;
    var _clockseq;
    var _lastMSecs = 0;
    var _lastNSecs = 0;
    function v1(options, buf, offset) {
      let i = buf && offset || 0;
      const b = buf || new Array(16);
      options = options || {};
      let node = options.node || _nodeId;
      let clockseq = options.clockseq !== void 0 ? options.clockseq : _clockseq;
      if (node == null || clockseq == null) {
        const seedBytes = options.random || (options.rng || _rng.default)();
        if (node == null) {
          node = _nodeId = [seedBytes[0] | 1, seedBytes[1], seedBytes[2], seedBytes[3], seedBytes[4], seedBytes[5]];
        }
        if (clockseq == null) {
          clockseq = _clockseq = (seedBytes[6] << 8 | seedBytes[7]) & 16383;
        }
      }
      let msecs = options.msecs !== void 0 ? options.msecs : Date.now();
      let nsecs = options.nsecs !== void 0 ? options.nsecs : _lastNSecs + 1;
      const dt = msecs - _lastMSecs + (nsecs - _lastNSecs) / 1e4;
      if (dt < 0 && options.clockseq === void 0) {
        clockseq = clockseq + 1 & 16383;
      }
      if ((dt < 0 || msecs > _lastMSecs) && options.nsecs === void 0) {
        nsecs = 0;
      }
      if (nsecs >= 1e4) {
        throw new Error("uuid.v1(): Can't create more than 10M uuids/sec");
      }
      _lastMSecs = msecs;
      _lastNSecs = nsecs;
      _clockseq = clockseq;
      msecs += 122192928e5;
      const tl = ((msecs & 268435455) * 1e4 + nsecs) % 4294967296;
      b[i++] = tl >>> 24 & 255;
      b[i++] = tl >>> 16 & 255;
      b[i++] = tl >>> 8 & 255;
      b[i++] = tl & 255;
      const tmh = msecs / 4294967296 * 1e4 & 268435455;
      b[i++] = tmh >>> 8 & 255;
      b[i++] = tmh & 255;
      b[i++] = tmh >>> 24 & 15 | 16;
      b[i++] = tmh >>> 16 & 255;
      b[i++] = clockseq >>> 8 | 128;
      b[i++] = clockseq & 255;
      for (let n = 0; n < 6; ++n) {
        b[i + n] = node[n];
      }
      return buf || (0, _stringify.unsafeStringify)(b);
    }
    var _default = v1;
    exports.default = _default;
  }
});
// node_modules/uuid/dist/commonjs-browser/parse.js
var require_parse = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/parse.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _validate = _interopRequireDefault(require_validate());
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    function parse(uuid) {
      if (!(0, _validate.default)(uuid)) {
        throw TypeError("Invalid UUID");
      }
      let v;
      const arr = new Uint8Array(16);
      arr[0] = (v = parseInt(uuid.slice(0, 8), 16)) >>> 24;
      arr[1] = v >>> 16 & 255;
      arr[2] = v >>> 8 & 255;
      arr[3] = v & 255;
      arr[4] = (v = parseInt(uuid.slice(9, 13), 16)) >>> 8;
      arr[5] = v & 255;
      arr[6] = (v = parseInt(uuid.slice(14, 18), 16)) >>> 8;
      arr[7] = v & 255;
      arr[8] = (v = parseInt(uuid.slice(19, 23), 16)) >>> 8;
      arr[9] = v & 255;
      arr[10] = (v = parseInt(uuid.slice(24, 36), 16)) / 1099511627776 & 255;
      arr[11] = v / 4294967296 & 255;
      arr[12] = v >>> 24 & 255;
      arr[13] = v >>> 16 & 255;
      arr[14] = v >>> 8 & 255;
      arr[15] = v & 255;
      return arr;
    }
    var _default = parse;
    exports.default = _default;
  }
});
// node_modules/uuid/dist/commonjs-browser/v35.js
var require_v35 = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/v35.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.URL = exports.DNS = void 0;
    exports.default = v35;
    var _stringify = require_stringify();
    var _parse = _interopRequireDefault(require_parse());
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    function stringToBytes(str) {
      str = unescape(encodeURIComponent(str));
      const bytes = [];
      for (let i = 0; i < str.length; ++i) {
        bytes.push(str.charCodeAt(i));
      }
      return bytes;
    }
    var DNS = "6ba7b810-9dad-11d1-80b4-00c04fd430c8";
    exports.DNS = DNS;
    var URL2 = "6ba7b811-9dad-11d1-80b4-00c04fd430c8";
    exports.URL = URL2;
    function v35(name, version, hashfunc) {
      function generateUUID(value, namespace, buf, offset) {
        var _namespace;
        if (typeof value === "string") {
          value = stringToBytes(value);
        }
        if (typeof namespace === "string") {
          namespace = (0, _parse.default)(namespace);
        }
        if (((_namespace = namespace) === null || _namespace === void 0 ? void 0 : _namespace.length) !== 16) {
          throw TypeError("Namespace must be array-like (16 iterable integer values, 0-255)");
        }
        let bytes = new Uint8Array(16 + value.length);
        bytes.set(namespace);
        bytes.set(value, namespace.length);
        bytes = hashfunc(bytes);
        bytes[6] = bytes[6] & 15 | version;
        bytes[8] = bytes[8] & 63 | 128;
        if (buf) {
          offset = offset || 0;
          for (let i = 0; i < 16; ++i) {
            buf[offset + i] = bytes[i];
          }
          return buf;
        }
        return (0, _stringify.unsafeStringify)(bytes);
      }
      try {
        generateUUID.name = name;
      } catch (err) {
      }
      generateUUID.DNS = DNS;
      generateUUID.URL = URL2;
      return generateUUID;
    }
  }
});
// node_modules/uuid/dist/commonjs-browser/md5.js
var require_md5 = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/md5.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    function md5(bytes) {
      if (typeof bytes === "string") {
        const msg = unescape(encodeURIComponent(bytes));
        bytes = new Uint8Array(msg.length);
        for (let i = 0; i < msg.length; ++i) {
          bytes[i] = msg.charCodeAt(i);
        }
      }
      return md5ToHexEncodedArray(wordsToMd5(bytesToWords(bytes), bytes.length * 8));
    }
    function md5ToHexEncodedArray(input) {
      const output = [];
      const length32 = input.length * 32;
      const hexTab = "0123456789abcdef";
      for (let i = 0; i < length32; i += 8) {
        const x = input[i >> 5] >>> i % 32 & 255;
        const hex = parseInt(hexTab.charAt(x >>> 4 & 15) + hexTab.charAt(x & 15), 16);
        output.push(hex);
      }
      return output;
    }
    function getOutputLength(inputLength8) {
      return (inputLength8 + 64 >>> 9 << 4) + 14 + 1;
    }
    function wordsToMd5(x, len) {
      x[len >> 5] |= 128 << len % 32;
      x[getOutputLength(len) - 1] = len;
      let a = 1732584193;
      let b = -271733879;
      let c = -1732584194;
      let d = 271733878;
      for (let i = 0; i < x.length; i += 16) {
        const olda = a;
        const oldb = b;
        const oldc = c;
        const oldd = d;
        a = md5ff(a, b, c, d, x[i], 7, -680876936);
        d = md5ff(d, a, b, c, x[i + 1], 12, -389564586);
        c = md5ff(c, d, a, b, x[i + 2], 17, 606105819);
        b = md5ff(b, c, d, a, x[i + 3], 22, -1044525330);
        a = md5ff(a, b, c, d, x[i + 4], 7, -176418897);
        d = md5ff(d, a, b, c, x[i + 5], 12, 1200080426);
        c = md5ff(c, d, a, b, x[i + 6], 17, -1473231341);
        b = md5ff(b, c, d, a, x[i + 7], 22, -45705983);
        a = md5ff(a, b, c, d, x[i + 8], 7, 1770035416);
        d = md5ff(d, a, b, c, x[i + 9], 12, -1958414417);
        c = md5ff(c, d, a, b, x[i + 10], 17, -42063);
        b = md5ff(b, c, d, a, x[i + 11], 22, -1990404162);
        a = md5ff(a, b, c, d, x[i + 12], 7, 1804603682);
        d = md5ff(d, a, b, c, x[i + 13], 12, -40341101);
        c = md5ff(c, d, a, b, x[i + 14], 17, -1502002290);
        b = md5ff(b, c, d, a, x[i + 15], 22, 1236535329);
        a = md5gg(a, b, c, d, x[i + 1], 5, -165796510);
        d = md5gg(d, a, b, c, x[i + 6], 9, -1069501632);
        c = md5gg(c, d, a, b, x[i + 11], 14, 643717713);
        b = md5gg(b, c, d, a, x[i], 20, -373897302);
        a = md5gg(a, b, c, d, x[i + 5], 5, -701558691);
        d = md5gg(d, a, b, c, x[i + 10], 9, 38016083);
        c = md5gg(c, d, a, b, x[i + 15], 14, -660478335);
        b = md5gg(b, c, d, a, x[i + 4], 20, -405537848);
        a = md5gg(a, b, c, d, x[i + 9], 5, 568446438);
        d = md5gg(d, a, b, c, x[i + 14], 9, -1019803690);
        c = md5gg(c, d, a, b, x[i + 3], 14, -187363961);
        b = md5gg(b, c, d, a, x[i + 8], 20, 1163531501);
        a = md5gg(a, b, c, d, x[i + 13], 5, -1444681467);
        d = md5gg(d, a, b, c, x[i + 2], 9, -51403784);
        c = md5gg(c, d, a, b, x[i + 7], 14, 1735328473);
        b = md5gg(b, c, d, a, x[i + 12], 20, -1926607734);
        a = md5hh(a, b, c, d, x[i + 5], 4, -378558);
        d = md5hh(d, a, b, c, x[i + 8], 11, -2022574463);
        c = md5hh(c, d, a, b, x[i + 11], 16, 1839030562);
        b = md5hh(b, c, d, a, x[i + 14], 23, -35309556);
        a = md5hh(a, b, c, d, x[i + 1], 4, -1530992060);
        d = md5hh(d, a, b, c, x[i + 4], 11, 1272893353);
        c = md5hh(c, d, a, b, x[i + 7], 16, -155497632);
        b = md5hh(b, c, d, a, x[i + 10], 23, -1094730640);
        a = md5hh(a, b, c, d, x[i + 13], 4, 681279174);
        d = md5hh(d, a, b, c, x[i], 11, -358537222);
        c = md5hh(c, d, a, b, x[i + 3], 16, -722521979);
        b = md5hh(b, c, d, a, x[i + 6], 23, 76029189);
        a = md5hh(a, b, c, d, x[i + 9], 4, -640364487);
        d = md5hh(d, a, b, c, x[i + 12], 11, -421815835);
        c = md5hh(c, d, a, b, x[i + 15], 16, 530742520);
        b = md5hh(b, c, d, a, x[i + 2], 23, -995338651);
        a = md5ii(a, b, c, d, x[i], 6, -198630844);
        d = md5ii(d, a, b, c, x[i + 7], 10, 1126891415);
        c = md5ii(c, d, a, b, x[i + 14], 15, -1416354905);
        b = md5ii(b, c, d, a, x[i + 5], 21, -57434055);
        a = md5ii(a, b, c, d, x[i + 12], 6, 1700485571);
        d = md5ii(d, a, b, c, x[i + 3], 10, -1894986606);
        c = md5ii(c, d, a, b, x[i + 10], 15, -1051523);
        b = md5ii(b, c, d, a, x[i + 1], 21, -2054922799);
        a = md5ii(a, b, c, d, x[i + 8], 6, 1873313359);
        d = md5ii(d, a, b, c, x[i + 15], 10, -30611744);
        c = md5ii(c, d, a, b, x[i + 6], 15, -1560198380);
        b = md5ii(b, c, d, a, x[i + 13], 21, 1309151649);
        a = md5ii(a, b, c, d, x[i + 4], 6, -145523070);
        d = md5ii(d, a, b, c, x[i + 11], 10, -1120210379);
        c = md5ii(c, d, a, b, x[i + 2], 15, 718787259);
        b = md5ii(b, c, d, a, x[i + 9], 21, -343485551);
        a = safeAdd(a, olda);
        b = safeAdd(b, oldb);
        c = safeAdd(c, oldc);
        d = safeAdd(d, oldd);
      }
      return [a, b, c, d];
    }
    function bytesToWords(input) {
      if (input.length === 0) {
        return [];
      }
      const length8 = input.length * 8;
      const output = new Uint32Array(getOutputLength(length8));
      for (let i = 0; i < length8; i += 8) {
        output[i >> 5] |= (input[i / 8] & 255) << i % 32;
      }
      return output;
    }
    function safeAdd(x, y) {
      const lsw = (x & 65535) + (y & 65535);
      const msw = (x >> 16) + (y >> 16) + (lsw >> 16);
      return msw << 16 | lsw & 65535;
    }
    function bitRotateLeft(num, cnt) {
      return num << cnt | num >>> 32 - cnt;
    }
    function md5cmn(q, a, b, x, s, t) {
      return safeAdd(bitRotateLeft(safeAdd(safeAdd(a, q), safeAdd(x, t)), s), b);
    }
    function md5ff(a, b, c, d, x, s, t) {
      return md5cmn(b & c | ~b & d, a, b, x, s, t);
    }
    function md5gg(a, b, c, d, x, s, t) {
      return md5cmn(b & d | c & ~d, a, b, x, s, t);
    }
    function md5hh(a, b, c, d, x, s, t) {
      return md5cmn(b ^ c ^ d, a, b, x, s, t);
    }
    function md5ii(a, b, c, d, x, s, t) {
      return md5cmn(c ^ (b | ~d), a, b, x, s, t);
    }
    var _default = md5;
    exports.default = _default;
  }
});
// node_modules/uuid/dist/commonjs-browser/v3.js
var require_v3 = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/v3.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _v = _interopRequireDefault(require_v35());
    var _md = _interopRequireDefault(require_md5());
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    var v3 = (0, _v.default)("v3", 48, _md.default);
    var _default = v3;
    exports.default = _default;
  }
});
// node_modules/uuid/dist/commonjs-browser/native.js
var require_native = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/native.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var randomUUID = typeof crypto !== "undefined" && crypto.randomUUID && crypto.randomUUID.bind(crypto);
    var _default = {
      randomUUID
    };
    exports.default = _default;
  }
});
// node_modules/uuid/dist/commonjs-browser/v4.js
var require_v4 = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/v4.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _native = _interopRequireDefault(require_native());
    var _rng = _interopRequireDefault(require_rng());
    var _stringify = require_stringify();
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    function v4(options, buf, offset) {
      if (_native.default.randomUUID && !buf && !options) {
        return _native.default.randomUUID();
      }
      options = options || {};
      const rnds = options.random || (options.rng || _rng.default)();
      rnds[6] = rnds[6] & 15 | 64;
      rnds[8] = rnds[8] & 63 | 128;
      if (buf) {
        offset = offset || 0;
        for (let i = 0; i < 16; ++i) {
          buf[offset + i] = rnds[i];
        }
        return buf;
      }
      return (0, _stringify.unsafeStringify)(rnds);
    }
    var _default = v4;
    exports.default = _default;
  }
});
// node_modules/uuid/dist/commonjs-browser/sha1.js
var require_sha1 = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/sha1.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    function f(s, x, y, z) {
      switch (s) {
        case 0:
          return x & y ^ ~x & z;
        case 1:
          return x ^ y ^ z;
        case 2:
          return x & y ^ x & z ^ y & z;
        case 3:
          return x ^ y ^ z;
      }
    }
    function ROTL(x, n) {
      return x << n | x >>> 32 - n;
    }
    function sha1(bytes) {
      const K = [1518500249, 1859775393, 2400959708, 3395469782];
      const H = [1732584193, 4023233417, 2562383102, 271733878, 3285377520];
      if (typeof bytes === "string") {
        const msg = unescape(encodeURIComponent(bytes));
        bytes = [];
        for (let i = 0; i < msg.length; ++i) {
          bytes.push(msg.charCodeAt(i));
        }
      } else if (!Array.isArray(bytes)) {
        bytes = Array.prototype.slice.call(bytes);
      }
      bytes.push(128);
      const l = bytes.length / 4 + 2;
      const N = Math.ceil(l / 16);
      const M = new Array(N);
      for (let i = 0; i < N; ++i) {
        const arr = new Uint32Array(16);
        for (let j = 0; j < 16; ++j) {
          arr[j] = bytes[i * 64 + j * 4] << 24 | bytes[i * 64 + j * 4 + 1] << 16 | bytes[i * 64 + j * 4 + 2] << 8 | bytes[i * 64 + j * 4 + 3];
        }
        M[i] = arr;
      }
      M[N - 1][14] = (bytes.length - 1) * 8 / Math.pow(2, 32);
      M[N - 1][14] = Math.floor(M[N - 1][14]);
      M[N - 1][15] = (bytes.length - 1) * 8 & 4294967295;
      for (let i = 0; i < N; ++i) {
        const W = new Uint32Array(80);
        for (let t = 0; t < 16; ++t) {
          W[t] = M[i][t];
        }
        for (let t = 16; t < 80; ++t) {
          W[t] = ROTL(W[t - 3] ^ W[t - 8] ^ W[t - 14] ^ W[t - 16], 1);
        }
        let a = H[0];
        let b = H[1];
        let c = H[2];
        let d = H[3];
        let e = H[4];
        for (let t = 0; t < 80; ++t) {
          const s = Math.floor(t / 20);
          const T = ROTL(a, 5) + f(s, b, c, d) + e + K[s] + W[t] >>> 0;
          e = d;
          d = c;
          c = ROTL(b, 30) >>> 0;
          b = a;
          a = T;
        }
        H[0] = H[0] + a >>> 0;
        H[1] = H[1] + b >>> 0;
        H[2] = H[2] + c >>> 0;
        H[3] = H[3] + d >>> 0;
        H[4] = H[4] + e >>> 0;
      }
      return [H[0] >> 24 & 255, H[0] >> 16 & 255, H[0] >> 8 & 255, H[0] & 255, H[1] >> 24 & 255, H[1] >> 16 & 255, H[1] >> 8 & 255, H[1] & 255, H[2] >> 24 & 255, H[2] >> 16 & 255, H[2] >> 8 & 255, H[2] & 255, H[3] >> 24 & 255, H[3] >> 16 & 255, H[3] >> 8 & 255, H[3] & 255, H[4] >> 24 & 255, H[4] >> 16 & 255, H[4] >> 8 & 255, H[4] & 255];
    }
    var _default = sha1;
    exports.default = _default;
  }
});
// node_modules/uuid/dist/commonjs-browser/v5.js
var require_v5 = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/v5.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _v = _interopRequireDefault(require_v35());
    var _sha = _interopRequireDefault(require_sha1());
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    var v5 = (0, _v.default)("v5", 80, _sha.default);
    var _default = v5;
    exports.default = _default;
  }
});
// node_modules/uuid/dist/commonjs-browser/nil.js
var require_nil = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/nil.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _default = "00000000-0000-0000-0000-000000000000";
    exports.default = _default;
  }
});
// node_modules/uuid/dist/commonjs-browser/version.js
var require_version = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/version.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = void 0;
    var _validate = _interopRequireDefault(require_validate());
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    function version(uuid) {
      if (!(0, _validate.default)(uuid)) {
        throw TypeError("Invalid UUID");
      }
      return parseInt(uuid.slice(14, 15), 16);
    }
    var _default = version;
    exports.default = _default;
  }
});
// node_modules/uuid/dist/commonjs-browser/index.js
var require_commonjs_browser = __commonJS({
  "node_modules/uuid/dist/commonjs-browser/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    Object.defineProperty(exports, "NIL", {
      enumerable: true,
      get: function get() {
        return _nil.default;
      }
    });
    Object.defineProperty(exports, "parse", {
      enumerable: true,
      get: function get() {
        return _parse.default;
      }
    });
    Object.defineProperty(exports, "stringify", {
      enumerable: true,
      get: function get() {
        return _stringify.default;
      }
    });
    Object.defineProperty(exports, "v1", {
      enumerable: true,
      get: function get() {
        return _v.default;
      }
    });
    Object.defineProperty(exports, "v3", {
      enumerable: true,
      get: function get() {
        return _v2.default;
      }
    });
    Object.defineProperty(exports, "v4", {
      enumerable: true,
      get: function get() {
        return _v3.default;
      }
    });
    Object.defineProperty(exports, "v5", {
      enumerable: true,
      get: function get() {
        return _v4.default;
      }
    });
    Object.defineProperty(exports, "validate", {
      enumerable: true,
      get: function get() {
        return _validate.default;
      }
    });
    Object.defineProperty(exports, "version", {
      enumerable: true,
      get: function get() {
        return _version.default;
      }
    });
    var _v = _interopRequireDefault(require_v1());
    var _v2 = _interopRequireDefault(require_v3());
    var _v3 = _interopRequireDefault(require_v4());
    var _v4 = _interopRequireDefault(require_v5());
    var _nil = _interopRequireDefault(require_nil());
    var _version = _interopRequireDefault(require_version());
    var _validate = _interopRequireDefault(require_validate());
    var _stringify = _interopRequireDefault(require_stringify());
    var _parse = _interopRequireDefault(require_parse());
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
  }
});
// node_modules/@babel/runtime/helpers/interopRequireDefault.js
var require_interopRequireDefault = __commonJS({
  "node_modules/@babel/runtime/helpers/interopRequireDefault.js"(exports, module2) {
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : {
        "default": obj
      };
    }
    module2.exports = _interopRequireDefault, module2.exports.__esModule = true, module2.exports["default"] = module2.exports;
  }
});
// node_modules/@babel/runtime/helpers/typeof.js
var require_typeof = __commonJS({
  "node_modules/@babel/runtime/helpers/typeof.js"(exports, module2) {
    function _typeof(obj) {
      "@babel/helpers - typeof";
      return module2.exports = _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(obj2) {
        return typeof obj2;
      } : function(obj2) {
        return obj2 && "function" == typeof Symbol && obj2.constructor === Symbol && obj2 !== Symbol.prototype ? "symbol" : typeof obj2;
      }, module2.exports.__esModule = true, module2.exports["default"] = module2.exports, _typeof(obj);
    }
    module2.exports = _typeof, module2.exports.__esModule = true, module2.exports["default"] = module2.exports;
  }
});
// node_modules/@babel/runtime/helpers/regeneratorRuntime.js
var require_regeneratorRuntime = __commonJS({
  "node_modules/@babel/runtime/helpers/regeneratorRuntime.js"(exports, module2) {
    var _typeof = require_typeof()["default"];
    function _regeneratorRuntime() {
      "use strict";
      module2.exports = _regeneratorRuntime = function _regeneratorRuntime2() {
        return exports2;
      }, module2.exports.__esModule = true, module2.exports["default"] = module2.exports;
      var exports2 = {}, Op = Object.prototype, hasOwn = Op.hasOwnProperty, defineProperty = Object.defineProperty || function(obj, key, desc) {
        obj[key] = desc.value;
      }, $Symbol = "function" == typeof Symbol ? Symbol : {}, iteratorSymbol = $Symbol.iterator || "@@iterator", asyncIteratorSymbol = $Symbol.asyncIterator || "@@asyncIterator", toStringTagSymbol = $Symbol.toStringTag || "@@toStringTag";
      function define(obj, key, value) {
        return Object.defineProperty(obj, key, {
          value,
          enumerable: true,
          configurable: true,
          writable: true
        }), obj[key];
      }
      try {
        define({}, "");
      } catch (err) {
        define = function define2(obj, key, value) {
          return obj[key] = value;
        };
      }
      function wrap(innerFn, outerFn, self, tryLocsList) {
        var protoGenerator = outerFn && outerFn.prototype instanceof Generator ? outerFn : Generator, generator = Object.create(protoGenerator.prototype), context = new Context(tryLocsList || []);
        return defineProperty(generator, "_invoke", {
          value: makeInvokeMethod(innerFn, self, context)
        }), generator;
      }
      function tryCatch(fn, obj, arg) {
        try {
          return {
            type: "normal",
            arg: fn.call(obj, arg)
          };
        } catch (err) {
          return {
            type: "throw",
            arg: err
          };
        }
      }
      exports2.wrap = wrap;
      var ContinueSentinel = {};
      function Generator() {
      }
      function GeneratorFunction() {
      }
      function GeneratorFunctionPrototype() {
      }
      var IteratorPrototype = {};
      define(IteratorPrototype, iteratorSymbol, function() {
        return this;
      });
      var getProto = Object.getPrototypeOf, NativeIteratorPrototype = getProto && getProto(getProto(values([])));
      NativeIteratorPrototype && NativeIteratorPrototype !== Op && hasOwn.call(NativeIteratorPrototype, iteratorSymbol) && (IteratorPrototype = NativeIteratorPrototype);
      var Gp = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(IteratorPrototype);
      function defineIteratorMethods(prototype) {
        ["next", "throw", "return"].forEach(function(method) {
          define(prototype, method, function(arg) {
            return this._invoke(method, arg);
          });
        });
      }
      function AsyncIterator(generator, PromiseImpl) {
        function invoke(method, arg, resolve, reject) {
          var record = tryCatch(generator[method], generator, arg);
          if ("throw" !== record.type) {
            var result = record.arg, value = result.value;
            return value && "object" == _typeof(value) && hasOwn.call(value, "__await") ? PromiseImpl.resolve(value.__await).then(function(value2) {
              invoke("next", value2, resolve, reject);
            }, function(err) {
              invoke("throw", err, resolve, reject);
            }) : PromiseImpl.resolve(value).then(function(unwrapped) {
              result.value = unwrapped, resolve(result);
            }, function(error) {
              return invoke("throw", error, resolve, reject);
            });
          }
          reject(record.arg);
        }
        var previousPromise;
        defineProperty(this, "_invoke", {
          value: function value(method, arg) {
            function callInvokeWithMethodAndArg() {
              return new PromiseImpl(function(resolve, reject) {
                invoke(method, arg, resolve, reject);
              });
            }
            return previousPromise = previousPromise ? previousPromise.then(callInvokeWithMethodAndArg, callInvokeWithMethodAndArg) : callInvokeWithMethodAndArg();
          }
        });
      }
      function makeInvokeMethod(innerFn, self, context) {
        var state = "suspendedStart";
        return function(method, arg) {
          if ("executing" === state)
            throw new Error("Generator is already running");
          if ("completed" === state) {
            if ("throw" === method)
              throw arg;
            return doneResult();
          }
          for (context.method = method, context.arg = arg; ; ) {
            var delegate = context.delegate;
            if (delegate) {
              var delegateResult = maybeInvokeDelegate(delegate, context);
              if (delegateResult) {
                if (delegateResult === ContinueSentinel)
                  continue;
                return delegateResult;
              }
            }
            if ("next" === context.method)
              context.sent = context._sent = context.arg;
            else if ("throw" === context.method) {
              if ("suspendedStart" === state)
                throw state = "completed", context.arg;
              context.dispatchException(context.arg);
            } else
              "return" === context.method && context.abrupt("return", context.arg);
            state = "executing";
            var record = tryCatch(innerFn, self, context);
            if ("normal" === record.type) {
              if (state = context.done ? "completed" : "suspendedYield", record.arg === ContinueSentinel)
                continue;
              return {
                value: record.arg,
                done: context.done
              };
            }
            "throw" === record.type && (state = "completed", context.method = "throw", context.arg = record.arg);
          }
        };
      }
      function maybeInvokeDelegate(delegate, context) {
        var methodName = context.method, method = delegate.iterator[methodName];
        if (void 0 === method)
          return context.delegate = null, "throw" === methodName && delegate.iterator["return"] && (context.method = "return", context.arg = void 0, maybeInvokeDelegate(delegate, context), "throw" === context.method) || "return" !== methodName && (context.method = "throw", context.arg = new TypeError("The iterator does not provide a '" + methodName + "' method")), ContinueSentinel;
        var record = tryCatch(method, delegate.iterator, context.arg);
        if ("throw" === record.type)
          return context.method = "throw", context.arg = record.arg, context.delegate = null, ContinueSentinel;
        var info = record.arg;
        return info ? info.done ? (context[delegate.resultName] = info.value, context.next = delegate.nextLoc, "return" !== context.method && (context.method = "next", context.arg = void 0), context.delegate = null, ContinueSentinel) : info : (context.method = "throw", context.arg = new TypeError("iterator result is not an object"), context.delegate = null, ContinueSentinel);
      }
      function pushTryEntry(locs) {
        var entry = {
          tryLoc: locs[0]
        };
        1 in locs && (entry.catchLoc = locs[1]), 2 in locs && (entry.finallyLoc = locs[2], entry.afterLoc = locs[3]), this.tryEntries.push(entry);
      }
      function resetTryEntry(entry) {
        var record = entry.completion || {};
        record.type = "normal", delete record.arg, entry.completion = record;
      }
      function Context(tryLocsList) {
        this.tryEntries = [{
          tryLoc: "root"
        }], tryLocsList.forEach(pushTryEntry, this), this.reset(true);
      }
      function values(iterable) {
        if (iterable) {
          var iteratorMethod = iterable[iteratorSymbol];
          if (iteratorMethod)
            return iteratorMethod.call(iterable);
          if ("function" == typeof iterable.next)
            return iterable;
          if (!isNaN(iterable.length)) {
            var i = -1, next = function next2() {
              for (; ++i < iterable.length; )
                if (hasOwn.call(iterable, i))
                  return next2.value = iterable[i], next2.done = false, next2;
              return next2.value = void 0, next2.done = true, next2;
            };
            return next.next = next;
          }
        }
        return {
          next: doneResult
        };
      }
      function doneResult() {
        return {
          value: void 0,
          done: true
        };
      }
      return GeneratorFunction.prototype = GeneratorFunctionPrototype, defineProperty(Gp, "constructor", {
        value: GeneratorFunctionPrototype,
        configurable: true
      }), defineProperty(GeneratorFunctionPrototype, "constructor", {
        value: GeneratorFunction,
        configurable: true
      }), GeneratorFunction.displayName = define(GeneratorFunctionPrototype, toStringTagSymbol, "GeneratorFunction"), exports2.isGeneratorFunction = function(genFun) {
        var ctor = "function" == typeof genFun && genFun.constructor;
        return !!ctor && (ctor === GeneratorFunction || "GeneratorFunction" === (ctor.displayName || ctor.name));
      }, exports2.mark = function(genFun) {
        return Object.setPrototypeOf ? Object.setPrototypeOf(genFun, GeneratorFunctionPrototype) : (genFun.__proto__ = GeneratorFunctionPrototype, define(genFun, toStringTagSymbol, "GeneratorFunction")), genFun.prototype = Object.create(Gp), genFun;
      }, exports2.awrap = function(arg) {
        return {
          __await: arg
        };
      }, defineIteratorMethods(AsyncIterator.prototype), define(AsyncIterator.prototype, asyncIteratorSymbol, function() {
        return this;
      }), exports2.AsyncIterator = AsyncIterator, exports2.async = function(innerFn, outerFn, self, tryLocsList, PromiseImpl) {
        void 0 === PromiseImpl && (PromiseImpl = Promise);
        var iter = new AsyncIterator(wrap(innerFn, outerFn, self, tryLocsList), PromiseImpl);
        return exports2.isGeneratorFunction(outerFn) ? iter : iter.next().then(function(result) {
          return result.done ? result.value : iter.next();
        });
      }, defineIteratorMethods(Gp), define(Gp, toStringTagSymbol, "Generator"), define(Gp, iteratorSymbol, function() {
        return this;
      }), define(Gp, "toString", function() {
        return "[object Generator]";
      }), exports2.keys = function(val) {
        var object = Object(val), keys = [];
        for (var key in object)
          keys.push(key);
        return keys.reverse(), function next() {
          for (; keys.length; ) {
            var key2 = keys.pop();
            if (key2 in object)
              return next.value = key2, next.done = false, next;
          }
          return next.done = true, next;
        };
      }, exports2.values = values, Context.prototype = {
        constructor: Context,
        reset: function reset(skipTempReset) {
          if (this.prev = 0, this.next = 0, this.sent = this._sent = void 0, this.done = false, this.delegate = null, this.method = "next", this.arg = void 0, this.tryEntries.forEach(resetTryEntry), !skipTempReset)
            for (var name in this)
              "t" === name.charAt(0) && hasOwn.call(this, name) && !isNaN(+name.slice(1)) && (this[name] = void 0);
        },
        stop: function stop() {
          this.done = true;
          var rootRecord = this.tryEntries[0].completion;
          if ("throw" === rootRecord.type)
            throw rootRecord.arg;
          return this.rval;
        },
        dispatchException: function dispatchException(exception) {
          if (this.done)
            throw exception;
          var context = this;
          function handle(loc, caught) {
            return record.type = "throw", record.arg = exception, context.next = loc, caught && (context.method = "next", context.arg = void 0), !!caught;
          }
          for (var i = this.tryEntries.length - 1; i >= 0; --i) {
            var entry = this.tryEntries[i], record = entry.completion;
            if ("root" === entry.tryLoc)
              return handle("end");
            if (entry.tryLoc <= this.prev) {
              var hasCatch = hasOwn.call(entry, "catchLoc"), hasFinally = hasOwn.call(entry, "finallyLoc");
              if (hasCatch && hasFinally) {
                if (this.prev < entry.catchLoc)
                  return handle(entry.catchLoc, true);
                if (this.prev < entry.finallyLoc)
                  return handle(entry.finallyLoc);
              } else if (hasCatch) {
                if (this.prev < entry.catchLoc)
                  return handle(entry.catchLoc, true);
              } else {
                if (!hasFinally)
                  throw new Error("try statement without catch or finally");
                if (this.prev < entry.finallyLoc)
                  return handle(entry.finallyLoc);
              }
            }
          }
        },
        abrupt: function abrupt(type, arg) {
          for (var i = this.tryEntries.length - 1; i >= 0; --i) {
            var entry = this.tryEntries[i];
            if (entry.tryLoc <= this.prev && hasOwn.call(entry, "finallyLoc") && this.prev < entry.finallyLoc) {
              var finallyEntry = entry;
              break;
            }
          }
          finallyEntry && ("break" === type || "continue" === type) && finallyEntry.tryLoc <= arg && arg <= finallyEntry.finallyLoc && (finallyEntry = null);
          var record = finallyEntry ? finallyEntry.completion : {};
          return record.type = type, record.arg = arg, finallyEntry ? (this.method = "next", this.next = finallyEntry.finallyLoc, ContinueSentinel) : this.complete(record);
        },
        complete: function complete(record, afterLoc) {
          if ("throw" === record.type)
            throw record.arg;
          return "break" === record.type || "continue" === record.type ? this.next = record.arg : "return" === record.type ? (this.rval = this.arg = record.arg, this.method = "return", this.next = "end") : "normal" === record.type && afterLoc && (this.next = afterLoc), ContinueSentinel;
        },
        finish: function finish(finallyLoc) {
          for (var i = this.tryEntries.length - 1; i >= 0; --i) {
            var entry = this.tryEntries[i];
            if (entry.finallyLoc === finallyLoc)
              return this.complete(entry.completion, entry.afterLoc), resetTryEntry(entry), ContinueSentinel;
          }
        },
        "catch": function _catch(tryLoc) {
          for (var i = this.tryEntries.length - 1; i >= 0; --i) {
            var entry = this.tryEntries[i];
            if (entry.tryLoc === tryLoc) {
              var record = entry.completion;
              if ("throw" === record.type) {
                var thrown = record.arg;
                resetTryEntry(entry);
              }
              return thrown;
            }
          }
          throw new Error("illegal catch attempt");
        },
        delegateYield: function delegateYield(iterable, resultName, nextLoc) {
          return this.delegate = {
            iterator: values(iterable),
            resultName,
            nextLoc
          }, "next" === this.method && (this.arg = void 0), ContinueSentinel;
        }
      }, exports2;
    }
    module2.exports = _regeneratorRuntime, module2.exports.__esModule = true, module2.exports["default"] = module2.exports;
  }
});
// node_modules/@babel/runtime/regenerator/index.js
var require_regenerator = __commonJS({
  "node_modules/@babel/runtime/regenerator/index.js"(exports, module2) {
    var runtime = require_regeneratorRuntime()();
    module2.exports = runtime;
    try {
      regeneratorRuntime = runtime;
    } catch (accidentalStrictMode) {
      if (typeof globalThis === "object") {
        globalThis.regeneratorRuntime = runtime;
      } else {
        Function("r", "regeneratorRuntime = r")(runtime);
      }
    }
  }
});
// node_modules/@babel/runtime/helpers/asyncToGenerator.js
var require_asyncToGenerator = __commonJS({
  "node_modules/@babel/runtime/helpers/asyncToGenerator.js"(exports, module2) {
    function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) {
      try {
        var info = gen[key](arg);
        var value = info.value;
      } catch (error) {
        reject(error);
        return;
      }
      if (info.done) {
        resolve(value);
      } else {
        Promise.resolve(value).then(_next, _throw);
      }
    }
    function _asyncToGenerator(fn) {
      return function() {
        var self = this, args = arguments;
        return new Promise(function(resolve, reject) {
          var gen = fn.apply(self, args);
          function _next(value) {
            asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value);
          }
          function _throw(err) {
            asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err);
          }
          _next(void 0);
        });
      };
    }
    module2.exports = _asyncToGenerator, module2.exports.__esModule = true, module2.exports["default"] = module2.exports;
  }
});
// node_modules/@babel/runtime/helpers/toPrimitive.js
var require_toPrimitive = __commonJS({
  "node_modules/@babel/runtime/helpers/toPrimitive.js"(exports, module2) {
    var _typeof = require_typeof()["default"];
    function _toPrimitive(input, hint) {
      if (_typeof(input) !== "object" || input === null)
        return input;
      var prim = input[Symbol.toPrimitive];
      if (prim !== void 0) {
        var res = prim.call(input, hint || "default");
        if (_typeof(res) !== "object")
          return res;
        throw new TypeError("@@toPrimitive must return a primitive value.");
      }
      return (hint === "string" ? String : Number)(input);
    }
    module2.exports = _toPrimitive, module2.exports.__esModule = true, module2.exports["default"] = module2.exports;
  }
});
// node_modules/@babel/runtime/helpers/toPropertyKey.js
var require_toPropertyKey = __commonJS({
  "node_modules/@babel/runtime/helpers/toPropertyKey.js"(exports, module2) {
    var _typeof = require_typeof()["default"];
    var toPrimitive = require_toPrimitive();
    function _toPropertyKey(arg) {
      var key = toPrimitive(arg, "string");
      return _typeof(key) === "symbol" ? key : String(key);
    }
    module2.exports = _toPropertyKey, module2.exports.__esModule = true, module2.exports["default"] = module2.exports;
  }
});
// node_modules/@babel/runtime/helpers/defineProperty.js
var require_defineProperty = __commonJS({
  "node_modules/@babel/runtime/helpers/defineProperty.js"(exports, module2) {
    var toPropertyKey = require_toPropertyKey();
    function _defineProperty(obj, key, value) {
      key = toPropertyKey(key);
      if (key in obj) {
        Object.defineProperty(obj, key, {
          value,
          enumerable: true,
          configurable: true,
          writable: true
        });
      } else {
        obj[key] = value;
      }
      return obj;
    }
    module2.exports = _defineProperty, module2.exports.__esModule = true, module2.exports["default"] = module2.exports;
  }
});
// node_modules/is-retry-allowed/index.js
var require_is_retry_allowed = __commonJS({
  "node_modules/is-retry-allowed/index.js"(exports, module2) {
    "use strict";
    var denyList = /* @__PURE__ */ new Set([
      "ENOTFOUND",
      "ENETUNREACH",
      // SSL errors from https://github.com/nodejs/node/blob/fc8e3e2cdc521978351de257030db0076d79e0ab/src/crypto/crypto_common.cc#L301-L328
      "UNABLE_TO_GET_ISSUER_CERT",
      "UNABLE_TO_GET_CRL",
      "UNABLE_TO_DECRYPT_CERT_SIGNATURE",
      "UNABLE_TO_DECRYPT_CRL_SIGNATURE",
      "UNABLE_TO_DECODE_ISSUER_PUBLIC_KEY",
      "CERT_SIGNATURE_FAILURE",
      "CRL_SIGNATURE_FAILURE",
      "CERT_NOT_YET_VALID",
      "CERT_HAS_EXPIRED",
      "CRL_NOT_YET_VALID",
      "CRL_HAS_EXPIRED",
      "ERROR_IN_CERT_NOT_BEFORE_FIELD",
      "ERROR_IN_CERT_NOT_AFTER_FIELD",
      "ERROR_IN_CRL_LAST_UPDATE_FIELD",
      "ERROR_IN_CRL_NEXT_UPDATE_FIELD",
      "OUT_OF_MEM",
      "DEPTH_ZERO_SELF_SIGNED_CERT",
      "SELF_SIGNED_CERT_IN_CHAIN",
      "UNABLE_TO_GET_ISSUER_CERT_LOCALLY",
      "UNABLE_TO_VERIFY_LEAF_SIGNATURE",
      "CERT_CHAIN_TOO_LONG",
      "CERT_REVOKED",
      "INVALID_CA",
      "PATH_LENGTH_EXCEEDED",
      "INVALID_PURPOSE",
      "CERT_UNTRUSTED",
      "CERT_REJECTED",
      "HOSTNAME_MISMATCH"
    ]);
    module2.exports = (error) => !denyList.has(error && error.code);
  }
});
// node_modules/axios-retry/lib/cjs/index.js
var require_cjs = __commonJS({
  "node_modules/axios-retry/lib/cjs/index.js"(exports) {
    "use strict";
    var _interopRequireDefault = require_interopRequireDefault();
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.isNetworkError = isNetworkError;
    exports.isRetryableError = isRetryableError;
    exports.isSafeRequestError = isSafeRequestError;
    exports.isIdempotentRequestError = isIdempotentRequestError;
    exports.isNetworkOrIdempotentRequestError = isNetworkOrIdempotentRequestError;
    exports.exponentialDelay = exponentialDelay;
    exports.default = axiosRetry;
    exports.namespace = void 0;
    var _regenerator = _interopRequireDefault(require_regenerator());
    var _typeof2 = _interopRequireDefault(require_typeof());
    var _asyncToGenerator2 = _interopRequireDefault(require_asyncToGenerator());
    var _defineProperty2 = _interopRequireDefault(require_defineProperty());
    var _isRetryAllowed = _interopRequireDefault(require_is_retry_allowed());
    function ownKeys(object, enumerableOnly) {
      var keys = Object.keys(object);
      if (Object.getOwnPropertySymbols) {
        var symbols = Object.getOwnPropertySymbols(object);
        if (enumerableOnly) {
          symbols = symbols.filter(function(sym) {
            return Object.getOwnPropertyDescriptor(object, sym).enumerable;
          });
        }
        keys.push.apply(keys, symbols);
      }
      return keys;
    }
    function _objectSpread(target) {
      for (var i = 1; i < arguments.length; i++) {
        var source = arguments[i] != null ? arguments[i] : {};
        if (i % 2) {
          ownKeys(Object(source), true).forEach(function(key) {
            (0, _defineProperty2.default)(target, key, source[key]);
          });
        } else if (Object.getOwnPropertyDescriptors) {
          Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
        } else {
          ownKeys(Object(source)).forEach(function(key) {
            Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
          });
        }
      }
      return target;
    }
    var namespace = "axios-retry";
    exports.namespace = namespace;
    function isNetworkError(error) {
      return !error.response && Boolean(error.code) && // Prevents retrying cancelled requests
      error.code !== "ECONNABORTED" && // Prevents retrying timed out requests
      (0, _isRetryAllowed.default)(error);
    }
    var SAFE_HTTP_METHODS = ["get", "head", "options"];
    var IDEMPOTENT_HTTP_METHODS = SAFE_HTTP_METHODS.concat(["put", "delete"]);
    function isRetryableError(error) {
      return error.code !== "ECONNABORTED" && (!error.response || error.response.status >= 500 && error.response.status <= 599);
    }
    function isSafeRequestError(error) {
      if (!error.config) {
        return false;
      }
      return isRetryableError(error) && SAFE_HTTP_METHODS.indexOf(error.config.method) !== -1;
    }
    function isIdempotentRequestError(error) {
      if (!error.config) {
        return false;
      }
      return isRetryableError(error) && IDEMPOTENT_HTTP_METHODS.indexOf(error.config.method) !== -1;
    }
    function isNetworkOrIdempotentRequestError(error) {
      return isNetworkError(error) || isIdempotentRequestError(error);
    }
    function noDelay() {
      return 0;
    }
    function exponentialDelay() {
      var retryNumber = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 0;
      var delay = Math.pow(2, retryNumber) * 100;
      var randomSum = delay * 0.2 * Math.random();
      return delay + randomSum;
    }
    function getCurrentState(config) {
      var currentState = config[namespace] || {};
      currentState.retryCount = currentState.retryCount || 0;
      config[namespace] = currentState;
      return currentState;
    }
    function getRequestOptions(config, defaultOptions) {
      return _objectSpread(_objectSpread({}, defaultOptions), config[namespace]);
    }
    function fixConfig(axios, config) {
      if (axios.defaults.agent === config.agent) {
        delete config.agent;
      }
      if (axios.defaults.httpAgent === config.httpAgent) {
        delete config.httpAgent;
      }
      if (axios.defaults.httpsAgent === config.httpsAgent) {
        delete config.httpsAgent;
      }
    }
    function shouldRetry(_x, _x2, _x3, _x4) {
      return _shouldRetry.apply(this, arguments);
    }
    function _shouldRetry() {
      _shouldRetry = (0, _asyncToGenerator2.default)(/* @__PURE__ */ _regenerator.default.mark(function _callee2(retries, retryCondition, currentState, error) {
        var shouldRetryOrPromise, shouldRetryPromiseResult;
        return _regenerator.default.wrap(function _callee2$(_context2) {
          while (1) {
            switch (_context2.prev = _context2.next) {
              case 0:
                shouldRetryOrPromise = currentState.retryCount < retries && retryCondition(error);
                if (!((0, _typeof2.default)(shouldRetryOrPromise) === "object")) {
                  _context2.next = 12;
                  break;
                }
                _context2.prev = 2;
                _context2.next = 5;
                return shouldRetryOrPromise;
              case 5:
                shouldRetryPromiseResult = _context2.sent;
                return _context2.abrupt("return", shouldRetryPromiseResult !== false);
              case 9:
                _context2.prev = 9;
                _context2.t0 = _context2["catch"](2);
                return _context2.abrupt("return", false);
              case 12:
                return _context2.abrupt("return", shouldRetryOrPromise);
              case 13:
              case "end":
                return _context2.stop();
            }
          }
        }, _callee2, null, [[2, 9]]);
      }));
      return _shouldRetry.apply(this, arguments);
    }
    function axiosRetry(axios, defaultOptions) {
      axios.interceptors.request.use(function(config) {
        var currentState = getCurrentState(config);
        currentState.lastRequestTime = Date.now();
        return config;
      });
      axios.interceptors.response.use(null, /* @__PURE__ */ function() {
        var _ref = (0, _asyncToGenerator2.default)(/* @__PURE__ */ _regenerator.default.mark(function _callee(error) {
          var config, _getRequestOptions, _getRequestOptions$re, retries, _getRequestOptions$re2, retryCondition, _getRequestOptions$re3, retryDelay, _getRequestOptions$sh, shouldResetTimeout, _getRequestOptions$on, onRetry, currentState, delay, lastRequestDuration, timeout;
          return _regenerator.default.wrap(function _callee$(_context) {
            while (1) {
              switch (_context.prev = _context.next) {
                case 0:
                  config = error.config;
                  if (config) {
                    _context.next = 3;
                    break;
                  }
                  return _context.abrupt("return", Promise.reject(error));
                case 3:
                  _getRequestOptions = getRequestOptions(config, defaultOptions), _getRequestOptions$re = _getRequestOptions.retries, retries = _getRequestOptions$re === void 0 ? 3 : _getRequestOptions$re, _getRequestOptions$re2 = _getRequestOptions.retryCondition, retryCondition = _getRequestOptions$re2 === void 0 ? isNetworkOrIdempotentRequestError : _getRequestOptions$re2, _getRequestOptions$re3 = _getRequestOptions.retryDelay, retryDelay = _getRequestOptions$re3 === void 0 ? noDelay : _getRequestOptions$re3, _getRequestOptions$sh = _getRequestOptions.shouldResetTimeout, shouldResetTimeout = _getRequestOptions$sh === void 0 ? false : _getRequestOptions$sh, _getRequestOptions$on = _getRequestOptions.onRetry, onRetry = _getRequestOptions$on === void 0 ? function() {
                  } : _getRequestOptions$on;
                  currentState = getCurrentState(config);
                  _context.next = 7;
                  return shouldRetry(retries, retryCondition, currentState, error);
                case 7:
                  if (!_context.sent) {
                    _context.next = 20;
                    break;
                  }
                  currentState.retryCount += 1;
                  delay = retryDelay(currentState.retryCount, error);
                  fixConfig(axios, config);
                  if (!(!shouldResetTimeout && config.timeout && currentState.lastRequestTime)) {
                    _context.next = 17;
                    break;
                  }
                  lastRequestDuration = Date.now() - currentState.lastRequestTime;
                  timeout = config.timeout - lastRequestDuration - delay;
                  if (!(timeout <= 0)) {
                    _context.next = 16;
                    break;
                  }
                  return _context.abrupt("return", Promise.reject(error));
                case 16:
                  config.timeout = timeout;
                case 17:
                  config.transformRequest = [function(data) {
                    return data;
                  }];
                  onRetry(currentState.retryCount, error, config);
                  return _context.abrupt("return", new Promise(function(resolve) {
                    return setTimeout(function() {
                      return resolve(axios(config));
                    }, delay);
                  }));
                case 20:
                  return _context.abrupt("return", Promise.reject(error));
                case 21:
                case "end":
                  return _context.stop();
              }
            }
          }, _callee);
        }));
        return function(_x5) {
          return _ref.apply(this, arguments);
        };
      }());
    }
    axiosRetry.isNetworkError = isNetworkError;
    axiosRetry.isSafeRequestError = isSafeRequestError;
    axiosRetry.isIdempotentRequestError = isIdempotentRequestError;
    axiosRetry.isNetworkOrIdempotentRequestError = isNetworkOrIdempotentRequestError;
    axiosRetry.exponentialDelay = exponentialDelay;
    axiosRetry.isRetryableError = isRetryableError;
  }
});
// node_modules/axios-retry/index.js
var require_axios_retry = __commonJS({
  "node_modules/axios-retry/index.js"(exports, module2) {
    var axiosRetry = require_cjs().default;
    module2.exports = axiosRetry;
    module2.exports.default = axiosRetry;
  }
});
// node_modules/@doist/todoist-api-typescript/dist/restClient.js
var require_restClient = __commonJS({
  "node_modules/@doist/todoist-api-typescript/dist/restClient.js"(exports) {
    "use strict";
    var __assign2 = exports && exports.__assign || function() {
      __assign2 = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
          s = arguments[i];
          for (var p in s)
            if (Object.prototype.hasOwnProperty.call(s, p))
              t[p] = s[p];
        }
        return t;
      };
      return __assign2.apply(this, arguments);
    };
    var __awaiter2 = exports && exports.__awaiter || function(thisArg, _arguments, P, generator) {
      function adopt(value) {
        return value instanceof P ? value : new P(function(resolve) {
          resolve(value);
        });
      }
      return new (P || (P = Promise))(function(resolve, reject) {
        function fulfilled(value) {
          try {
            step(generator.next(value));
          } catch (e) {
            reject(e);
          }
        }
        function rejected(value) {
          try {
            step(generator["throw"](value));
          } catch (e) {
            reject(e);
          }
        }
        function step(result) {
          result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
        }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
      });
    };
    var __generator2 = exports && exports.__generator || function(thisArg, body) {
      var _ = { label: 0, sent: function() {
        if (t[0] & 1)
          throw t[1];
        return t[1];
      }, trys: [], ops: [] }, f, y, t, g;
      return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() {
        return this;
      }), g;
      function verb(n) {
        return function(v) {
          return step([n, v]);
        };
      }
      function step(op) {
        if (f)
          throw new TypeError("Generator is already executing.");
        while (_)
          try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done)
              return t;
            if (y = 0, t)
              op = [op[0] & 2, t.value];
            switch (op[0]) {
              case 0:
              case 1:
                t = op;
                break;
              case 4:
                _.label++;
                return { value: op[1], done: false };
              case 5:
                _.label++;
                y = op[1];
                op = [0];
                continue;
              case 7:
                op = _.ops.pop();
                _.trys.pop();
                continue;
              default:
                if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
                  _ = 0;
                  continue;
                }
                if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
                  _.label = op[1];
                  break;
                }
                if (op[0] === 6 && _.label < t[1]) {
                  _.label = t[1];
                  t = op;
                  break;
                }
                if (t && _.label < t[2]) {
                  _.label = t[2];
                  _.ops.push(op);
                  break;
                }
                if (t[2])
                  _.ops.pop();
                _.trys.pop();
                continue;
            }
            op = body.call(thisArg, _);
          } catch (e) {
            op = [6, e];
            y = 0;
          } finally {
            f = t = 0;
          }
        if (op[0] & 5)
          throw op[1];
        return { value: op[0] ? op[1] : void 0, done: true };
      }
    };
    var __importDefault2 = exports && exports.__importDefault || function(mod) {
      return mod && mod.__esModule ? mod : { "default": mod };
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.request = exports.isSuccess = exports.paramsSerializer = void 0;
    var axios_1 = __importDefault2(require_axios2());
    var axios_case_converter_1 = __importDefault2(require_lib2());
    var errors_1 = require_errors2();
    var uuid_1 = require_commonjs_browser();
    var axios_retry_1 = __importDefault2(require_axios_retry());
    function paramsSerializer(params) {
      var qs = new URLSearchParams();
      Object.keys(params).forEach(function(key) {
        var value = params[key];
        if (Array.isArray(value)) {
          qs.append(key, value.join(","));
        } else {
          qs.append(key, String(value));
        }
      });
      return qs.toString();
    }
    exports.paramsSerializer = paramsSerializer;
    var defaultHeaders = {
      "Content-Type": "application/json"
    };
    function getAuthHeader(apiKey) {
      return "Bearer ".concat(apiKey);
    }
    function isNetworkError(error) {
      return Boolean(!error.response && error.code !== "ECONNABORTED");
    }
    function getRetryDelay(retryCount) {
      return retryCount === 1 ? 0 : 500;
    }
    function isAxiosError(error) {
      return Boolean(error === null || error === void 0 ? void 0 : error.isAxiosError);
    }
    function getTodoistRequestError(error, originalStack) {
      var requestError = new errors_1.TodoistRequestError(error.message);
      requestError.stack = isAxiosError(error) && originalStack ? originalStack.stack : error.stack;
      if (isAxiosError(error) && error.response) {
        requestError.httpStatusCode = error.response.status;
        requestError.responseData = error.response.data;
      }
      return requestError;
    }
    function getRequestConfiguration(baseURL, apiToken, requestId) {
      var authHeader = apiToken ? { Authorization: getAuthHeader(apiToken) } : void 0;
      var requestIdHeader = requestId ? { "X-Request-Id": requestId } : void 0;
      var headers = __assign2(__assign2(__assign2({}, defaultHeaders), authHeader), requestIdHeader);
      return { baseURL, headers };
    }
    function getAxiosClient(baseURL, apiToken, requestId) {
      var configuration = getRequestConfiguration(baseURL, apiToken, requestId);
      var client = (0, axios_case_converter_1.default)(axios_1.default.create(configuration));
      (0, axios_retry_1.default)(client, {
        retries: 3,
        retryCondition: isNetworkError,
        retryDelay: getRetryDelay
      });
      return client;
    }
    function isSuccess(response) {
      return response.status >= 200 && response.status < 300;
    }
    exports.isSuccess = isSuccess;
    function request(httpMethod, baseUri, relativePath, apiToken, payload, requestId) {
      return __awaiter2(this, void 0, void 0, function() {
        var originalStack, axiosClient, _a, error_1;
        return __generator2(this, function(_b) {
          switch (_b.label) {
            case 0:
              originalStack = new Error();
              _b.label = 1;
            case 1:
              _b.trys.push([1, 9, , 10]);
              if (httpMethod === "POST" && !requestId) {
                requestId = (0, uuid_1.v4)();
              }
              axiosClient = getAxiosClient(baseUri, apiToken, requestId);
              _a = httpMethod;
              switch (_a) {
                case "GET":
                  return [3, 2];
                case "POST":
                  return [3, 4];
                case "DELETE":
                  return [3, 6];
              }
              return [3, 8];
            case 2:
              return [4, axiosClient.get(relativePath, {
                params: payload,
                paramsSerializer
              })];
            case 3:
              return [2, _b.sent()];
            case 4:
              return [4, axiosClient.post(relativePath, payload)];
            case 5:
              return [2, _b.sent()];
            case 6:
              return [4, axiosClient.delete(relativePath)];
            case 7:
              return [2, _b.sent()];
            case 8:
              return [3, 10];
            case 9:
              error_1 = _b.sent();
              if (!isAxiosError(error_1) && !(error_1 instanceof Error)) {
                throw new Error("An unknown error occurred during the request");
              }
              throw getTodoistRequestError(error_1, originalStack);
            case 10:
              return [
                2
                /*return*/
              ];
          }
        });
      });
    }
    exports.request = request;
  }
});
// node_modules/@doist/todoist-api-typescript/dist/utils/taskConverters.js
var require_taskConverters = __commonJS({
  "node_modules/@doist/todoist-api-typescript/dist/utils/taskConverters.js"(exports) {
    "use strict";
    var __assign2 = exports && exports.__assign || function() {
      __assign2 = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
          s = arguments[i];
          for (var p in s)
            if (Object.prototype.hasOwnProperty.call(s, p))
              t[p] = s[p];
        }
        return t;
      };
      return __assign2.apply(this, arguments);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getTaskFromQuickAddResponse = void 0;
    var showTaskEndpoint = "https://todoist.com/showTask";
    function getTaskUrlFromQuickAddResponse(responseData) {
      return "".concat(showTaskEndpoint, "?id=").concat(responseData.id);
    }
    function getTaskFromQuickAddResponse(responseData) {
      var _a;
      var due = responseData.due ? __assign2(__assign2({ isRecurring: responseData.due.isRecurring, string: responseData.due.string, date: responseData.due.date }, responseData.due.timezone !== null && { datetime: responseData.due.date }), responseData.due.timezone !== null && { timezone: responseData.due.timezone }) : void 0;
      var task = __assign2(__assign2(__assign2({ id: responseData.id, order: responseData.childOrder, content: responseData.content, description: responseData.description, projectId: responseData.projectId, sectionId: responseData.sectionId ? responseData.sectionId : void 0, isCompleted: responseData.checked, labels: responseData.labels, priority: responseData.priority, commentCount: 0, createdAt: responseData.addedAt, url: getTaskUrlFromQuickAddResponse(responseData), creatorId: (_a = responseData.addedByUid) !== null && _a !== void 0 ? _a : "" }, due !== void 0 && { due }), responseData.parentId !== null && { parentId: responseData.parentId }), responseData.responsibleUid !== null && {
        assigneeId: responseData.responsibleUid
      });
      return task;
    }
    exports.getTaskFromQuickAddResponse = getTaskFromQuickAddResponse;
  }
});
// node_modules/@doist/todoist-api-typescript/dist/consts/endpoints.js
var require_endpoints = __commonJS({
  "node_modules/@doist/todoist-api-typescript/dist/consts/endpoints.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ENDPOINT_REVOKE_TOKEN = exports.ENDPOINT_GET_TOKEN = exports.ENDPOINT_AUTHORIZATION = exports.ENDPOINT_SYNC_QUICK_ADD = exports.ENDPOINT_REST_PROJECT_COLLABORATORS = exports.ENDPOINT_REST_TASK_REOPEN = exports.ENDPOINT_REST_TASK_CLOSE = exports.ENDPOINT_REST_COMMENTS = exports.ENDPOINT_REST_LABELS_SHARED_REMOVE = exports.ENDPOINT_REST_LABELS_SHARED_RENAME = exports.ENDPOINT_REST_LABELS_SHARED = exports.ENDPOINT_REST_LABELS = exports.ENDPOINT_REST_SECTIONS = exports.ENDPOINT_REST_PROJECTS = exports.ENDPOINT_REST_TASKS = exports.getAuthBaseUri = exports.getSyncBaseUri = exports.getRestBaseUri = void 0;
    var BASE_URI = "https://api.todoist.com";
    var API_REST_BASE_URI = "/rest/v2/";
    var API_SYNC_BASE_URI = "/sync/v9/";
    var TODOIST_URI = "https://todoist.com";
    var API_AUTHORIZATION_BASE_URI = "/oauth/";
    function getRestBaseUri(domainBase) {
      if (domainBase === void 0) {
        domainBase = BASE_URI;
      }
      return new URL(API_REST_BASE_URI, domainBase).toString();
    }
    exports.getRestBaseUri = getRestBaseUri;
    function getSyncBaseUri(domainBase) {
      if (domainBase === void 0) {
        domainBase = BASE_URI;
      }
      return new URL(API_SYNC_BASE_URI, domainBase).toString();
    }
    exports.getSyncBaseUri = getSyncBaseUri;
    function getAuthBaseUri(domainBase) {
      if (domainBase === void 0) {
        domainBase = TODOIST_URI;
      }
      return new URL(API_AUTHORIZATION_BASE_URI, domainBase).toString();
    }
    exports.getAuthBaseUri = getAuthBaseUri;
    exports.ENDPOINT_REST_TASKS = "tasks";
    exports.ENDPOINT_REST_PROJECTS = "projects";
    exports.ENDPOINT_REST_SECTIONS = "sections";
    exports.ENDPOINT_REST_LABELS = "labels";
    exports.ENDPOINT_REST_LABELS_SHARED = exports.ENDPOINT_REST_LABELS + "/shared";
    exports.ENDPOINT_REST_LABELS_SHARED_RENAME = exports.ENDPOINT_REST_LABELS_SHARED + "/rename";
    exports.ENDPOINT_REST_LABELS_SHARED_REMOVE = exports.ENDPOINT_REST_LABELS_SHARED + "/remove";
    exports.ENDPOINT_REST_COMMENTS = "comments";
    exports.ENDPOINT_REST_TASK_CLOSE = "close";
    exports.ENDPOINT_REST_TASK_REOPEN = "reopen";
    exports.ENDPOINT_REST_PROJECT_COLLABORATORS = "collaborators";
    exports.ENDPOINT_SYNC_QUICK_ADD = "quick/add";
    exports.ENDPOINT_AUTHORIZATION = "authorize";
    exports.ENDPOINT_GET_TOKEN = "access_token";
    exports.ENDPOINT_REVOKE_TOKEN = "access_tokens/revoke";
  }
});
// node_modules/@doist/todoist-api-typescript/dist/types/entities.js
var require_entities = __commonJS({
  "node_modules/@doist/todoist-api-typescript/dist/types/entities.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.User = exports.Comment = exports.Attachment = exports.Label = exports.Section = exports.Project = exports.Task = exports.DueDate = exports.Int = void 0;
    var runtypes_1 = require_lib();
    exports.Int = runtypes_1.Number.withConstraint(function(n) {
      return Number.isInteger(n) || "".concat(n, " is not a valid entity id. Should be a string");
    });
    exports.DueDate = (0, runtypes_1.Record)({
      isRecurring: runtypes_1.Boolean,
      string: runtypes_1.String,
      date: runtypes_1.String
    }).And((0, runtypes_1.Partial)({
      datetime: runtypes_1.String.Or(runtypes_1.Null),
      timezone: runtypes_1.String.Or(runtypes_1.Null)
    }));
    exports.Task = (0, runtypes_1.Record)({
      id: runtypes_1.String,
      order: exports.Int,
      content: runtypes_1.String,
      description: runtypes_1.String,
      projectId: runtypes_1.String,
      isCompleted: runtypes_1.Boolean,
      labels: (0, runtypes_1.Array)(runtypes_1.String),
      priority: exports.Int,
      commentCount: exports.Int,
      createdAt: runtypes_1.String,
      url: runtypes_1.String,
      creatorId: runtypes_1.String
    }).And((0, runtypes_1.Partial)({
      due: exports.DueDate.Or(runtypes_1.Null),
      assigneeId: runtypes_1.String.Or(runtypes_1.Null),
      assignerId: runtypes_1.String.Or(runtypes_1.Null),
      parentId: runtypes_1.String.Or(runtypes_1.Null),
      sectionId: runtypes_1.String.Or(runtypes_1.Null)
    }));
    exports.Project = (0, runtypes_1.Record)({
      id: runtypes_1.String,
      name: runtypes_1.String,
      color: runtypes_1.String,
      commentCount: exports.Int,
      isShared: runtypes_1.Boolean,
      isFavorite: runtypes_1.Boolean,
      url: runtypes_1.String,
      isInboxProject: runtypes_1.Boolean,
      isTeamInbox: runtypes_1.Boolean,
      order: exports.Int,
      viewStyle: runtypes_1.String
    }).And((0, runtypes_1.Partial)({
      parentId: runtypes_1.String.Or(runtypes_1.Null)
    }));
    exports.Section = (0, runtypes_1.Record)({
      id: runtypes_1.String,
      order: exports.Int,
      name: runtypes_1.String,
      projectId: runtypes_1.String
    });
    exports.Label = (0, runtypes_1.Record)({
      id: runtypes_1.String,
      order: exports.Int,
      name: runtypes_1.String,
      color: runtypes_1.String,
      isFavorite: runtypes_1.Boolean
    });
    exports.Attachment = (0, runtypes_1.Record)({
      resourceType: runtypes_1.String
    }).And((0, runtypes_1.Partial)({
      fileName: runtypes_1.String.Or(runtypes_1.Null),
      fileSize: exports.Int.Or(runtypes_1.Null),
      fileType: runtypes_1.String.Or(runtypes_1.Null),
      fileUrl: runtypes_1.String.Or(runtypes_1.Null),
      fileDuration: exports.Int.Or(runtypes_1.Null),
      uploadState: (0, runtypes_1.Union)((0, runtypes_1.Literal)("pending"), (0, runtypes_1.Literal)("completed")).Or(runtypes_1.Null),
      image: runtypes_1.String.Or(runtypes_1.Null),
      imageWidth: exports.Int.Or(runtypes_1.Null),
      imageHeight: exports.Int.Or(runtypes_1.Null),
      url: runtypes_1.String.Or(runtypes_1.Null),
      title: runtypes_1.String.Or(runtypes_1.Null)
    }));
    exports.Comment = (0, runtypes_1.Record)({
      id: runtypes_1.String,
      content: runtypes_1.String,
      postedAt: runtypes_1.String
    }).And((0, runtypes_1.Partial)({
      taskId: runtypes_1.String.Or(runtypes_1.Null),
      projectId: runtypes_1.String.Or(runtypes_1.Null),
      attachment: exports.Attachment.Or(runtypes_1.Null)
    }));
    exports.User = (0, runtypes_1.Record)({
      id: runtypes_1.String,
      name: runtypes_1.String,
      email: runtypes_1.String
    });
  }
});
// node_modules/@doist/todoist-api-typescript/dist/types/requests.js
var require_requests = __commonJS({
  "node_modules/@doist/todoist-api-typescript/dist/types/requests.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
  }
});
// node_modules/@doist/todoist-api-typescript/dist/types/index.js
var require_types2 = __commonJS({
  "node_modules/@doist/todoist-api-typescript/dist/types/index.js"(exports) {
    "use strict";
    var __createBinding2 = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0)
        k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m[k];
        } };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0)
        k2 = k;
      o[k2] = m[k];
    });
    var __exportStar2 = exports && exports.__exportStar || function(m, exports2) {
      for (var p in m)
        if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p))
          __createBinding2(exports2, m, p);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    __exportStar2(require_entities(), exports);
    __exportStar2(require_errors2(), exports);
    __exportStar2(require_requests(), exports);
  }
});
// node_modules/@doist/todoist-api-typescript/dist/utils/validators.js
var require_validators = __commonJS({
  "node_modules/@doist/todoist-api-typescript/dist/utils/validators.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.validateUserArray = exports.validateUser = exports.validateCommentArray = exports.validateComment = exports.validateLabelArray = exports.validateLabel = exports.validateSectionArray = exports.validateSection = exports.validateProjectArray = exports.validateProject = exports.validateTaskArray = exports.validateTask = void 0;
    var types_1 = require_types2();
    function validateTask(input) {
      return types_1.Task.check(input);
    }
    exports.validateTask = validateTask;
    function validateTaskArray(input) {
      return input.map(validateTask);
    }
    exports.validateTaskArray = validateTaskArray;
    function validateProject(input) {
      return types_1.Project.check(input);
    }
    exports.validateProject = validateProject;
    function validateProjectArray(input) {
      return input.map(validateProject);
    }
    exports.validateProjectArray = validateProjectArray;
    function validateSection(input) {
      return types_1.Section.check(input);
    }
    exports.validateSection = validateSection;
    function validateSectionArray(input) {
      return input.map(validateSection);
    }
    exports.validateSectionArray = validateSectionArray;
    function validateLabel(input) {
      return types_1.Label.check(input);
    }
    exports.validateLabel = validateLabel;
    function validateLabelArray(input) {
      return input.map(validateLabel);
    }
    exports.validateLabelArray = validateLabelArray;
    function validateComment(input) {
      return types_1.Comment.check(input);
    }
    exports.validateComment = validateComment;
    function validateCommentArray(input) {
      return input.map(validateComment);
    }
    exports.validateCommentArray = validateCommentArray;
    function validateUser(input) {
      return types_1.User.check(input);
    }
    exports.validateUser = validateUser;
    function validateUserArray(input) {
      return input.map(validateUser);
    }
    exports.validateUserArray = validateUserArray;
  }
});
// node_modules/@doist/todoist-api-typescript/dist/TodoistApi.js
var require_TodoistApi = __commonJS({
  "node_modules/@doist/todoist-api-typescript/dist/TodoistApi.js"(exports) {
    "use strict";
    var __awaiter2 = exports && exports.__awaiter || function(thisArg, _arguments, P, generator) {
      function adopt(value) {
        return value instanceof P ? value : new P(function(resolve) {
          resolve(value);
        });
      }
      return new (P || (P = Promise))(function(resolve, reject) {
        function fulfilled(value) {
          try {
            step(generator.next(value));
          } catch (e) {
            reject(e);
          }
        }
        function rejected(value) {
          try {
            step(generator["throw"](value));
          } catch (e) {
            reject(e);
          }
        }
        function step(result) {
          result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
        }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
      });
    };
    var __generator2 = exports && exports.__generator || function(thisArg, body) {
      var _ = { label: 0, sent: function() {
        if (t[0] & 1)
          throw t[1];
        return t[1];
      }, trys: [], ops: [] }, f, y, t, g;
      return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() {
        return this;
      }), g;
      function verb(n) {
        return function(v) {
          return step([n, v]);
        };
      }
      function step(op) {
        if (f)
          throw new TypeError("Generator is already executing.");
        while (_)
          try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done)
              return t;
            if (y = 0, t)
              op = [op[0] & 2, t.value];
            switch (op[0]) {
              case 0:
              case 1:
                t = op;
                break;
              case 4:
                _.label++;
                return { value: op[1], done: false };
              case 5:
                _.label++;
                y = op[1];
                op = [0];
                continue;
              case 7:
                op = _.ops.pop();
                _.trys.pop();
                continue;
              default:
                if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
                  _ = 0;
                  continue;
                }
                if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
                  _.label = op[1];
                  break;
                }
                if (op[0] === 6 && _.label < t[1]) {
                  _.label = t[1];
                  t = op;
                  break;
                }
                if (t && _.label < t[2]) {
                  _.label = t[2];
                  _.ops.push(op);
                  break;
                }
                if (t[2])
                  _.ops.pop();
                _.trys.pop();
                continue;
            }
            op = body.call(thisArg, _);
          } catch (e) {
            op = [6, e];
            y = 0;
          } finally {
            f = t = 0;
          }
        if (op[0] & 5)
          throw op[1];
        return { value: op[0] ? op[1] : void 0, done: true };
      }
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.TodoistApi = void 0;
    var runtypes_1 = require_lib();
    var restClient_1 = require_restClient();
    var taskConverters_1 = require_taskConverters();
    var endpoints_1 = require_endpoints();
    var validators_1 = require_validators();
    function generatePath() {
      var segments = [];
      for (var _i = 0; _i < arguments.length; _i++) {
        segments[_i] = arguments[_i];
      }
      return segments.join("/");
    }
    var TodoistApi2 = (
      /** @class */
      function() {
        function TodoistApi3(authToken, baseUrl) {
          this.authToken = authToken;
          this.restApiBase = (0, endpoints_1.getRestBaseUri)(baseUrl);
          this.syncApiBase = (0, endpoints_1.getSyncBaseUri)(baseUrl);
        }
        TodoistApi3.prototype.getTask = function(id) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  runtypes_1.String.check(id);
                  return [4, (0, restClient_1.request)("GET", this.restApiBase, generatePath(endpoints_1.ENDPOINT_REST_TASKS, id), this.authToken)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateTask)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.getTasks = function(args) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  return [4, (0, restClient_1.request)("GET", this.restApiBase, endpoints_1.ENDPOINT_REST_TASKS, this.authToken, args)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateTaskArray)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.addTask = function(args, requestId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  return [4, (0, restClient_1.request)("POST", this.restApiBase, endpoints_1.ENDPOINT_REST_TASKS, this.authToken, args, requestId)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateTask)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.quickAddTask = function(args) {
          return __awaiter2(this, void 0, void 0, function() {
            var response, task;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  return [4, (0, restClient_1.request)("POST", this.syncApiBase, endpoints_1.ENDPOINT_SYNC_QUICK_ADD, this.authToken, args)];
                case 1:
                  response = _a.sent();
                  task = (0, taskConverters_1.getTaskFromQuickAddResponse)(response.data);
                  return [2, (0, validators_1.validateTask)(task)];
              }
            });
          });
        };
        TodoistApi3.prototype.updateTask = function(id, args, requestId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  runtypes_1.String.check(id);
                  return [4, (0, restClient_1.request)("POST", this.restApiBase, generatePath(endpoints_1.ENDPOINT_REST_TASKS, id), this.authToken, args, requestId)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateTask)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.closeTask = function(id, requestId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  runtypes_1.String.check(id);
                  return [4, (0, restClient_1.request)("POST", this.restApiBase, generatePath(endpoints_1.ENDPOINT_REST_TASKS, id, endpoints_1.ENDPOINT_REST_TASK_CLOSE), this.authToken, void 0, requestId)];
                case 1:
                  response = _a.sent();
                  return [2, (0, restClient_1.isSuccess)(response)];
              }
            });
          });
        };
        TodoistApi3.prototype.reopenTask = function(id, requestId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  runtypes_1.String.check(id);
                  return [4, (0, restClient_1.request)("POST", this.restApiBase, generatePath(endpoints_1.ENDPOINT_REST_TASKS, id, endpoints_1.ENDPOINT_REST_TASK_REOPEN), this.authToken, void 0, requestId)];
                case 1:
                  response = _a.sent();
                  return [2, (0, restClient_1.isSuccess)(response)];
              }
            });
          });
        };
        TodoistApi3.prototype.deleteTask = function(id, requestId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  runtypes_1.String.check(id);
                  return [4, (0, restClient_1.request)("DELETE", this.restApiBase, generatePath(endpoints_1.ENDPOINT_REST_TASKS, id), this.authToken, void 0, requestId)];
                case 1:
                  response = _a.sent();
                  return [2, (0, restClient_1.isSuccess)(response)];
              }
            });
          });
        };
        TodoistApi3.prototype.getProject = function(id) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  runtypes_1.String.check(id);
                  return [4, (0, restClient_1.request)("GET", this.restApiBase, generatePath(endpoints_1.ENDPOINT_REST_PROJECTS, id), this.authToken)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateProject)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.getProjects = function() {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  return [4, (0, restClient_1.request)("GET", this.restApiBase, endpoints_1.ENDPOINT_REST_PROJECTS, this.authToken)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateProjectArray)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.addProject = function(args, requestId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  return [4, (0, restClient_1.request)("POST", this.restApiBase, endpoints_1.ENDPOINT_REST_PROJECTS, this.authToken, args, requestId)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateProject)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.updateProject = function(id, args, requestId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  runtypes_1.String.check(id);
                  return [4, (0, restClient_1.request)("POST", this.restApiBase, generatePath(endpoints_1.ENDPOINT_REST_PROJECTS, id), this.authToken, args, requestId)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateProject)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.deleteProject = function(id, requestId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  runtypes_1.String.check(id);
                  return [4, (0, restClient_1.request)("DELETE", this.restApiBase, generatePath(endpoints_1.ENDPOINT_REST_PROJECTS, id), this.authToken, void 0, requestId)];
                case 1:
                  response = _a.sent();
                  return [2, (0, restClient_1.isSuccess)(response)];
              }
            });
          });
        };
        TodoistApi3.prototype.getProjectCollaborators = function(projectId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  runtypes_1.String.check(projectId);
                  return [4, (0, restClient_1.request)("GET", this.restApiBase, generatePath(endpoints_1.ENDPOINT_REST_PROJECTS, projectId, endpoints_1.ENDPOINT_REST_PROJECT_COLLABORATORS), this.authToken)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateUserArray)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.getSections = function(projectId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  return [4, (0, restClient_1.request)("GET", this.restApiBase, endpoints_1.ENDPOINT_REST_SECTIONS, this.authToken, projectId ? { projectId } : void 0)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateSectionArray)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.getSection = function(id) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  runtypes_1.String.check(id);
                  return [4, (0, restClient_1.request)("GET", this.restApiBase, generatePath(endpoints_1.ENDPOINT_REST_SECTIONS, id), this.authToken)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateSection)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.addSection = function(args, requestId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  return [4, (0, restClient_1.request)("POST", this.restApiBase, endpoints_1.ENDPOINT_REST_SECTIONS, this.authToken, args, requestId)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateSection)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.updateSection = function(id, args, requestId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  runtypes_1.String.check(id);
                  return [4, (0, restClient_1.request)("POST", this.restApiBase, generatePath(endpoints_1.ENDPOINT_REST_SECTIONS, id), this.authToken, args, requestId)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateSection)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.deleteSection = function(id, requestId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  runtypes_1.String.check(id);
                  return [4, (0, restClient_1.request)("DELETE", this.restApiBase, generatePath(endpoints_1.ENDPOINT_REST_SECTIONS, id), this.authToken, void 0, requestId)];
                case 1:
                  response = _a.sent();
                  return [2, (0, restClient_1.isSuccess)(response)];
              }
            });
          });
        };
        TodoistApi3.prototype.getLabel = function(id) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  runtypes_1.String.check(id);
                  return [4, (0, restClient_1.request)("GET", this.restApiBase, generatePath(endpoints_1.ENDPOINT_REST_LABELS, id), this.authToken)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateLabel)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.getLabels = function() {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  return [4, (0, restClient_1.request)("GET", this.restApiBase, endpoints_1.ENDPOINT_REST_LABELS, this.authToken)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateLabelArray)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.addLabel = function(args, requestId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  return [4, (0, restClient_1.request)("POST", this.restApiBase, endpoints_1.ENDPOINT_REST_LABELS, this.authToken, args, requestId)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateLabel)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.updateLabel = function(id, args, requestId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  runtypes_1.String.check(id);
                  return [4, (0, restClient_1.request)("POST", this.restApiBase, generatePath(endpoints_1.ENDPOINT_REST_LABELS, id), this.authToken, args, requestId)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateLabel)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.deleteLabel = function(id, requestId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  runtypes_1.String.check(id);
                  return [4, (0, restClient_1.request)("DELETE", this.restApiBase, generatePath(endpoints_1.ENDPOINT_REST_LABELS, id), this.authToken, void 0, requestId)];
                case 1:
                  response = _a.sent();
                  return [2, (0, restClient_1.isSuccess)(response)];
              }
            });
          });
        };
        TodoistApi3.prototype.getSharedLabels = function() {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  return [4, (0, restClient_1.request)("GET", this.restApiBase, endpoints_1.ENDPOINT_REST_LABELS_SHARED, this.authToken)];
                case 1:
                  response = _a.sent();
                  return [2, response.data];
              }
            });
          });
        };
        TodoistApi3.prototype.renameSharedLabel = function(args) {
          return __awaiter2(this, void 0, void 0, function() {
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  return [4, (0, restClient_1.request)("POST", this.restApiBase, endpoints_1.ENDPOINT_REST_LABELS_SHARED_RENAME, this.authToken, args)];
                case 1:
                  _a.sent();
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        TodoistApi3.prototype.removeSharedLabel = function(args) {
          return __awaiter2(this, void 0, void 0, function() {
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  return [4, (0, restClient_1.request)("POST", this.restApiBase, endpoints_1.ENDPOINT_REST_LABELS_SHARED_REMOVE, this.authToken, args)];
                case 1:
                  _a.sent();
                  return [
                    2
                    /*return*/
                  ];
              }
            });
          });
        };
        TodoistApi3.prototype.getComments = function(args) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  return [4, (0, restClient_1.request)("GET", this.restApiBase, endpoints_1.ENDPOINT_REST_COMMENTS, this.authToken, args)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateCommentArray)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.getComment = function(id) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  runtypes_1.String.check(id);
                  return [4, (0, restClient_1.request)("GET", this.restApiBase, generatePath(endpoints_1.ENDPOINT_REST_COMMENTS, id), this.authToken)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateComment)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.addComment = function(args, requestId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  return [4, (0, restClient_1.request)("POST", this.restApiBase, endpoints_1.ENDPOINT_REST_COMMENTS, this.authToken, args, requestId)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateComment)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.updateComment = function(id, args, requestId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  runtypes_1.String.check(id);
                  return [4, (0, restClient_1.request)("POST", this.restApiBase, generatePath(endpoints_1.ENDPOINT_REST_COMMENTS, id), this.authToken, args, requestId)];
                case 1:
                  response = _a.sent();
                  return [2, (0, validators_1.validateComment)(response.data)];
              }
            });
          });
        };
        TodoistApi3.prototype.deleteComment = function(id, requestId) {
          return __awaiter2(this, void 0, void 0, function() {
            var response;
            return __generator2(this, function(_a) {
              switch (_a.label) {
                case 0:
                  runtypes_1.String.check(id);
                  return [4, (0, restClient_1.request)("DELETE", this.restApiBase, generatePath(endpoints_1.ENDPOINT_REST_COMMENTS, id), this.authToken, void 0, requestId)];
                case 1:
                  response = _a.sent();
                  return [2, (0, restClient_1.isSuccess)(response)];
              }
            });
          });
        };
        return TodoistApi3;
      }()
    );
    exports.TodoistApi = TodoistApi2;
  }
});
// node_modules/@doist/todoist-api-typescript/dist/authentication.js
var require_authentication = __commonJS({
  "node_modules/@doist/todoist-api-typescript/dist/authentication.js"(exports) {
    "use strict";
    var __awaiter2 = exports && exports.__awaiter || function(thisArg, _arguments, P, generator) {
      function adopt(value) {
        return value instanceof P ? value : new P(function(resolve) {
          resolve(value);
        });
      }
      return new (P || (P = Promise))(function(resolve, reject) {
        function fulfilled(value) {
          try {
            step(generator.next(value));
          } catch (e) {
            reject(e);
          }
        }
        function rejected(value) {
          try {
            step(generator["throw"](value));
          } catch (e) {
            reject(e);
          }
        }
        function step(result) {
          result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
        }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
      });
    };
    var __generator2 = exports && exports.__generator || function(thisArg, body) {
      var _ = { label: 0, sent: function() {
        if (t[0] & 1)
          throw t[1];
        return t[1];
      }, trys: [], ops: [] }, f, y, t, g;
      return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() {
        return this;
      }), g;
      function verb(n) {
        return function(v) {
          return step([n, v]);
        };
      }
      function step(op) {
        if (f)
          throw new TypeError("Generator is already executing.");
        while (_)
          try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done)
              return t;
            if (y = 0, t)
              op = [op[0] & 2, t.value];
            switch (op[0]) {
              case 0:
              case 1:
                t = op;
                break;
              case 4:
                _.label++;
                return { value: op[1], done: false };
              case 5:
                _.label++;
                y = op[1];
                op = [0];
                continue;
              case 7:
                op = _.ops.pop();
                _.trys.pop();
                continue;
              default:
                if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
                  _ = 0;
                  continue;
                }
                if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
                  _.label = op[1];
                  break;
                }
                if (op[0] === 6 && _.label < t[1]) {
                  _.label = t[1];
                  t = op;
                  break;
                }
                if (t && _.label < t[2]) {
                  _.label = t[2];
                  _.ops.push(op);
                  break;
                }
                if (t[2])
                  _.ops.pop();
                _.trys.pop();
                continue;
            }
            op = body.call(thisArg, _);
          } catch (e) {
            op = [6, e];
            y = 0;
          } finally {
            f = t = 0;
          }
        if (op[0] & 5)
          throw op[1];
        return { value: op[0] ? op[1] : void 0, done: true };
      }
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.revokeAuthToken = exports.getAuthToken = exports.getAuthorizationUrl = exports.getAuthStateParameter = void 0;
    var restClient_1 = require_restClient();
    var uuid_1 = require_commonjs_browser();
    var types_1 = require_types2();
    var endpoints_1 = require_endpoints();
    function getAuthStateParameter() {
      return (0, uuid_1.v4)();
    }
    exports.getAuthStateParameter = getAuthStateParameter;
    function getAuthorizationUrl(clientId, permissions, state, baseUrl) {
      if (!(permissions === null || permissions === void 0 ? void 0 : permissions.length)) {
        throw new Error("At least one scope value should be passed for permissions.");
      }
      var scope = permissions.join(",");
      return "".concat((0, endpoints_1.getAuthBaseUri)(baseUrl)).concat(endpoints_1.ENDPOINT_AUTHORIZATION, "?client_id=").concat(clientId, "&scope=").concat(scope, "&state=").concat(state);
    }
    exports.getAuthorizationUrl = getAuthorizationUrl;
    function getAuthToken(args, baseUrl) {
      var _a;
      return __awaiter2(this, void 0, void 0, function() {
        var response;
        return __generator2(this, function(_b) {
          switch (_b.label) {
            case 0:
              return [4, (0, restClient_1.request)("POST", (0, endpoints_1.getAuthBaseUri)(baseUrl), endpoints_1.ENDPOINT_GET_TOKEN, void 0, args)];
            case 1:
              response = _b.sent();
              if (response.status !== 200 || !((_a = response.data) === null || _a === void 0 ? void 0 : _a.accessToken)) {
                throw new types_1.TodoistRequestError("Authentication token exchange failed.", response.status, response.data);
              }
              return [2, response.data];
          }
        });
      });
    }
    exports.getAuthToken = getAuthToken;
    function revokeAuthToken(args, baseUrl) {
      return __awaiter2(this, void 0, void 0, function() {
        var response;
        return __generator2(this, function(_a) {
          switch (_a.label) {
            case 0:
              return [4, (0, restClient_1.request)("POST", (0, endpoints_1.getSyncBaseUri)(baseUrl), endpoints_1.ENDPOINT_REVOKE_TOKEN, void 0, args)];
            case 1:
              response = _a.sent();
              return [2, (0, restClient_1.isSuccess)(response)];
          }
        });
      });
    }
    exports.revokeAuthToken = revokeAuthToken;
  }
});
// node_modules/@doist/todoist-api-typescript/dist/utils/colors.js
var require_colors = __commonJS({
  "node_modules/@doist/todoist-api-typescript/dist/utils/colors.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getColorByKey = exports.getColorByName = exports.getColorById = exports.defaultColor = exports.colors = exports.taupe = exports.gray = exports.charcoal = exports.salmon = exports.magenta = exports.lavender = exports.violet = exports.grape = exports.blue = exports.lightBlue = exports.skyBlue = exports.turquoise = exports.mintGreen = exports.green = exports.limeGreen = exports.oliveGreen = exports.yellow = exports.orange = exports.red = exports.berryRed = void 0;
    exports.berryRed = {
      id: 30,
      key: "berry_red",
      displayName: "Berry Red",
      name: "Berry Red",
      hexValue: "#b8255f",
      value: "#b8255f"
    };
    exports.red = {
      id: 31,
      key: "red",
      displayName: "Red",
      name: "Red",
      hexValue: "#db4035",
      value: "#db4035"
    };
    exports.orange = {
      id: 32,
      key: "orange",
      displayName: "Orange",
      name: "Orange",
      hexValue: "#ff9933",
      value: "#ff9933"
    };
    exports.yellow = {
      id: 33,
      key: "yellow",
      displayName: "Yellow",
      name: "Yellow",
      hexValue: "#fad000",
      value: "#fad000"
    };
    exports.oliveGreen = {
      id: 34,
      key: "olive_green",
      displayName: "Olive Green",
      name: "Olive Green",
      hexValue: "#afb83b",
      value: "#afb83b"
    };
    exports.limeGreen = {
      id: 35,
      key: "lime_green",
      displayName: "Lime Green",
      name: "Lime Green",
      hexValue: "#7ecc49",
      value: "#7ecc49"
    };
    exports.green = {
      id: 36,
      key: "green",
      displayName: "Green",
      name: "Green",
      hexValue: "#299438",
      value: "#299438"
    };
    exports.mintGreen = {
      id: 37,
      key: "mint_green",
      displayName: "Mint Green",
      name: "Mint Green",
      hexValue: "#6accbc",
      value: "#6accbc"
    };
    exports.turquoise = {
      id: 38,
      key: "turquoise",
      displayName: "Turquoise",
      name: "Turquoise",
      hexValue: "#158fad",
      value: "#158fad"
    };
    exports.skyBlue = {
      id: 39,
      key: "sky_blue",
      displayName: "Sky Blue",
      name: "Sky Blue",
      hexValue: "#14aaf5",
      value: "#14aaf5"
    };
    exports.lightBlue = {
      id: 40,
      key: "light_blue",
      displayName: "Light Blue",
      name: "Light Blue",
      hexValue: "#96c3eb",
      value: "#96c3eb"
    };
    exports.blue = {
      id: 41,
      key: "blue",
      displayName: "Blue",
      name: "Blue",
      hexValue: "#4073ff",
      value: "#4073ff"
    };
    exports.grape = {
      id: 42,
      key: "grape",
      displayName: "Grape",
      name: "Grape",
      hexValue: "#884dff",
      value: "#884dff"
    };
    exports.violet = {
      id: 43,
      key: "violet",
      displayName: "Violet",
      name: "Violet",
      hexValue: "#af38eb",
      value: "#af38eb"
    };
    exports.lavender = {
      id: 44,
      key: "lavender",
      displayName: "Lavender",
      name: "Lavender",
      hexValue: "#eb96eb",
      value: "#eb96eb"
    };
    exports.magenta = {
      id: 45,
      key: "magenta",
      displayName: "Magenta",
      name: "Magenta",
      hexValue: "#e05194",
      value: "#e05194"
    };
    exports.salmon = {
      id: 46,
      key: "salmon",
      displayName: "Salmon",
      name: "Salmon",
      hexValue: "#ff8d85",
      value: "#ff8d85"
    };
    exports.charcoal = {
      id: 47,
      key: "charcoal",
      displayName: "Charcoal",
      name: "Charcoal",
      hexValue: "#808080",
      value: "#808080"
    };
    exports.gray = {
      id: 48,
      key: "gray",
      displayName: "Gray",
      name: "Gray",
      hexValue: "#b8b8b8",
      value: "#b8b8b8"
    };
    exports.taupe = {
      id: 49,
      key: "taupe",
      displayName: "Taupe",
      name: "Taupe",
      hexValue: "#ccac93",
      value: "#ccac93"
    };
    exports.colors = [
      exports.berryRed,
      exports.red,
      exports.orange,
      exports.yellow,
      exports.oliveGreen,
      exports.limeGreen,
      exports.green,
      exports.mintGreen,
      exports.turquoise,
      exports.skyBlue,
      exports.lightBlue,
      exports.blue,
      exports.grape,
      exports.violet,
      exports.lavender,
      exports.magenta,
      exports.salmon,
      exports.charcoal,
      exports.gray,
      exports.taupe
    ];
    exports.defaultColor = exports.charcoal;
    function getColorById(colorId) {
      var color = exports.colors.find(function(color2) {
        return color2.id === colorId;
      });
      return color !== null && color !== void 0 ? color : exports.defaultColor;
    }
    exports.getColorById = getColorById;
    function getColorByName(colorName) {
      var color = exports.colors.find(function(color2) {
        return color2.name === colorName;
      });
      return color !== null && color !== void 0 ? color : exports.defaultColor;
    }
    exports.getColorByName = getColorByName;
    function getColorByKey(colorKey) {
      var color = exports.colors.find(function(color2) {
        return color2.key === colorKey;
      });
      return color !== null && color !== void 0 ? color : exports.defaultColor;
    }
    exports.getColorByKey = getColorByKey;
  }
});
// node_modules/@doist/todoist-api-typescript/dist/utils/sanitization.js
var require_sanitization = __commonJS({
  "node_modules/@doist/todoist-api-typescript/dist/utils/sanitization.js"(exports) {
    "use strict";
    var __assign2 = exports && exports.__assign || function() {
      __assign2 = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
          s = arguments[i];
          for (var p in s)
            if (Object.prototype.hasOwnProperty.call(s, p))
              t[p] = s[p];
        }
        return t;
      };
      return __assign2.apply(this, arguments);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getSanitizedTasks = exports.getSanitizedContent = void 0;
    var BOLD_FORMAT = /(^|[\s!?,;>:]+)(?:\*\*|__|!!)(.+?)(\*\*|__|!!)(?=$|[\s!?,;><:]+)/gi;
    var ITALIC_FORMAT = /(^|[\s!?,;>:]+)(?:\*|_|!)(.+?)(\*|_|!)(?=$|[\s!?,;><:]+)/gi;
    var BOLD_ITALIC_FORMAT = /(^|[\s!?,;>:]+)(?:\*\*\*|___|!!!)(.+?)(\*\*\*|___|!!!)(?=$|[\s!?,;><:]+)/gi;
    var CODE_BLOCK_FORMAT = /```([\s\S]*?)```/gi;
    var CODE_INLINE_FORMAT = /`([^`]+)`/gi;
    var TODOIST_LINK = /((?:(?:onenote:)?[\w-]+):\/\/[^\s]+)\s+[[(]([^)]+)[\])]/gi;
    var MARKDOWN_LINK = /\[(.+?)\]\((.+?)\)/gi;
    var GMAIL_LINK = /\[\[gmail=(.+?),\s*(.+?)\]\]/gi;
    var OUTLOOK_LINK = /\[\[outlook=(.+?),\s*(.+?)\]\]/gi;
    var THUNDERBIRD_LINK = /\[\[thunderbird\n(.+)\n(.+)\n\s*\]\]/gi;
    var FAKE_SECTION_PREFIX = "* ";
    var FAKE_SECTION_SUFFIX = ":";
    function removeStyleFormatting(input) {
      if (!input.includes("!") && !input.includes("*") && !input.includes("_")) {
        return input;
      }
      function removeMarkdown(match, prefix, text) {
        return "".concat(prefix).concat(text);
      }
      input = input.replace(BOLD_ITALIC_FORMAT, removeMarkdown);
      input = input.replace(BOLD_FORMAT, removeMarkdown);
      input = input.replace(ITALIC_FORMAT, removeMarkdown);
      return input;
    }
    function removeCodeFormatting(input) {
      function removeMarkdown(match, text) {
        return text;
      }
      input = input.replace(CODE_BLOCK_FORMAT, removeMarkdown);
      input = input.replace(CODE_INLINE_FORMAT, removeMarkdown);
      return input;
    }
    function removeFakeSectionFormatting(input) {
      if (input.startsWith(FAKE_SECTION_PREFIX)) {
        input = input.slice(FAKE_SECTION_PREFIX.length);
      }
      if (input.endsWith(FAKE_SECTION_SUFFIX)) {
        input = input.slice(0, input.length - FAKE_SECTION_SUFFIX.length);
      }
      return input;
    }
    function removeMarkdownLinks(input) {
      if (!input.includes("[") || !input.includes("]")) {
        return input;
      }
      function removeMarkdown(match, text) {
        return text;
      }
      return input.replace(MARKDOWN_LINK, removeMarkdown);
    }
    function removeTodoistLinks(input) {
      if (!input.includes("(") || !input.includes(")")) {
        return input;
      }
      function removeMarkdown(match, url, text) {
        return text;
      }
      return input.replace(TODOIST_LINK, removeMarkdown);
    }
    function removeAppLinks(input) {
      if (input.includes("gmail")) {
        input = input.replace(GMAIL_LINK, function(match, id, text) {
          return text;
        });
      }
      if (input.includes("outlook")) {
        input = input.replace(OUTLOOK_LINK, function(match, id, text) {
          return text;
        });
      }
      if (input.includes("thunderbird")) {
        input = input.replace(THUNDERBIRD_LINK, function(match, text) {
          return text;
        });
      }
      return input;
    }
    function getSanitizedContent(input) {
      input = removeStyleFormatting(input);
      input = removeCodeFormatting(input);
      input = removeFakeSectionFormatting(input);
      input = removeMarkdownLinks(input);
      input = removeTodoistLinks(input);
      input = removeAppLinks(input);
      return input;
    }
    exports.getSanitizedContent = getSanitizedContent;
    function getSanitizedTasks(tasks) {
      return tasks.map(function(task) {
        return __assign2(__assign2({}, task), { sanitizedContent: getSanitizedContent(task.content) });
      });
    }
    exports.getSanitizedTasks = getSanitizedTasks;
  }
});
// node_modules/@doist/todoist-api-typescript/dist/utils/index.js
var require_utils2 = __commonJS({
  "node_modules/@doist/todoist-api-typescript/dist/utils/index.js"(exports) {
    "use strict";
    var __createBinding2 = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0)
        k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m[k];
        } };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0)
        k2 = k;
      o[k2] = m[k];
    });
    var __exportStar2 = exports && exports.__exportStar || function(m, exports2) {
      for (var p in m)
        if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p))
          __createBinding2(exports2, m, p);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    __exportStar2(require_colors(), exports);
    __exportStar2(require_sanitization(), exports);
  }
});
// node_modules/@doist/todoist-api-typescript/dist/index.js
var require_dist10 = __commonJS({
  "node_modules/@doist/todoist-api-typescript/dist/index.js"(exports) {
    "use strict";
    var __createBinding2 = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
      if (k2 === void 0)
        k2 = k;
      var desc = Object.getOwnPropertyDescriptor(m, k);
      if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m[k];
        } };
      }
      Object.defineProperty(o, k2, desc);
    } : function(o, m, k, k2) {
      if (k2 === void 0)
        k2 = k;
      o[k2] = m[k];
    });
    var __exportStar2 = exports && exports.__exportStar || function(m, exports2) {
      for (var p in m)
        if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p))
          __createBinding2(exports2, m, p);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    __exportStar2(require_TodoistApi(), exports);
    __exportStar2(require_restClient(), exports);
    __exportStar2(require_authentication(), exports);
    __exportStar2(require_types2(), exports);
    __exportStar2(require_utils2(), exports);
  }
});
// main.ts
var main_exports = {};
__export(main_exports, {
  default: () => TodoistBridgeForObsidian
});
module.exports = __toCommonJS(main_exports);
var import_obsidian4 = require("obsidian");
var import_bridge_state_store = (() => {
  const STATE_VERSION = 1;
  const ADAPTER_STATE_PATH = "plugins/todoist-bridge/todoist-bridge-state.json";
  const RUNTIME_SETTING_KEYS = new Set(["todoistTasksData", "fileMetadata", "statistics"]);
  const PERSISTED_SETTING_KEYS = new Set(["todoistAPISecretName", "defaultProjectId", "automaticSynchronizationInterval", "automaticSynchronizationEnabled", "disableTodoistInboundSync", "debugMode"]);
  const cloneJson = (value) => value == null ? value : JSON.parse(JSON.stringify(value));
  const joinPath = (...parts) => parts.filter(Boolean).join("/").replace(/\/+/g, "/");
  const emptyState = () => ({ version: STATE_VERSION, revision: 0, tasks: [], projects: [], events: [], fileMetadata: {}, dirtyFiles: [], dirtyTaskIds: [], reconcileCursor: 0, tombstones: [], lastWriterDevice: null, lastWriteReason: null, lastWriteAt: null, updatedAt: null });
  const normalizeId = (value) => value == null ? "" : String(value).trim();
  const normalizePathValue = (value) => typeof value === "string" ? value.trim().split(/[\\/]+/).join("/") : "";
  const normalizeUniqueStrings = (values) => {
    const seen = /* @__PURE__ */ new Set();
    const output = [];
    for (const value of Array.isArray(values) ? values : []) {
      const normalized = normalizePathValue(value);
      if (!normalized || seen.has(normalized)) continue;
      seen.add(normalized);
      output.push(normalized);
    }
    return output;
  };
  const normalizeTombstones = (tombstones) => {
    const byTaskId = /* @__PURE__ */ new Map();
    for (const tombstone of Array.isArray(tombstones) ? tombstones : []) {
      if (!tombstone || tombstone.taskId == null) continue;
      const taskId = normalizeId(tombstone.taskId);
      if (!taskId) continue;
      const entry = {
        taskId,
        path: normalizePathValue(tombstone.path),
        reason: tombstone.reason ? String(tombstone.reason) : "detached",
        source: tombstone.source ? String(tombstone.source) : "unknown",
        createdAt: tombstone.createdAt || new Date().toISOString()
      };
      const existing = byTaskId.get(taskId);
      if (!existing || String(entry.createdAt) >= String(existing.createdAt)) byTaskId.set(taskId, entry);
    }
    return Array.from(byTaskId.values());
  };
  const getEventCacheKey = (event) => {
    if (!event) return "";
    if (event.eventKey) return String(event.eventKey);
    const eventType = event.event_type != null ? String(event.event_type) : "";
    const objectType = event.object_type != null ? String(event.object_type) : "";
    const objectId = event.object_id != null ? String(event.object_id) : "";
    const parentId = event.parent_item_id != null ? String(event.parent_item_id) : "";
    const eventDate = event.event_date != null ? String(event.event_date) : "";
    return [eventType, objectType, objectId, parentId, eventDate].join("|");
  };
  const normalizeEvent = (event) => {
    const eventKey = getEventCacheKey(event);
    if (!eventKey || eventKey === "||||") return null;
    return {
      id: event.id != null ? String(event.id) : eventKey,
      eventKey,
      event_type: event.event_type ?? null,
      object_type: event.object_type ?? null,
      object_id: event.object_id != null ? String(event.object_id) : null,
      parent_item_id: event.parent_item_id != null ? String(event.parent_item_id) : null,
      event_date: event.event_date ?? null
    };
  };
  const normalizeTaskForStore = (task, now = () => new Date().toISOString()) => {
    if (!task) return null;
    const id = normalizeId(task.id);
    const taskPath = normalizePathValue(task.path);
    const isCompleted = task.checked === true || task.completed === true || task.isCompleted === true;
    if (!id || !taskPath || isCompleted) return null;
    return { ...cloneJson(task), id, path: taskPath, isCompleted: false, origin: task.origin || "todoist", addedAt: task.addedAt || task.createdAt || now() };
  };
  const normalizeFileMetadata = (fileMetadata) => {
    const normalized = {};
    if (!fileMetadata || typeof fileMetadata !== "object") return normalized;
    for (const [rawPath, metadata] of Object.entries(fileMetadata)) {
      const filepath = normalizePathValue(rawPath);
      if (!filepath) continue;
      const ids = Array.isArray(metadata && metadata.todoistTasks) ? metadata.todoistTasks.map(normalizeId).filter(Boolean) : [];
      const unique = Array.from(new Set(ids));
      normalized[filepath] = { todoistTasks: unique, todoistCount: unique.length };
    }
    return normalized;
  };
  const normalizeState = (state) => {
    const base = { ...emptyState(), ...(state || {}) };
    const seenTasks = /* @__PURE__ */ new Set();
    const tasks = [];
    for (const task of Array.isArray(base.tasks) ? base.tasks : []) {
      const normalized = normalizeTaskForStore(task);
      if (!normalized || seenTasks.has(normalized.id)) continue;
      seenTasks.add(normalized.id);
      tasks.push(normalized);
    }
    const seenProjects = /* @__PURE__ */ new Set();
    const projects = [];
    for (const project of Array.isArray(base.projects) ? base.projects : []) {
      if (!project || project.id == null) continue;
      const id = String(project.id);
      if (seenProjects.has(id)) continue;
      seenProjects.add(id);
      projects.push({ ...cloneJson(project), id });
    }
    const seenEvents = /* @__PURE__ */ new Set();
    const events = [];
    for (const event of Array.isArray(base.events) ? base.events : []) {
      const normalized = normalizeEvent(event);
      if (!normalized || seenEvents.has(normalized.eventKey)) continue;
      seenEvents.add(normalized.eventKey);
      events.push(normalized);
    }
    return {
      version: STATE_VERSION,
      revision: Number.isFinite(Number(base.revision)) ? Number(base.revision) : 0,
      tasks,
      projects,
      events: events.slice(-1e3),
      fileMetadata: normalizeFileMetadata(base.fileMetadata),
      dirtyFiles: normalizeUniqueStrings(base.dirtyFiles),
      dirtyTaskIds: normalizeUniqueStrings(base.dirtyTaskIds),
      reconcileCursor: Math.max(0, Number.isFinite(Number(base.reconcileCursor)) ? Number(base.reconcileCursor) : 0),
      tombstones: normalizeTombstones(base.tombstones),
      lastWriterDevice: base.lastWriterDevice ? String(base.lastWriterDevice) : null,
      lastWriteReason: base.lastWriteReason ? String(base.lastWriteReason) : null,
      lastWriteAt: base.lastWriteAt || null,
      updatedAt: base.updatedAt || null
    };
  };
  const stateFromSettings = (settings) => {
    const taskData = settings && settings.todoistTasksData || {};
    return normalizeState({
      tasks: Array.isArray(taskData.tasks) ? taskData.tasks : [],
      projects: Array.isArray(taskData.projects) ? taskData.projects : [],
      events: Array.isArray(taskData.events) ? taskData.events : [],
      fileMetadata: settings && settings.fileMetadata || {}
    });
  };
  const settingsWithoutRuntimeState = (settings) => {
    const output = {};
    for (const [key, value] of Object.entries(settings || {})) {
      if (RUNTIME_SETTING_KEYS.has(key)) continue;
      if (!PERSISTED_SETTING_KEYS.has(key)) continue;
      output[key] = cloneJson(value);
    }
    return output;
  };
  const stateToLegacyRuntime = (state) => {
    const normalized = normalizeState(state);
    return {
      todoistTasksData: { tasks: cloneJson(normalized.tasks), projects: cloneJson(normalized.projects), events: cloneJson(normalized.events) },
      fileMetadata: cloneJson(normalized.fileMetadata),
      statistics: {}
    };
  };
  const fileMetadataFromTasks = (tasks) => {
    const fileMetadata = {};
    for (const task of Array.isArray(tasks) ? tasks : []) {
      const normalized = normalizeTaskForStore(task);
      if (!normalized) continue;
      if (!fileMetadata[normalized.path]) fileMetadata[normalized.path] = { todoistTasks: [], todoistCount: 0 };
      if (!fileMetadata[normalized.path].todoistTasks.includes(normalized.id)) fileMetadata[normalized.path].todoistTasks.push(normalized.id);
      fileMetadata[normalized.path].todoistCount = fileMetadata[normalized.path].todoistTasks.length;
    }
    return fileMetadata;
  };
  const buildStateFromOpenRecords = (openRecords, previousState = emptyState()) => {
    const tasks = [];
    const seen = /* @__PURE__ */ new Set();
    const fileMetadata = {};
    for (const record of Array.isArray(openRecords) ? openRecords : []) {
      const task = record && record.task ? record.task : record;
      const taskPath = normalizePathValue(record && record.path || task && task.path);
      const normalized = normalizeTaskForStore({ ...(task || {}), path: taskPath });
      if (!normalized || seen.has(normalized.id)) continue;
      seen.add(normalized.id);
      tasks.push(normalized);
      if (!fileMetadata[normalized.path]) fileMetadata[normalized.path] = { todoistTasks: [], todoistCount: 0 };
      fileMetadata[normalized.path].todoistTasks.push(normalized.id);
      fileMetadata[normalized.path].todoistCount = fileMetadata[normalized.path].todoistTasks.length;
    }
    const previous = normalizeState(previousState);
    const openIds = new Set(tasks.map((task) => task.id));
    return normalizeState({ ...previous, tasks, fileMetadata, dirtyFiles: previous.dirtyFiles, dirtyTaskIds: previous.dirtyTaskIds, reconcileCursor: previous.reconcileCursor, tombstones: previous.tombstones.filter((tombstone) => !openIds.has(tombstone.taskId)) });
  };
  const mergeRuntimeStates = ({ loaded, current, local }) => {
    const loadedState = normalizeState(loaded);
    const currentState = normalizeState(current);
    const localState = normalizeState(local);
    const loadedIds = new Set(loadedState.tasks.map((task) => task.id));
    const currentIds = new Set(currentState.tasks.map((task) => task.id));
    const localById = new Map(localState.tasks.map((task) => [task.id, task]));
    const mergedById = new Map();
    for (const currentTask of currentState.tasks) mergedById.set(currentTask.id, currentTask);
    for (const localTask of localState.tasks) {
      const existedWhenLoaded = loadedIds.has(localTask.id);
      const stillExistsInCurrent = currentIds.has(localTask.id);
      if (existedWhenLoaded && !stillExistsInCurrent) continue;
      mergedById.set(localTask.id, localTask);
    }
    for (const loadedTask of loadedState.tasks) {
      if (!localById.has(loadedTask.id)) mergedById.delete(loadedTask.id);
    }
    const eventsByKey = new Map();
    for (const event of currentState.events) eventsByKey.set(event.eventKey, event);
    for (const event of localState.events) eventsByKey.set(event.eventKey, event);
    const tasks = Array.from(mergedById.values());
    const tombstones = normalizeTombstones([...currentState.tombstones, ...localState.tombstones]);
    const tombstoneIds = new Set(tombstones.map((tombstone) => tombstone.taskId));
    const activeTasks = tasks.filter((task) => !tombstoneIds.has(task.id));
    return normalizeState({ ...currentState, tasks: activeTasks, events: Array.from(eventsByKey.values()).slice(-1e3), fileMetadata: fileMetadataFromTasks(activeTasks), dirtyFiles: normalizeUniqueStrings([...currentState.dirtyFiles, ...localState.dirtyFiles]), dirtyTaskIds: normalizeUniqueStrings([...currentState.dirtyTaskIds, ...localState.dirtyTaskIds]), reconcileCursor: Math.max(currentState.reconcileCursor || 0, localState.reconcileCursor || 0), tombstones });
  };
  const addRuntimeTombstone = (state, tombstone) => {
    const normalized = normalizeState(state);
    const entry = normalizeTombstones([{ taskId: tombstone && tombstone.taskId, path: tombstone && tombstone.path, reason: tombstone && tombstone.reason, source: tombstone && tombstone.source, createdAt: tombstone && tombstone.createdAt }])[0];
    if (!entry) return normalized;
    const tombstones = normalizeTombstones([...normalized.tombstones.filter((item) => item.taskId !== entry.taskId), entry]);
    const tasks = normalized.tasks.filter((task) => task.id !== entry.taskId);
    return normalizeState({ ...normalized, tasks, fileMetadata: fileMetadataFromTasks(tasks), tombstones });
  };
  const isEmptyRuntimeState = (state) => {
    const normalized = normalizeState(state);
    return normalized.tasks.length === 0 && normalized.projects.length === 0 && normalized.events.length === 0 && Object.keys(normalized.fileMetadata).length === 0 && normalized.dirtyFiles.length === 0 && normalized.dirtyTaskIds.length === 0 && normalized.tombstones.length === 0;
  };
  const compareStateFreshness = (left, right) => {
    const a = normalizeState(left);
    const b = normalizeState(right);
    if (a.revision !== b.revision) return a.revision > b.revision ? 1 : -1;
    const aTime = Date.parse(a.updatedAt || a.lastWriteAt || "");
    const bTime = Date.parse(b.updatedAt || b.lastWriteAt || "");
    if (Number.isFinite(aTime) && Number.isFinite(bTime) && aTime !== bTime) return aTime > bTime ? 1 : -1;
    return 0;
  };
  const resolvePersistentStates = (primaryState, mirrorState) => {
    const primary = primaryState ? normalizeState(primaryState) : null;
    const mirror = mirrorState ? normalizeState(mirrorState) : null;
    if (!primary || isEmptyRuntimeState(primary)) return mirror || emptyState();
    if (!mirror || isEmptyRuntimeState(mirror)) return primary;
    const comparison = compareStateFreshness(primary, mirror);
    if (comparison > 0) return primary;
    if (comparison < 0) return mirror;
    return normalizeState({ ...mergeRuntimeStates({ loaded: emptyState(), current: primary, local: mirror }), revision: Math.max(primary.revision, mirror.revision), updatedAt: primary.updatedAt || mirror.updatedAt, lastWriterDevice: primary.lastWriterDevice || mirror.lastWriterDevice, lastWriteReason: primary.lastWriteReason || mirror.lastWriteReason, lastWriteAt: primary.lastWriteAt || mirror.lastWriteAt });
  };
  class AdapterBridgeStateStore {
    constructor({ adapter, statePath = ADAPTER_STATE_PATH, writerDevice = "mobile-adapter" } = {}) {
      if (!adapter || typeof adapter.read !== "function" || typeof adapter.write !== "function") throw new Error("AdapterBridgeStateStore requires an Obsidian adapter with read/write");
      this.adapter = adapter;
      this.statePath = normalizePathValue(statePath || ADAPTER_STATE_PATH);
      this.backend = "adapter";
      this.writerDevice = writerDevice;
    }
    async ensureParentDirectory() {
      if (typeof this.adapter.mkdir !== "function") return;
      const parts = this.statePath.split("/").slice(0, -1);
      let current = "";
      for (const part of parts) {
        current = current ? joinPath(current, part) : part;
        try {
          if (typeof this.adapter.exists === "function" && await this.adapter.exists(current)) continue;
          await this.adapter.mkdir(current);
        } catch {
        }
      }
    }
    async readState() {
      try {
        if (typeof this.adapter.exists === "function" && !await this.adapter.exists(this.statePath)) return emptyState();
        return normalizeState(JSON.parse(await this.adapter.read(this.statePath)));
      } catch {
        return emptyState();
      }
    }
    async writeState(state, options = {}) {
      const current = await this.readState();
      const now = new Date().toISOString();
      const next = normalizeState({ ...state, revision: current.revision + 1, updatedAt: now, lastWriterDevice: options.writerDevice || this.writerDevice, lastWriteReason: options.reason || state.lastWriteReason || "runtime-save", lastWriteAt: now });
      await this.ensureParentDirectory();
      await this.adapter.write(this.statePath, JSON.stringify(next, null, 2));
      return next;
    }
    async snapshot() {
      return { backend: this.backend, ...cloneJson(await this.readState()) };
    }
    async migrateFromSettings(settings) {
      await this.writeState(stateFromSettings(settings), { reason: "migrate-legacy-settings" });
      return this.snapshot();
    }
    async rebuildFromOpenRecords(openRecords) {
      await this.writeState(buildStateFromOpenRecords(openRecords, await this.readState()), { reason: "rebuild-open-records" });
      return this.snapshot();
    }
    async replaceState(state, options = {}) {
      await this.writeState(state, options);
      return this.snapshot();
    }
  }
  return { AdapterBridgeStateStore, ADAPTER_STATE_PATH, addRuntimeTombstone, buildStateFromOpenRecords, mergeRuntimeStates, resolvePersistentStates, settingsWithoutRuntimeState, stateFromSettings, stateToLegacyRuntime };
})();
var import_repair_core = (() => {
  const TODOIST_TAG_RE = /(^|\s)#todoist\b/i;
  const TODOIST_ID_RE = /(?:(?:%%\s*)|(?:<!--\s*)|(?:<span class="todoist-bridge">\s*))?\[todoist_id::\s*([A-Za-z0-9_-]+)\](?:(?:\s*%%)|(?:\s*-->)|(?:\s*<\/span>))?/i;
  const TODOIST_LINK_RE = /\[link\]\((https?:\/\/[^)\s]+)\)/i;
  const TASK_RE = /^(\s*[-*]\s*)\[(x|X| )\](\s*)/;
  const getTodoistIdFromLine = (line) => {
    const match = TODOIST_ID_RE.exec(line || "");
    return match ? match[1] : null;
  };
  const getTodoistLinkTaskId = (line) => {
    const match = TODOIST_LINK_RE.exec(line || "");
    if (!match) return null;
    const taskMatch = /\/task\/([A-Za-z0-9_-]+)/.exec(match[1]);
    return taskMatch ? taskMatch[1] : null;
  };
  const isLegacyNumericTodoistId = (taskId) => /^[0-9]+$/.test(String(taskId || "").trim());
  const isUncheckedTodoistTaskLine = (line) => {
    const match = TASK_RE.exec(line || "");
    return !!match && match[2] === " " && TODOIST_TAG_RE.test(line || "") && !!getTodoistIdFromLine(line);
  };
  const isCheckedTodoistTaskLine = (line) => {
    const match = TASK_RE.exec(line || "");
    return !!match && /x/i.test(match[2]) && TODOIST_TAG_RE.test(line || "") && !!getTodoistIdFromLine(line);
  };
  const normalizeCompletionDate = (value) => {
    const parsed = value ? new Date(value) : new Date();
    const date = Number.isNaN(parsed.getTime()) ? new Date() : parsed;
    return { iso: date.toISOString(), displayDate: date.toISOString().slice(0, 10) };
  };
  const normalizeCompletionLine = (line, taskId, completedAt, metadataKey = "todoist_completion") => {
    const timestamp = normalizeCompletionDate(completedAt);
    let next = String(line || "");
    if (TASK_RE.test(next)) next = next.replace(TASK_RE, `$1[x]$3`);
    const escapedKey = metadataKey.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    const completionTokenPattern = new RegExp(`\\[${escapedKey}::\\s*[^\\]]+\\]`, "gi");
    const legacyTokenPattern = /\[completed::\s*[^\]]+\]/gi;
    const metadataToken = `[${metadataKey}:: ${timestamp.displayDate}]`;
    const spanPattern = /<span class="todoist-bridge">([\s\S]*?)<\/span>/i;
    const ensureMetadata = (metadata) => {
      let result = String(metadata || "").replace(/\s+/g, " ").trim();
      result = result.replace(legacyTokenPattern, "").replace(completionTokenPattern, "").trim();
      if (!/\[todoist_id::\s*[^\]]+\]/i.test(result)) result = `[todoist_id:: ${taskId}] ${result}`.trim();
      return `${result.replace(/\s+/g, " ").trim()} ${metadataToken}`.replace(/\s+/g, " ").trim() + " ";
    };
    if (spanPattern.test(next)) {
      next = next.replace(spanPattern, (_match, metadata) => `<span class="todoist-bridge">${ensureMetadata(metadata)}</span>`);
    } else {
      next = `${next.trimEnd()} <span class="todoist-bridge">${ensureMetadata("")}</span>`;
    }
    next = next.replace(legacyTokenPattern, " ");
    next = next.replace(new RegExp(`(^|\\s)(?:completed|${escapedKey})::\\s*[\\w:-]+`, "gi"), "$1");
    return next.replace(/\s{2,}/g, " ").trimEnd();
  };
  const isRemoteCompleted = (remoteTask) => !!remoteTask && (remoteTask.checked === true || remoteTask.isCompleted === true || remoteTask.completed === true);
  const isRemoteOpen = (remoteTask) => !!remoteTask && !remoteTask.__missing && !remoteTask.__error && !isRemoteCompleted(remoteTask);
  const normalizeBridgePathForCompare = (filepath) => String(filepath || "").trim().split("\\").join("/").normalize("NFC");
  const bridgePathsEqual = (left, right) => normalizeBridgePathForCompare(left) === normalizeBridgePathForCompare(right);
  const classifyBridgeLine = ({ line, path: filepath, lineNumber, remoteTask, cachedTask }) => {
    const taskId = getTodoistIdFromLine(line);
    const linkTaskId = getTodoistLinkTaskId(line);
    const legacyNumeric = isLegacyNumericTodoistId(taskId);
    const issues = [];
    if (!taskId) issues.push("malformed id");
    if (taskId && linkTaskId && linkTaskId !== taskId) issues.push(`URL/id mismatch (${linkTaskId})`);
    if (cachedTask && cachedTask.path && !bridgePathsEqual(cachedTask.path, filepath)) issues.push(`stale cached path (${cachedTask.path})`);
    if (legacyNumeric) {
      issues.push("legacy numeric todoist_id is local-only under Todoist API v1");
    } else if (remoteTask && remoteTask.__missing) {
      issues.push("missing in Todoist");
    }
    if (!legacyNumeric && remoteTask && remoteTask.__error) issues.push(`Todoist fetch error: ${remoteTask.__error}`);
    return { taskId, path: filepath, lineNumber, checked: isCheckedTodoistTaskLine(line), legacyNumeric, shouldComplete: !legacyNumeric && isUncheckedTodoistTaskLine(line) && isRemoteCompleted(remoteTask), issues };
  };
  const normalizeRemoteOpenTask = (remoteTask, candidate, cachedTask) => ({
    ...(cachedTask || {}),
    ...remoteTask,
    id: String(remoteTask.id || candidate.taskId),
    content: remoteTask.content != null ? String(remoteTask.content) : cachedTask && cachedTask.content || "",
    path: candidate.path,
    isCompleted: false,
    url: remoteTask.url || `https://app.todoist.com/app/task/${candidate.taskId}`,
    origin: remoteTask.origin || (cachedTask && cachedTask.origin) || "todoist"
  });
  return { bridgePathsEqual, classifyBridgeLine, getTodoistIdFromLine, getTodoistLinkTaskId, isLegacyNumericTodoistId, isRemoteOpen, isUncheckedTodoistTaskLine, normalizeCompletionLine, normalizeRemoteOpenTask };
})();
var import_sync_core = (() => {
  const BRIDGE_LABELS = /* @__PURE__ */ new Set(["obsidian", "todoist", "task"]);
  const normalizeString = (value) => value == null ? "" : String(value).trim();
  const normalizePathValue = (value) => normalizeString(value).split("\\").join("/");
  const escapeRegExp = (value) => String(value).replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  const uniqueStrings = (values) => {
    const seen = /* @__PURE__ */ new Set();
    const output = [];
    for (const value of Array.isArray(values) ? values : []) {
      const normalized = normalizePathValue(value);
      if (!normalized || seen.has(normalized)) continue;
      seen.add(normalized);
      output.push(normalized);
    }
    return output;
  };
  const hasTodoistTag = (line) => /(^|\s)#todoist\b/i.test(String(line || ""));
  const lineContainsTaskId = (line, taskId) => {
    const id = normalizeString(taskId);
    if (!id) return false;
    return new RegExp(`\\b${escapeRegExp(id)}\\b`).test(String(line || ""));
  };
  const removeBridgeLabels = (labels, bridgeLabels = BRIDGE_LABELS) => {
    const control = new Set(Array.from(bridgeLabels || []).map((label) => String(label).toLowerCase()));
    return (Array.isArray(labels) ? labels : []).filter((label) => {
      const normalized = normalizeString(label).toLowerCase();
      return normalized && !control.has(normalized);
    });
  };
  const buildObsidianDetachPlan = ({ path: filepath, content, trackedTaskIds, cachedTasksById }) => {
    const lines = String(content || "").replace(/^---[\s\S]*?---\n/, "").split("\n");
    const plan = { path: normalizePathValue(filepath), detach: [], keep: [] };
    for (const rawTaskId of uniqueStrings(trackedTaskIds)) {
      const taskId = String(rawTaskId);
      const cachedTask = cachedTasksById && typeof cachedTasksById.get === "function" ? cachedTasksById.get(taskId) || null : cachedTasksById && cachedTasksById[taskId] || null;
      let location = null;
      for (let index = 0; index < lines.length; index++) {
        const line = lines[index];
        if (lineContainsTaskId(line, taskId)) {
          location = { line, lineIndex: index, lineNumber: index + 1 };
          break;
        }
      }
      if (!location) {
        plan.detach.push({ taskId, path: plan.path, cachedTask, reason: "missing-line" });
        continue;
      }
      if (!hasTodoistTag(location.line)) {
        plan.detach.push({ taskId, path: plan.path, cachedTask, reason: "todoist-tag-removed", ...location });
        continue;
      }
      plan.keep.push({ taskId, path: plan.path, cachedTask, ...location, parsedTaskId: import_repair_core.getTodoistIdFromLine(location.line) });
    }
    return plan;
  };
  const selectDirtyFilesForSync = ({ dirtyFiles, activeFile, maxFiles = 25 }) => {
    const limit = Math.max(0, Number.isFinite(Number(maxFiles)) ? Number(maxFiles) : 25);
    const ordered = uniqueStrings([activeFile, ...uniqueStrings(dirtyFiles)]);
    const files = ordered.slice(0, limit);
    const selected = new Set(files);
    const remainingDirtyFiles = uniqueStrings(dirtyFiles).filter((file) => !selected.has(file));
    return { files, remainingDirtyFiles };
  };
  const selectRollingReconciliationBatch = ({ tasks, cursor = 0, limit = 25 }) => {
    const list = (Array.isArray(tasks) ? tasks : []).filter((task) => task && task.id != null);
    if (!list.length) return { tasks: [], nextCursor: 0 };
    const batchLimit = Math.max(0, Math.min(Number.isFinite(Number(limit)) ? Number(limit) : 25, list.length));
    const start = Math.max(0, Number.isFinite(Number(cursor)) ? Number(cursor) : 0) % list.length;
    const selected = [];
    for (let offset = 0; offset < batchLimit; offset++) selected.push(list[(start + offset) % list.length]);
    return { tasks: selected, nextCursor: (start + batchLimit) % list.length };
  };
  return { buildObsidianDetachPlan, removeBridgeLabels, selectDirtyFilesForSync, selectRollingReconciliationBatch };
})();
// src/settings.ts
var import_obsidian = require("obsidian");
var todoistBridgeDebugLoggingEnabled = false;
function setTodoistBridgeDebugLoggingEnabled(value) {
  todoistBridgeDebugLoggingEnabled = value === true;
}
function todoistBridgeDebugLog(...args) {
  if (todoistBridgeDebugLoggingEnabled) {
    globalThis.console["log"](...args);
  }
}
var DEFAULT_SETTINGS = {
  todoistAPISecretName: "",
  defaultProjectId: "",
  automaticSynchronizationInterval: 300,
  automaticSynchronizationEnabled: true,
  disableTodoistInboundSync: false,
  debugMode: false,
  todoistTasksData: { "projects": [], "tasks": [], "events": [] },
  fileMetadata: {},
  statistics: {}
};
var TodoistBridgeSettingTab = class extends import_obsidian.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
  }
  display() {
    var _a, _b;
    const { containerEl } = this;
    containerEl.empty();
    containerEl.createEl("h2", { text: "Todoist Bridge" });
    const myProjectsOptions = ((_b = (_a = this.plugin.settings.todoistTasksData) == null ? void 0 : _a.projects) == null ? void 0 : _b.reduce((obj, item) => {
      obj[item.id.toString()] = item.name;
      return obj;
    }, {})) || {};
    new import_obsidian.Setting(containerEl).setName("Todoist API token").setDesc("Select a Todoist API token from Obsidian SecretStorage. The token value is not saved in plugin data.").addComponent(
      (el) => new import_obsidian.SecretComponent(this.app, el).setValue(this.plugin.settings.todoistAPISecretName || "").onChange(async (value) => {
        this.plugin.settings.todoistAPISecretName = value;
        this.plugin.settings.apiInitialized = false;
        await this.plugin.saveSettings();
      })
    ).addExtraButton((button) => {
      button.setIcon("send").onClick(async () => {
        await this.plugin.modifyTodoistAPI();
        this.display();
      });
    });
    new import_obsidian.Setting(containerEl).setName("Default project").setDesc("Todoist project used for new Obsidian #todoist tasks. Existing bridged tasks stay where they are.").addDropdown(
      (component) => {
        const currentProjectId = this.plugin.settings.defaultProjectId || Object.keys(myProjectsOptions)[0] || "";
        if (currentProjectId && !myProjectsOptions[currentProjectId]) {
          component.addOption(currentProjectId, currentProjectId);
        }
        component.addOptions(myProjectsOptions).setValue(currentProjectId).onChange((value) => {
          this.plugin.settings.defaultProjectId = value;
          this.plugin.saveSettings();
        });
      }
    );
    new import_obsidian.Setting(containerEl).setName("Automatic sync").setDesc("On: run lightweight sync while Obsidian is open. Off: sync only when you use Sync now. Mobile timers are best-effort.").addToggle(
      (component) => component.setValue(this.plugin.settings.automaticSynchronizationEnabled).onChange((value) => {
        this.plugin.settings.automaticSynchronizationEnabled = value;
        this.plugin.saveSettings();
        this.plugin.updateAutomaticSyncInterval();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Two-way sync").setDesc("On: pull Todoist completions, reopens, label removals, and content updates into Obsidian. Off: Obsidian changes can still push, but Todoist changes are ignored.").addToggle(
      (component) => component.setValue(this.plugin.settings.disableTodoistInboundSync !== true).onChange((value) => {
        this.plugin.settings.disableTodoistInboundSync = !value;
        this.plugin.saveSettings();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Sync interval").setDesc("Seconds between automatic sync runs. Minimum 20; mobile background timers are best-effort.").addText(
      (text) => text.setPlaceholder("Sync interval").setValue(this.plugin.settings.automaticSynchronizationInterval.toString()).onChange(async (value) => {
        const intervalNum = Number(value);
        if (isNaN(intervalNum)) {
          return;
        }
        if (intervalNum < 20) {
          return;
        }
        if (!Number.isInteger(intervalNum)) {
          return;
        }
        this.plugin.settings.automaticSynchronizationInterval = intervalNum;
        this.plugin.saveSettings();
        this.plugin.updateAutomaticSyncInterval();
      })
    );
    new import_obsidian.Setting(containerEl).setName("Debug mode").setDesc("On: write detailed sync and diagnostics messages to the developer console. Off: keep logging minimal.").addToggle(
      (component) => component.setValue(this.plugin.settings.debugMode).onChange((value) => {
        this.plugin.settings.debugMode = value;
        setTodoistBridgeDebugLoggingEnabled(value);
        this.plugin.saveSettings();
      })
    );
    const runMaintenanceAction = async (label, action, confirmMessage = "") => {
      try {
        if (confirmMessage && !confirm(confirmMessage)) {
          return;
        }
        await action();
      } catch (error) {
        console.error(`Todoist Bridge: ${label} failed`, error);
        new import_obsidian.Notice(`Todoist Bridge: ${label} failed`);
      }
    };
    containerEl.createEl("h3", { text: "Maintenance" });
    new import_obsidian.Setting(containerEl).setName("Audit Todoist Bridge").setDesc("Dry-run scan. Writes a report only; does not change notes or Todoist.").addButton(
      (button) => button.setButtonText("Run audit").onClick(async () => {
        await runMaintenanceAction("audit", () => this.plugin.runBridgeAudit());
      })
    );
    new import_obsidian.Setting(containerEl).setName("Repair Todoist Bridge").setDesc("Applies Todoist-confirmed completions and rebuilds verified open-task state. Review an audit first.").addButton(
      (button) => button.setButtonText("Apply repair").onClick(async () => {
        await runMaintenanceAction("repair", () => this.plugin.runBridgeRepairCommand(), "Repair Todoist Bridge?\n\nThis can update Obsidian completion marks and rebuild bridge state. Run Audit first if you have not reviewed a dry-run report.");
      })
    );
    new import_obsidian.Setting(containerEl).setName("Export Todoist Bridge Diagnostics").setDesc("Writes a sanitized diagnostics report without exposing the Todoist API token.").addButton(
      (button) => button.setButtonText("Export diagnostics").onClick(async () => {
        await runMaintenanceAction("diagnostics export", () => this.plugin.runDiagnosticsExport());
      })
    );
    new import_obsidian.Setting(containerEl).setName("Rebuild Todoist Bridge Cache").setDesc("Rebuilds local bridge state from unchecked #todoist task lines and Todoist verification.").addButton(
      (button) => button.setButtonText("Rebuild cache").onClick(async () => {
        await runMaintenanceAction("cache rebuild", () => this.plugin.runCacheRebuild(), "Rebuild Todoist Bridge Cache?\n\nThis rewrites local bridge state from Markdown and Todoist verification. Use Audit or Diagnostics first if you are unsure.");
      })
    );
    new import_obsidian.Setting(containerEl).setName("Backup Todoist Data").setDesc("Writes a Todoist backup JSON file into the vault.").addButton(
      (button) => button.setButtonText("Create backup").onClick(async () => {
        await runMaintenanceAction("Todoist backup", async () => {
          if (!this.plugin.settings.apiInitialized) {
            new import_obsidian.Notice("Todoist Bridge: API not initialized");
            return;
          }
          await this.plugin.todoistSync.backupTodoistAllResources();
        }, "Backup Todoist Data?\n\nThis writes a Todoist backup JSON file into the vault.");
      })
    );
  }
};
// src/todoistRestAPI.ts
var import_todoist_api_typescript = __toESM(require_dist10());
function localDateStringToUTCDatetimeString(localDateString) {
  try {
    if (localDateString === null) {
      return null;
    }
    localDateString = localDateString + "T08:00";
    let localDateObj = new Date(localDateString);
    let ISOString = localDateObj.toISOString();
    return ISOString;
  } catch (error) {
    console.error(`Error extracting date from string '${localDateString}': ${error}`);
    return null;
  }
}
var TodoistRestAPI = class {
  constructor(app, plugin) {
    this.app = app;
    this.plugin = plugin;
    this.unsupportedLegacyTaskIds = /* @__PURE__ */ new Set();
  }
  initializeAPI() {
    const token = this.plugin.getTodoistAPIToken();
    const api = new import_todoist_api_typescript.TodoistApi(token);
    return api;
  }
  getAuthHeaders() {
    return {
      "Authorization": `Bearer ${this.plugin.getTodoistAPIToken()}`,
      "Content-Type": "application/json"
    };
  }
  normalizeV1Value(value) {
    if (Array.isArray(value)) {
      return value.map((item) => this.normalizeV1Value(item));
    }
    if (value && typeof value === "object") {
      const normalized = {};
      for (const [key, val] of Object.entries(value)) {
        const camelKey = key.replace(/_([a-z])/g, (_match, chr) => chr.toUpperCase());
        normalized[camelKey] = this.normalizeV1Value(val);
      }
      return normalized;
    }
    return value;
  }
  normalizeV1Task(task) {
    const normalized = this.normalizeV1Value(task || {});
    if (typeof normalized.id !== "undefined" && normalized.id !== null) {
      normalized.id = String(normalized.id);
    }
    if (typeof normalized.projectId !== "undefined" && normalized.projectId !== null) {
      normalized.projectId = String(normalized.projectId);
    }
    if (typeof normalized.parentId !== "undefined" && normalized.parentId !== null) {
      normalized.parentId = String(normalized.parentId);
    }
    if (typeof normalized.checked === "boolean" && typeof normalized.isCompleted !== "boolean") {
      normalized.isCompleted = normalized.checked;
    }
    if (!normalized.url && normalized.id) {
      normalized.url = `https://app.todoist.com/app/task/${normalized.id}`;
    }
    return normalized;
  }
  isLegacyNumericTaskId(taskId) {
    return /^[0-9]+$/.test(String(taskId || ""));
  }
  isDeprecatedIdError(error) {
    const message = error && typeof error.message === "string" ? error.message : "";
    return /V1_ID_CANNOT_BE_USED|deprecated and cannot be used|deprecated format/i.test(message);
  }
  logDeprecatedLegacyId(taskId) {
    const id = taskId != null ? String(taskId) : "";
    if (!id) {
      return;
    }
    if (this.unsupportedLegacyTaskIds.has(id)) {
      return;
    }
    this.unsupportedLegacyTaskIds.add(id);
    console.warn(`Todoist Bridge: task ${id} uses a legacy numeric Todoist ID unsupported by API v1. Remote updates will be skipped for this task.`);
  }
  async requestV1(path, method = "GET", body = void 0, query = void 0) {
    const queryString = query && typeof query === "object" && Object.keys(query).length ? `?${new URLSearchParams(query).toString()}` : "";
    const url = `https://api.todoist.com/api/v1/${path}${queryString}`;
    const options = {
      url,
      method,
      headers: this.getAuthHeaders(),
      throw: false
    };
    if (typeof body !== "undefined") {
      options.body = JSON.stringify(body);
    }
    let response;
    try {
      response = await (0, import_obsidian4.requestUrl)(options);
    } catch (error) {
      throw new Error(`Todoist request failed: ${error.message}`);
    }
    const responseText = typeof response.text === "string" ? response.text : "";
    if (response.status < 200 || response.status >= 300) {
      let errorMessage = responseText;
      try {
        const parsed = responseText ? JSON.parse(responseText) : null;
        if (parsed && parsed.error) {
          errorMessage = parsed.error;
        }
      } catch (error) {
      }
      throw new Error(`Todoist API ${method} ${path} failed (${response.status}): ${errorMessage}`);
    }
    if (!responseText) {
      return null;
    }
    try {
      return JSON.parse(responseText);
    } catch (error) {
      return responseText;
    }
  }
  generateSyncCommandUuid() {
    return `${Date.now()}-${Math.random().toString(16).slice(2, 12)}`;
  }
  async requestSyncV1(formBody = {}) {
    const url = "https://api.todoist.com/api/v1/sync";
    const params = new URLSearchParams();
    if (formBody && typeof formBody === "object") {
      for (const [key, value] of Object.entries(formBody)) {
        if (typeof value === "undefined" || value === null) {
          continue;
        }
        params.append(key, String(value));
      }
    }
    const options = {
      url,
      method: "POST",
      headers: {
        "Authorization": `Bearer ${this.plugin.getTodoistAPIToken()}`,
        "Content-Type": "application/x-www-form-urlencoded"
      },
      throw: false
    };
    const body = params.toString();
    if (body.length) {
      options.body = body;
    }
    let response;
    try {
      response = await (0, import_obsidian4.requestUrl)(options);
    } catch (error) {
      throw new Error(`Todoist sync request failed: ${error.message}`);
    }
    const responseText = typeof response.text === "string" ? response.text : "";
    if (response.status < 200 || response.status >= 300) {
      let errorMessage = responseText;
      try {
        const parsed = responseText ? JSON.parse(responseText) : null;
        if (parsed && parsed.error) {
          errorMessage = parsed.error;
        }
      } catch (error) {
      }
      throw new Error(`Todoist Sync API POST sync failed (${response.status}): ${errorMessage}`);
    }
    if (!responseText) {
      return {};
    }
    try {
      return JSON.parse(responseText);
    } catch (error) {
      return {};
    }
  }
  async runSyncCommand(type, args = {}) {
    const uuid = this.generateSyncCommandUuid();
    const payload = [{ type, uuid, args }];
    const data = await this.requestSyncV1({ commands: JSON.stringify(payload) });
    const status = data && data.sync_status && Object.prototype.hasOwnProperty.call(data.sync_status, uuid) ? data.sync_status[uuid] : "ok";
    if (status !== "ok") {
      const statusText = typeof status === "string" ? status : JSON.stringify(status);
      throw new Error(`Todoist sync command '${type}' failed: ${statusText}`);
    }
    return data;
  }
  async AddTask({ projectId, content, parentId = null, dueDate, dueDatetime, labels, description, priority }) {
    try {
      const payload = {
        content: content || ""
      };
      if (Array.isArray(labels) && labels.length) {
        payload.labels = labels;
      }
      if (description) {
        payload.description = description;
      }
      if (priority) {
        payload.priority = priority;
      }
      if (projectId) {
        payload.project_id = projectId.toString();
      }
      if (parentId) {
        payload.parent_id = parentId.toString();
      }
      if (dueDate) {
        payload.due_date = dueDate;
      }
      if (dueDatetime) {
        payload.due_datetime = dueDatetime;
      }
      const newTask = await this.requestV1("tasks", "POST", payload);
      return this.normalizeV1Task(newTask);
    } catch (error) {
      throw new Error(`Error adding task: ${error.message}`);
    }
  }
  //options:{ projectId?: string, section_id?: string, label?: string , filter?: string,lang?: string, ids?: Array<string>}
  async GetActiveTasks(options) {
    try {
      const query = {};
      if (options && typeof options === "object") {
        if (options.projectId) {
          query.project_id = options.projectId.toString();
        }
        if (options.sectionId) {
          query.section_id = options.sectionId.toString();
        }
        if (options.label) {
          query.label = options.label;
        }
        if (options.filter) {
          query.filter = options.filter;
        }
        if (options.lang) {
          query.lang = options.lang;
        }
        if (options.ids) {
          query.ids = Array.isArray(options.ids) ? options.ids.join(",") : String(options.ids);
        }
      }
      const result = await this.requestV1("tasks", "GET", void 0, query);
      const taskArray = Array.isArray(result == null ? void 0 : result.results) ? result.results : Array.isArray(result) ? result : [];
      return taskArray.map((task) => this.normalizeV1Task(task));
    } catch (error) {
      throw new Error(`Error get active tasks: ${error.message}`);
    }
  }
  //Also note that to remove the due date of a task completely, you should set the due_string parameter to no date or no due date.
  // API does not provide a function to update the task project ID
  async UpdateTask(taskId, updates) {
    if (!taskId) {
      throw new Error("taskId is required");
    }
    if (!updates.content && !updates.description && !updates.dueDate && !updates.dueDatetime && !updates.dueString && !updates.labels && !updates.parentId && !updates.priority) {
      throw new Error("At least one update is required");
    }
    try {
      const taskIdText = taskId.toString();
      if (this.isLegacyNumericTaskId(taskIdText)) {
        if (this.unsupportedLegacyTaskIds.has(taskIdText)) {
          return this.normalizeV1Task({ id: taskIdText, ...updates });
        }
        const args = { id: Number(taskIdText) };
        if (updates.content) {
          args.content = updates.content;
        }
        if (typeof updates.description === "string") {
          args.description = updates.description;
        }
        if (Array.isArray(updates.labels)) {
          args.labels = updates.labels;
        }
        if (updates.parentId) {
          args.parent_id = updates.parentId.toString();
        }
        if (updates.priority) {
          args.priority = updates.priority;
        }
        if (updates.dueString) {
          args.due_string = updates.dueString;
        }
        try {
          await this.runSyncCommand("item_update", args);
        } catch (error) {
          if (this.isDeprecatedIdError(error)) {
            this.logDeprecatedLegacyId(taskIdText);
            return this.normalizeV1Task({ id: taskIdText, ...updates });
          }
          throw error;
        }
        return this.normalizeV1Task({ id: taskIdText, ...updates });
      }
      const payload = {};
      const encodedTaskId = encodeURIComponent(taskIdText);
      if (updates.content) {
        payload.content = updates.content;
      }
      if (typeof updates.description === "string") {
        payload.description = updates.description;
      }
      if (Array.isArray(updates.labels)) {
        payload.labels = updates.labels;
      }
      if (updates.parentId) {
        payload.parent_id = updates.parentId.toString();
      }
      if (updates.priority) {
        payload.priority = updates.priority;
      }
      if (updates.dueDate) {
        payload.due_date = updates.dueDate;
      }
      if (updates.dueDatetime) {
        payload.due_datetime = updates.dueDatetime;
      }
      if (updates.dueString) {
        payload.due_string = updates.dueString;
      }
      const updatedTask = await this.requestV1(`tasks/${encodedTaskId}`, "POST", payload);
      return this.normalizeV1Task(updatedTask);
    } catch (error) {
      throw new Error(`Error updating task: ${error.message}`);
    }
  }
  async RemoveBridgeLabels(taskId, knownLabels) {
    if (!taskId) {
      throw new Error("taskId is required");
    }
    let labels = Array.isArray(knownLabels) ? knownLabels : null;
    if (!labels) {
      const task = await this.getTaskById(taskId);
      labels = Array.isArray(task == null ? void 0 : task.labels) ? task.labels : [];
    }
    const nextLabels = import_sync_core.removeBridgeLabels(labels);
    const sameLabels = labels.length === nextLabels.length && labels.every((label, index) => String(label) === String(nextLabels[index]));
    if (sameLabels) {
      return { id: String(taskId), labels: nextLabels };
    }
    return await this.UpdateTask(taskId, { labels: nextLabels });
  }
  //open a task
  async OpenTask(taskId) {
    try {
      const taskIdText = taskId.toString();
      if (this.isLegacyNumericTaskId(taskIdText)) {
        if (this.unsupportedLegacyTaskIds.has(taskIdText)) {
          return false;
        }
        try {
          await this.runSyncCommand("item_uncomplete", { id: Number(taskIdText) });
        } catch (error) {
          if (this.isDeprecatedIdError(error)) {
            this.logDeprecatedLegacyId(taskIdText);
            return false;
          }
          throw error;
        }
      } else {
        const encodedTaskId = encodeURIComponent(taskIdText);
        await this.requestV1(`tasks/${encodedTaskId}/reopen`, "POST");
      }
      todoistBridgeDebugLog(`Task ${taskId} is reopend`);
      return true;
    } catch (error) {
      console.error("Error open a  task:", error);
      return;
    }
  }
  // Close a task in Todoist API
  async CloseTask(taskId) {
    try {
      const taskIdText = taskId.toString();
      if (this.isLegacyNumericTaskId(taskIdText)) {
        if (this.unsupportedLegacyTaskIds.has(taskIdText)) {
          return false;
        }
        try {
          await this.runSyncCommand("item_close", { id: Number(taskIdText) });
        } catch (error) {
          if (this.isDeprecatedIdError(error)) {
            this.logDeprecatedLegacyId(taskIdText);
            return false;
          }
          throw error;
        }
      } else {
        const encodedTaskId = encodeURIComponent(taskIdText);
        await this.requestV1(`tasks/${encodedTaskId}/close`, "POST");
      }
      todoistBridgeDebugLog(`Task ${taskId} is closed`);
      return true;
    } catch (error) {
      console.error("Error closing task:", error);
      throw error;
    }
  }
  async DeleteTask(taskId) {
    try {
      const taskIdText = taskId.toString();
      if (this.isLegacyNumericTaskId(taskIdText)) {
        if (this.unsupportedLegacyTaskIds.has(taskIdText)) {
          return false;
        }
        try {
          await this.runSyncCommand("item_delete", { id: Number(taskIdText) });
        } catch (error) {
          if (this.isDeprecatedIdError(error)) {
            this.logDeprecatedLegacyId(taskIdText);
            return false;
          }
          throw error;
        }
      } else {
        const encodedTaskId = encodeURIComponent(taskIdText);
        await this.requestV1(`tasks/${encodedTaskId}`, "DELETE");
      }
      return true;
    } catch (error) {
      console.error("Error deleting task:", error);
      throw error;
    }
  }
  async AddComment(taskId, comment) {
    if (!taskId) {
      throw new Error("taskId is required");
    }
    const content = comment && typeof comment.content === "string" ? comment.content : "";
    if (!content) {
      throw new Error("comment content is required");
    }
    try {
      const taskIdText = taskId.toString();
      if (this.isLegacyNumericTaskId(taskIdText)) {
        if (this.unsupportedLegacyTaskIds.has(taskIdText)) {
          return { taskId: taskIdText, content };
        }
        try {
          await this.runSyncCommand("note_add", {
            item_id: Number(taskIdText),
            content
          });
          return { taskId: taskIdText, content };
        } catch (error) {
          if (this.isDeprecatedIdError(error)) {
            this.logDeprecatedLegacyId(taskIdText);
            return { taskId: taskIdText, content };
          }
          throw error;
        }
      }
      const payload = {
        task_id: taskIdText,
        content
      };
      const result = await this.requestV1("comments", "POST", payload);
      return this.normalizeV1Value(result || {});
    } catch (error) {
      throw new Error(`Error adding comment: ${error.message}`);
    }
  }
  // get a task by Id
  async getTaskById(taskId) {
    if (!taskId) {
      throw new Error("taskId is required");
    }
    try {
      const taskIdText = taskId.toString();
      if (this.isLegacyNumericTaskId(taskIdText)) {
        if (this.unsupportedLegacyTaskIds.has(taskIdText)) {
          return null;
        }
        try {
          const resources = await this.requestSyncV1({
            sync_token: "*",
            resource_types: '["items"]'
          });
          const items = Array.isArray(resources == null ? void 0 : resources.items) ? resources.items : [];
          const legacyTask = items.find((item) => String(item == null ? void 0 : item.id) === taskIdText);
          return legacyTask ? this.normalizeV1Task(legacyTask) : null;
        } catch (error) {
          if (this.isDeprecatedIdError(error)) {
            this.logDeprecatedLegacyId(taskIdText);
            return null;
          }
          throw error;
        }
      }
      const encodedTaskId = encodeURIComponent(taskIdText);
      const task = await this.requestV1(`tasks/${encodedTaskId}`, "GET");
      return this.normalizeV1Task(task);
    } catch (error) {
      throw new Error(`Error retrieving task: ${error.message}`);
    }
  }
  //get a task due by id
  async getTaskDueById(taskId) {
    if (!taskId) {
      throw new Error("taskId is required");
    }
    try {
      const task = await this.getTaskById(taskId);
      const due = task != null && task.due ? task.due : null;
      return due;
    } catch (error) {
      throw new Error(`Error updating task: ${error.message}`);
    }
  }
  //get all projects
  async GetAllProjects() {
    try {
      const result = await this.requestV1("projects", "GET");
      const projects = Array.isArray(result == null ? void 0 : result.results) ? result.results : Array.isArray(result) ? result : [];
      return projects.map((project) => {
        const normalized = this.normalizeV1Value(project || {});
        if (typeof normalized.id !== "undefined" && normalized.id !== null) {
          normalized.id = String(normalized.id);
        }
        return normalized;
      });
    } catch (error) {
      console.error("Error get all projects", error);
      return false;
    }
  }
};
// src/todoistSyncAPI.ts
var TodoistSyncAPI = class {
  constructor(app, plugin) {
    this.app = app;
    this.plugin = plugin;
  }
  async requestApiV1(path, options = {}) {
    const requestOptions = options || {};
    const method = (requestOptions.method || "GET").toString().toUpperCase();
    const query = requestOptions.query && typeof requestOptions.query === "object" ? requestOptions.query : null;
    const formBody = requestOptions.formBody && typeof requestOptions.formBody === "object" ? requestOptions.formBody : null;
    const jsonBody = typeof requestOptions.jsonBody !== "undefined" ? requestOptions.jsonBody : void 0;
    const accessToken = this.plugin.getTodoistAPIToken();
    const queryString = query && Object.keys(query).length ? `?${new URLSearchParams(query).toString()}` : "";
    const url = `https://api.todoist.com/api/v1/${path}${queryString}`;
    const headers = {
      "Authorization": `Bearer ${accessToken}`
    };
    const opts = {
      url,
      method,
      headers,
      throw: false
    };
    if (typeof jsonBody !== "undefined") {
      headers["Content-Type"] = "application/json";
      opts.body = JSON.stringify(jsonBody);
    } else if (formBody && Object.keys(formBody).length) {
      const params = new URLSearchParams();
      for (const [key, value] of Object.entries(formBody)) {
        if (typeof value === "undefined" || value === null) {
          continue;
        }
        params.append(key, String(value));
      }
      const body = params.toString();
      if (body.length) {
        headers["Content-Type"] = "application/x-www-form-urlencoded";
        opts.body = body;
      }
    }
    let response;
    try {
      response = await (0, import_obsidian4.requestUrl)(opts);
    } catch (error) {
      throw new Error(`Todoist API request failed: ${error.message}`);
    }
    const responseText = typeof response.text === "string" ? response.text : "";
    if (response.status < 200 || response.status >= 300) {
      let errorMessage = responseText;
      try {
        const parsed = responseText ? JSON.parse(responseText) : null;
        if (parsed && parsed.error) {
          errorMessage = parsed.error;
        }
      } catch (error) {
      }
      throw new Error(`Todoist API ${method} ${path} failed (${response.status}): ${errorMessage}`);
    }
    if (!responseText) {
      return {};
    }
    try {
      return JSON.parse(responseText);
    } catch (error) {
      throw new Error(`Todoist response parse failed for ${path}: ${error.message}`);
    }
  }
  extractActivityEvents(payload) {
    if (Array.isArray(payload == null ? void 0 : payload.events)) {
      return payload.events;
    }
    if (Array.isArray(payload == null ? void 0 : payload.results)) {
      return payload.results;
    }
    return [];
  }
  //backup todoist
  async getAllResources() {
    try {
      return await this.requestApiV1("sync", {
        method: "POST",
        formBody: {
          sync_token: "*",
          resource_types: '["all"]'
        }
      });
    } catch (error) {
      console.error(error);
      throw new Error("Failed to fetch all resources due to network error");
    }
  }
  //backup todoist
  async getUserResource() {
    try {
      const data = await this.requestApiV1("sync", {
        method: "POST",
        formBody: {
          sync_token: "*",
          resource_types: '["user_plan_limits"]'
        }
      });
      todoistBridgeDebugLog(data);
      return data;
    } catch (error) {
      console.error(error);
      throw new Error("Failed to fetch user resources due to network error");
    }
  }
  //update user timezone
  async updateUserTimezone() {
    const unixTimestampString = Math.floor(Date.now() / 1e3).toString();
    const commands = [
      {
        "type": "user_update",
        "uuid": unixTimestampString,
        "args": { "timezone": "Asia/Shanghai" }
      }
    ];
    try {
      const data = await this.requestApiV1("sync", {
        method: "POST",
        formBody: { commands: JSON.stringify(commands) }
      });
      todoistBridgeDebugLog(data);
      return data;
    } catch (error) {
      console.error(error);
      throw new Error("Failed to fetch user resources due to network error");
    }
  }
  //get activity logs
  //result  {count:number,events:[]}
  async getAllActivityEvents() {
    try {
      const data = await this.requestApiV1("activities", {
        method: "GET",
        query: { limit: "100" }
      });
      return { events: this.extractActivityEvents(data) };
    } catch (error) {
      throw error;
    }
  }
  async getNonObsidianAllActivityEvents() {
    try {
      const allActivity = await this.getAllActivityEvents();
      const allActivityEvents = this.extractActivityEvents(allActivity);
      const filteredArray = allActivityEvents.filter((obj) => {
        var _a, _b;
        const client = (_b = (_a = obj == null ? void 0 : obj.extra_data) == null ? void 0 : _a.client) == null ? void 0 : _b.toString().toLowerCase();
        return !client || !client.includes("obsidian");
      });
      return filteredArray;
    } catch (err) {
      console.error("An error occurred:", err);
      return [];
    }
  }
  filterActivityEvents(events, options) {
    const list = Array.isArray(events) ? events : [];
    const filterOptions = options || {};
    return list.filter(
      (event) => (filterOptions.event_type ? event.event_type === filterOptions.event_type : true) && (filterOptions.object_type ? event.object_type === filterOptions.object_type : true)
    );
  }
  //get completed items activity
  //result  {count:number,events:[]}
  async getCompletedItemsActivity() {
    try {
      return await this.requestApiV1("activities", {
        method: "GET",
        query: {
          object_type: "item",
          event_type: "completed",
          limit: "100"
        }
      });
    } catch (error) {
      console.error(error);
      throw new Error("Failed to fetch completed items due to network error");
    }
  }
  //get uncompleted items activity
  //result  {count:number,events:[]}
  async getUncompletedItemsActivity() {
    try {
      return await this.requestApiV1("activities", {
        method: "GET",
        query: {
          object_type: "item",
          event_type: "uncompleted",
          limit: "100"
        }
      });
    } catch (error) {
      console.error(error);
      throw new Error("Failed to fetch uncompleted items due to network error");
    }
  }
  //get non-obsidian completed event
  async getNonObsidianCompletedItemsActivity() {
    const completedItemsActivity = await this.getCompletedItemsActivity();
    const completedItemsActivityEvents = this.extractActivityEvents(completedItemsActivity);
    const filteredArray = completedItemsActivityEvents.filter((obj) => {
      var _a, _b;
      const client = (_b = (_a = obj == null ? void 0 : obj.extra_data) == null ? void 0 : _a.client) == null ? void 0 : _b.toString().toLowerCase();
      return !client || !client.includes("obsidian");
    });
    return filteredArray;
  }
  //get non-obsidian uncompleted event
  async getNonObsidianUncompletedItemsActivity() {
    const uncompletedItemsActivity = await this.getUncompletedItemsActivity();
    const uncompletedItemsActivityEvents = this.extractActivityEvents(uncompletedItemsActivity);
    const filteredArray = uncompletedItemsActivityEvents.filter((obj) => {
      var _a, _b;
      const client = (_b = (_a = obj == null ? void 0 : obj.extra_data) == null ? void 0 : _a.client) == null ? void 0 : _b.toString().toLowerCase();
      return !client || !client.includes("obsidian");
    });
    return filteredArray;
  }
  //get updated items activity
  //result  {count:number,events:[]}
  async getUpdatedItemsActivity() {
    try {
      return await this.requestApiV1("activities", {
        method: "GET",
        query: {
          object_type: "item",
          event_type: "updated",
          limit: "100"
        }
      });
    } catch (error) {
      console.error(error);
      throw new Error("Failed to fetch updated items due to network error");
    }
  }
  //get non-obsidian updated event
  async getNonObsidianUpdatedItemsActivity() {
    const updatedItemsActivity = await this.getUpdatedItemsActivity();
    const updatedItemsActivityEvents = this.extractActivityEvents(updatedItemsActivity);
    const filteredArray = updatedItemsActivityEvents.filter((obj) => {
      const client = obj.extra_data && obj.extra_data.client ? obj.extra_data.client.toString().toLowerCase() : "";
      return !client || !client.includes("obsidian");
    });
    return filteredArray;
  }
  //get completed items activity
  //result  {count:number,events:[]}
  async getProjectsActivity() {
    try {
      return await this.requestApiV1("activities", {
        method: "GET",
        query: {
          object_type: "project",
          limit: "100"
        }
      });
    } catch (error) {
      console.error(error);
      throw new Error("Failed to fetch projects activities due to network error");
    }
  }
};
// src/taskParser.ts
var keywords = {
  TODOIST_TAG: "#todoist",
  DUE_DATE: "\u{1F5D3}\uFE0F|\u{1F4C5}|\u{1F4C6}|\u{1F5D3}"
};
var REGEX = {
  TODOIST_TAG: new RegExp(`^[\\s]*[-*]\\s+\\[[x ]\\]\\s+[\\s\\S]*${keywords.TODOIST_TAG}[\\s\\S]*$`, "i"),
  // Match [todoist_id:: N], optionally wrapped by %% ... %% or metadata spans
  TODOIST_ID: /(?:(?:%%\s*)|(?:<!--\s*)|(?:<span class="todoist-bridge">\s*))?\[todoist_id::\s*[A-Za-z0-9_-]+\](?:(?:\s*%%)|(?:\s*-->)|(?:\s*<\/span>))?/,
  TODOIST_ID_NUM: /(?:(?:%%\s*)|(?:<!--\s*)|(?:<span class="todoist-bridge">\s*))?\[todoist_id::\s*(.*?)\](?:(?:\s*%%)|(?:\s*-->)|(?:\s*<\/span>))?/,
  TODOIST_LINK: /\[link\]\(.*?\)/,
  DUE_DATE_WITH_EMOJ: new RegExp(`(${keywords.DUE_DATE})\\s?\\d{4}-\\d{2}-\\d{2}`),
  DUE_DATE: new RegExp(`(?:${keywords.DUE_DATE})\\s?(\\d{4}-\\d{2}-\\d{2})`),
  PROJECT_NAME: /\[project::\s*(.*?)\]/,
  TASK_CONTENT: {
    REMOVE_PRIORITY: /\s!!([1-4])\s/,
    REMOVE_TAGS: /(^|\s)(#[a-zA-Z\d\u4e00-\u9fa5-]+)/g,
    REMOVE_SPACE: /^\s+|\s+$/g,
    REMOVE_DATE: new RegExp(`(${keywords.DUE_DATE})\\s?\\d{4}-\\d{2}-\\d{2}`),
    // Remove inline metadata in any of the following forms:
    //   %%[key:: value]%%, <span class="todoist-bridge">[key:: value] -->, <span class="todoist-bridge">[key:: value]</span>, or bare [key:: value]
    REMOVE_INLINE_METADATA: /(%%\s*\[[^\]]+::[^\]]+\]\s*%%|<!--\s*\[[^\]]+::[^\]]+\]\s*-->|<span class="todoist-bridge">\s*\[[^\]]+::[^\]]+\]\s*<\/span>|\[[^\]]+::[^\]]+\])/g,
    REMOVE_CHECKBOX: /^(-|\*)\s+\[(x|X| )\]\s/,
    REMOVE_CHECKBOX_WITH_INDENTATION: /^([ \t]*)?(-|\*)\s+\[(x|X| )\]\s/,
    REMOVE_TODOIST_LINK: /\[link\]\(.*?\)/
  },
  ALL_TAGS: /#[\w\u4e00-\u9fa5-]+/g,
  TASK_CHECKBOX_CHECKED: /- \[(x|X)\] /,
  TASK_INDENTATION: /^(\s{2,}|\t)(-|\*)\s+\[(x|X| )\]/,
  TAB_INDENTATION: /^(\t+)/,
  TASK_PRIORITY: /\s!!([1-4])\s/,
  BLANK_LINE: /^\s*$/,
  TODOIST_EVENT_DATE: /(\d{4})-(\d{2})-(\d{2})/
};
var TaskParser = class {
  constructor(app, plugin) {
    this.app = app;
    this.plugin = plugin;
  }
  //convert line text to a task object
  async convertTextToTodoistTaskObject(lineText, filepath, lineNumber, fileContent) {
    let hasParent = false;
    let parentId = null;
    let parentTaskObject = null;
    let textWithoutIndentation = lineText;
    if (this.getTabIndentation(lineText) > 0) {
      textWithoutIndentation = this.removeTaskIndentation(lineText);
      const lines = fileContent.split("\n");
      for (let i = lineNumber - 1; i >= 0; i--) {
        const line = lines[i];
        if (this.isLineBlank(line)) {
          break;
        }
        if (this.getTabIndentation(line) >= this.getTabIndentation(lineText)) {
          continue;
        }
        if (this.getTabIndentation(line) < this.getTabIndentation(lineText)) {
          if (this.hasTodoistId(line)) {
            parentId = this.getTodoistIdFromLineText(line);
            hasParent = true;
            parentTaskObject = this.plugin.cacheOperation.loadTaskFromCacheyID(parentId);
            break;
          } else {
            break;
          }
        }
      }
    }
    // Disable due date extraction/sync
    const dueDate = "";
    // Extract labels from hashtags, but do not propagate control tags
    let labels = this.getAllTagsFromLineText(textWithoutIndentation) || [];
    labels = labels.filter((l) => {
      const k = (l || "").toString().toLowerCase();
      return k !== "todoist" && k !== "task";
    });
    // Always add a Todoist label to mark origin, without altering the Obsidian line
    if (!labels.map((x) => x.toLowerCase()).includes("obsidian")) {
      labels.push("obsidian");
    }
    let projectId = this.plugin.settings.defaultProjectId;
    if (!projectId) {
      const fallbackProjectId = this.plugin.cacheOperation.getProjectIdByNameFromCache("Inbox");
      if (fallbackProjectId) {
        projectId = fallbackProjectId;
      }
    }
    let projectName = this.plugin.cacheOperation.getProjectNameByIdFromCache(projectId);
    if (hasParent) {
      if (parentTaskObject && parentTaskObject.projectId) {
        projectId = parentTaskObject.projectId;
        projectName = this.plugin.cacheOperation.getProjectNameByIdFromCache(projectId);
      } else {
        hasParent = false;
        parentId = null;
        parentTaskObject = null;
      }
    }
    if (!hasParent) {
      for (const label of labels) {
        const labelName = label.replace(/#/g, "");
        const hasProjectId = this.plugin.cacheOperation.getProjectIdByNameFromCache(labelName);
        if (!hasProjectId) {
          continue;
        }
        projectName = labelName;
        projectId = hasProjectId;
        break;
      }
    }
    if (!projectId) {
      projectId = this.plugin.settings.defaultProjectId;
      if (!projectId) {
        const fallbackProjectId = this.plugin.cacheOperation.getProjectIdByNameFromCache("Inbox");
        if (fallbackProjectId) {
          projectId = fallbackProjectId;
        }
      }
    }
    const content = this.getTaskContentFromLineText(textWithoutIndentation);
    const isCompleted = this.isTaskCheckboxChecked(textWithoutIndentation);
    let description = "";
    const todoist_id = this.getTodoistIdFromLineText(textWithoutIndentation);
    const priority = this.getTaskPriority(textWithoutIndentation);
    if (filepath) {
      let url = encodeURI(`obsidian://open?vault=${this.app.vault.getName()}&file=${filepath}`);
      description = `[${filepath}](${url})`;
    }
    const todoistTask = {
      projectId,
      content: content || "",
      parentId: parentId || null,
      dueDate: dueDate || "",
      labels: labels || [],
      description,
      isCompleted,
      todoist_id: todoist_id || null,
      hasParent,
      priority
    };
    return todoistTask;
  }
  hasTodoistTag(text) {
    return REGEX.TODOIST_TAG.test(text);
  }
  hasTodoistId(text) {
    const result = REGEX.TODOIST_ID.test(text);
    return result;
  }
  hasDueDate(text) {
    return REGEX.DUE_DATE_WITH_EMOJ.test(text);
  }
  getDueDateFromLineText(text) {
    const result = REGEX.DUE_DATE.exec(text);
    return result ? result[1] : null;
  }
  getProjectNameFromLineText(text) {
    const result = REGEX.PROJECT_NAME.exec(text);
    return result ? result[1] : null;
  }
  getTodoistIdFromLineText(text) {
    const result = REGEX.TODOIST_ID_NUM.exec(text);
    return result ? result[1] : null;
  }
  getDueDateFromDataview(dataviewTask) {
    if (!dataviewTask.due) {
      return "";
    } else {
      const dataviewTaskDue = dataviewTask.due.toString().slice(0, 10);
      return dataviewTaskDue;
    }
  }
  /*
  //convert line task to dataview task object
  async  getLineTask(filepath,line){
      //const tasks = this.app.plugins.plugins.dataview.api.pages(`"${filepath}"`).file.tasks
      const tasks = await getAPI(this.app).pages(`"${filepath}"`).file.tasks
      const tasksValues = tasks.values
      //todoistBridgeDebugLog(`dataview filepath is ${filepath}`)
      //todoistBridgeDebugLog(`dataview line is ${line}`)
      //todoistBridgeDebugLog(tasksValues)
      const currentLineTask = tasksValues.find(obj => obj.line === line )
      todoistBridgeDebugLog(currentLineTask)
      return(currentLineTask)

  }
  */
  getTaskContentFromLineText(lineText) {
    let TaskContent = lineText
      .replace(REGEX.TASK_CONTENT.REMOVE_INLINE_METADATA, "")
      .replace(REGEX.TASK_CONTENT.REMOVE_TODOIST_LINK, "")
      // Remove any remaining HTML comments (e.g., <!-- -->)
      .replace(/<!--[\s\S]*?-->/g, "")
      .replace(/<span class="todoist-bridge">[\s\S]*?<\/span>/gi, "")
      .replace(REGEX.TASK_CONTENT.REMOVE_PRIORITY, " ")
      .replace(REGEX.TASK_CONTENT.REMOVE_TAGS, "")
      .replace(/completed::\s*\d{4}-\d{2}-\d{2}/gi, "")
      .replace(REGEX.TASK_CONTENT.REMOVE_DATE, "")
      .replace(REGEX.TASK_CONTENT.REMOVE_CHECKBOX, "")
      .replace(REGEX.TASK_CONTENT.REMOVE_CHECKBOX_WITH_INDENTATION, "")
      .replace(REGEX.TASK_CONTENT.REMOVE_SPACE, "");
    // Remove @todoist and @task control markers from task content
    TaskContent = TaskContent.replace(/(^|\s)@todoist\b/gi, " ").replace(/(^|\s)@task\b/gi, " ").replace(/\s+/g, " ").trim();
    return TaskContent;
  }
  //get all tags from task text
  getAllTagsFromLineText(lineText) {
    let tags = lineText.match(REGEX.ALL_TAGS);
    if (tags) {
      tags = tags.map((tag) => tag.replace("#", ""));
    }
    return tags;
  }
  //get checkbox status
  isTaskCheckboxChecked(lineText) {
    return REGEX.TASK_CHECKBOX_CHECKED.test(lineText);
  }
  //task content compare
  taskContentCompare(lineTask, todoistTask) {
    const lineTaskContent = lineTask.content;
    const todoistTaskContent = todoistTask.content;
    const contentModified = lineTaskContent === todoistTaskContent;
    return contentModified;
  }
  //tag compare
  taskTagCompare(lineTask, todoistTask) {
    const lineTaskTags = lineTask.labels;
    const todoistTaskTags = todoistTask.labels;
    const tagsModified = lineTaskTags.length === todoistTaskTags.length && lineTaskTags.sort().every((val, index) => val === todoistTaskTags.sort()[index]);
    return tagsModified;
  }
  //task status compare
  taskStatusCompare(lineTask, todoistTask) {
    const statusModified = lineTask.isCompleted === todoistTask.isCompleted;
    return statusModified;
  }
  //task due date compare
  async compareTaskDueDate(lineTask, todoistTask) {
    // Date syncing is disabled; always treat due date as unchanged
    return true;
  }
  //task project id compare
  async taskProjectCompare(lineTask, todoistTask) {
    return lineTask.projectId === todoistTask.projectId;
  }
  // Determine whether the task is indented
  isIndentedTask(text) {
    return REGEX.TASK_INDENTATION.test(text);
  }
  // Determine the number of tab characters
  //todoistBridgeDebugLog(getTabIndentation("\t\t- [x] This is a task with two tabs")); // 2
  //todoistBridgeDebugLog(getTabIndentation("  - [x] This is a task without tabs")); // 0
  getTabIndentation(lineText) {
    const match = REGEX.TAB_INDENTATION.exec(lineText);
    return match ? match[1].length : 0;
  }
  //	Task priority from 1 (normal) to 4 (urgent).
  getTaskPriority(lineText) {
    const match = REGEX.TASK_PRIORITY.exec(lineText);
    return match ? Number(match[1]) : 1;
  }
  //remove task indentation
  removeTaskIndentation(text) {
    const regex = /^([ \t]*)?- \[(x| )\] /;
    return text.replace(regex, "- [$2] ");
  }
  // Determine whether the line is blank
  isLineBlank(lineText) {
    return REGEX.BLANK_LINE.test(lineText);
  }
  // Insert a date into linetext
  insertDueDateBeforeTodoist(text, dueDate) {
    const regex = new RegExp(`(${keywords.TODOIST_TAG})`);
    return text.replace(regex, `\u{1F4C5} ${dueDate} $1`);
  }
  //extra date from obsidian event
  // Usage example
  //const str = "2023-03-27T15:59:59.000000Z";
  //const dateStr = ISOStringToLocalDateString(str);
  //todoistBridgeDebugLog(dateStr); // outputs 2023-03-27
  ISOStringToLocalDateString(utcTimeString) {
    try {
      if (utcTimeString === null) {
        return null;
      }
      let utcDateString = utcTimeString;
      let dateObj = new Date(utcDateString);
      let year = dateObj.getFullYear();
      let month = (dateObj.getMonth() + 1).toString().padStart(2, "0");
      let date = dateObj.getDate().toString().padStart(2, "0");
      let localDateString = `${year}-${month}-${date}`;
      return localDateString;
      return localDateString;
    } catch (error) {
      console.error(`Error extracting date from string '${utcTimeString}': ${error}`);
      return null;
    }
  }
  //extra date from obsidian event
  // Usage example
  //const str = "2023-03-27T15:59:59.000000Z";
  //const dateStr = ISOStringToLocalDatetimeString(str);
  //todoistBridgeDebugLog(dateStr); // outputs Mon Mar 27 2023 23:59:59 GMT+0800 (China Standard Time)
  ISOStringToLocalDatetimeString(utcTimeString) {
    try {
      if (utcTimeString === null) {
        return null;
      }
      let utcDateString = utcTimeString;
      let dateObj = new Date(utcDateString);
      let result = dateObj.toString();
      return result;
    } catch (error) {
      console.error(`Error extracting date from string '${utcTimeString}': ${error}`);
      return null;
    }
  }
  //convert date from obsidian event
  // Usage example
  //const str = "2023-03-27";
  //const utcStr = localDateStringToUTCDatetimeString(str);
  //todoistBridgeDebugLog(dateStr); // outputs 2023-03-27T00:00:00.000Z
  localDateStringToUTCDatetimeString(localDateString) {
    try {
      if (localDateString === null) {
        return null;
      }
      localDateString = localDateString + "T08:00";
      let localDateObj = new Date(localDateString);
      let ISOString = localDateObj.toISOString();
      return ISOString;
    } catch (error) {
      console.error(`Error extracting date from string '${localDateString}': ${error}`);
      return null;
    }
  }
  //convert date from obsidian event
  // Usage example
  //const str = "2023-03-27";
  //const utcStr = localDateStringToUTCDateString(str);
  //todoistBridgeDebugLog(dateStr); // outputs 2023-03-27
  localDateStringToUTCDateString(localDateString) {
    try {
      if (localDateString === null) {
        return null;
      }
      localDateString = localDateString + "T08:00";
      let localDateObj = new Date(localDateString);
      let ISOString = localDateObj.toISOString();
      let utcDateString = ISOString.slice(0, 10);
      return utcDateString;
    } catch (error) {
      console.error(`Error extracting date from string '${localDateString}': ${error}`);
      return null;
    }
  }
  isMarkdownTask(str) {
    const taskRegex = /^\s*-\s+\[([x ])\]/;
    return taskRegex.test(str);
  }
  addTodoistTag(str) {
    return str + ` ${keywords.TODOIST_TAG}`;
  }
  getObsidianUrlFromFilepath(filepath) {
    const url = encodeURI(`obsidian://open?vault=${this.app.vault.getName()}&file=${filepath}`);
    const obsidianUrl = `[${filepath}](${url})`;
    return obsidianUrl;
  }
  addTodoistLink(linetext, todoistLink) {
    if (!todoistLink) {
      return linetext;
    }
    let workingText = typeof linetext === "string" ? linetext : "";
    const linkRegex = new RegExp(REGEX.TODOIST_LINK.source, REGEX.TODOIST_LINK.flags);
    if (linkRegex.test(workingText)) {
      workingText = workingText.replace(linkRegex, " ").replace(/\s{2,}/g, " ").trimEnd();
    }
    if (workingText.length === 0) {
      workingText = todoistLink;
    } else {
      workingText = `${workingText} ${todoistLink}`.replace(/\s{2,}/g, " ").trim();
    }
    return this.normalizeTodoistMarkersOrder(workingText);
  }
  normalizeTodoistMarkersOrder(line) {
    if (typeof line !== "string" || line.length === 0) {
      return line;
    }
    const leadingWhitespace = (line.match(/^\s*/) || [""])[0];
    let body = line.trim();
    const anchorRegex = /(<span class="todoist-bridge">[\s\S]*?<\/span>|<!--[\s\S]*?-->|%%[\s\S]*?%%)/i;
    const linkRegex = new RegExp(REGEX.TODOIST_LINK.source, REGEX.TODOIST_LINK.flags);
    const tagRegex = new RegExp(`${keywords.TODOIST_TAG}`, "i");
    const anchorMatch = anchorRegex.exec(body);
    const linkMatch = linkRegex.exec(body);
    if (!anchorMatch && !linkMatch) {
      return line;
    }
    if (anchorMatch) {
      body = body.replace(anchorRegex, " ").trim();
    }
    if (linkMatch) {
      body = body.replace(linkRegex, " ").trim();
    }
    body = body.replace(/\s{2,}/g, " ").trim();
    const normalizeAnchor = (anchor) => anchor.replace(/(<span class="todoist-bridge">)([\s\S]*?)(<\/span>)/i, (_match, open, inner, close) => {
      const cleanedInner = inner.replace(/\[\s*\]/g, " ").replace(/\s{2,}/g, " ").trim();
      return `${open}${cleanedInner ? `${cleanedInner} ` : ""}${close}`;
    });
    const segments = [];
    if (anchorMatch) {
      segments.push(normalizeAnchor(anchorMatch[0]).trim());
    }
    if (linkMatch) {
      segments.push(linkMatch[0].trim());
    }
    const insertion = segments.join(" ");
    if (insertion.length === 0) {
      return `${leadingWhitespace}${body}`.replace(/\s{2,}/g, " ").trimEnd();
    }
    if (tagRegex.test(body)) {
      body = body.replace(tagRegex, (match) => `${match.trim()} ${insertion}`.trim());
    } else {
      body = `${body} ${insertion}`.trim();
    }
    return `${leadingWhitespace}${body}`.replace(/\s{2,}/g, " ").trimEnd();
  }
  // Check whether the content includes a Todoist link
  hasTodoistLink(lineText) {
    return REGEX.TODOIST_LINK.test(lineText);
  }
};
// src/cacheOperation.ts
var CacheOperation = class {
  constructor(app, plugin) {
    this.app = app;
    this.plugin = plugin;
  }
  markRuntimeStateDirty() {
    if (this.plugin && typeof this.plugin.markRuntimeStateDirty === "function") {
      this.plugin.markRuntimeStateDirty();
    }
  }
  async getFileMetadata(filepath) {
    var _a;
    return (_a = this.plugin.settings.fileMetadata[filepath]) != null ? _a : null;
  }
  async getFileMetadatas() {
    var _a;
    return (_a = this.plugin.settings.fileMetadata) != null ? _a : null;
  }
  async newEmptyFileMetadata(filepath) {
    const metadatas = this.plugin.settings.fileMetadata;
    if (metadatas[filepath]) {
      return;
    } else {
      metadatas[filepath] = {};
    }
    metadatas[filepath].todoistTasks = [];
    metadatas[filepath].todoistCount = 0;
    this.plugin.settings.fileMetadata = metadatas;
    this.markRuntimeStateDirty();
  }
  async updateFileMetadata(filepath, newMetadata) {
    const metadatas = this.plugin.settings.fileMetadata;
    if (!metadatas[filepath]) {
      metadatas[filepath] = {};
    }
    metadatas[filepath].todoistTasks = newMetadata.todoistTasks;
    metadatas[filepath].todoistCount = newMetadata.todoistCount;
    this.plugin.settings.fileMetadata = metadatas;
    this.markRuntimeStateDirty();
  }
  removeTaskFromMetadata(filepath, taskId) {
    if (!filepath) {
      return;
    }
    const metadatas = this.plugin.settings.fileMetadata;
    if (!metadatas || !metadatas[filepath]) {
      return;
    }
    const metadata = metadatas[filepath];
    const taskIdStr = String(taskId);
    if (Array.isArray(metadata.todoistTasks)) {
      metadata.todoistTasks = metadata.todoistTasks.filter((id) => String(id) !== taskIdStr);
    } else {
      metadata.todoistTasks = [];
    }
    if (typeof metadata.todoistCount === "number") {
      metadata.todoistCount = metadata.todoistTasks.length;
    } else {
      metadata.todoistCount = metadata.todoistTasks.length;
    }
    metadatas[filepath] = metadata;
    this.plugin.settings.fileMetadata = metadatas;
    this.markRuntimeStateDirty();
  }
  addTaskToMetadata(filepath, taskId) {
    if (!filepath) {
      return;
    }
    const metadatas = this.plugin.settings.fileMetadata;
    if (!metadatas[filepath]) {
      metadatas[filepath] = { todoistTasks: [], todoistCount: 0 };
    }
    const metadata = metadatas[filepath];
    const taskIdStr = String(taskId);
    if (!Array.isArray(metadata.todoistTasks)) {
      metadata.todoistTasks = [];
    } else {
      metadata.todoistTasks = metadata.todoistTasks.map((id) => String(id));
    }
    if (!metadata.todoistTasks.includes(taskIdStr)) {
      metadata.todoistTasks.push(taskIdStr);
    }
    metadata.todoistCount = Array.isArray(metadata.todoistTasks) ? metadata.todoistTasks.length : 0;
    metadatas[filepath] = metadata;
    this.plugin.settings.fileMetadata = metadatas;
    this.markRuntimeStateDirty();
  }
  canonicalizeTaskPath(taskId, oldpath, newpath) {
    if (!taskId || !newpath || oldpath === newpath) {
      return;
    }
    try {
      const taskIdStr = String(taskId);
      const savedTasks = this.plugin.settings.todoistTasksData?.tasks;
      if (Array.isArray(savedTasks)) {
        const task = savedTasks.find((entry) => entry && String(entry.id) === taskIdStr);
        if (task) {
          task.path = newpath;
        }
      }
      if (oldpath) {
        this.removeTaskFromMetadata(oldpath, taskIdStr);
      }
      this.addTaskToMetadata(newpath, taskIdStr);
    } catch (error) {
      console.error(`Error canonicalizing Todoist task path: ${error}`);
    }
  }
  async deleteTaskIdFromMetadata(filepath, taskId) {
    todoistBridgeDebugLog(filepath);
    const metadata = await this.getFileMetadata(filepath);
    todoistBridgeDebugLog(metadata);
    const newTodoistTasks = metadata.todoistTasks.filter(function(element) {
      return element !== taskId;
    });
    const newTodoistCount = metadata.todoistCount - 1;
    let newMetadata = {};
    newMetadata.todoistTasks = newTodoistTasks;
    newMetadata.todoistCount = newTodoistCount;
    todoistBridgeDebugLog(`new metadata ${newMetadata}`);
  }
  //delete filepath from filemetadata
  async deleteFilepathFromMetadata(filepath) {
    Reflect.deleteProperty(this.plugin.settings.fileMetadata, filepath);
    this.markRuntimeStateDirty();
    this.plugin.saveSettings();
    todoistBridgeDebugLog(`${filepath} is deleted from file metadatas.`);
  }
  //Check errors in filemata where the filepath is incorrect.
  async checkFileMetadata() {
    var _a, _b;
    const metadatas = await this.getFileMetadatas();
    for (const key in metadatas) {
      let filepath = key;
      const value = metadatas[key];
      let file = this.app.vault.getAbstractFileByPath(key);
      if (!file && (((_a = value.todoistTasks) == null ? void 0 : _a.length) === 0 || !value.todoistTasks)) {
        todoistBridgeDebugLog(`${key} is not existed and metadata is empty.`);
        await this.deleteFilepathFromMetadata(key);
        continue;
      }
      if (((_b = value.todoistTasks) == null ? void 0 : _b.length) === 0 || !value.todoistTasks) {
        continue;
      }
      if (!file) {
        todoistBridgeDebugLog(`file ${filepath} is not exist`);
        const todoistId1 = value.todoistTasks[0];
        todoistBridgeDebugLog(todoistId1);
        const searchResult = await this.plugin.fileOperation.searchFilepathsByTaskidInVault(todoistId1);
        todoistBridgeDebugLog(`new file path is`);
        todoistBridgeDebugLog(searchResult);
        await this.updateRenamedFilePath(filepath, searchResult);
        this.plugin.saveSettings();
      }
    }
  }
  // Read all tasks from the cache
  loadTasksFromCache() {
    try {
      const savedTasks = this.plugin.settings.todoistTasksData.tasks;
      return savedTasks;
    } catch (error) {
      console.error(`Error loading tasks from Cache: ${error}`);
      return [];
    }
  }
  getActiveTasksByFilepath(filepath) {
    if (!filepath) {
      return [];
    }
    const savedTasks = this.loadTasksFromCache();
    const targetPath = String(filepath);
    return savedTasks.filter((task) => task && task.path === targetPath && task.isCompleted !== true);
  }
  getActiveTaskIdsByFilepath(filepath) {
    const activeTasks = this.getActiveTasksByFilepath(filepath);
    return new Set(activeTasks.map((task) => task.id));
  }
  // Overwrite and save all tasks to the cache
  saveTasksToCache(newTasks) {
    try {
      const normalizedTasks = Array.isArray(newTasks) ? newTasks.map((task) => this.normalizeTaskForCache(task)).filter((task) => !!task) : [];
      const uniqueTasks = [];
      const seen = new Set();
      for (const task of normalizedTasks) {
        if (!task) {
          continue;
        }
        if (seen.has(task.id)) {
          continue;
        }
        seen.add(task.id);
        uniqueTasks.push(task);
      }
      this.plugin.settings.todoistTasksData.tasks = uniqueTasks;
      this.markRuntimeStateDirty();
    } catch (error) {
      console.error(`Error saving tasks to Cache: ${error}`);
      return false;
    }
  }
  // Append event to the cache
  appendEventToCache(event) {
    try {
      this.appendEventsToCache([event]);
    } catch (error) {
      console.error(`Error append event to Cache: ${error}`);
    }
  }
  getEventCacheKey(event) {
    if (!event) {
      return "";
    }
    const eventType = event.event_type != null ? String(event.event_type) : "";
    const objectType = event.object_type != null ? String(event.object_type) : "";
    const objectId = event.object_id != null ? String(event.object_id) : "";
    const parentId = event.parent_item_id != null ? String(event.parent_item_id) : "";
    const eventDate = event.event_date != null ? String(event.event_date) : "";
    if (event.eventKey) {
      return String(event.eventKey);
    }
    return [eventType, objectType, objectId, parentId, eventDate].join("|");
  }
  normalizeEventForCache(event) {
    if (!event) {
      return null;
    }
    const eventKey = this.getEventCacheKey(event);
    if (!eventKey || eventKey === "||||") {
      return null;
    }
    return {
      id: event.id != null ? String(event.id) : eventKey,
      eventKey,
      event_type: event.event_type ?? null,
      object_type: event.object_type ?? null,
      object_id: event.object_id != null ? String(event.object_id) : null,
      parent_item_id: event.parent_item_id != null ? String(event.parent_item_id) : null,
      event_date: event.event_date ?? null
    };
  }
  // Append events to the cache
  appendEventsToCache(events) {
    try {
      if (!this.plugin.settings.todoistTasksData) {
        this.plugin.settings.todoistTasksData = { tasks: [], projects: [], events: [] };
      }
      if (!Array.isArray(this.plugin.settings.todoistTasksData.events)) {
        this.plugin.settings.todoistTasksData.events = [];
      }
      const existing = this.plugin.settings.todoistTasksData.events;
      const seen = new Set(existing.map((event) => this.getEventCacheKey(event)).filter((key) => !!key));
      for (const event of Array.isArray(events) ? events : []) {
        const normalized = this.normalizeEventForCache(event);
        if (!normalized || seen.has(normalized.eventKey)) {
          continue;
        }
        existing.push(normalized);
        seen.add(normalized.eventKey);
      }
      this.plugin.settings.todoistTasksData.events = existing.slice(-1e3);
      this.markRuntimeStateDirty();
    } catch (error) {
      console.error(`Error append events to Cache: ${error}`);
    }
  }
  // Read all events from the cache file
  loadEventsFromCache() {
    try {
      const savedEvents = this.plugin.settings.todoistTasksData.events;
      return Array.isArray(savedEvents) ? savedEvents : [];
    } catch (error) {
      console.error(`Error loading events from Cache: ${error}`);
      return [];
    }
  }
  // Normalize a task before storing it in the cache
  normalizeTaskForCache(task) {
    try {
      if (!task) {
        return null;
      }
      const taskId = task.id != null ? String(task.id) : null;
      if (!taskId) {
        return null;
      }
      const normalizedPath = typeof task.path === "string" ? task.path.trim() : "";
      if (!normalizedPath) {
        return null;
      }
      const isCompleted = task.completed === true || task.isCompleted === true;
      if (isCompleted) {
        return null;
      }
      const normalizedTask = { ...task, id: taskId, path: normalizedPath, isCompleted: false };
      if (!normalizedTask.origin) {
        normalizedTask.origin = "todoist";
      }
      if (!normalizedTask.addedAt) {
        normalizedTask.addedAt = normalizedTask.createdAt || new Date().toISOString();
      }
      return normalizedTask;
    } catch (error) {
      console.error(`Error normalizing task for Cache: ${error}`);
      return null;
    }
  }

  appendTaskToCache(task) {
    try {
      if (!this.plugin.settings.todoistTasksData || !Array.isArray(this.plugin.settings.todoistTasksData.tasks)) {
        this.plugin.settings.todoistTasksData.tasks = [];
      }
      const savedTasks = this.plugin.settings.todoistTasksData.tasks;
      const taskId = task && task.id != null ? String(task.id) : null;
      if (!taskId) {
        return;
      }
      const existingIndex = savedTasks.findIndex((t) => t.id === taskId);
      const normalizedTask = this.normalizeTaskForCache(task);
      if (!normalizedTask) {
        if (existingIndex !== -1) {
          savedTasks.splice(existingIndex, 1);
          this.plugin.settings.todoistTasksData.tasks = savedTasks;
          this.markRuntimeStateDirty();
        }
        return;
      }
      if (existingIndex !== -1) {
        savedTasks[existingIndex] = { ...savedTasks[existingIndex], ...normalizedTask };
      } else {
        savedTasks.push(normalizedTask);
      }
      this.plugin.settings.todoistTasksData.tasks = savedTasks;
      this.markRuntimeStateDirty();
    } catch (error) {
      console.error(`Error appending task to Cache: ${error}`);
    }
  }
  // Read the task with the specified ID
  loadTaskFromCacheyID(taskId) {
    try {
      const savedTasks = this.plugin.settings.todoistTasksData.tasks;
      const id = taskId != null ? String(taskId) : null;
      if (!id) {
        return null;
      }
      const savedTask = savedTasks.find((t) => t.id === id);
      return savedTask || null;
    } catch (error) {
      console.error(`Error finding task from Cache: ${error}`);
      return null;
    }
  }
  // Overwrite and update the task with the specified ID
  updateTaskToCacheByID(task) {
    try {
      this.deleteTaskFromCache(task.id);
      this.appendTaskToCache(task);
    } catch (error) {
      console.error(`Error updating task to Cache: ${error}`);
      return [];
    }
  }
  // Structure of due {date: "2025-02-25",isRecurring: false,lang: "en",string: "2025-02-25"}
  modifyTaskToCacheByID(taskId, { content, due }) {
    try {
      const savedTasks = this.plugin.settings.todoistTasksData.tasks;
      const taskIndex = savedTasks.findIndex((task) => task.id === taskId);
      if (taskIndex !== -1) {
        const updatedTask = { ...savedTasks[taskIndex] };
        if (content !== void 0) {
          updatedTask.content = content;
        }
        if (due !== void 0) {
          if (due === null) {
            updatedTask.due = null;
          } else {
            updatedTask.due = due;
          }
        }
        savedTasks[taskIndex] = updatedTask;
        this.plugin.settings.todoistTasksData.tasks = savedTasks;
        this.markRuntimeStateDirty();
      } else {
        throw new Error(`Task with ID ${taskId} not found in cache.`);
      }
    } catch (error) {
    }
  }
  setTaskContentInCache(taskId, content) {
    try {
      const savedTasks = this.plugin.settings.todoistTasksData.tasks;
      const index = savedTasks.findIndex((task) => task.id === taskId);
      if (index !== -1) {
        savedTasks[index].content = content;
        this.plugin.settings.todoistTasksData.tasks = savedTasks;
        this.markRuntimeStateDirty();
      }
    } catch (error) {
      console.error(`Error updating task content in cache: ${error}`);
    }
  }
  //open a task status
  async reopenTaskToCacheByID(taskId) {
    try {
      const savedTasks = this.plugin.settings.todoistTasksData.tasks;
      const existingIndex = savedTasks.findIndex((task) => task.id === taskId);
      if (existingIndex !== -1) {
        savedTasks[existingIndex].isCompleted = false;
        this.plugin.settings.todoistTasksData.tasks = savedTasks;
        this.markRuntimeStateDirty();
        return savedTasks[existingIndex];
      }
      const task = await this.plugin.todoistRestAPI.getTaskById(taskId);
      if (!task) {
        return null;
      }
      let filepath = task.path;
      if (!filepath) {
        try {
          filepath = await this.plugin.fileOperation.searchFilepathsByTaskidInVault(taskId);
        } catch (err) {
          console.error(`Error resolving filepath for task ${taskId}:`, err);
        }
      }
      if (!filepath) {
        return null;
      }
      task.path = filepath;
      task.isCompleted = false;
      task.id = taskId;
      if (!task.origin) {
        task.origin = "todoist";
      }
      if (!task.addedAt) {
        task.addedAt = task.createdAt || new Date().toISOString();
      }
      savedTasks.push(task);
      this.plugin.settings.todoistTasksData.tasks = savedTasks;
      this.markRuntimeStateDirty();
      return task;
    } catch (error) {
      console.error(`Error open task to Cache file: ${error}`);
      return null;
    }
  }
  //close a task status
  closeTaskToCacheByID(taskId) {
    try {
      this.deleteTaskFromCache(taskId);
    } catch (error) {
      console.error(`Error close task to Cache file: ${error}`);
      throw error;
    }
  }
  // Delete a task by ID
  deleteTaskFromCache(taskId) {
    try {
      const savedTasks = this.plugin.settings.todoistTasksData.tasks;
      const newSavedTasks = savedTasks.filter((t) => t.id !== taskId);
      this.plugin.settings.todoistTasksData.tasks = newSavedTasks;
      this.markRuntimeStateDirty();
    } catch (error) {
      console.error(`Error deleting task from Cache file: ${error}`);
    }
  }
  // Delete tasks by an array of IDs
  deleteTaskFromCacheByIDs(deletedTaskIds) {
    try {
      const savedTasks = this.plugin.settings.todoistTasksData.tasks;
      const newSavedTasks = savedTasks.filter((t) => !deletedTaskIds.includes(t.id));
      this.plugin.settings.todoistTasksData.tasks = newSavedTasks;
      this.markRuntimeStateDirty();
    } catch (error) {
      console.error(`Error deleting task from Cache : ${error}`);
    }
  }
  // Find the project ID by name
  getProjectIdByNameFromCache(projectName) {
    try {
      const savedProjects = this.plugin.settings.todoistTasksData.projects;
      const targetProject = savedProjects.find((obj) => obj.name === projectName);
      const projectId = targetProject ? targetProject.id : null;
      return projectId;
    } catch (error) {
      console.error(`Error finding project from Cache file: ${error}`);
      return false;
    }
  }
  getProjectNameByIdFromCache(projectId) {
    try {
      const savedProjects = this.plugin.settings.todoistTasksData.projects;
      const targetProject = savedProjects.find((obj) => obj.id === projectId);
      const projectName = targetProject ? targetProject.name : null;
      return projectName;
    } catch (error) {
      console.error(`Error finding project from Cache file: ${error}`);
      return false;
    }
  }
  //save projects data to json file
  async saveProjectsToCache() {
    try {
      const projects = await this.plugin.todoistRestAPI.GetAllProjects();
      if (!projects) {
        return false;
      }
      this.plugin.settings.todoistTasksData.projects = projects;
      this.markRuntimeStateDirty();
      return true;
    } catch (error) {
      return false;
      todoistBridgeDebugLog(`error downloading projects: ${error}`);
    }
  }
  async updateRenamedFilePath(oldpath, newpath) {
    try {
      todoistBridgeDebugLog(`oldpath is ${oldpath}`);
      todoistBridgeDebugLog(`newpath is ${newpath}`);
      const savedTask = await this.loadTasksFromCache();
      const newTasks = savedTask.map((obj) => {
        if (obj.path === oldpath) {
          return { ...obj, path: newpath };
        } else {
          return obj;
        }
      });
      await this.saveTasksToCache(newTasks);
      const fileMetadatas = this.plugin.settings.fileMetadata;
      fileMetadatas[newpath] = fileMetadatas[oldpath];
      delete fileMetadatas[oldpath];
      this.plugin.settings.fileMetadata = fileMetadatas;
      this.markRuntimeStateDirty();
    } catch (error) {
      todoistBridgeDebugLog(`Error updating renamed file path to cache: ${error}`);
    }
  }
};
// src/fileOperation.ts
var FileOperation = class {
  constructor(app, plugin) {
    this.app = app;
    this.plugin = plugin;
  }
  /*
  async getFrontMatter(file:TFile): Promise<FrontMatter | null> {
      return new Promise((resolve) => {
        this.app.fileManager.processFrontMatter(file, (frontMatter) => {
          resolve(frontMatter);
        });
      });
  }
  */
  /*
  async updateFrontMatter(
  file:TFile,
  updater: (frontMatter: FrontMatter) => void
  ): Promise<void> {
      //todoistBridgeDebugLog(`prepare to update front matter`)
      this.app.fileManager.processFrontMatter(file, (frontMatter) => {
      if (frontMatter !== null) {
      const updatedFrontMatter = { ...frontMatter } as FrontMatter;
      updater(updatedFrontMatter);
      this.app.fileManager.processFrontMatter(file, (newFrontMatter) => {
          if (newFrontMatter !== null) {
          newFrontMatter.todoistTasks = updatedFrontMatter.todoistTasks;
          newFrontMatter.todoistCount = updatedFrontMatter.todoistCount;
          }
      });
      }
  });
  }
  */
  escapeRegExp(value) {
    return String(value || "").replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  }
  lineContainsTodoistTaskId(line, taskId) {
    if (!line || !taskId) {
      return false;
    }
    const escaped = this.escapeRegExp(taskId);
    const idPattern = new RegExp(`\\[todoist_id::\\s*${escaped}\\]`, "i");
    const linkPattern = new RegExp(`/task/${escaped}\\b`, "i");
    return idPattern.test(line) || linkPattern.test(line);
  }
  async findTaskLocationById(taskId, preferredPath) {
    const tryFile = async (filepath) => {
      if (!filepath) {
        return null;
      }
      const file = this.app.vault.getAbstractFileByPath(filepath);
      if (!file) {
        return null;
      }
      let content = "";
      try {
        content = await this.app.vault.read(file);
      } catch (error) {
        console.error(`Todoist Bridge: failed to read candidate file ${filepath}`, error);
        return null;
      }
      const lines = content.split("\n");
      for (let i = 0; i < lines.length; i++) {
        const line = lines[i];
        if (this.lineContainsTodoistTaskId(line, taskId) && this.plugin.taskParser.hasTodoistTag(line)) {
          return { file, filepath: file.path || filepath, content, lines, lineIndex: i, line };
        }
      }
      return null;
    };
    const preferred = await tryFile(preferredPath);
    if (preferred) {
      return preferred;
    }
    try {
      const markdownFiles = this.app.vault.getMarkdownFiles ? this.app.vault.getMarkdownFiles() : [];
      for (const file of markdownFiles) {
        if (preferredPath && file.path === preferredPath) {
          continue;
        }
        const found = await tryFile(file.path);
        if (found) {
          return found;
        }
      }
    } catch (error) {
      console.error("Todoist Bridge: failed to scan vault for task location", error);
    }
    return null;
  }
  // Complete a task and mark it as done
  async completeTaskInTheFile(taskId, completedAt, options = {}) {
    const currentTask = await this.plugin.cacheOperation.loadTaskFromCacheyID(taskId);
    const preferredPath = (options == null ? void 0 : options.filepath) || (currentTask == null ? void 0 : currentTask.path) || null;
    const location = await this.findTaskLocationById(taskId, preferredPath);
    if (!location) {
      return { found: false, modified: false, filepath: preferredPath };
    }
    const lines = location.lines;
    let modified = false;
    const metadataKey = (options == null ? void 0 : options.metadataKey) || "todoist_completion";
    const deriveCompletionDate = (value) => {
      if (!value) {
        return new Date();
      }
      const parsed = new Date(value);
      return isNaN(parsed.getTime()) ? new Date() : parsed;
    };
    const completionDate = deriveCompletionDate(completedAt);
    const isoTimestamp = completionDate.toISOString();
    const dateToken = (options == null ? void 0 : options.displayDate) || isoTimestamp.slice(0, 10);
    const metadataToken = `[${metadataKey}:: ${dateToken}]`;
    const escapedMetadataKey = this.escapeRegExp(metadataKey);
    const completionInlinePattern = new RegExp(`(^|\\s)(?:completed|${escapedMetadataKey})::\\s*[\\w:-]+`, "gi");
    const metadataTokenPattern = new RegExp(`\\[${escapedMetadataKey}::\\s*[^\\]]+\\]`, "gi");
    const legacyTokenPattern = /\[completed::\s*[^\]]+\]/gi;
    for (let i = location.lineIndex; i <= location.lineIndex; i++) {
      const line = lines[i];
      if (this.lineContainsTodoistTaskId(line, taskId) && this.plugin.taskParser.hasTodoistTag(line)) {
        let newLine = line;
        const checkboxPattern = /([-*]\s*)\[(?:x|X| )\]/;
        if (checkboxPattern.test(newLine)) {
          newLine = newLine.replace(checkboxPattern, "$1[x]");
        } else {
          newLine = newLine.replace(/^(\s*)([-*]?)/, (match, indent, bullet) => {
            const prefix = bullet && bullet.trim() ? `${indent}${bullet.trim()} ` : `${indent}- `;
            return `${prefix}[x]`;
          });
        }
        try {
          const spanPattern = /<span class="todoist-bridge">([\s\S]*?)<\/span>/i;
          const commentPattern = /<!--\s*([\s\S]*?)\s*-->/;
          const ensureMetadata = (metadata) => {
            let result = (metadata || "").replace(/\s+/g, " ").trim();
            result = result.replace(legacyTokenPattern, "");
            result = result.replace(metadataTokenPattern, "");
            if (!/\[todoist_id::\s*[^\]]+\]/.test(result)) {
              result = `[todoist_id:: ${taskId}] ${result}`.trim();
            }
            result = result.replace(legacyTokenPattern, "").replace(metadataTokenPattern, "").trim();
            if (result.length && !result.endsWith(" ")) {
              result += " ";
            }
            result += metadataToken;
            return result.replace(/\s{2,}/g, " ").trim() + " ";
          };
          if (spanPattern.test(newLine)) {
            const match = spanPattern.exec(newLine);
            const metadata = ensureMetadata(match && match[1]);
            newLine = newLine.replace(spanPattern, `<span class="todoist-bridge">${metadata}</span>`);
          } else if (commentPattern.test(newLine)) {
            const match = commentPattern.exec(newLine);
            const metadata = ensureMetadata(match && match[1]);
            newLine = newLine.replace(commentPattern, `<span class="todoist-bridge">${metadata}</span>`);
          } else {
            const metadata = ensureMetadata("");
            newLine = `${newLine.trimEnd()} <span class="todoist-bridge">${metadata}</span>`;
          }
          newLine = this.plugin.taskParser.normalizeTodoistMarkersOrder(newLine);
        } catch (error) {
          console.error("Failed to normalize Todoist metadata span:", error);
        }
        newLine = newLine.replace(legacyTokenPattern, " ");
        newLine = newLine.replace(completionInlinePattern, "$1");
        newLine = newLine.replace(/\s{2,}/g, " ").trimEnd();
        if (newLine !== line) {
          lines[i] = newLine;
          modified = true;
        }
        break;
      }
    }
    if (modified) {
      const newContent = lines.join("\n");
      await this.app.vault.modify(location.file, newContent);
    }
    if (currentTask && currentTask.path !== location.filepath) {
      this.plugin.cacheOperation.canonicalizeTaskPath(taskId, currentTask.path, location.filepath);
    }
    return { found: true, modified, filepath: location.filepath };
  }
  // Uncheck a completed task
  async uncompleteTaskInTheFile(taskId) {
    const currentTask = await this.plugin.cacheOperation.loadTaskFromCacheyID(taskId);
    const preferredPath = (currentTask == null ? void 0 : currentTask.path) || null;
    const location = await this.findTaskLocationById(taskId, preferredPath);
    if (!location) {
      return { found: false, modified: false, filepath: preferredPath };
    }
    const lines = location.lines;
    let modified = false;
    const completionInlinePattern = /\s*(?:completed|todoist_completion)::\s*[\w:-]+/gi;
    const metadataCompletionPattern = /\[(?:completed|todoist_completion)::\s*[^\]]+\]/gi;
    const completionCommentPattern = /<!--\s*\[(?:completed|todoist_completion)::\s*[^\]]+\]\s*-->/gi;
    for (let i = location.lineIndex; i <= location.lineIndex; i++) {
      const line = lines[i];
      if (this.lineContainsTodoistTaskId(line, taskId) && this.plugin.taskParser.hasTodoistTag(line)) {
        let newLine = line.replace(/- \[(x|X)\]/g, "- [ ]");
        newLine = newLine.replace(completionCommentPattern, " ");
        newLine = newLine.replace(metadataCompletionPattern, " ");
        newLine = newLine.replace(completionInlinePattern, " ");
        newLine = this.plugin.taskParser.normalizeTodoistMarkersOrder(newLine);
        newLine = newLine.replace(/\s{2,}/g, " ").trimEnd();
        if (newLine !== line) {
          lines[i] = newLine;
          modified = true;
        }
        break;
      }
    }
    if (modified) {
      const newContent = lines.join("\n");
      await this.app.vault.modify(location.file, newContent);
    }
    if (currentTask && currentTask.path !== location.filepath) {
      this.plugin.cacheOperation.canonicalizeTaskPath(taskId, currentTask.path, location.filepath);
    }
    return { found: true, modified, filepath: location.filepath };
  }

      // Remove #todoist, [link](...), and todoist_id markers for a specific task id in a file
  async removeTodoistMarkersForTask(filepath, taskId) {
    const file = this.app.vault.getAbstractFileByPath(filepath);
    const content = await this.app.vault.read(file);
    const lines = content.split("\n");
    let modified = false;
    const idPattern = new RegExp(`\\b${taskId}\\b`);
    const linkIdPattern = new RegExp(`https?://[^\\s)]*/task/${taskId}\\b`);
    const removeIdPatterns = [
      /<span class="todoist-bridge">[\s\S]*?<\/span>/g,
      /<!--[\s\S]*?\[todoist_id::\s*[A-Za-z0-9_-]+\][\s\S]*?-->/g,
      /%%\s*\[todoist_id::\s*[^\]]+\]\s*%%/g,
      /\[todoist_id::\s*[^\]]+\]/g
    ];
    for (let i = 0; i < lines.length; i++) {
      const matchesThisTask = idPattern.test(lines[i]) || linkIdPattern.test(lines[i]);
      if (!matchesThisTask) continue;
      let line = lines[i];
      // Remove link
      line = line.replace(REGEX.TODOIST_LINK, "");
      // Remove #todoist tag (anywhere on line)
      line = line.replace(/(^|\s)#todoist\b/gi, " ");
      line = line.replace(/\s*completed::\s*\d{4}-\d{2}-\d{2}/gi, " ");
      // Remove id comments
      for (const p of removeIdPatterns) line = line.replace(p, "");
      line = line.replace(/<span class="todoist-bridge">\s*<\/span>/gi, "");
      // Collapse spaces
      line = line.replace(/\s{2,}/g, " ").trimEnd();
      lines[i] = line;
      modified = true;
      break;
    }
    // Fallback: if the specific id isn't found on any line, but there is
    // exactly one #todoist task line without a todoist_id, clean that line.
    if (!modified) {
      const candidates = [];
      for (let i = 0; i < lines.length; i++) {
        const line = lines[i];
        if (REGEX.TODOIST_TAG.test(line) && !REGEX.TODOIST_ID.test(line)) {
          candidates.push(i);
        }
      }
      if (candidates.length === 1) {
        let line = lines[candidates[0]];
        line = line.replace(REGEX.TODOIST_LINK, "");
        line = line.replace(/(^|\s)#todoist\b/gi, " ");
        line = line.replace(/\s*completed::\s*\d{4}-\d{2}-\d{2}/gi, " ");
        line = line.replace(/<!--\s*-->/g, "");
        line = line.replace(/<span class="todoist-bridge">\s*<\/span>/gi, "");
        line = line.replace(/\s{2,}/g, " ").trimEnd();
        lines[candidates[0]] = line;
        modified = true;
      }
    }
    if (modified) {
      const newContent = lines.join("\n");
      await this.app.vault.modify(file, newContent);
    }
    return modified;
  }
  async removeTodoistMarkersForTaskAnywhere(taskId) {
    try {
      const markdownFiles = this.app.vault.getMarkdownFiles ? this.app.vault.getMarkdownFiles() : [];
      for (const file of markdownFiles) {
        try {
          const removed = await this.removeTodoistMarkersForTask(file.path, taskId);
          if (removed) {
            return file.path;
          }
        } catch (error) {
          console.error(`Error removing Todoist markers in ${file.path}:`, error);
        }
      }
    } catch (error) {
      console.error("Error scanning vault for Todoist markers:", error);
    }
    return null;
  }
  // sync updated task content  to file
  async syncUpdatedTaskContentToTheFile(evt) {
    const taskId = evt.object_id;
    const currentTask = await this.plugin.cacheOperation.loadTaskFromCacheyID(taskId);
    const filepath = currentTask.path;
    const file = this.app.vault.getAbstractFileByPath(filepath);
    const content = await this.app.vault.read(file);
    const lines = content.split("\n");
    let modified = false;
    for (let i = 0; i < lines.length; i++) {
      const line = lines[i];
      if (line.includes(taskId) && this.plugin.taskParser.hasTodoistTag(line)) {
        const oldTaskContent = this.plugin.taskParser.getTaskContentFromLineText(line);
        const newTaskContent = evt.extra_data.content;
        lines[i] = line.replace(oldTaskContent, newTaskContent);
        modified = true;
        break;
      }
    }
    if (modified) {
      const newContent = lines.join("\n");
      await this.app.vault.modify(file, newContent);
    }
  }
  // sync updated task due date  to the file
  async syncUpdatedTaskDueDateToTheFile(evt) {
    // Date syncing disabled: do nothing
    return;
    const taskId = evt.object_id;
    const currentTask = await this.plugin.cacheOperation.loadTaskFromCacheyID(taskId);
    const filepath = currentTask.path;
    const file = this.app.vault.getAbstractFileByPath(filepath);
    const content = await this.app.vault.read(file);
    const lines = content.split("\n");
    let modified = false;
    for (let i = 0; i < lines.length; i++) {
      const line = lines[i];
      if (line.includes(taskId) && this.plugin.taskParser.hasTodoistTag(line)) {
        const oldTaskDueDate = this.plugin.taskParser.getDueDateFromLineText(line) || "";
        const newTaskDueDate = this.plugin.taskParser.ISOStringToLocalDateString(evt.extra_data.due_date) || "";
        todoistBridgeDebugLog(oldTaskDueDate);
        todoistBridgeDebugLog(newTaskDueDate);
        if (oldTaskDueDate === "") {
          lines[i] = this.plugin.taskParser.insertDueDateBeforeTodoist(line, newTaskDueDate);
          modified = true;
        } else if (newTaskDueDate === "") {
          const regexRemoveDate = /(🗓️|📅|📆|🗓)\s?\d{4}-\d{2}-\d{2}/;
          lines[i] = line.replace(regexRemoveDate, "");
          modified = true;
        } else {
          lines[i] = line.replace(oldTaskDueDate, newTaskDueDate);
          modified = true;
        }
        break;
      }
    }
    if (modified) {
      const newContent = lines.join("\n");
      await this.app.vault.modify(file, newContent);
    }
  }
  // sync new task note to file
  async syncAddedTaskNoteToTheFile(evt) {
    const taskId = evt.parent_item_id;
    const note = evt.extra_data.content;
    const datetime = this.plugin.taskParser.ISOStringToLocalDatetimeString(evt.event_date);
    const currentTask = await this.plugin.cacheOperation.loadTaskFromCacheyID(taskId);
    const filepath = currentTask.path;
    const file = this.app.vault.getAbstractFileByPath(filepath);
    const content = await this.app.vault.read(file);
    const lines = content.split("\n");
    let modified = false;
    for (let i = 0; i < lines.length; i++) {
      const line = lines[i];
      if (line.includes(taskId) && this.plugin.taskParser.hasTodoistTag(line)) {
        const indent = "	".repeat(line.length - line.trimStart().length + 1);
        const noteLine = `${indent}- ${datetime} ${note}`;
        lines.splice(i + 1, 0, noteLine);
        modified = true;
        break;
      }
    }
    if (modified) {
      const newContent = lines.join("\n");
      await this.app.vault.modify(file, newContent);
    }
  }
  // Avoid using this method; view provides real-time value updates
  async readContentFromFilePath(filepath) {
    try {
      const file = this.app.vault.getAbstractFileByPath(filepath);
      const content = await this.app.vault.read(file);
      return content;
    } catch (error) {
      console.error(`Error loading content from ${filepath}: ${error}`);
      return false;
    }
  }
  //get line text from file path
  // Use view.editor.getLine; the read method has a delay
  async getLineTextFromFilePath(filepath, lineNumber) {
    const file = this.app.vault.getAbstractFileByPath(filepath);
    const content = await this.app.vault.read(file);
    const lines = content.split("\n");
    return lines[lineNumber];
  }
  //search todoist_id by content
  async searchTodoistIdFromFilePath(filepath, searchTerm) {
    const file = this.app.vault.getAbstractFileByPath(filepath);
    const fileContent = await this.app.vault.read(file);
    const fileLines = fileContent.split("\n");
    let todoistId = null;
    for (let i = 0; i < fileLines.length; i++) {
      const line = fileLines[i];
      if (line.includes(searchTerm)) {
        const regexResult = /(?:(?:%%\s*)|(?:<!--\s*)|(?:<span class="todoist-bridge">\s*))?\[todoist_id::\s*([A-Za-z0-9_-]+)\](?:(?:\s*%%)|(?:\s*-->)|(?:\s*<\/span>))?/.exec(line);
        if (regexResult) {
          todoistId = regexResult[1];
        }
        break;
      }
    }
    return todoistId;
  }
  //get all files in the vault
  async getAllFilesInTheVault() {
    const files = this.app.vault.getFiles();
    return files;
  }
  //search filepath by taskid in vault
  async searchFilepathsByTaskidInVault(taskId) {
    todoistBridgeDebugLog(`preprare to search task ${taskId}`);
    const files = await this.getAllFilesInTheVault();
    const tasks = files.map(async (file) => {
      if (!this.isMarkdownFile(file.path)) {
        return;
      }
      const fileContent = await this.app.vault.cachedRead(file);
      if (fileContent.includes(taskId)) {
        return file.path;
      }
    });
    const results = await Promise.all(tasks);
    const filePaths = results.filter((filePath) => filePath !== void 0);
    return filePaths[0] || null;
  }
  isMarkdownFile(filename) {
    let extension = filename.split(".").pop();
    extension = extension.toLowerCase();
    if (extension === "md") {
      return true;
    } else {
      return false;
    }
  }
};
// src/syncModule.ts
var import_obsidian2 = require("obsidian");
var TodoistSync = class {
  constructor(app, plugin) {
    this.app = app;
    this.plugin = plugin;
  }
  async deletedTaskCheck(file_path, context = {}) {
    let file = context && context.file ? context.file : null;
    let filepath = file_path;
    let fileContent = context && typeof context.content === "string" ? context.content : null;
    try {
      if (file_path) {
        if (!file) {
          const abstract = this.app.vault.getAbstractFileByPath(file_path);
          if (!abstract || !(abstract instanceof import_obsidian2.TFile)) {
            return false;
          }
          file = abstract;
        }
        filepath = file.path;
        if (fileContent === null) {
          fileContent = await this.app.vault.read(file);
        }
      } else {
        const view = this.app.workspace.getActiveViewOfType(import_obsidian2.MarkdownView);
        const activeFile = this.app.workspace.getActiveFile();
        if (!activeFile || !(activeFile instanceof import_obsidian2.TFile)) {
          return false;
        }
        file = activeFile;
        filepath = file.path;
        if (fileContent === null) {
          if (typeof (view == null ? void 0 : view.data) === "string") {
            fileContent = view.data;
          } else {
            fileContent = await this.app.vault.read(file);
          }
        }
      }
    } catch (error) {
      console.error("Error reading file during deletedTaskCheck:", error);
      return false;
    }
    if (context && typeof context === "object" && file) {
      context.file = file;
    }
    if (!filepath || typeof fileContent !== "string") {
      return false;
    }
    const frontMatter = await this.plugin.cacheOperation.getFileMetadata(filepath);
    if (!frontMatter || !Array.isArray(frontMatter.todoistTasks) || !frontMatter.todoistTasks.length) {
      return false;
    }
    const cachedById = /* @__PURE__ */ new Map();
    for (const task of Array.isArray(this.plugin.cacheOperation.loadTasksFromCache()) ? this.plugin.cacheOperation.loadTasksFromCache() : []) {
      if (task && task.id != null) {
        cachedById.set(String(task.id), task);
      }
    }
    const detachPlan = import_sync_core.buildObsidianDetachPlan({
      path: filepath,
      content: fileContent,
      trackedTaskIds: frontMatter.todoistTasks,
      cachedTasksById: cachedById
    });
    if (!detachPlan.detach.length) {
      if (context && typeof context === "object" && typeof fileContent === "string") {
        context.content = fileContent;
      }
      return false;
    }
    for (const detachment of detachPlan.detach) {
      const taskId = detachment.taskId;
      const taskIdStr = String(taskId);
      let cachedTask = detachment.cachedTask || null;
      try {
        cachedTask = cachedTask || this.plugin.cacheOperation.loadTaskFromCacheyID(taskIdStr);
      } catch (error) {
        console.error("Error loading cached task while pruning deleted tasks:", error);
      }
      const taskForLog = cachedTask ? { ...cachedTask } : { id: taskIdStr };
      if (!taskForLog.path) {
        taskForLog.path = filepath;
      }
      if (typeof taskForLog.content !== "string") {
        taskForLog.content = taskForLog.content != null ? String(taskForLog.content) : "";
      }
      if (!("url" in taskForLog)) {
        taskForLog.url = null;
      }
      if (!("addedAt" in taskForLog)) {
        taskForLog.addedAt = null;
      }
      if (!("createdAt" in taskForLog)) {
        taskForLog.createdAt = null;
      }
      try {
        const timestamp = this.plugin.normalizeCompletionTimestamp();
        const detachedRemote = await this.plugin.detachRemoteBridge(taskIdStr, cachedTask);
        if (!detachedRemote) {
          this.plugin.markDirtyFile(filepath);
          continue;
        }
        if (detachment.reason === "todoist-tag-removed") {
          const markersRemoved = await this.plugin.fileOperation.removeTodoistMarkersForTask(filepath, taskIdStr);
          if (markersRemoved && file) {
            try {
              fileContent = await this.app.vault.cachedRead(file);
            } catch (error) {
              console.error("Todoist Bridge: failed to refresh file after marker detach", error);
            }
          }
        }
        await this.plugin.logTaskActivity(taskForLog, {
          status: "detached",
          statusSource: "obsidian",
          source: "obsidian",
          dateClosedIso: null,
          eventLoggedIso: timestamp.iso
        });
        this.plugin.recordBridgeTombstone(taskIdStr, filepath, detachment.reason, "obsidian");
      } catch (error) {
        console.error("Todoist Bridge: failed to detach remote bridge after Obsidian change", error);
      }
      try {
        this.plugin.cacheOperation.removeTaskFromMetadata(filepath, taskIdStr);
      } catch (error) {
        console.error("Error updating metadata while pruning deleted tasks:", error);
      }
      try {
        this.plugin.cacheOperation.deleteTaskFromCache(taskIdStr);
      } catch (error) {
        console.error("Error removing task from cache while pruning deleted tasks:", error);
      }
    }
    await this.plugin.saveSettings();
    if (context && typeof context === "object" && typeof fileContent === "string") {
      context.content = fileContent;
    }
    return true;
  }
  async lineContentNewTaskCheck(editor, view) {
    var _a, _b, _c, _d;
    const filepath = (_a = view.file) == null ? void 0 : _a.path;
    const fileContent = view == null ? void 0 : view.data;
    const cursor = editor.getCursor();
    const line = cursor.line;
    const linetxt = editor.getLine(line);
    if (!this.plugin.taskParser.hasTodoistId(linetxt) && this.plugin.taskParser.hasTodoistTag(linetxt)) {
      todoistBridgeDebugLog("this is a new task");
      todoistBridgeDebugLog(linetxt);
      const currentTask = await this.plugin.taskParser.convertTextToTodoistTaskObject(linetxt, filepath, line, fileContent);
      try {
        const newTask = await this.plugin.todoistRestAPI.AddTask(currentTask);
        const { id: todoist_id, projectId: todoist_projectId, url: todoist_url } = newTask;
        newTask.path = filepath;
        newTask.addedAt = new Date().toISOString();
        newTask.origin = "obsidian";
        newTask.isCompleted = false;
        this.plugin.cacheOperation.appendTaskToCache(newTask);
        if (currentTask.isCompleted === true) {
          await this.plugin.todoistRestAPI.CloseTask(newTask.id);
          this.plugin.cacheOperation.closeTaskToCacheByID(todoist_id);
        }
        this.plugin.saveSettings();
        // Clean the line in Obsidian: remove @todoist and @task; keep hashtags for plugin logic; wrap todoist_id in a metadata span
        const cleaned = linetxt.replace(/(^|\s)@todoist\b/gi, " ").replace(/(^|\s)@task\b/gi, " ");
        const text_with_out_link = `${cleaned} <span class="todoist-bridge">[todoist_id:: ${todoist_id}] </span>`;
        const link = `[link](${newTask.url})`;
        const text = this.plugin.taskParser.addTodoistLink(text_with_out_link, link);
        const from = { line: cursor.line, ch: 0 };
        const to = { line: cursor.line, ch: linetxt.length };
        (_c = (_b = view.app.workspace.activeEditor) == null ? void 0 : _b.editor) == null ? void 0 : _c.replaceRange(text, from, to);
        try {
          const frontMatter = await this.plugin.cacheOperation.getFileMetadata(filepath);
          if (!frontMatter) {
          }
          const newFrontMatter = { ...frontMatter };
          newFrontMatter.todoistCount = ((_d = newFrontMatter.todoistCount) != null ? _d : 0) + 1;
          newFrontMatter.todoistTasks = [...newFrontMatter.todoistTasks || [], todoist_id];
          await this.plugin.cacheOperation.updateFileMetadata(filepath, newFrontMatter);
        } catch (error) {
          console.error(error);
        }
      } catch (error) {
        console.error("Error adding task:", error);
        todoistBridgeDebugLog(`The error occurred in the file: ${filepath}`);
        return;
      }
    }
  }
  async fullTextNewTaskCheck(file_path, context = {}) {
    var _a;
    let file = context && context.file ? context.file : void 0;
    let currentFileValue = context && typeof context.content === "string" ? context.content : null;
    let view;
    let filepath = file_path;
    try {
      if (file_path) {
        if (!file) {
          const abstract = this.app.vault.getAbstractFileByPath(file_path);
          if (!abstract || !(abstract instanceof import_obsidian2.TFile)) {
            return false;
          }
          file = abstract;
        }
        filepath = file.path;
        if (currentFileValue === null) {
          currentFileValue = await this.app.vault.read(file);
        }
      } else {
        view = this.app.workspace.getActiveViewOfType(import_obsidian2.MarkdownView);
        const activeFile = this.app.workspace.getActiveFile();
        if (!activeFile || !(activeFile instanceof import_obsidian2.TFile)) {
          return false;
        }
        file = activeFile;
        filepath = file.path;
        if (typeof (view == null ? void 0 : view.data) === "string") {
          currentFileValue = view.data;
        } else if (currentFileValue === null) {
          currentFileValue = await this.app.vault.read(file);
        }
      }
    } catch (error) {
      console.error("Error reading file during fullTextNewTaskCheck:", error);
      return false;
    }
    if (context && typeof context === "object" && file) {
      context.file = file;
    }
    if (!filepath || typeof currentFileValue !== "string") {
      return false;
    }
    const content = currentFileValue;
    let newFrontMatter;
    const frontMatter = await this.plugin.cacheOperation.getFileMetadata(filepath);
    if (!frontMatter) {
      todoistBridgeDebugLog("frontmatter is empty");
      newFrontMatter = {};
    } else {
      newFrontMatter = { ...frontMatter };
    }
    let hasNewTask = false;
    const lines = content.split("\n");
    for (let i = 0; i < lines.length; i++) {
      const line = lines[i];
      if (!this.plugin.taskParser.hasTodoistId(line) && this.plugin.taskParser.hasTodoistTag(line)) {
        const currentTask = await this.plugin.taskParser.convertTextToTodoistTaskObject(line, filepath, i, content);
        if (typeof currentTask === "undefined") {
          continue;
        }
        try {
          const newTask = await this.plugin.todoistRestAPI.AddTask(currentTask);
          const { id: todoist_id } = newTask;
          newTask.path = filepath;
          newTask.addedAt = new Date().toISOString();
          newTask.origin = "obsidian";
          newTask.isCompleted = false;
          this.plugin.cacheOperation.appendTaskToCache(newTask);
          if (currentTask.isCompleted === true) {
            await this.plugin.todoistRestAPI.CloseTask(newTask.id);
            this.plugin.cacheOperation.closeTaskToCacheByID(todoist_id);
          }
          this.plugin.saveSettings();
          const cleaned2 = line.replace(/(^|\s)@todoist\b/gi, " ").replace(/(^|\s)@task\b/gi, " ");
          const text_with_out_link = `${cleaned2} <span class="todoist-bridge">[todoist_id:: ${todoist_id}] </span>`;
          const link = `[link](${newTask.url})`;
          const text = this.plugin.taskParser.addTodoistLink(text_with_out_link, link);
          lines[i] = text;
          newFrontMatter.todoistCount = ((_a = newFrontMatter.todoistCount) != null ? _a : 0) + 1;
          newFrontMatter.todoistTasks = [...newFrontMatter.todoistTasks || [], todoist_id];
          hasNewTask = true;
        } catch (error) {
          console.error("Error adding task:", error);
          continue;
        }
      }
    }
    if (hasNewTask) {
      try {
        const newContent = lines.join("\n");
        const targetFile = file ?? this.app.vault.getAbstractFileByPath(filepath);
        if (targetFile && targetFile instanceof import_obsidian2.TFile) {
          await this.app.vault.modify(targetFile, newContent);
        }
        if (context && typeof context === "object") {
          context.content = newContent;
          context.file = targetFile ?? file;
        }
        await this.plugin.cacheOperation.updateFileMetadata(filepath, newFrontMatter);
      } catch (error) {
        console.error("Error finalising new task sync:", error);
      }
      return true;
    }
    if (context && typeof context === "object" && typeof currentFileValue === "string") {
      context.content = currentFileValue;
      if (!context.file && file) {
        context.file = file;
      }
    }
    return false;
  }
  async lineModifiedTaskCheck(filepath, lineText, lineNumber, fileContent) {
    if (this.plugin.taskParser.hasTodoistId(lineText) && this.plugin.taskParser.hasTodoistTag(lineText)) {
      const lineTask = await this.plugin.taskParser.convertTextToTodoistTaskObject(lineText, filepath, lineNumber, fileContent);
      const lineTask_todoist_id = lineTask.todoist_id.toString();
      const savedTask = await this.plugin.cacheOperation.loadTaskFromCacheyID(lineTask_todoist_id);
      if (!savedTask) {
        if (this.plugin.settings.debugMode) {
          todoistBridgeDebugLog(`Task ${lineTask.todoist_id} not found in local cache`);
          const url = this.plugin.taskParser.getObsidianUrlFromFilepath(filepath);
          todoistBridgeDebugLog(url);
        }
        return;
      }
      const lineTaskContent = lineTask.content;
      const contentModified = !this.plugin.taskParser.taskContentCompare(lineTask, savedTask);
      const statusModified = !this.plugin.taskParser.taskStatusCompare(lineTask, savedTask);
      try {
        let contentChanged = false;
        let statusChanged = false;
        if (contentModified) {
          todoistBridgeDebugLog(`Content modified for task ${lineTask_todoist_id}`);
          const updatedContent = { content: lineTaskContent };
          try {
            await this.plugin.todoistRestAPI.UpdateTask(lineTask.todoist_id.toString(), updatedContent);
            this.plugin.cacheOperation.setTaskContentInCache(lineTask_todoist_id, lineTaskContent);
            contentChanged = true;
          } catch (error) {
            console.error("Error updating Todoist task content:", error);
          }
        }
        if (statusModified) {
          todoistBridgeDebugLog(`Status modified for task ${lineTask_todoist_id}`);
          if (lineTask.isCompleted === true) {
            const timestamp = this.plugin.normalizeCompletionTimestamp(new Date());
            const descriptionBase = (savedTask == null ? void 0 : savedTask.description) || this.plugin.taskParser.getObsidianUrlFromFilepath(filepath) || "";
            const completionSuffixPattern = /\s*-\s*Completed(?:\s+in\s+Obsidian\s+on\s+[0-9TZ:.+-]+|\s+\d{4}-\d{2}-\d{2})$/i;
            const cleanedDescription = descriptionBase.replace(completionSuffixPattern, "").trim();
            const completionNote = cleanedDescription ? `${cleanedDescription} - Completed in Obsidian on ${timestamp.iso}` : `Completed in Obsidian on ${timestamp.iso}`;
            try {
              await this.plugin.todoistRestAPI.UpdateTask(lineTask.todoist_id.toString(), { description: completionNote });
            } catch (error) {
              console.error("Error updating Todoist description for completion:", error);
            }
            await this.plugin.todoistRestAPI.CloseTask(lineTask.todoist_id.toString());
            const commentDisplay = timestamp.iso.replace('T', ' ').replace('Z', ' UTC');
            try {
              await this.plugin.todoistRestAPI.AddComment(lineTask.todoist_id.toString(), { content: `Task completed in Obsidian on ${commentDisplay}` });
            } catch (error) {
              console.error("Error adding Todoist completion comment:", error);
            }
            await this.plugin.handleTaskCompletion(lineTask.todoist_id.toString(), { source: "obsidian", completedAt: timestamp.iso });
          } else {
            await this.plugin.todoistRestAPI.OpenTask(lineTask.todoist_id.toString());
            const completionSuffixPattern = /\s*-\s*Completed(?:\s+in\s+Obsidian\s+on\s+[0-9TZ:.+-]+|\s+\d{4}-\d{2}-\d{2})$/i;
            const reopenBase = ((savedTask == null ? void 0 : savedTask.description) || this.plugin.taskParser.getObsidianUrlFromFilepath(filepath) || "").replace(completionSuffixPattern, "").trim();
            if (reopenBase) {
              try {
                await this.plugin.todoistRestAPI.UpdateTask(lineTask.todoist_id.toString(), { description: reopenBase });
              } catch (error) {
                console.error("Error restoring Todoist description on reopen:", error);
              }
            }
            const reopenedTask = await this.plugin.cacheOperation.reopenTaskToCacheByID(lineTask.todoist_id.toString());
            await this.plugin.fileOperation.uncompleteTaskInTheFile(lineTask.todoist_id.toString());
            const targetPath = (reopenedTask == null ? void 0 : reopenedTask.path) || filepath;
            if (targetPath) {
              this.plugin.cacheOperation.addTaskToMetadata(targetPath, lineTask.todoist_id.toString());
            }
            this.plugin.saveSettings();
          }
          statusChanged = true;
        }
        if (contentChanged && !statusChanged) {
          this.plugin.saveSettings();
        }
      } catch (error) {
        console.error("Error updating task:", error);
      }
    }
  }
  async fullTextModifiedTaskCheck(file_path, context = {}) {
    let file = context && context.file ? context.file : null;
    let currentFileValue = context && typeof context.content === "string" ? context.content : null;
    let view;
    let filepath;
    try {
      if (file_path) {
        if (!file) {
          file = this.app.vault.getAbstractFileByPath(file_path);
        }
        if (!file || !(file instanceof import_obsidian2.TFile)) {
          return false;
        }
        filepath = file_path;
        if (currentFileValue === null) {
          currentFileValue = await this.app.vault.read(file);
        }
      } else {
        view = this.app.workspace.getActiveViewOfType(import_obsidian2.MarkdownView);
        file = this.app.workspace.getActiveFile();
        if (!file || !(file instanceof import_obsidian2.TFile)) {
          return false;
        }
        filepath = file.path;
        if (currentFileValue === null) {
          if (typeof (view == null ? void 0 : view.data) === "string") {
            currentFileValue = view.data;
          } else {
            currentFileValue = await this.app.vault.read(file);
          }
        }
      }
      const content = currentFileValue;
      if (typeof content !== "string") {
        return false;
      }
      if (!filepath) {
        return false;
      }
      const trackedIds = this.plugin.cacheOperation.getActiveTaskIdsByFilepath(filepath);
      if (!trackedIds || trackedIds.size === 0) {
        return false;
      }
      let hasModifiedTask = false;
      const seenInFile = new Set();
      const lines = content.split("\n");
      for (let i = 0; i < lines.length; i++) {
        const line = lines[i];
        if (!this.plugin.taskParser.hasTodoistTag(line)) {
          continue;
        }
        if (!this.plugin.taskParser.hasTodoistId(line)) {
          continue;
        }
        const todoistId = this.plugin.taskParser.getTodoistIdFromLineText(line);
        if (!todoistId) {
          continue;
        }
        const normalizedId = todoistId.toString();
        if (!trackedIds.has(normalizedId)) {
          continue;
        }
        seenInFile.add(normalizedId);
        try {
          await this.lineModifiedTaskCheck(filepath, line, i, content);
          hasModifiedTask = true;
        } catch (error) {
          console.error("Error modifying task:", error);
          continue;
        }
      }
      if (this.plugin.settings.debugMode) {
        const missing = Array.from(trackedIds).filter((id) => !seenInFile.has(id));
        if (missing.length) {
          todoistBridgeDebugLog(`Skipped ${missing.length} tracked task(s) in ${filepath} because markers were not found in the note.`);
        }
      }
      if (hasModifiedTask && context && typeof context === "object" && file instanceof import_obsidian2.TFile) {
        try {
          context.content = await this.app.vault.read(file);
          context.file = file;
        } catch (error) {
          console.error("Error refreshing file content after modifications:", error);
        }
      } else if (context && typeof context === "object" && typeof currentFileValue === "string") {
        context.content = currentFileValue;
        if (!context.file && file) {
          context.file = file;
        }
      }
      return hasModifiedTask;
    } catch (error) {
      console.error("Error:", error);
      return false;
    }
  }
  // Close a task by calling API and updating JSON file
  async closeTask(taskId) {
    try {
      await this.plugin.todoistRestAPI.CloseTask(taskId);
      const completedAt = new Date().toISOString();
      await this.plugin.handleTaskCompletion(taskId, { source: "obsidian", completedAt });
      await this.plugin.saveSettings();
      // notification suppressed
    } catch (error) {
      console.error("Error closing task:", error);
      throw error;
    }
  }
  //open task
  async repoenTask(taskId) {
    try {
      await this.plugin.todoistRestAPI.OpenTask(taskId);
      await this.plugin.fileOperation.uncompleteTaskInTheFile(taskId);
      await this.plugin.cacheOperation.reopenTaskToCacheByID(taskId);
      this.plugin.saveSettings();    } catch (error) {
      console.error("Error opening task:", error);
      throw error;
    }
  }
  /**
   * Remove the tasks with the specified IDs from the list and update the JSON file
   * @param taskIds Array of task IDs to delete
   * @returns Array of task IDs that were deleted successfully
   */
  async deleteTasksByIds(taskIds) {
    const deletedTaskIds = [];
    for (const taskId of taskIds) {
      try {
        const response = await this.plugin.todoistRestAPI.DeleteTask(taskId);
        todoistBridgeDebugLog(`response is ${response}`);
        if (response) {
          // notification suppressed
          deletedTaskIds.push(taskId);
        }
      } catch (error) {
        console.error(`Failed to delete task ${taskId}: ${error}`);
      }
    }
    if (!deletedTaskIds.length) {
      todoistBridgeDebugLog("\u6CA1\u6709\u5220\u9664\u4EFB\u52A1");
      return [];
    }
    await this.plugin.cacheOperation.deleteTaskFromCacheByIDs(deletedTaskIds);
    this.plugin.saveSettings();
    return deletedTaskIds;
  }
  // Sync the status of completed tasks to the Obsidian file
  async syncCompletedTaskStatusToObsidian(unSynchronizedEvents) {
    const processedEvents = [];
    try {
      for (const e of unSynchronizedEvents) {
        const taskId = e.object_id != null ? String(e.object_id) : "";
        if (!taskId) {
          continue;
        }
        const handled = await this.plugin.handleTaskCompletion(taskId, { source: "todoist", completedAt: e.event_date });
        if (handled) {
          processedEvents.push(e);
        }
      }
      if (processedEvents.length) {
        await this.plugin.cacheOperation.appendEventsToCache(processedEvents);
        this.plugin.saveSettings();
      }
    } catch (error) {
      console.error("Error syncing task status:", error);
    }
    return processedEvents.length;
  }
  // Sync the status of uncompleted tasks to the Obsidian file
  async syncUncompletedTaskStatusToObsidian(unSynchronizedEvents) {
    const processedEvents = [];
    try {
      for (const e of unSynchronizedEvents) {
        const taskId = e.object_id != null ? String(e.object_id) : "";
        if (!taskId) {
          continue;
        }
        await this.plugin.fileOperation.uncompleteTaskInTheFile(taskId);
        const reopenedTask = await this.plugin.cacheOperation.reopenTaskToCacheByID(taskId);
        if (reopenedTask && reopenedTask.path) {
          this.plugin.cacheOperation.addTaskToMetadata(reopenedTask.path, taskId);
        }
        processedEvents.push(e);
      }
      if (processedEvents.length) {
        await this.plugin.cacheOperation.appendEventsToCache(processedEvents);
        this.plugin.saveSettings();
      }
    } catch (error) {
      console.error("Error syncing task status:", error);
    }
    return processedEvents.length;
  }
  async syncDeletedTasksToObsidian(unSynchronizedEvents) {
    let deletionCount = 0;
    try {
      if (!unSynchronizedEvents || !unSynchronizedEvents.length) {
        return deletionCount;
      }
      const processedEvents = [];
      for (const e of unSynchronizedEvents) {
        const rawTaskId = e.object_id;
        const taskId = rawTaskId != null ? String(rawTaskId) : "";
        if (!taskId) {
          continue;
        }
        let filepath = null;
        let cachedTask = null;
        try {
          cachedTask = await this.plugin.cacheOperation.loadTaskFromCacheyID(taskId);
          filepath = (cachedTask == null ? void 0 : cachedTask.path) || null;
        } catch (error) {
          console.error("Error loading deleted Todoist task from cache:", error);
        }
        let metadataSnapshot = null;
        if (!filepath) {
          try {
            metadataSnapshot = await this.plugin.cacheOperation.getFileMetadatas();
            for (const key in metadataSnapshot) {
              const value = metadataSnapshot[key];
              if (value && Array.isArray(value.todoistTasks) && value.todoistTasks.map(String).includes(taskId)) {
                filepath = key;
                break;
              }
            }
          } catch (error) {
            console.error("Error searching metadata for deleted task:", error);
          }
        }
        const trimTaskFromMetadata = async (targetPath) => {
          if (!targetPath) {
            return false;
          }
          try {
            const metadata = await this.plugin.cacheOperation.getFileMetadata(targetPath);
            if (!metadata || !Array.isArray(metadata.todoistTasks)) {
              return false;
            }
            const includesId = metadata.todoistTasks.map(String).includes(taskId);
            if (includesId) {
              this.plugin.cacheOperation.removeTaskFromMetadata(targetPath, taskId);
            }
            return includesId;
          } catch (error) {
            console.error("Error updating metadata for deleted task:", error);
            return false;
          }
        };
        let metadataHandled = filepath ? await trimTaskFromMetadata(filepath) : false;
        if (!metadataHandled) {
          try {
            const metadatas = metadataSnapshot || await this.plugin.cacheOperation.getFileMetadatas();
            for (const key in metadatas) {
              const value = metadatas[key];
              if (value && Array.isArray(value.todoistTasks) && value.todoistTasks.map(String).includes(taskId)) {
                metadataHandled = await trimTaskFromMetadata(key);
                if (metadataHandled) {
                  if (!filepath) {
                    filepath = key;
                  }
                  break;
                }
              }
            }
          } catch (error) {
            console.error("Error sweeping metadata for deleted task:", error);
          }
        }
        let markersRemoved = false;
        if (filepath) {
          try {
            const removed = await this.plugin.fileOperation.removeTodoistMarkersForTask(filepath, taskId);
            if (removed) {
              markersRemoved = true;
            }
          } catch (error) {
            console.error("Error removing Todoist markers for deleted task:", error);
          }
        }
        if (!markersRemoved) {
          try {
            const fallbackPath = await this.plugin.fileOperation.removeTodoistMarkersForTaskAnywhere(taskId);
            if (fallbackPath) {
              markersRemoved = true;
              if (!filepath) {
                filepath = fallbackPath;
              }
              if (!metadataHandled) {
                metadataHandled = await trimTaskFromMetadata(fallbackPath) || metadataHandled;
              }
            }
          } catch (error) {
            console.error("Error sweeping vault for deleted Todoist markers:", error);
          }
        }
        try {
          this.plugin.cacheOperation.deleteTaskFromCache(taskId);
        } catch (error) {
          console.error("Error deleting Todoist task from cache:", error);
        }
        if (markersRemoved || metadataHandled || cachedTask) {
          deletionCount++;
        }
        try {
          const timestamp = this.plugin.normalizeCompletionTimestamp();
          const taskForLog = cachedTask ? { ...cachedTask } : { id: taskId };
          if (!taskForLog.path) {
            taskForLog.path = filepath ?? "";
          }
          if (!("content" in taskForLog) || taskForLog.content == null) {
            taskForLog.content = "";
          }
          if (!("url" in taskForLog)) {
            taskForLog.url = null;
          }
          if (!("addedAt" in taskForLog)) {
            taskForLog.addedAt = null;
          }
          if (!("createdAt" in taskForLog)) {
            taskForLog.createdAt = null;
          }
          await this.plugin.logTaskActivity(taskForLog, { dateClosedIso: timestamp.iso, status: "deleted", statusSource: "todoist", source: "todoist" });
        } catch (error) {
          console.error("Error logging deleted Todoist task:", error);
        }
        processedEvents.push(e);
      }
      if (processedEvents.length) {
        await this.plugin.cacheOperation.appendEventsToCache(processedEvents);
        this.plugin.saveSettings();
      }
    } catch (error) {
      console.error("Error syncing deleted Todoist tasks:", error);
    }
    return deletionCount;
  }
  // Sync the status of updated items to Obsidian
  async syncUpdatedTaskToObsidian(unSynchronizedEvents) {
    let labelRemovals = 0;
    const processedEvents = [];
    try {
      for (const e of unSynchronizedEvents) {
        const taskId = e.object_id != null ? String(e.object_id) : "";
        if (!taskId) {
          continue;
        }
        const extra = e.extra_data || {};
        const labelCandidates = ["labels_removed", "removed_labels", "labels_lost", "labels_removed_names"];
        let triggeredRemoval = false;
        for (const key of labelCandidates) {
          const value = extra[key];
          if (Array.isArray(value) && value.some((label) => this.plugin.isTrackedTodoistLabel(label))) {
            triggeredRemoval = true;
            break;
          }
        }
        let remoteTask = null;
        if (!triggeredRemoval) {
          try {
            remoteTask = await this.plugin.todoistRestAPI.getTaskById(taskId);
            const hasTrackedLabel = Array.isArray(remoteTask == null ? void 0 : remoteTask.labels) && remoteTask.labels.some((label) => this.plugin.isTrackedTodoistLabel(label));
            if (!hasTrackedLabel) {
              triggeredRemoval = true;
            }
          } catch (error) {
            console.error("Error loading Todoist task while evaluating label removal:", error);
          }
        }
        if (triggeredRemoval) {
          const handled = await this.plugin.handleTodoistTagRemoval(taskId, remoteTask);
          if (handled) {
            labelRemovals++;
          }
          processedEvents.push(e);
          continue;
        }
        if (!(typeof extra.last_content === "undefined")) {
          await this.syncUpdatedTaskContentToObsidian(e);
        }
        processedEvents.push(e);
      }
      if (processedEvents.length) {
        await this.plugin.cacheOperation.appendEventsToCache(processedEvents);
        this.plugin.saveSettings();
      }
    } catch (error) {
      console.error("Error syncing updated item", error);
    }
    return { processed: processedEvents.length, labelRemovals };
  }
  async syncUpdatedTaskContentToObsidian(e) {
    this.plugin.fileOperation.syncUpdatedTaskContentToTheFile(e);
    const content = e.extra_data.content;
    this.plugin.cacheOperation.setTaskContentInCache(e.object_id, content);
  }
  async syncUpdatedTaskDueDateToObsidian(e) {
    this.plugin.fileOperation.syncUpdatedTaskDueDateToTheFile(e);
    const due = await this.plugin.todoistRestAPI.getTaskDueById(e.object_id);
    this.plugin.cacheOperation.modifyTaskToCacheByID(e.object_id, { due });
    // notification suppressed
  }
  // sync added task note to obsidian
  async syncAddedTaskNoteToObsidian(unSynchronizedEvents) {
    try {
      const processedEvents = [];
      for (const e of unSynchronizedEvents) {
        todoistBridgeDebugLog(e);
        await this.plugin.fileOperation.syncAddedTaskNoteToTheFile(e);
        // notification suppressed
        processedEvents.push(e);
      }
      await this.plugin.cacheOperation.appendEventsToCache(processedEvents);
      this.plugin.saveSettings();
    } catch (error) {
      console.error("\u540C\u6B65\u4EFB\u52A1\u72B6\u6001\u65F6\u51FA\u9519\uFF1A", error);
    }
  }
  async syncTodoistToObsidian(options = {}) {
    const summary = { completed: 0, deleted: 0, cached: 0 };
    const inboundSyncDisabled = this.plugin.settings.disableTodoistInboundSync === true;
    if (inboundSyncDisabled) {
      const cachedTasks = this.plugin.cacheOperation.loadTasksFromCache();
      summary.cached = Array.isArray(cachedTasks) ? cachedTasks.length : 0;
      return summary;
    }
    try {
      const all_activity_events = await this.plugin.todoistSyncAPI.getNonObsidianAllActivityEvents();
      const savedEvents = await this.plugin.cacheOperation.loadEventsFromCache();
      const savedEventKeys = new Set((savedEvents || []).map((event) => this.plugin.cacheOperation.getEventCacheKey(event)).filter((key) => !!key));
      const result1 = all_activity_events.filter(
        (objA) => !savedEventKeys.has(this.plugin.cacheOperation.getEventCacheKey(objA))
      );
      const savedTasks = await this.plugin.cacheOperation.loadTasksFromCache();
      const result2 = result1.filter(
        (objA) => savedTasks.some((objB) => String(objB == null ? void 0 : objB.id) === String(objA == null ? void 0 : objA.object_id))
      );
      const result3 = result1.filter(
        (objA) => savedTasks.some((objB) => String(objB == null ? void 0 : objB.id) === String(objA == null ? void 0 : objA.parent_item_id))
      );
      const unsynchronized_item_completed_events = this.plugin.todoistSyncAPI.filterActivityEvents(result2, { event_type: "completed", object_type: "item" });
      const unsynchronized_item_uncompleted_events = this.plugin.todoistSyncAPI.filterActivityEvents(result1, { event_type: "uncompleted", object_type: "item" });
      const unsynchronized_item_deleted_events = this.plugin.todoistSyncAPI.filterActivityEvents(result1, { event_type: "deleted", object_type: "item" });
      const unsynchronized_item_updated_events = this.plugin.todoistSyncAPI.filterActivityEvents(result2, { event_type: "updated", object_type: "item" });
      const unsynchronized_notes_added_events = this.plugin.todoistSyncAPI.filterActivityEvents(result3, { event_type: "added", object_type: "note" });
      const unsynchronized_project_events = this.plugin.todoistSyncAPI.filterActivityEvents(result1, { object_type: "project" });
      if (this.plugin.settings.debugMode) {
        todoistBridgeDebugLog(unsynchronized_item_completed_events);
        todoistBridgeDebugLog(unsynchronized_item_uncompleted_events);
        todoistBridgeDebugLog(unsynchronized_item_deleted_events);
        todoistBridgeDebugLog(unsynchronized_item_updated_events);
        todoistBridgeDebugLog(unsynchronized_project_events);
        todoistBridgeDebugLog(unsynchronized_notes_added_events);
      }
      summary.completed += await this.syncCompletedTaskStatusToObsidian(unsynchronized_item_completed_events);
      await this.syncUncompletedTaskStatusToObsidian(unsynchronized_item_uncompleted_events);
      summary.deleted += await this.syncDeletedTasksToObsidian(unsynchronized_item_deleted_events);
      const updateSummary = await this.syncUpdatedTaskToObsidian(unsynchronized_item_updated_events);
      if (updateSummary && typeof updateSummary.labelRemovals === "number") {
        summary.deleted += updateSummary.labelRemovals;
      }
      await this.syncAddedTaskNoteToObsidian(unsynchronized_notes_added_events);
      if (unsynchronized_project_events.length) {
        if (this.plugin.settings.debugMode) {
          todoistBridgeDebugLog("New project event");
        }
        await this.plugin.cacheOperation.saveProjectsToCache();
        await this.plugin.cacheOperation.appendEventsToCache(unsynchronized_project_events);
      }
      const shouldReconcile = options && options.interactive === true || this.plugin.shouldRunScheduledCompletionReconciliation();
      if (shouldReconcile) {
        summary.completed += await this.reconcileCompletedCachedTasks({ limit: options && options.interactive === true ? 100 : 25 });
      }
    } catch (err) {
      console.error("An error occurred while synchronizing:", err);
    }
    const cachedTasks = this.plugin.cacheOperation.loadTasksFromCache();
    summary.cached = Array.isArray(cachedTasks) ? cachedTasks.length : 0;
    return summary;
  }
  async reconcileCompletedCachedTasks(options = {}) {
    let completedCount = 0;
    const cachedTasks = Array.isArray(this.plugin.cacheOperation.loadTasksFromCache()) ? [...this.plugin.cacheOperation.loadTasksFromCache()] : [];
    const batch = import_sync_core.selectRollingReconciliationBatch({
      tasks: cachedTasks,
      cursor: this.plugin.reconcileCursor || 0,
      limit: options && options.limit || 25
    });
    this.plugin.reconcileCursor = batch.nextCursor;
    this.plugin.markRuntimeStateDirty();
    for (const task of batch.tasks) {
      if (!task || task.id == null || task.isCompleted === true) {
        continue;
      }
      const taskId = String(task.id);
      try {
        const remoteTask = await this.plugin.todoistRestAPI.getTaskById(taskId);
        const remoteCompleted = remoteTask && (remoteTask.checked === true || remoteTask.completed === true || remoteTask.isCompleted === true);
        if (remoteCompleted) {
          const completedAt = remoteTask.completed_at || remoteTask.completedAt || remoteTask.updated_at || remoteTask.updatedAt || new Date().toISOString();
          const handled = await this.plugin.handleTaskCompletion(taskId, { source: "todoist", completedAt });
          if (handled) {
            completedCount++;
          }
        } else if (remoteTask && Array.isArray(remoteTask.labels) && !remoteTask.labels.some((label) => this.plugin.isTrackedTodoistLabel(label))) {
          await this.plugin.handleTodoistTagRemoval(taskId, remoteTask);
        } else if (remoteTask && task.path) {
          const location = await this.plugin.fileOperation.findTaskLocationById(taskId, task.path);
          if (location && location.filepath !== task.path) {
            this.plugin.cacheOperation.canonicalizeTaskPath(taskId, task.path, location.filepath);
          }
        }
      } catch (error) {
        console.error("Todoist Bridge: reconciliation lookup failed", taskId, error);
      }
    }
    if (completedCount) {
      await this.plugin.saveSettings();
    }
    return completedCount;
  }
  async backupTodoistAllResources() {
    try {
      const resources = await this.plugin.todoistSyncAPI.getAllResources();
      const now = new Date();
      const timeString = `${now.getFullYear()}${now.getMonth() + 1}${now.getDate()}${now.getHours()}${now.getMinutes()}${now.getSeconds()}`;
      const name = "todoist-backup-" + timeString + ".json";
      this.app.vault.create(name, JSON.stringify(resources));
      // notification suppressed
    } catch (error) {
      console.error("An error occurred while creating Todoist backup:", error);
    }
  }
  //After renaming the file, check all tasks in the file and update all links.
  async updateTaskDescription(filepath) {
    const metadata = await this.plugin.cacheOperation.getFileMetadata(filepath);
    if (!metadata || !metadata.todoistTasks) {
      return;
    }
    const description = this.plugin.taskParser.getObsidianUrlFromFilepath(filepath);
    let updatedContent = {};
    updatedContent.description = description;
    try {
      metadata.todoistTasks.forEach(async (taskId) => {
        const updatedTask = await this.plugin.todoistRestAPI.UpdateTask(taskId, updatedContent);
        updatedTask.path = filepath;
        this.plugin.cacheOperation.updateTaskToCacheByID(updatedTask);
      });
    } catch (error) {
      console.error("An error occurred in updateTaskDescription:", error);
    }
  }
};
// src/modal.ts
// main.ts
var TodoistBridgeForObsidian = class extends import_obsidian4.Plugin {
  async onload() {
    const isSettingsLoaded = await this.loadSettings();
    if (!isSettingsLoaded) {
      return;
    }
    this.autoSyncIntervalId = void 0;
    this.addSettingTab(new TodoistBridgeSettingTab(this.app, this));
    await this.migrateLegacyTodoistTokenToSecretStorage();
    if (!this.getTodoistAPIToken()) {
    } else {
      await this.initializePlugin();
    }
    this.lastLines = /* @__PURE__ */ new Map();
    this.registerDomEvent(document, "keyup", async (evt) => {
      var _a, _b, _c;
      if (!this.settings.apiInitialized) {
        return;
      }
      if (!((_b = (_a = this.app.workspace.activeEditor) == null ? void 0 : _a.editor) == null ? void 0 : _b.hasFocus())) {
        if (this.settings.debugMode) {
          todoistBridgeDebugLog(`editor is not focused`);
        }
        return;
      }
      const view = this.app.workspace.getActiveViewOfType(import_obsidian4.MarkdownView);
      const editor = (_c = view == null ? void 0 : view.app.workspace.activeEditor) == null ? void 0 : _c.editor;
      if (evt.key === "ArrowUp" || evt.key === "ArrowDown" || evt.key === "ArrowLeft" || evt.key === "ArrowRight" || evt.key === "PageUp" || evt.key === "PageDown") {
        if (!this.checkModuleClass()) {
          return;
        }
        this.lineNumberCheck();
      }
      if (evt.key === "Delete" || evt.key === "Backspace") {
        try {
          if (!this.checkModuleClass()) {
            return;
          }
          if (!await this.checkAndHandleSyncLock())
            return;
          await this.todoistSync.deletedTaskCheck();
          this.syncLock = false;
          this.saveSettings();
        } catch (error) {
          console.error(`An error occurred while deleting tasks: ${error}`);
          this.syncLock = false;
        }
      }
    });
    this.registerDomEvent(document, "click", async (evt) => {
      var _a, _b, _c;
      if (!this.settings.apiInitialized) {
        return;
      }
      if ((_b = (_a = this.app.workspace.activeEditor) == null ? void 0 : _a.editor) == null ? void 0 : _b.hasFocus()) {
        const view = this.app.workspace.getActiveViewOfType(import_obsidian4.MarkdownView);
        const editor = (_c = this.app.workspace.activeEditor) == null ? void 0 : _c.editor;
        this.lineNumberCheck();
      } else {
      }
      const target = evt.target;
      if (target.type === "checkbox") {
        if (!this.checkModuleClass()) {
          return;
        }
        this.checkboxEventhandle(evt);
      }
    });
    this.registerEvent(this.app.workspace.on("editor-change", async (editor, view) => {
      try {
        if (!this.settings.apiInitialized) {
          return;
        }
        if (view && view.file && view.file.path) {
          this.markDirtyFile(view.file.path);
        }
        this.lineNumberCheck();
        if (!this.checkModuleClass()) {
          return;
        }
        if (!await this.checkAndHandleSyncLock())
          return;
        await this.todoistSync.lineContentNewTaskCheck(editor, view);
        this.syncLock = false;
        this.saveSettings();
      } catch (error) {
        console.error(`An error occurred while check new task in line: ${error.message}`);
        this.syncLock = false;
      }
    }));
    this.registerEvent(this.app.vault.on("rename", async (file, oldpath) => {
      if (!this.settings.apiInitialized) {
        return;
      }
      todoistBridgeDebugLog(`${oldpath} is renamed`);
      const frontMatter = await this.cacheOperation.getFileMetadata(oldpath);
      todoistBridgeDebugLog(frontMatter);
      if (frontMatter === null || frontMatter.todoistTasks === void 0) {
        return;
      }
      if (!this.checkModuleClass()) {
        return;
      }
      this.markDirtyFile(file.path);
      await this.cacheOperation.updateRenamedFilePath(oldpath, file.path);
      this.saveSettings();
      if (!await this.checkAndHandleSyncLock())
        return;
      try {
        await this.todoistSync.updateTaskDescription(file.path);
      } catch (error) {
        console.error("An error occurred in updateTaskDescription:", error);
      }
      this.syncLock = false;
    }));
    this.registerEvent(this.app.vault.on("modify", async (file) => {
      try {
        if (!this.settings.apiInitialized) {
          return;
        }
        const filepath = file.path;
        this.markDirtyFile(filepath);
        todoistBridgeDebugLog(`${filepath} is modified`);
        const activateFile = this.app.workspace.getActiveFile();
        todoistBridgeDebugLog(activateFile == null ? void 0 : activateFile.path);
        if ((activateFile == null ? void 0 : activateFile.path) == filepath) {
          return;
        }
        if (!await this.checkAndHandleSyncLock())
          return;
        await this.todoistSync.fullTextNewTaskCheck(filepath);
        this.syncLock = false;
      } catch (error) {
        console.error(`An error occurred while modifying the file: ${error.message}`);
        this.syncLock = false;
      }
    }));
    this.updateAutomaticSyncInterval();
    this.app.workspace.on("active-leaf-change", (leaf) => {
      this.setStatusBarText();
    });
    // Manual sync command to run the full synchronization immediately
    this.addCommand({
      id: "todoist-bridge-now",
      name: "Sync now (Todoist ⇄ Obsidian)",
      callback: async () => {
        try {
          if (!this.settings.apiInitialized) {

            return;
          }
          const includeTimestamp = this.settings.debugMode === true;
          const startTimestamp = includeTimestamp ? this.getTimestampStrings() : null;
          const startMessage = includeTimestamp ? `Todoist Bridge: Manual sync started (${startTimestamp.display})` : "Todoist Bridge: Manual sync started";
          new import_obsidian4.Notice(startMessage);
          const summary = await this.scheduledSynchronization({ interactive: true });
          if (summary) {
            const finishMessage = this.formatSyncSummary(summary);
            new import_obsidian4.Notice(finishMessage);
          }
        } catch (error) {
          console.error("Manual sync failed:", error);

        }
      }
    });
    // Remove the status bar item so nothing is shown in the footer
    this.statusBar = this.addStatusBarItem();
    try {
      // Detach from DOM to effectively hide it while keeping calls safe
      this.statusBar.remove && this.statusBar.remove();
    } catch (e) {
      // Fallback: hide via style if remove isn't available
      try { this.statusBar.style.display = "none"; } catch {}
    }
  }
  async onunload() {
    todoistBridgeDebugLog(`Todoist Bridge id unloaded!`);
    if (typeof this.autoSyncIntervalId !== "undefined") {
      window.clearInterval(this.autoSyncIntervalId);
      this.autoSyncIntervalId = void 0;
    }
    await this.saveSettings();
  }
  getTodoistAPIToken() {
    const secretName = this.settings && this.settings.todoistAPISecretName ? String(this.settings.todoistAPISecretName) : "";
    if (secretName) {
      const token = this.getSecretStorageValue(secretName);
      if (token) {
        return token;
      }
    }
    return "";
  }
  getSecretStorage() {
    return this.app && this.app.secretStorage ? this.app.secretStorage : null;
  }
  getSecretStorageValue(secretName) {
    const storage = this.getSecretStorage();
    if (!storage || !secretName) {
      return "";
    }
    if (typeof storage.getSecret === "function") {
      return storage.getSecret(secretName) || "";
    }
    if (typeof storage.get === "function") {
      return storage.get(secretName) || "";
    }
    return "";
  }
  setSecretStorageValue(secretName, secretValue) {
    const storage = this.getSecretStorage();
    if (!storage || !secretName || !secretValue) {
      return false;
    }
    if (typeof storage.setSecret === "function") {
      storage.setSecret(secretName, secretValue);
      return true;
    }
    if (typeof storage.set === "function") {
      storage.set(secretName, secretValue);
      return true;
    }
    return false;
  }
  async migrateLegacyTodoistTokenToSecretStorage() {
    const legacyToken = this.settings && this.settings["todoistAPIToken"] ? String(this.settings["todoistAPIToken"]) : "";
    if (!legacyToken || this.getTodoistAPIToken()) {
      return false;
    }
    const secretName = this.settings.todoistAPISecretName || "todoist-bridge-todoist-api-token";
    if (!this.setSecretStorageValue(secretName, legacyToken)) {
      return false;
    }
    this.settings.todoistAPISecretName = secretName;
    await this.saveSettings();
    return true;
  }
  getPluginId() {
    return this.manifest && this.manifest.id ? String(this.manifest.id) : "todoist-bridge";
  }
  getVaultConfigDir() {
    const vault = this.app && this.app.vault ? this.app.vault : null;
    if (!vault || typeof vault.configDir !== "string" || !vault.configDir.trim()) {
      return "";
    }
    return vault.configDir.trim().split(/[\\/]+/).filter(Boolean).join("/");
  }
  getAdapterPluginDirectoryPath() {
    const configDir = this.getVaultConfigDir();
    if (!configDir) {
      return "";
    }
    return `${configDir}/plugins/${this.getPluginId()}`;
  }
  getAdapterRuntimeStatePath() {
    const directory = this.getAdapterPluginDirectoryPath();
    return directory ? `${directory}/todoist-bridge-state.json` : "";
  }
  getAdapterActivityLogPath() {
    const directory = this.getAdapterPluginDirectoryPath();
    return directory ? `${directory}/todoist-completions.log` : "";
  }
  async loadSettings() {
    try {
      const data = await this.loadData() || {};
      this.settings = Object.assign({}, DEFAULT_SETTINGS, data);
      if (typeof this.settings.disableTodoistInboundSync !== "boolean") {
        this.settings.disableTodoistInboundSync = false;
      }
      setTodoistBridgeDebugLoggingEnabled(this.settings.debugMode === true);
      this.settings.apiInitialized = false;
      await this.initializeRuntimeStateFromDisk(data);
      return true;
    } catch (error) {
      console.error("Failed to load data:", error);
      return false;
    }
  }
  getPluginDataDirectory() {
    return null;
  }
  getAdapterRuntimeStateStore() {
    try {
      const adapter = this.app && this.app.vault ? this.app.vault.adapter : null;
      if (!adapter || typeof adapter.read !== "function" || typeof adapter.write !== "function") {
        return null;
      }
      const statePath = this.getAdapterRuntimeStatePath();
      if (!statePath) {
        return null;
      }
      return new import_bridge_state_store.AdapterBridgeStateStore({ adapter, statePath, writerDevice: "vault-adapter" });
    } catch (error) {
      console.error("Todoist Bridge: failed to create adapter runtime state store", error);
      return null;
    }
  }
  async ensureAdapterParentDirectory(adapter, filePath) {
    if (!adapter || typeof adapter.mkdir !== "function" || !filePath) {
      return;
    }
    const parts = String(filePath).split("/").slice(0, -1);
    let current = "";
    for (const part of parts) {
      if (!part) {
        continue;
      }
      current = current ? `${current}/${part}` : part;
      try {
        if (typeof adapter.exists === "function" && await adapter.exists(current)) {
          continue;
        }
        await adapter.mkdir(current);
      } catch {
      }
    }
  }
  async appendAdapterTextFile(filePath, text) {
    const adapter = this.app && this.app.vault ? this.app.vault.adapter : null;
    if (!adapter || typeof adapter.write !== "function" || !filePath) {
      return false;
    }
    await this.ensureAdapterParentDirectory(adapter, filePath);
    let existing = "";
    try {
      if (typeof adapter.exists !== "function" || await adapter.exists(filePath)) {
        if (typeof adapter.read === "function") {
          existing = await adapter.read(filePath);
        }
      }
    } catch {
      existing = "";
    }
    await adapter.write(filePath, `${existing || ""}${text}`);
    return true;
  }
  getRuntimeStateConflictFileName(filePath) {
    const value = String(filePath || "").split(/[\\/]+/).pop() || "";
    if (!value || value === "todoist-bridge-state.json") {
      return "";
    }
    if (!value.startsWith("todoist-bridge-state")) {
      return "";
    }
    if (!value.includes(".json")) {
      return "";
    }
    return value;
  }
  async findRuntimeStateConflictFiles() {
    const found = /* @__PURE__ */ new Set();
    const addCandidate = (filePath) => {
      const name = this.getRuntimeStateConflictFileName(filePath);
      if (name) {
        found.add(name);
      }
    };
    try {
      const adapter = this.app && this.app.vault ? this.app.vault.adapter : null;
      if (adapter && typeof adapter.list === "function") {
        const adapterDir = this.getAdapterPluginDirectoryPath();
        if (adapterDir) {
          const listed = await adapter.list(adapterDir);
          for (const filePath of Array.isArray(listed == null ? void 0 : listed.files) ? listed.files : []) {
            addCandidate(filePath);
          }
        }
      }
    } catch {
    }
    return Array.from(found.values()).sort();
  }
  getRuntimeStateWarnings(stateSnapshot, conflictFiles) {
    const warnings = [];
    if (Array.isArray(conflictFiles) && conflictFiles.length) {
      warnings.push("Possible synced runtime-state conflict files found. Run diagnostics/repair on desktop before trusting stale state.");
    }
    const writeTime = Date.parse((stateSnapshot == null ? void 0 : stateSnapshot.lastWriteAt) || (stateSnapshot == null ? void 0 : stateSnapshot.updatedAt) || "");
    if (!Number.isFinite(writeTime)) {
      warnings.push("Runtime state has no write timestamp.");
    } else {
      const ageMs = Date.now() - writeTime;
      if (ageMs > 7 * 24 * 60 * 60 * 1e3) {
        warnings.push("Runtime state has not been written in more than 7 days.");
      }
    }
    return warnings;
  }
  async initializeRuntimeStateFromDisk(legacyData = {}) {
    try {
      this.runtimeStateStore = this.getAdapterRuntimeStateStore();
      if (!this.runtimeStateStore) {
        this.settings.todoistTasksData = this.settings.todoistTasksData || { projects: [], tasks: [], events: [] };
        this.settings.fileMetadata = this.settings.fileMetadata || {};
        this.runtimeStateDirty = false;
        return;
      }
      let snapshot = await this.runtimeStateStore.snapshot();
      const hasLegacyRuntime = !!(legacyData && (legacyData.todoistTasksData || legacyData.fileMetadata));
      const stateEmpty = (!Array.isArray(snapshot.tasks) || snapshot.tasks.length === 0) && (!Array.isArray(snapshot.events) || snapshot.events.length === 0) && (!snapshot.fileMetadata || Object.keys(snapshot.fileMetadata).length === 0);
      if (hasLegacyRuntime && stateEmpty) {
        snapshot = await this.runtimeStateStore.migrateFromSettings(legacyData);
      }
      this.applyRuntimeStateSnapshot(snapshot);
    } catch (error) {
      console.error("Todoist Bridge: failed to initialize runtime state", error);
      this.settings.todoistTasksData = this.settings.todoistTasksData || { projects: [], tasks: [], events: [] };
      this.settings.fileMetadata = this.settings.fileMetadata || {};
      this.runtimeStateDirty = false;
    }
  }
  applyRuntimeStateSnapshot(snapshot) {
    const runtime = import_bridge_state_store.stateToLegacyRuntime(snapshot || {});
    this.settings.todoistTasksData = runtime.todoistTasksData;
    this.settings.fileMetadata = runtime.fileMetadata;
    this.settings.statistics = runtime.statistics;
    this.dirtyFiles = new Set(Array.isArray(snapshot == null ? void 0 : snapshot.dirtyFiles) ? snapshot.dirtyFiles : []);
    this.dirtyTaskIds = new Set(Array.isArray(snapshot == null ? void 0 : snapshot.dirtyTaskIds) ? snapshot.dirtyTaskIds : []);
    this.reconcileCursor = Number.isFinite(Number(snapshot == null ? void 0 : snapshot.reconcileCursor)) ? Number(snapshot.reconcileCursor) : 0;
    this.bridgeTombstones = Array.isArray(snapshot == null ? void 0 : snapshot.tombstones) ? snapshot.tombstones : [];
    this.runtimeStateLoadedSnapshot = {
      tasks: runtime.todoistTasksData.tasks,
      projects: runtime.todoistTasksData.projects,
      events: runtime.todoistTasksData.events,
      fileMetadata: runtime.fileMetadata,
      dirtyFiles: Array.from(this.dirtyFiles),
      dirtyTaskIds: Array.from(this.dirtyTaskIds),
      tombstones: this.bridgeTombstones,
      reconcileCursor: this.reconcileCursor,
      revision: snapshot && snapshot.revision || 0
    };
    this.runtimeStateRevision = snapshot && snapshot.revision || 0;
    this.runtimeStateDirty = false;
  }
  markRuntimeStateDirty() {
    this.runtimeStateDirty = true;
  }
  markDirtyFile(filepath) {
    if (!filepath) {
      return;
    }
    if (!this.dirtyFiles) {
      this.dirtyFiles = /* @__PURE__ */ new Set();
    }
    this.dirtyFiles.add(String(filepath));
    this.markRuntimeStateDirty();
  }
  recordBridgeTombstone(taskId, path, reason, source) {
    const currentState = Object.assign(import_bridge_state_store.stateFromSettings(this.settings || {}), this.getRuntimeStateExtras());
    const nextState = import_bridge_state_store.addRuntimeTombstone(currentState, {
      taskId,
      path,
      reason,
      source,
      createdAt: new Date().toISOString()
    });
    const runtime = import_bridge_state_store.stateToLegacyRuntime(nextState);
    this.settings.todoistTasksData = runtime.todoistTasksData;
    this.settings.fileMetadata = runtime.fileMetadata;
    this.settings.statistics = runtime.statistics;
    this.bridgeTombstones = nextState.tombstones;
    this.markRuntimeStateDirty();
  }
  selectFilesForScheduledSync({ interactive = false } = {}) {
    const activeFile = this.app && this.app.workspace && this.app.workspace.getActiveFile ? this.app.workspace.getActiveFile() : null;
    const activePath = activeFile && activeFile.path ? activeFile.path : null;
    const result = import_sync_core.selectDirtyFilesForSync({
      dirtyFiles: Array.from(this.dirtyFiles || []),
      activeFile: interactive ? activePath : null,
      maxFiles: interactive ? 75 : 25
    });
    this.dirtyFiles = new Set(result.remainingDirtyFiles);
    this.markRuntimeStateDirty();
    return result.files;
  }
  getRuntimeStateExtras() {
    return {
      dirtyFiles: Array.from(this.dirtyFiles || []),
      dirtyTaskIds: Array.from(this.dirtyTaskIds || []),
      tombstones: Array.isArray(this.bridgeTombstones) ? this.bridgeTombstones : [],
      reconcileCursor: Number.isFinite(Number(this.reconcileCursor)) ? Number(this.reconcileCursor) : 0
    };
  }
  async saveRuntimeStateIfDirty() {
    if (!this.runtimeStateStore || !this.runtimeStateDirty) {
      return;
    }
    try {
      const localState = Object.assign(import_bridge_state_store.stateFromSettings(this.settings || {}), this.getRuntimeStateExtras());
      const currentSnapshot = await this.runtimeStateStore.snapshot();
      const externalChanged = this.runtimeStateRevision != null && currentSnapshot.revision !== this.runtimeStateRevision;
      const stateToSave = externalChanged ? import_bridge_state_store.mergeRuntimeStates({
        loaded: this.runtimeStateLoadedSnapshot,
        current: currentSnapshot,
        local: localState
      }) : localState;
      const savedSnapshot = await this.runtimeStateStore.replaceState(stateToSave, { reason: "runtime-save" });
      this.applyRuntimeStateSnapshot(savedSnapshot);
    } catch (error) {
      console.error("Todoist Bridge: failed to save runtime state", error);
    }
  }
  async saveSettings() {
    try {
      if (this.settings && Object.keys(this.settings).length > 0) {
        await this.saveRuntimeStateIfDirty();
        const currentData = await this.loadData().catch(() => ({})) || {};
        const persisted = Object.assign({}, import_bridge_state_store.settingsWithoutRuntimeState(currentData), this.prepareSettingsForSave());
        await this.saveData(persisted);
      } else {
        console.error("Settings are empty or invalid, not saving to avoid data loss.");
      }
    } catch (error) {
      console.error("Error saving settings:", error);
    }
  }
  pruneFileMetadataReferences() {
    try {
      const metadatas = this.settings?.fileMetadata;
      if (!metadatas || typeof metadatas !== "object") {
        return;
      }
      const taskList = this.settings?.todoistTasksData?.tasks;
      const activeIds = new Set(Array.isArray(taskList) ? taskList.map((task) => {
        if (!task) {
          return null;
        }
        if (typeof task === "string" || typeof task === "number") {
          return String(task);
        }
        if (typeof task === "object" && task.id != null) {
          return String(task.id);
        }
        return null;
      }).filter((id) => typeof id === "string") : []);
      let mutated = false;
      for (const key of Object.keys(metadatas)) {
        const metadata = metadatas[key];
        if (!metadata || typeof metadata !== "object") {
          continue;
        }
        const originalTasks = Array.isArray(metadata.todoistTasks) ? metadata.todoistTasks : [];
        const filteredTasks = originalTasks.filter((id) => activeIds.has(String(id)));
        if (filteredTasks.length !== originalTasks.length) {
          metadata.todoistTasks = filteredTasks;
          mutated = true;
        } else if (!Array.isArray(metadata.todoistTasks)) {
          metadata.todoistTasks = filteredTasks;
        }
        const count = filteredTasks.length;
        if (metadata.todoistCount !== count) {
          metadata.todoistCount = count;
          mutated = true;
        }
        metadatas[key] = metadata;
      }
      if (mutated) {
        this.settings.fileMetadata = metadatas;
      }
    } catch (error) {
      console.error("Error pruning Todoist metadata references:", error);
    }
  }
  updateAutomaticSyncInterval() {
    if (typeof this.autoSyncIntervalId !== "undefined") {
      window.clearInterval(this.autoSyncIntervalId);
      this.autoSyncIntervalId = void 0;
    }
    if (!this.settings || !this.settings.automaticSynchronizationEnabled) {
      return;
    }
    const intervalSeconds = Number(this.settings.automaticSynchronizationInterval);
    if (!Number.isFinite(intervalSeconds) || intervalSeconds < 20) {
      return;
    }
    this.autoSyncIntervalId = this.registerInterval(window.setInterval(async () => await this.scheduledSynchronization(), intervalSeconds * 1e3));
  }
  prepareSettingsForSave() {
    return import_bridge_state_store.settingsWithoutRuntimeState(this.settings || {});
  }
  sanitizeTasksForPersistence(tasks) {
    if (!Array.isArray(tasks)) {
      return [];
    }
    const seen = new Set();
    const active = [];
    for (const task of tasks) {
      if (!task) {
        continue;
      }
      const id = task.id != null ? String(task.id) : null;
      if (!id || seen.has(id)) {
        continue;
      }
      if (task.isCompleted) {
        continue;
      }
      if (!task.path) {
        continue;
      }
      seen.add(id);
      active.push({ ...task, id });
    }
    return active;
  }
  sanitizeEventsForPersistence(events) {
    if (!Array.isArray(events)) {
      return [];
    }
    const seen = new Set();
    const persisted = [];
    for (const event of events) {
      if (!event) {
        continue;
      }
      const eventKey = this.cacheOperation && typeof this.cacheOperation.getEventCacheKey === "function" ? this.cacheOperation.getEventCacheKey(event) : event.eventKey || "";
      if (!eventKey || seen.has(eventKey)) {
        continue;
      }
      seen.add(eventKey);
      persisted.push({
        id: event.id != null ? String(event.id) : eventKey,
        eventKey,
        event_type: event.event_type ?? null,
        object_type: event.object_type ?? null,
        object_id: event.object_id != null ? String(event.object_id) : null,
        parent_item_id: event.parent_item_id != null ? String(event.parent_item_id) : null,
        event_date: event.event_date ?? null
      });
    }
    return persisted.slice(-1e3);
  }
  shouldRunScheduledCompletionReconciliation() {
    const now = Date.now();
    if (!this.lastCompletionReconciliationAt || now - this.lastCompletionReconciliationAt > 60 * 60 * 1e3) {
      this.lastCompletionReconciliationAt = now;
      return true;
    }
    return false;
  }
  getTimestampStrings(timeZone = "America/Sao_Paulo") {
    const now = new Date();
    const formatter = new Intl.DateTimeFormat("en-CA", {
      timeZone,
      year: "numeric",
      month: "2-digit",
      day: "2-digit",
      hour: "2-digit",
      minute: "2-digit",
      second: "2-digit",
      hour12: false
    });
    const parts = formatter.formatToParts(now);
    const part = (type) => {
      const match = parts.find((p) => p.type === type);
      return match ? match.value : "";
    };
    const dateSegment = `${part("year")}-${part("month")}-${part("day")}`;
    const timeSegment = `${part("hour")}:${part("minute")}:${part("second")}`;
    return {
      display: `${dateSegment} ${timeSegment}`,
      filename: `${dateSegment}_${part("hour")}-${part("minute")}-${part("second")}`
    };
  }
  normalizeCompletionTimestamp(value) {
    let date = value ? new Date(value) : new Date();
    if (isNaN(date.getTime())) {
      date = new Date();
    }
    const iso = date.toISOString();
    return { iso, date: iso.slice(0, 10) };
  }
  formatSyncSummary(summary) {
    const metrics = summary || {};
    const completed = Number(metrics.completed ?? 0);
    const deleted = Number(metrics.deleted ?? 0);
    const cached = Number(metrics.cached ?? 0);
    const includeTimestamp = this.settings?.debugMode === true;
    const header = includeTimestamp ? `Todoist Bridge: Manual sync finished (${this.getTimestampStrings().display})` : "Todoist Bridge: Manual sync finished";
    return [
      header,
      `${completed} tasks completed`,
      `${deleted} tasks detached`,
      `${cached} tasks on cache`
    ].join("\n");
  }
  async logTaskActivity(task, context = {}) {
    const status = (context == null ? void 0 : context.status) ?? "completed";
    const statusSource = (context == null ? void 0 : context.statusSource) ?? ((context == null ? void 0 : context.source) ?? "todoist");
    const source = (context == null ? void 0 : context.source) ?? statusSource;
    const eventLoggedIso = (context == null ? void 0 : context.eventLoggedIso) ?? new Date().toISOString();
    const dateClosedIso = (context == null ? void 0 : context.dateClosedIso) ?? ((context == null ? void 0 : context.iso) ?? ((status === "completed" || status === "deleted") ? eventLoggedIso : null));
    const entry = {
      taskId: (task == null ? void 0 : task.id) ?? null,
      taskName: (task == null ? void 0 : task.content) ?? "",
      filePath: (task == null ? void 0 : task.path) ?? "",
      todoistUrl: (task == null ? void 0 : task.url) ?? null,
      dateOpenedIso: (task == null ? void 0 : task.addedAt) ?? (task == null ? void 0 : task.createdAt) ?? null,
      status,
      statusSource,
      source,
      dateClosedIso,
      eventLoggedIso
    };
    const line = JSON.stringify(entry) + "\n";
    try {
      const adapterLogPath = this.getAdapterActivityLogPath();
      if (adapterLogPath && await this.appendAdapterTextFile(adapterLogPath, line)) {
        return;
      }
    } catch (adapterError) {
      console.error("Todoist Bridge: failed to write adapter activity log", adapterError);
      return;
    }
  }
  async handleTaskCompletion(taskId, options = {}) {
    try {
      let cachedTask = await this.cacheOperation.loadTaskFromCacheyID(taskId);
      let fallbackPath = null;
      if (!cachedTask) {
        try {
          const remoteTask = await this.todoistRestAPI.getTaskById(taskId);
          if (remoteTask) {
            cachedTask = { ...remoteTask };
          }
        } catch (error) {
          console.error("Todoist Bridge: unable to fetch task for completion", error);
        }
      }
      if (!cachedTask) {
        fallbackPath = await this.fileOperation.searchFilepathsByTaskidInVault(taskId);
        if (fallbackPath) {
          cachedTask = { id: taskId, content: "", path: fallbackPath };
        } else {
          return false;
        }
      }
      const timestamp = this.normalizeCompletionTimestamp(options.completedAt);
      const preferredPath = (cachedTask == null ? void 0 : cachedTask.path) || fallbackPath || null;
      let completionResult = { found: false, modified: false, filepath: preferredPath };
      try {
        completionResult = await this.fileOperation.completeTaskInTheFile(taskId, timestamp.iso, { metadataKey: "todoist_completion", displayDate: timestamp.date, filepath: preferredPath });
      } catch (error) {
        console.error("Todoist Bridge: failed to update completion state in note", error);
      }
      if (!completionResult || completionResult.found !== true) {
        console.warn(`Todoist Bridge: completion for ${taskId} was not applied because the note line was not found.`);
        return false;
      }
      const completedPath = completionResult.filepath || preferredPath;
      try {
        this.cacheOperation.closeTaskToCacheByID(taskId);
        if (cachedTask.path) {
          this.cacheOperation.removeTaskFromMetadata(cachedTask.path, taskId);
        }
        if (completedPath && completedPath !== cachedTask.path) {
          this.cacheOperation.removeTaskFromMetadata(completedPath, taskId);
        }
      } catch (error) {
        console.error("Todoist Bridge: failed to update cache after completion", error);
      }
      if (completedPath) {
        cachedTask.path = completedPath;
      }
      await this.logTaskActivity(cachedTask, { dateClosedIso: timestamp.iso, status: "completed", statusSource: options.source ?? "todoist", source: options.source ?? "todoist" });
      this.saveSettings();
      return true;
    } catch (error) {
      console.error("Todoist Bridge: unexpected error while handling completion", error);
      return false;
    }
  }
  async detachRemoteBridge(taskId, cachedTask = null) {
    try {
      if (!taskId || !this.todoistRestAPI || typeof this.todoistRestAPI.RemoveBridgeLabels !== "function") {
        return false;
      }
      let labels = null;
      if (typeof this.todoistRestAPI.getTaskById === "function") {
        let remoteTask = null;
        try {
          remoteTask = await this.todoistRestAPI.getTaskById(taskId);
        } catch (error) {
          const message = error && error.message ? error.message : String(error);
          if (/404|not found/i.test(message)) {
            return true;
          }
          console.error("Todoist Bridge: unable to load remote labels for detach", error);
        }
        labels = Array.isArray(remoteTask == null ? void 0 : remoteTask.labels) ? remoteTask.labels : [];
      }
      if (!Array.isArray(labels)) {
        labels = Array.isArray(cachedTask == null ? void 0 : cachedTask.labels) ? cachedTask.labels : [];
      }
      await this.todoistRestAPI.RemoveBridgeLabels(taskId, labels);
      return true;
    } catch (error) {
      console.error("Todoist Bridge: failed to remove bridge labels from Todoist task", error);
      return false;
    }
  }
  async handleTodoistTagRemoval(taskId, remoteTask) {
    let removalHandled = false;
    let tombstonePath = "";
    try {
      const cachedTask = await this.cacheOperation.loadTaskFromCacheyID(taskId);
      const taskRecord = cachedTask || remoteTask || null;
      let filepath = (taskRecord == null ? void 0 : taskRecord.path) || null;
      if (!filepath && cachedTask && cachedTask.path) {
        filepath = cachedTask.path;
      }
      if (filepath) {
        tombstonePath = filepath;
        try {
          const markersRemoved = await this.fileOperation.removeTodoistMarkersForTask(filepath, taskId);
          if (markersRemoved) {
            removalHandled = true;
          }
        } catch (error) {
          console.error("Todoist Bridge: failed to remove markers after tag removal", error);
        }
        try {
          this.cacheOperation.removeTaskFromMetadata(filepath, taskId);
          removalHandled = true;
        } catch (error) {
          console.error("Todoist Bridge: failed to update metadata after tag removal", error);
        }
      } else {
        try {
          const fallbackPath = await this.fileOperation.removeTodoistMarkersForTaskAnywhere(taskId);
          if (fallbackPath) {
            tombstonePath = fallbackPath;
            removalHandled = true;
            try {
              this.cacheOperation.removeTaskFromMetadata(fallbackPath, taskId);
            } catch (error) {
              console.error("Todoist Bridge: failed to update metadata for fallback path", error);
            }
          }
        } catch (error) {
          console.error("Todoist Bridge: failed to scan vault for tag removal", error);
        }
      }
      try {
        this.cacheOperation.deleteTaskFromCache(taskId);
        this.recordBridgeTombstone(taskId, tombstonePath || filepath || "", "todoist-label-removed", "todoist");
        removalHandled = true;
      } catch (error) {
        console.error("Todoist Bridge: failed to drop task from cache after tag removal", error);
      }
    } catch (error) {
      console.error("Todoist Bridge: unexpected error while handling tag removal", error);
    }
    return removalHandled;
  }
  isTrackedTodoistLabel(label) {
    if (!label) {
      return false;
    }
    const normalized = label.toString().toLowerCase();
    return normalized === "todoist" || normalized === "obsidian" || normalized === "task";
  }
  async scanUncheckedBridgeTasksForRepair() {
    const candidates = [];
    const markdownFiles = this.app.vault.getMarkdownFiles ? this.app.vault.getMarkdownFiles() : [];
    for (const file of markdownFiles) {
      let content = "";
      try {
        content = await this.app.vault.cachedRead(file);
      } catch (error) {
        console.error("Todoist Bridge: failed to read file during bridge audit", file.path, error);
        continue;
      }
      const lines = String(content || "").split("\n");
      for (let i = 0; i < lines.length; i++) {
        const line = lines[i];
        if (!import_repair_core.isUncheckedTodoistTaskLine(line)) {
          continue;
        }
        candidates.push({
          file,
          path: file.path,
          lineIndex: i,
          lineNumber: i + 1,
          line,
          taskId: import_repair_core.getTodoistIdFromLine(line),
          linkTaskId: import_repair_core.getTodoistLinkTaskId(line)
        });
      }
    }
    return candidates;
  }
  async fetchTodoistTaskForRepair(taskId) {
    try {
      return await this.todoistRestAPI.getTaskById(taskId);
    } catch (error) {
      const message = error && error.message ? error.message : String(error);
      if (/404|not found/i.test(message)) {
        return { __missing: true };
      }
      return { __error: message };
    }
  }
  async writeBridgeRepairReport(result) {
    const timestamp = this.getTimestampStrings("America/Sao_Paulo");
    const mode = result.apply ? "Apply" : "Dry Run";
    const lines = [
      `# Todoist Bridge Repair - ${mode} (${timestamp.display})`,
      "",
      "## Summary",
      `- Unchecked bridged tasks scanned: ${result.scanned}`,
      `- Verified Todoist completions: ${result.toComplete.length}`,
      `- Verified open tasks retained in bridge state: ${result.openRecords.length}`,
      `- Broken or unverifiable links: ${result.broken.length}`,
      `- Legacy numeric links kept local-only: ${result.legacyNumeric.length}`,
      `- URL/id mismatches: ${result.urlMismatches.length}`,
      `- Stale cached paths: ${result.stalePaths.length}`,
      `- Files changed: ${result.changedFiles.length}`,
      ""
    ];
    const addSection = (title, rows, render) => {
      lines.push(`## ${title}`);
      if (!rows.length) {
        lines.push("- None", "");
        return;
      }
      for (const row of rows) {
        lines.push(render(row));
      }
      lines.push("");
    };
    addSection("Verified Completions", result.toComplete, (row) => `- ${row.path}:${row.lineNumber} \`${row.taskId}\` - ${(row.remoteTask && (row.remoteTask.completed_at || row.remoteTask.completedAt || row.remoteTask.updated_at || row.remoteTask.updatedAt)) || "completed date unavailable"}`);
    addSection("Legacy Numeric Links", result.legacyNumeric, (row) => `- ${row.path}:${row.lineNumber} \`${row.taskId}\` - ${row.reason}`);
    addSection("Broken or Unverifiable Links", result.broken, (row) => `- ${row.path}:${row.lineNumber} \`${row.taskId}\` - ${row.reason}`);
    addSection("URL/id Mismatches", result.urlMismatches, (row) => `- ${row.path}:${row.lineNumber} \`${row.taskId}\` link points to \`${row.linkTaskId}\``);
    addSection("Stale Cached Paths", result.stalePaths, (row) => `- \`${row.taskId}\` cache: \`${row.cachedPath}\` -> note: \`${row.path}\``);
    const reportContent = lines.join("\n");
    let reportPath = `todoist_bridge_repair_${timestamp.filename}${result.apply ? "_applied" : "_dry_run"}.md`;
    let suffix = 1;
    while (await this.app.vault.adapter.exists(reportPath)) {
      reportPath = `todoist_bridge_repair_${timestamp.filename}${result.apply ? "_applied" : "_dry_run"}_${suffix}.md`;
      suffix++;
    }
    await this.app.vault.create(reportPath, reportContent);
    return reportPath;
  }
  async runBridgeAudit() {
    return this.runBridgeRepair({ apply: false });
  }
  async runBridgeRepairCommand() {
    return this.runBridgeRepair({ apply: true });
  }
  async runBridgeRepair(options = {}) {
    if (!this.checkModuleClass()) {
      new import_obsidian4.Notice("Todoist Bridge: API not initialized");
      return null;
    }
    const apply = options.apply === true;
    const cachedById = /* @__PURE__ */ new Map();
    for (const task of Array.isArray(this.cacheOperation.loadTasksFromCache()) ? this.cacheOperation.loadTasksFromCache() : []) {
      if (task && task.id != null) {
        cachedById.set(String(task.id), task);
      }
    }
    const candidates = await this.scanUncheckedBridgeTasksForRepair();
    const result = {
      apply,
      scanned: candidates.length,
      toComplete: [],
      openRecords: [],
      broken: [],
      legacyNumeric: [],
      urlMismatches: [],
      stalePaths: [],
      changedFiles: []
    };
    const contentByFile = /* @__PURE__ */ new Map();
    for (const candidate of candidates) {
      const cachedTask = cachedById.get(candidate.taskId) || null;
      const remoteTask = import_repair_core.isLegacyNumericTodoistId(candidate.taskId) ? null : await this.fetchTodoistTaskForRepair(candidate.taskId);
      const classification = import_repair_core.classifyBridgeLine({ ...candidate, remoteTask, cachedTask });
      if (candidate.linkTaskId && candidate.linkTaskId !== candidate.taskId) {
        result.urlMismatches.push(candidate);
      }
      if (cachedTask && cachedTask.path && cachedTask.path !== candidate.path) {
        result.stalePaths.push({ ...candidate, cachedPath: cachedTask.path });
      }
      if (classification.shouldComplete) {
        result.toComplete.push({ ...candidate, remoteTask, cachedTask });
        if (apply) {
          if (!contentByFile.has(candidate.path)) {
            const content = await this.app.vault.cachedRead(candidate.file);
            contentByFile.set(candidate.path, { file: candidate.file, lines: String(content || "").split("\n") });
          }
          const context = contentByFile.get(candidate.path);
          const completedAt = remoteTask.completed_at || remoteTask.completedAt || remoteTask.updated_at || remoteTask.updatedAt || new Date().toISOString();
          context.lines[candidate.lineIndex] = import_repair_core.normalizeCompletionLine(context.lines[candidate.lineIndex], candidate.taskId, completedAt);
        }
        continue;
      }
      if (classification.legacyNumeric) {
        result.legacyNumeric.push({ ...candidate, remoteTask, cachedTask, reason: classification.issues.join("; ") });
        continue;
      }
      if (import_repair_core.isRemoteOpen(remoteTask)) {
        result.openRecords.push({
          path: candidate.path,
          task: import_repair_core.normalizeRemoteOpenTask(remoteTask, candidate, cachedTask)
        });
      } else {
        const reason = classification.issues.length ? classification.issues.join("; ") : "not verified open or complete";
        result.broken.push({ ...candidate, reason });
      }
    }
    if (apply) {
      for (const [filepath, context] of contentByFile.entries()) {
        await this.app.vault.modify(context.file, context.lines.join("\n"));
        result.changedFiles.push(filepath);
      }
      if (!this.runtimeStateStore) {
        await this.initializeRuntimeStateFromDisk({});
      }
      if (this.runtimeStateStore) {
        const snapshot = await this.runtimeStateStore.rebuildFromOpenRecords(result.openRecords);
        this.applyRuntimeStateSnapshot(snapshot);
      } else {
        const rebuilt = import_bridge_state_store.buildStateFromOpenRecords(result.openRecords, import_bridge_state_store.stateFromSettings(this.settings || {}));
        const runtime = import_bridge_state_store.stateToLegacyRuntime(rebuilt);
        this.settings.todoistTasksData = runtime.todoistTasksData;
        this.settings.fileMetadata = runtime.fileMetadata;
      }
      await this.saveSettings();
    }
    const reportPath = await this.writeBridgeRepairReport(result);
    const notice = apply ? `Todoist Bridge: repair applied (${result.toComplete.length} completed, ${result.broken.length} broken)` : `Todoist Bridge: audit complete (${result.toComplete.length} completions, ${result.broken.length} broken)`;
    new import_obsidian4.Notice(notice);
    todoistBridgeDebugLog(`Todoist Bridge: ${apply ? "repair" : "audit"} report written to ${reportPath}`);
    return result;
  }
  async runDiagnosticsExport() {
    const timestamp = this.getTimestampStrings("America/Sao_Paulo");
    const stateSnapshot = this.runtimeStateStore ? await this.runtimeStateStore.snapshot() : import_bridge_state_store.stateFromSettings(this.settings || {});
    const conflictFiles = await this.findRuntimeStateConflictFiles();
    const runtimeWarnings = this.getRuntimeStateWarnings(stateSnapshot, conflictFiles);
    let completionLogLines = 0;
    try {
      const adapter = this.app && this.app.vault ? this.app.vault.adapter : null;
      const logPath = this.getAdapterActivityLogPath();
      if (adapter && typeof adapter.read === "function" && logPath && (typeof adapter.exists !== "function" || await adapter.exists(logPath))) {
        completionLogLines = String(await adapter.read(logPath)).split("\n").filter((line) => line.trim()).length;
      }
    } catch (error) {
      console.error("Todoist Bridge: failed to read diagnostics log count", error);
    }
    const sanitizedSettings = import_bridge_state_store.settingsWithoutRuntimeState(this.settings || {});
    if (sanitizedSettings.todoistAPIToken) {
      sanitizedSettings.todoistAPIToken = "[redacted]";
    }
    const lines = [
      `# Todoist Bridge Diagnostics (${timestamp.display})`,
      "",
      "## Settings",
      "```json",
      JSON.stringify(sanitizedSettings, null, 2),
      "```",
      "",
      "## Runtime State",
      `- Backend: ${stateSnapshot.backend || "memory"}`,
      `- Revision: ${stateSnapshot.revision ?? "n/a"}`,
      `- Tasks: ${Array.isArray(stateSnapshot.tasks) ? stateSnapshot.tasks.length : 0}`,
      `- Projects: ${Array.isArray(stateSnapshot.projects) ? stateSnapshot.projects.length : 0}`,
      `- Events: ${Array.isArray(stateSnapshot.events) ? stateSnapshot.events.length : 0}`,
      `- File metadata entries: ${stateSnapshot.fileMetadata ? Object.keys(stateSnapshot.fileMetadata).length : 0}`,
      `- Dirty files: ${Array.isArray(stateSnapshot.dirtyFiles) ? stateSnapshot.dirtyFiles.length : 0}`,
      `- Dirty task IDs: ${Array.isArray(stateSnapshot.dirtyTaskIds) ? stateSnapshot.dirtyTaskIds.length : 0}`,
      `- Reconciliation cursor: ${stateSnapshot.reconcileCursor ?? "n/a"}`,
      `- Tombstones: ${Array.isArray(stateSnapshot.tombstones) ? stateSnapshot.tombstones.length : 0}`,
      `- Last writer: ${stateSnapshot.lastWriterDevice || "n/a"}`,
      `- Last write reason: ${stateSnapshot.lastWriteReason || "n/a"}`,
      `- Last write at: ${stateSnapshot.lastWriteAt || "n/a"}`,
      `- State conflict files: ${conflictFiles.length}`,
      `- Completion log lines: ${completionLogLines}`,
      ""
    ];
    if (runtimeWarnings.length || conflictFiles.length) {
      lines.push("## Runtime Warnings");
      if (!runtimeWarnings.length) {
        lines.push("- None");
      } else {
        for (const warning of runtimeWarnings) {
          lines.push(`- ${warning}`);
        }
      }
      if (conflictFiles.length) {
        lines.push("");
        lines.push("## State Conflict Files");
        for (const fileName of conflictFiles) {
          lines.push(`- ${fileName}`);
        }
      }
      lines.push("");
    }
    let reportPath = `todoist_bridge_diagnostics_${timestamp.filename}.md`;
    let suffix = 1;
    while (await this.app.vault.adapter.exists(reportPath)) {
      reportPath = `todoist_bridge_diagnostics_${timestamp.filename}_${suffix}.md`;
      suffix++;
    }
    await this.app.vault.create(reportPath, lines.join("\n"));
    new import_obsidian4.Notice(`Todoist Bridge: diagnostics exported to ${reportPath}`);
    return reportPath;
  }
  async runDatabaseCheck() {
    if (!this.checkModuleClass()) {
      new import_obsidian4.Notice("Todoist Bridge: API not initialized");
      return;
    }
    const timestamp = this.getTimestampStrings("America/Sao_Paulo");
    todoistBridgeDebugLog(`Check Database: started ${timestamp.display}`);
    const orphaned = [];
    const localOnly = [];
    const dataIssues = [];
    const orphanCandidates = [];
    const invalidMarkers = [];
    const localTasksById = new Map();
    const normalizeWhitespace = (value) => value ? value.replace(/\s+/g, " " ).trim() : "";
    const isUncheckedTask = (line) => /^\s*[-*]\s+\[\s\]/.test(line);
    const isCheckedTask = (line) => /^\s*[-*]\s+\[(x|X)\]/.test(line);
    const markdownFiles = this.app.vault.getMarkdownFiles ? this.app.vault.getMarkdownFiles() : [];
    for (const file of markdownFiles) {
      let content;
      try {
        content = await this.app.vault.cachedRead(file);
      } catch (error) {
        console.error("Todoist Bridge: failed to read file during database check", file.path, error);
        continue;
      }
      if (typeof content !== "string") {
        continue;
      }
      const lines = content.split("\n");
      for (let i = 0; i < lines.length; i++) {
        const line = lines[i];
        const lineNumber = i + 1;
        const hasBridgeSpan = line.includes('<span class="todoist-bridge">');
        let todoistId = "";
        if (hasBridgeSpan) {
          try {
            todoistId = (this.taskParser.getTodoistIdFromLineText(line) || "").trim();
          } catch {
            todoistId = "";
          }
        }
        if (hasBridgeSpan && !todoistId) {
          invalidMarkers.push({ path: file.path, lineNumber, lineText: line });
        }
        const unchecked = isUncheckedTask(line);
        const checked = isCheckedTask(line);
        const localContent = normalizeWhitespace(this.taskParser.getTaskContentFromLineText(line) ?? "");
        const localDue = this.taskParser.getDueDateFromLineText ? (this.taskParser.getDueDateFromLineText(line) || null) : null;
        if (todoistId) {
          localTasksById.set(todoistId, {
            todoistId,
            path: file.path,
            lineNumber,
            lineText: line,
            isChecked: checked,
            content: localContent,
            due: localDue
          });
        }
        if (unchecked && todoistId) {
          orphanCandidates.push({ todoistId, path: file.path, lineNumber, lineText: line });
        }
        if (unchecked && /(^|\s)#obsidian\b/i.test(line)) {
          localOnly.push({ path: file.path, lineNumber, lineText: line });
        }
      }
    }
    for (const marker of invalidMarkers) {
      dataIssues.push({
        todoistId: "(invalid)",
        issues: [`Invalid or missing todoist_id at ${marker.path}:${marker.lineNumber}`],
        locations: { obsidian: `${marker.path}:${marker.lineNumber}` },
        lineText: marker.lineText
      });
    }
    const remoteTaskCache = new Map();
    const fetchRemoteTask = async (taskId) => {
      if (remoteTaskCache.has(taskId)) {
        return remoteTaskCache.get(taskId);
      }
      try {
        const task = await this.todoistRestAPI.getTaskById(taskId);
        remoteTaskCache.set(taskId, task);
        return task;
      } catch (error) {
        const message = error && error.message ? error.message : String(error);
        if (message.includes("404")) {
          remoteTaskCache.set(taskId, null);
          return null;
        }
        console.error("Todoist Bridge: failed to fetch Todoist task", taskId, error);
        const info = { __error: message };
        remoteTaskCache.set(taskId, info);
        return info;
      }
    };
    for (const candidate of orphanCandidates) {
      const remote = await fetchRemoteTask(candidate.todoistId);
      if (remote === null) {
        orphaned.push(candidate);
      } else if (remote && remote.__error) {
        dataIssues.push({
          todoistId: candidate.todoistId,
          issues: [`Todoist fetch error: ${remote.__error}`],
          locations: { obsidian: `${candidate.path}:${candidate.lineNumber}` },
          lineText: candidate.lineText
        });
      }
    }
    const cachedTasks = Array.isArray(this.settings.todoistTasksData?.tasks) ? this.settings.todoistTasksData.tasks : [];
    for (const task of cachedTasks) {
      if (!task) {
        continue;
      }
      const taskId = task.id != null ? String(task.id) : "";
      if (!taskId) {
        continue;
      }
      const issues = [];
      const local = localTasksById.get(taskId);
      const remote = await fetchRemoteTask(taskId);
      if (!local) {
        issues.push("Missing in Obsidian");
      }
      if (remote === null) {
        issues.push("Missing in Todoist");
      } else if (remote && remote.__error) {
        issues.push(`Todoist fetch error: ${remote.__error}`);
      }
      const dataStatus = task.isCompleted ? "completed" : "open";
      if (local) {
        const localStatus = local.isChecked ? "completed" : "open";
        if (dataStatus !== localStatus) {
          issues.push(`Status mismatch (data.json=${dataStatus}, Obsidian=${localStatus})`);
        }
      }
      if (remote && !remote.__error && remote !== null) {
        const remoteStatus = remote.completed ? "completed" : "open";
        if (dataStatus !== remoteStatus) {
          issues.push(`Status mismatch (data.json=${dataStatus}, Todoist=${remoteStatus})`);
        }
        if (local) {
          const localStatus = local.isChecked ? "completed" : "open";
          if (localStatus !== remoteStatus) {
            issues.push(`Status mismatch (Obsidian=${localStatus}, Todoist=${remoteStatus})`);
          }
        }
      }
      const dataContent = normalizeWhitespace(task.content ?? "");
      if (local && dataContent && local.content && local.content !== dataContent) {
        issues.push("Content mismatch (data.json vs Obsidian)");
      }
      if (remote && !remote.__error && remote !== null) {
        const remoteContent = normalizeWhitespace(remote.content ?? "");
        if (dataContent && remoteContent && dataContent !== remoteContent) {
          issues.push("Content mismatch (data.json vs Todoist)");
        }
        if (local && local.content && remoteContent && local.content !== remoteContent) {
          issues.push("Content mismatch (Obsidian vs Todoist)");
        }
      }
      const dataDue = task.due && typeof task.due === "object" ? task.due.date ?? null : null;
      const localDue = local ? local.due : null;
      const remoteDue = remote && !remote.__error && remote !== null ? (remote.due ? remote.due.date ?? null : null) : null;
      const normalizedDue = [dataDue, localDue, remoteDue].map((value) => {
        if (!value) {
          return "";
        }
        return value.toString().trim();
      });
      const nonEmptyDue = normalizedDue.filter((value) => value !== "");
      if (nonEmptyDue.length) {
        const uniqueDue = Array.from(new Set(nonEmptyDue));
        if (uniqueDue.length > 1 || nonEmptyDue.length !== normalizedDue.length) {
          issues.push(`Due date mismatch (data.json=${dataDue || "n/a"}, Obsidian=${localDue || "n/a"}, Todoist=${remoteDue || "n/a"})`);
        }
      }
      const dataProject = task.projectId ? String(task.projectId) : null;
      const remoteProject = remote && !remote.__error && remote !== null ? (remote.projectId ? String(remote.projectId) : null) : null;
      if (dataProject && remoteProject && dataProject !== remoteProject) {
        issues.push(`Project mismatch (data.json=${dataProject}, Todoist=${remoteProject})`);
      }
      const dataSection = task.sectionId ? String(task.sectionId) : null;
      const remoteSection = remote && !remote.__error && remote !== null ? (remote.sectionId ? String(remote.sectionId) : null) : null;
      if (dataSection && remoteSection && dataSection !== remoteSection) {
        issues.push(`Section mismatch (data.json=${dataSection}, Todoist=${remoteSection})`);
      }
      if (issues.length) {
        const record = {
          todoistId: taskId,
          issues,
          locations: {
            obsidian: local ? `${local.path}:${local.lineNumber}` : null,
            todoistUrl: remote && !remote.__error && remote !== null ? remote.url ?? null : null
          }
        };
        if (local) {
          record.lineText = local.lineText;
        }
        dataIssues.push(record);
      }
    }
    const totalIssues = orphaned.length + localOnly.length + dataIssues.length;
    const reportLines = [];
    reportLines.push(`# Todoist Bridge — Database Check (${timestamp.display})`);
    reportLines.push("");
    reportLines.push("**Summary**");
    reportLines.push(`- Orphaned Bridge Marks: ${orphaned.length}`);
    reportLines.push(`- Local-only (#obsidian): ${localOnly.length}`);
    reportLines.push(`- data.json Inconsistencies: ${dataIssues.length}`);
    reportLines.push(`- Total issues: ${totalIssues}`);
    reportLines.push("");
    reportLines.push("## 1) Orphaned Bridge Marks");
    if (!orphaned.length) {
      reportLines.push("- None");
    } else {
      for (const entry of orphaned) {
        reportLines.push(`- ${entry.path}:${entry.lineNumber}`);
        reportLines.push(`  ${entry.lineText.trimEnd()}`);
        reportLines.push(`  - todoist_id: ${entry.todoistId}`);
        reportLines.push("  - Todoist: not found → **Remove bridge mark**");
      }
    }
    reportLines.push("");
    reportLines.push("## 2) Local-only (#obsidian)");
    if (!localOnly.length) {
      reportLines.push("- None");
    } else {
      for (const entry of localOnly) {
        reportLines.push(`- ${entry.path}:${entry.lineNumber}`);
        reportLines.push(`  ${entry.lineText.trimEnd()}`);
        reportLines.push("  - Note: local task (not synced)");
      }
    }
    reportLines.push("");
    reportLines.push("## 3) data.json Inconsistencies");
    if (!dataIssues.length) {
      reportLines.push("- None");
    } else {
      for (const entry of dataIssues) {
        reportLines.push(`- todoist_id: ${entry.todoistId}`);
        if (Array.isArray(entry.issues)) {
          for (const detail of entry.issues) {
            reportLines.push(`  - ${detail}`);
          }
        }
        if (entry.locations?.obsidian) {
          reportLines.push(`  - Obsidian: ${entry.locations.obsidian}`);
        }
        if (entry.locations?.todoistUrl) {
          reportLines.push(`  - Todoist URL: ${entry.locations.todoistUrl}`);
        }
        if (entry.lineText) {
          reportLines.push(`  - Line: ${entry.lineText.trim()}`);
        }
      }
    }
    const reportContent = reportLines.join("\n");
    let reportFilename = `todoist_bridgedatabase_check_${timestamp.filename}.md`;
    let reportPath = reportFilename;
    let suffix = 1;
    while (await this.app.vault.adapter.exists(reportPath)) {
      reportPath = `todoist_bridgedatabase_check_${timestamp.filename}_${suffix}.md`;
      suffix++;
    }
    try {
      await this.app.vault.create(reportPath, reportContent);
      todoistBridgeDebugLog(`Report: ${reportPath}`);
      const noticeMessage = totalIssues ? `Todoist Bridge: Database check found ${totalIssues} issues` : "Todoist Bridge: Database check completed (no issues)";
      new import_obsidian4.Notice(noticeMessage);
    } catch (error) {
      console.error("Todoist Bridge: failed to write database check report", error);
    }
    todoistBridgeDebugLog(`Check Database: ${totalIssues} issues | Orphaned: ${orphaned.length} | Local-only: ${localOnly.length} | data.json: ${dataIssues.length}`);
  }
  async runCacheRebuild() {
    if (!this.checkModuleClass()) {
      new import_obsidian4.Notice("Todoist Bridge: API not initialized");
      return;
    }
    const timestamp = this.getTimestampStrings("America/Sao_Paulo");
    todoistBridgeDebugLog(`Cache Rebuild: started ${timestamp.display}`);
    const isUncheckedTask = (line) => /^\s*[-*]\s+\[\s\]/.test(line);
    const hasTodoistTag = (line) => /(^|\s)#todoist\b/i.test(line);
    const linkCandidates = [];
    const missingIdEntries = [];
    const markdownFiles = this.app.vault.getMarkdownFiles ? this.app.vault.getMarkdownFiles() : [];
    for (const file of markdownFiles) {
      let content;
      try {
        content = await this.app.vault.cachedRead(file);
      } catch (error) {
        console.error("Todoist Bridge: failed to read file during cache rebuild", file.path, error);
        continue;
      }
      if (typeof content !== "string") {
        continue;
      }
      const lines = content.split("\n");
      for (let i = 0; i < lines.length; i++) {
        const line = lines[i];
        if (!isUncheckedTask(line) || !hasTodoistTag(line)) {
          continue;
        }
        const lineNumber = i + 1;
        let taskId = "";
        try {
          taskId = (this.taskParser.getTodoistIdFromLineText(line) || "").toString().trim();
        } catch {
          taskId = "";
        }
        if (!taskId) {
          missingIdEntries.push({ path: file.path, lineNumber, lineText: line });
          continue;
        }
        linkCandidates.push({ taskId, path: file.path, lineNumber, lineText: line });
      }
    }
    const uniqueCandidatesById = /* @__PURE__ */ new Map();
    for (const entry of linkCandidates) {
      if (!uniqueCandidatesById.has(entry.taskId)) {
        uniqueCandidatesById.set(entry.taskId, entry);
      }
    }
    const existingTasks = Array.isArray(this.settings.todoistTasksData?.tasks) ? this.settings.todoistTasksData.tasks : [];
    const existingById = /* @__PURE__ */ new Map();
    for (const task of existingTasks) {
      if (!task || task.id == null) {
        continue;
      }
      existingById.set(String(task.id), task);
    }
    const rebuiltTasks = [];
    const foundEntries = [];
    const removedEntries = [];
    const fetchErrorEntries = [];
    const legacyUnverifiableEntries = [];
    const fileTaskMap = /* @__PURE__ */ new Map();
    const addTaskToFileMap = (filepath, taskId) => {
      if (!filepath || !taskId) {
        return;
      }
      if (!fileTaskMap.has(filepath)) {
        fileTaskMap.set(filepath, /* @__PURE__ */ new Set());
      }
      fileTaskMap.get(filepath).add(String(taskId));
    };
    const remoteTasksById = /* @__PURE__ */ new Map();
    const remoteLookupErrorsById = /* @__PURE__ */ new Map();
    const withRateLimitRetry = async (requestFn, maxAttempts = 4) => {
      let attempt = 0;
      while (attempt < maxAttempts) {
        attempt++;
        try {
          return await requestFn();
        } catch (error) {
          const message = error && error.message ? error.message : String(error);
          const isRateLimited = /(?:^|[\s(])429(?:[\s):]|$)|too many requests/i.test(message);
          if (!isRateLimited || attempt >= maxAttempts) {
            throw error;
          }
          const waitMs = Math.min(2e3 * attempt, 1e4);
          await new Promise((resolve) => setTimeout(resolve, waitMs));
        }
      }
      throw new Error("Todoist lookup retry loop exhausted.");
    };
    const nonLegacyTaskIds = Array.from(uniqueCandidatesById.keys()).map((id) => String(id)).filter((id) => !this.todoistRestAPI.isLegacyNumericTaskId(id));
    const idsNeedingDirectLookup = /* @__PURE__ */ new Set();
    const chunkSize = 25;
    for (let start = 0; start < nonLegacyTaskIds.length; start += chunkSize) {
      const chunkIds = nonLegacyTaskIds.slice(start, start + chunkSize);
      if (!chunkIds.length) {
        continue;
      }
      const requestedIds = /* @__PURE__ */ new Set(chunkIds.map((id) => String(id)));
      let fetchedChunk = [];
      try {
        fetchedChunk = await withRateLimitRetry(() => this.todoistRestAPI.GetActiveTasks({ ids: chunkIds }));
      } catch (error) {
        for (const id of chunkIds) {
          idsNeedingDirectLookup.add(String(id));
        }
        continue;
      }
      let hasUnexpectedTaskId = false;
      if (Array.isArray(fetchedChunk)) {
        for (const task of fetchedChunk) {
          if (!task || task.id == null) {
            continue;
          }
          const remoteTaskId = String(task.id);
          if (!requestedIds.has(remoteTaskId)) {
            hasUnexpectedTaskId = true;
            continue;
          }
          remoteTasksById.set(remoteTaskId, task);
        }
      }
      if (hasUnexpectedTaskId) {
        for (const id of chunkIds) {
          idsNeedingDirectLookup.add(String(id));
        }
      }
    }
    if (idsNeedingDirectLookup.size) {
      for (const taskId of idsNeedingDirectLookup.values()) {
        if (remoteTasksById.has(taskId)) {
          continue;
        }
        try {
          const remoteTask = await withRateLimitRetry(() => this.todoistRestAPI.getTaskById(taskId), 3);
          if (remoteTask && remoteTask.id != null) {
            remoteTasksById.set(taskId, remoteTask);
          }
        } catch (error) {
          const message = error && error.message ? error.message : String(error);
          if (/404|not found/i.test(message)) {
            continue;
          }
          remoteLookupErrorsById.set(taskId, message);
        }
        await new Promise((resolve) => setTimeout(resolve, 120));
      }
    }
    for (const entry of uniqueCandidatesById.values()) {
      const taskIdText = String(entry.taskId);
      const isLegacyNumeric = this.todoistRestAPI.isLegacyNumericTaskId(taskIdText);
      if (isLegacyNumeric) {
        const fallbackTask = existingById.get(taskIdText);
        if (fallbackTask) {
          const normalizedFallback = this.cacheOperation.normalizeTaskForCache({
            ...fallbackTask,
            id: taskIdText,
            path: entry.path,
            isCompleted: false
          });
          if (normalizedFallback) {
            rebuiltTasks.push(normalizedFallback);
            addTaskToFileMap(entry.path, normalizedFallback.id);
          }
        }
        const legacyError = "Legacy numeric todoist_id cannot be verified via API v1; kept from existing cache when available.";
        fetchErrorEntries.push({
          taskId: taskIdText,
          path: entry.path,
          lineNumber: entry.lineNumber,
          lineText: entry.lineText,
          error: legacyError
        });
        legacyUnverifiableEntries.push({ taskId: taskIdText, path: entry.path, lineNumber: entry.lineNumber, lineText: entry.lineText });
        continue;
      }
      const lookupError = remoteLookupErrorsById.get(taskIdText);
      if (lookupError) {
        const fallbackTask = existingById.get(taskIdText);
        if (fallbackTask) {
          const normalizedFallback = this.cacheOperation.normalizeTaskForCache({
            ...fallbackTask,
            id: taskIdText,
            path: entry.path,
            isCompleted: false
          });
          if (normalizedFallback) {
            rebuiltTasks.push(normalizedFallback);
            addTaskToFileMap(entry.path, normalizedFallback.id);
          }
        }
        fetchErrorEntries.push({
          taskId: taskIdText,
          path: entry.path,
          lineNumber: entry.lineNumber,
          lineText: entry.lineText,
          error: lookupError
        });
        continue;
      }
      const remoteTask = remoteTasksById.get(taskIdText) || null;
      if (remoteTask) {
        const normalizedRemoteTask = this.cacheOperation.normalizeTaskForCache({
          ...remoteTask,
          id: remoteTask.id != null ? String(remoteTask.id) : taskIdText,
          path: entry.path,
          isCompleted: false,
          origin: remoteTask.origin || "todoist"
        });
        if (normalizedRemoteTask) {
          rebuiltTasks.push(normalizedRemoteTask);
          addTaskToFileMap(entry.path, normalizedRemoteTask.id);
          foundEntries.push({
            taskId: String(normalizedRemoteTask.id),
            path: entry.path,
            lineNumber: entry.lineNumber,
            lineText: entry.lineText,
            todoistUrl: normalizedRemoteTask.url || null
          });
          continue;
        }
      }
      removedEntries.push({
        taskId: taskIdText,
        path: entry.path,
        lineNumber: entry.lineNumber,
        lineText: entry.lineText
      });
    }
    const dedupedTasks = [];
    const seenTaskIds = /* @__PURE__ */ new Set();
    for (const task of rebuiltTasks) {
      if (!task || task.id == null) {
        continue;
      }
      const taskId = String(task.id);
      if (seenTaskIds.has(taskId)) {
        continue;
      }
      seenTaskIds.add(taskId);
      dedupedTasks.push(task);
    }
    this.cacheOperation.saveTasksToCache(dedupedTasks);
    const existingMetadata = await this.cacheOperation.getFileMetadatas();
    const rebuiltMetadata = {};
    if (existingMetadata && typeof existingMetadata === "object") {
      for (const key of Object.keys(existingMetadata)) {
        rebuiltMetadata[key] = { todoistTasks: [], todoistCount: 0 };
      }
    }
    for (const [filepath, taskIdSet] of fileTaskMap.entries()) {
      const todoistTasks = Array.from(taskIdSet.values());
      rebuiltMetadata[filepath] = {
        todoistTasks,
        todoistCount: todoistTasks.length
      };
    }
    this.settings.fileMetadata = rebuiltMetadata;
    this.markRuntimeStateDirty();
    await this.saveSettings();
    const reportLines = [];
    reportLines.push(`# Todoist Bridge — Cache Rebuild (${timestamp.display})`);
    reportLines.push("");
    reportLines.push("**Summary**");
    reportLines.push(`- Unchecked #todoist links scanned: ${linkCandidates.length}`);
    reportLines.push(`- Unique Todoist IDs scanned: ${uniqueCandidatesById.size}`);
    reportLines.push(`- Links with #todoist and found in Todoist: ${foundEntries.length}`);
    reportLines.push(`- Links with #todoist and not found in Todoist removed: ${removedEntries.length}`);
    reportLines.push(`- Legacy numeric IDs not verifiable via API v1: ${legacyUnverifiableEntries.length}`);
    reportLines.push(`- #todoist lines missing [todoist_id:: ...]: ${missingIdEntries.length}`);
    reportLines.push(`- Verification errors (kept from existing cache): ${fetchErrorEntries.length}`);
    reportLines.push(`- Cached tasks after rebuild: ${dedupedTasks.length}`);
    reportLines.push("");
    reportLines.push("## Links with #todoist and found in Todoist");
    if (!foundEntries.length) {
      reportLines.push("- None");
    } else {
      for (const entry of foundEntries) {
        reportLines.push(`- ${entry.path}:${entry.lineNumber}`);
        reportLines.push(`  - todoist_id: ${entry.taskId}`);
        if (entry.todoistUrl) {
          reportLines.push(`  - Todoist URL: ${entry.todoistUrl}`);
        }
        reportLines.push(`  - Line: ${entry.lineText.trim()}`);
      }
    }
    reportLines.push("");
    reportLines.push("## Links with #todoist and not found in Todoist removed");
    if (!removedEntries.length) {
      reportLines.push("- None");
    } else {
      for (const entry of removedEntries) {
        reportLines.push(`- ${entry.path}:${entry.lineNumber}`);
        reportLines.push(`  - todoist_id: ${entry.taskId}`);
        reportLines.push(`  - Line: ${entry.lineText.trim()}`);
      }
    }
    if (missingIdEntries.length) {
      reportLines.push("");
      reportLines.push("## #todoist lines without [todoist_id:: ...]");
      for (const entry of missingIdEntries) {
        reportLines.push(`- ${entry.path}:${entry.lineNumber}`);
        reportLines.push(`  - Line: ${entry.lineText.trim()}`);
      }
    }
    if (fetchErrorEntries.length) {
      reportLines.push("");
      reportLines.push("## Verification errors (kept from existing cache)");
      for (const entry of fetchErrorEntries) {
        reportLines.push(`- ${entry.path}:${entry.lineNumber}`);
        reportLines.push(`  - todoist_id: ${entry.taskId}`);
        reportLines.push(`  - Error: ${entry.error}`);
      }
    }
    const reportContent = reportLines.join("\n");
    let reportFilename = `todoist_bridge_cache_rebuild_${timestamp.filename}.md`;
    let reportPath = reportFilename;
    let suffix = 1;
    while (await this.app.vault.adapter.exists(reportPath)) {
      reportPath = `todoist_bridge_cache_rebuild_${timestamp.filename}_${suffix}.md`;
      suffix++;
    }
    try {
      await this.app.vault.create(reportPath, reportContent);
      todoistBridgeDebugLog(`Report: ${reportPath}`);
      new import_obsidian4.Notice(`Todoist Bridge: cache rebuild completed (${foundEntries.length} found, ${removedEntries.length} removed)`);
    } catch (error) {
      console.error("Todoist Bridge: failed to write cache rebuild report", error);
      new import_obsidian4.Notice("Todoist Bridge: cache rebuild completed, but failed to write report");
    }
    todoistBridgeDebugLog(`Cache Rebuild: scanned=${linkCandidates.length} unique=${uniqueCandidatesById.size} found=${foundEntries.length} removed=${removedEntries.length} cached=${dedupedTasks.length}`);
  }
  async modifyTodoistAPI(api) {
    await this.initializePlugin();
  }
  // return true of false
  async initializePlugin() {
    this.initializeModuleClass();
    const isProjectsSaved = await this.cacheOperation.saveProjectsToCache();
    if (!isProjectsSaved) {
      this.todoistRestAPI = void 0;
      this.todoistSyncAPI = void 0;
      this.taskParser = void 0;
      this.taskParser = void 0;
      this.cacheOperation = void 0;
      this.fileOperation = void 0;
      this.todoistSync = void 0;
      this.settings.apiInitialized = false;
      this.syncLock = false;

      return false;
    }
    if (!this.settings.defaultProjectId) {
      let fallbackProjectId = this.cacheOperation.getProjectIdByNameFromCache("Inbox");
      if (!fallbackProjectId && Array.isArray(this.settings.todoistTasksData.projects) && this.settings.todoistTasksData.projects.length > 0) {
        fallbackProjectId = this.settings.todoistTasksData.projects[0].id;
      }
      if (fallbackProjectId) {
        this.settings.defaultProjectId = fallbackProjectId.toString();
      }
    }
    this.settings.apiInitialized = true;
    this.syncLock = false;
    await this.saveSettings();
    this.updateAutomaticSyncInterval();
    return true;
  }
  async initializeModuleClass() {
    this.todoistRestAPI = new TodoistRestAPI(this.app, this);
    this.cacheOperation = new CacheOperation(this.app, this);
    this.taskParser = new TaskParser(this.app, this);
    this.fileOperation = new FileOperation(this.app, this);
    this.todoistSyncAPI = new TodoistSyncAPI(this.app, this);
    this.todoistSync = new TodoistSync(this.app, this);
  }
  async lineNumberCheck() {
    var _a, _b, _c, _d, _e, _f, _g;
    const view = this.app.workspace.getActiveViewOfType(import_obsidian4.MarkdownView);
    if (view) {
      const cursor = (_a = view.app.workspace.getActiveViewOfType(import_obsidian4.MarkdownView)) == null ? void 0 : _a.editor.getCursor();
      const line = cursor == null ? void 0 : cursor.line;
      const fileContent = view.data;
      const fileName = (_d = (_c = (_b = view.app.workspace.getActiveViewOfType(import_obsidian4.MarkdownView)) == null ? void 0 : _b.app.workspace.activeEditor) == null ? void 0 : _c.file) == null ? void 0 : _d.name;
      const filepath = (_g = (_f = (_e = view.app.workspace.getActiveViewOfType(import_obsidian4.MarkdownView)) == null ? void 0 : _e.app.workspace.activeEditor) == null ? void 0 : _f.file) == null ? void 0 : _g.path;
      if (typeof this.lastLines === "undefined" || typeof this.lastLines.get(fileName) === "undefined") {
        this.lastLines.set(fileName, line);
        return;
      }
      if (this.lastLines.has(fileName) && line !== this.lastLines.get(fileName)) {
        const lastLine = this.lastLines.get(fileName);
        if (this.settings.debugMode) {
          todoistBridgeDebugLog("Line changed!", `current line is ${line}`, `last line is ${lastLine}`);
        }
        const lastLineText = view.editor.getLine(lastLine);
        if (!this.checkModuleClass()) {
          return;
        }
        this.lastLines.set(fileName, line);
        try {
          if (!await this.checkAndHandleSyncLock())
            return;
          await this.todoistSync.lineModifiedTaskCheck(filepath, lastLineText, lastLine, fileContent);
          this.syncLock = false;
        } catch (error) {
          console.error(`An error occurred while check modified task in line text: ${error}`);
          this.syncLock = false;
        }
      } else {
      }
    }
  }
  async checkboxEventhandle(evt) {
    var _a;
    if (!this.checkModuleClass()) {
      return;
    }
    const target = evt.target;
    const taskElement = target.closest("div");
    if (!taskElement)
      return;
    const regex = /(?:(?:%%\s*)|(?:<!--\s*)|(?:<span class="todoist-bridge">\s*))?\[todoist_id::\s*([A-Za-z0-9_-]+)\](?:(?:\s*%%)|(?:\s*-->)|(?:\s*<\/span>))?/;
    const match = ((_a = taskElement.textContent) == null ? void 0 : _a.match(regex)) || false;
    if (match) {
      const taskId = match[1];
      if (target.checked) {
        this.todoistSync.closeTask(taskId);
      } else {
        this.todoistSync.repoenTask(taskId);
      }
    } else {
      try {
        if (!await this.checkAndHandleSyncLock())
          return;
        await this.todoistSync.fullTextModifiedTaskCheck();
        this.syncLock = false;
      } catch (error) {
        console.error(`An error occurred while check modified tasks in the file: ${error}`);
        this.syncLock = false;
      }
    }
  }
  //return true
  checkModuleClass() {
    if (this.settings.apiInitialized === true) {
      if (this.todoistRestAPI === void 0 || this.todoistSyncAPI === void 0 || this.cacheOperation === void 0 || this.fileOperation === void 0 || this.todoistSync === void 0 || this.taskParser === void 0) {
        this.initializeModuleClass();
      }
      return true;
    } else {

      return false;
    }
  }
  async setStatusBarText() {
    var _a;
    if (!this.checkModuleClass()) {
      return;
    }
    const view = this.app.workspace.getActiveViewOfType(import_obsidian4.MarkdownView);
    if (!view) {
      this.statusBar.setText("");
    } else {
      const filepath = (_a = this.app.workspace.getActiveViewOfType(import_obsidian4.MarkdownView)) == null ? void 0 : _a.file.path;
      if (filepath === void 0) {
        todoistBridgeDebugLog(`file path undefined`);
        return;
      }
      const defaultProjectName = this.cacheOperation.getProjectNameByIdFromCache(this.settings.defaultProjectId) || "";
      if (!defaultProjectName) {
        this.statusBar.setText("");
        return;
      }
      this.statusBar.setText(defaultProjectName);
    }
  }
  async scheduledSynchronization(options = {}) {
    if (!this.checkModuleClass()) {
      const tasks = this.cacheOperation.loadTasksFromCache();
      const cached = Array.isArray(tasks) ? tasks.length : 0;
      return { completed: 0, deleted: 0, cached };
    }
    const interactive = options && options.interactive === true;
    const computeCacheCount = () => {
      const tasks = this.cacheOperation.loadTasksFromCache();
      return Array.isArray(tasks) ? tasks.length : 0;
    };
    const summary = { completed: 0, deleted: 0, cached: computeCacheCount() };
    if (interactive) {
      const includeTimestamp = this.settings.debugMode === true;
      if (includeTimestamp) {
        const startTimestamp = this.getTimestampStrings();
        todoistBridgeDebugLog(`Todoist Bridge: Manual sync started (${startTimestamp.display})`);
      } else {
        todoistBridgeDebugLog("Todoist Bridge: Manual sync started");
      }
    } else if (this.settings.debugMode) {
      todoistBridgeDebugLog("Todoist scheduled synchronization task started at", new Date().toLocaleString());
    }
    try {
      if (!await this.checkAndHandleSyncLock()) {
        return summary;
      }
      let remoteSummary = { completed: 0, deleted: 0, cached: summary.cached };
      try {
        remoteSummary = await this.todoistSync.syncTodoistToObsidian(options);
      } catch (error) {
        console.error("An error occurred in syncTodoistToObsidian:", error);
      }
      this.syncLock = false;
      try {
        await this.saveSettings();
      } catch (error) {
        console.error("An error occurred in saveSettings:", error);
      }
      const filesToSync = this.selectFilesForScheduledSync({ interactive });
      if (this.settings.debugMode) {
        todoistBridgeDebugLog(filesToSync);
      }
      for (let fileKey of filesToSync) {
        if (!fileKey) {
          continue;
        }
        const syncFile = this.app.vault.getAbstractFileByPath(fileKey);
        if (!syncFile || !(syncFile instanceof import_obsidian4.TFile)) {
          continue;
        }
        if (this.settings.debugMode) {
          todoistBridgeDebugLog(fileKey);
        }
        let fileContent;
        try {
          fileContent = await this.app.vault.cachedRead(syncFile);
        } catch (error) {
          console.error(`Failed to read ${fileKey} for synchronization:`, error);
          this.markDirtyFile(fileKey);
          continue;
        }
        const sharedContext = { file: syncFile, content: fileContent, metadata: this.settings.fileMetadata[fileKey] };
        if (!await this.checkAndHandleSyncLock()) {
          return summary;
        }
        try {
          const created = await this.todoistSync.fullTextNewTaskCheck(fileKey, sharedContext);
          if (created && sharedContext.file instanceof import_obsidian4.TFile) {
            try {
              sharedContext.content = await this.app.vault.cachedRead(sharedContext.file);
            } catch (readError) {
              console.error(`Error refreshing content after new task sync for ${fileKey}:`, readError);
            }
          }
        } catch (error) {
          console.error("An error occurred in fullTextNewTaskCheck:", error);
          this.markDirtyFile(fileKey);
        }
        this.syncLock = false;
        if (!await this.checkAndHandleSyncLock()) {
          return summary;
        }
        try {
          await this.todoistSync.deletedTaskCheck(fileKey, sharedContext);
        } catch (error) {
          console.error("An error occurred in deletedTaskCheck:", error);
          this.markDirtyFile(fileKey);
        }
        this.syncLock = false;
        if (!await this.checkAndHandleSyncLock()) {
          return summary;
        }
        try {
          await this.todoistSync.fullTextModifiedTaskCheck(fileKey, sharedContext);
        } catch (error) {
          console.error("An error occurred in fullTextModifiedTaskCheck:", error);
          this.markDirtyFile(fileKey);
        }
        this.syncLock = false;
      }
      summary.completed = remoteSummary.completed ?? summary.completed;
      summary.deleted += remoteSummary.deleted ?? 0;
    } catch (error) {
      console.error("An error occurred:", error);
    } finally {
      this.syncLock = false;
    }
    try {
      await this.saveRuntimeStateIfDirty();
    } catch (error) {
      console.error("An error occurred in saveRuntimeStateIfDirty:", error);
    }
    summary.cached = computeCacheCount();
    if (interactive) {
      const finishMessage = this.formatSyncSummary(summary);
      todoistBridgeDebugLog(finishMessage);
    } else if (this.settings.debugMode) {
      todoistBridgeDebugLog("Todoist scheduled synchronization task completed at", new Date().toLocaleString());
    }
    return summary;
  }

  async checkSyncLock() {
    let checkCount = 0;
    while (this.syncLock == true && checkCount < 10) {
      await new Promise((resolve) => setTimeout(resolve, 1e3));
      checkCount++;
    }
    if (this.syncLock == true) {
      return false;
    }
    return true;
  }
  async checkAndHandleSyncLock() {
    if (this.syncLock) {
      todoistBridgeDebugLog("sync locked.");
      const isSyncLockChecked = await this.checkSyncLock();
      if (!isSyncLockChecked) {
        return false;
      }
      todoistBridgeDebugLog("sync unlocked.");
    }
    this.syncLock = true;
    return true;
  }
};
/*! Bundled license information:
@babel/runtime/helpers/regeneratorRuntime.js:
  (*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/facebook/regenerator/blob/main/LICENSE *)
*/
/* nosourcemap */
